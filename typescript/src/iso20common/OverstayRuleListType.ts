// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";
import { OverstayRuleType, encodeOverstayRuleType, decodeOverstayRuleType } from "./OverstayRuleType.ts";
import { RationalNumberType, encodeRationalNumberType, decodeRationalNumberType } from "./RationalNumberType.ts";

export class OverstayRuleListType {
    readonly overstayTimeThreshold: number | null;
    readonly overstayPowerThreshold: RationalNumberType | null;
    readonly overstayRule: readonly OverstayRuleType[];

    constructor(overstayTimeThreshold: number | null, overstayPowerThreshold: RationalNumberType | null, overstayRule: readonly OverstayRuleType[]) {
        this.overstayTimeThreshold = overstayTimeThreshold;
        this.overstayPowerThreshold = overstayPowerThreshold;
        this.overstayRule = overstayRule;
    }
}

export function encodeOverstayRuleListType(w: BitWriter, msg: OverstayRuleListType): void {
    let st204 = 0
    let done204 = false
    while (!done204) {
        { const _c205 = st204; if (false) {}   // dispatch
            else if (_c205 === 0) {
                if (msg.overstayTimeThreshold != null) {
                    w.writeBits(0, 2)   // OverstayTimeThreshold
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.overstayTimeThreshold!));
                    w.writeBits(0, 1)   // child EE
                    st204 = 1
                } else if (msg.overstayPowerThreshold != null) {
                    w.writeBits(1, 2)   // OverstayPowerThreshold
                    encodeRationalNumberType(w, msg.overstayPowerThreshold!)
                    st204 = 2
                } else {
                    if (msg.overstayRule.length < 1 || msg.overstayRule.length > 5) throw new RangeError("msg.overstayRule: list size out of schema range");
                    w.writeBits(2, 2)   // OverstayRule
                    encodeOverstayRuleType(w, msg.overstayRule[0])
                    for (let ci = 1; ci < msg.overstayRule.length; ci++) {
                        w.writeBits(0, 2)   // OverstayRule
                        encodeOverstayRuleType(w, msg.overstayRule[ci])
                    }
                    w.writeBits(1, 2)   // element EE (list end)
                    done204 = true
                }
            }
            else if (_c205 === 1) {
                if (msg.overstayPowerThreshold != null) {
                    w.writeBits(0, 2)   // OverstayPowerThreshold
                    encodeRationalNumberType(w, msg.overstayPowerThreshold!)
                    st204 = 2
                } else {
                    if (msg.overstayRule.length < 1 || msg.overstayRule.length > 5) throw new RangeError("msg.overstayRule: list size out of schema range");
                    w.writeBits(1, 2)   // OverstayRule
                    encodeOverstayRuleType(w, msg.overstayRule[0])
                    for (let ci = 1; ci < msg.overstayRule.length; ci++) {
                        w.writeBits(0, 2)   // OverstayRule
                        encodeOverstayRuleType(w, msg.overstayRule[ci])
                    }
                    w.writeBits(1, 2)   // element EE (list end)
                    done204 = true
                }
            }
            else if (_c205 === 2) {
                if (msg.overstayRule.length < 1 || msg.overstayRule.length > 5) throw new RangeError("msg.overstayRule: list size out of schema range");
                w.writeBits(0, 1)   // OverstayRule
                encodeOverstayRuleType(w, msg.overstayRule[0])
                for (let ci = 1; ci < msg.overstayRule.length; ci++) {
                    w.writeBits(0, 2)   // OverstayRule
                    encodeOverstayRuleType(w, msg.overstayRule[ci])
                }
                w.writeBits(1, 2)   // element EE (list end)
                done204 = true
            }
        }
    }
}

export function decodeOverstayRuleListType(r: BitReader): OverstayRuleListType {
    let _overstayTimeThreshold: number | null = null
    let _overstayPowerThreshold: RationalNumberType | null = null
    const overstayRuleList = new Array<OverstayRuleType>()
    let st206 = 0
    let done206 = false
    while (!done206) {
        { const _c207 = st206; if (false) {}   // dispatch
            else if (_c207 === 0) {
                { const _c208 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c208 === 0) {
                        r.readBits(1)   // value-start
                        _overstayTimeThreshold = Number(ExiPrimitives.readUnsignedInteger(r))
                        r.readBits(1)   // child EE
                        st206 = 1
                    }
                    else if (_c208 === 1) {
                        _overstayPowerThreshold = decodeRationalNumberType(r)
                        st206 = 2
                    }
                    else if (_c208 === 2) {   // OverstayRule
                        overstayRuleList.push(decodeOverstayRuleType(r));
                        while (true) {
                            const lc = r.readBits(2)
                            if (lc === 1) break;   // element EE (list end)
                            if (!(lc === 0 && overstayRuleList.length < 5)) throw ExiError.invalidEventCode("repeating element");
                            overstayRuleList.push(decodeOverstayRuleType(r));
                        }
                        done206 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c207 === 1) {
                { const _c209 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c209 === 0) {
                        _overstayPowerThreshold = decodeRationalNumberType(r)
                        st206 = 2
                    }
                    else if (_c209 === 1) {   // OverstayRule
                        overstayRuleList.push(decodeOverstayRuleType(r));
                        while (true) {
                            const lc = r.readBits(2)
                            if (lc === 1) break;   // element EE (list end)
                            if (!(lc === 0 && overstayRuleList.length < 5)) throw ExiError.invalidEventCode("repeating element");
                            overstayRuleList.push(decodeOverstayRuleType(r));
                        }
                        done206 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c207 === 2) {
                { const _c210 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c210 === 0) {   // OverstayRule
                        overstayRuleList.push(decodeOverstayRuleType(r));
                        while (true) {
                            const lc = r.readBits(2)
                            if (lc === 1) break;   // element EE (list end)
                            if (!(lc === 0 && overstayRuleList.length < 5)) throw ExiError.invalidEventCode("repeating element");
                            overstayRuleList.push(decodeOverstayRuleType(r));
                        }
                        done206 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new OverstayRuleListType(_overstayTimeThreshold, _overstayPowerThreshold, overstayRuleList);
}

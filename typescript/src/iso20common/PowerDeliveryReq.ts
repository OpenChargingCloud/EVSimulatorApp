// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, exiEnum } from "../runtime/index.ts";
import { ChannelSelection, ChannelSelectionNames } from "./ChannelSelection.ts";
import { ChargeProgress, ChargeProgressNames } from "./ChargeProgress.ts";
import { EVPowerProfileType, encodeEVPowerProfileType, decodeEVPowerProfileType } from "./EVPowerProfileType.ts";
import { MessageHeaderType, encodeMessageHeaderType, decodeMessageHeaderType } from "./MessageHeaderType.ts";
import { Processing, ProcessingNames } from "./Processing.ts";
import { V2GRequestType } from "./V2GRequestType.ts";

export class PowerDeliveryReq extends V2GRequestType {
    readonly eVProcessing: Processing;
    readonly chargeProgress: ChargeProgress;
    readonly eVPowerProfile: EVPowerProfileType | null;
    readonly bPT_ChannelSelection: ChannelSelection | null;

    constructor(header: MessageHeaderType, eVProcessing: Processing, chargeProgress: ChargeProgress, eVPowerProfile: EVPowerProfileType | null, bPT_ChannelSelection: ChannelSelection | null) {
        super(header);
        this.eVProcessing = eVProcessing;
        this.chargeProgress = chargeProgress;
        this.eVPowerProfile = eVPowerProfile;
        this.bPT_ChannelSelection = bPT_ChannelSelection;
    }
}

export function encodePowerDeliveryReq(w: BitWriter, msg: PowerDeliveryReq): void {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.eVProcessing, 2)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.chargeProgress, 2)
    w.writeBits(0, 1)   // child EE
    let st367 = 0
    let done367 = false
    while (!done367) {
        { const _c368 = st367; if (false) {}   // dispatch
            else if (_c368 === 0) {
                if (msg.eVPowerProfile != null) {
                    w.writeBits(0, 2)   // EVPowerProfile
                    encodeEVPowerProfileType(w, msg.eVPowerProfile!)
                    st367 = 1
                } else if (msg.bPT_ChannelSelection != null) {
                    w.writeBits(1, 2)   // BPT_ChannelSelection
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.bPT_ChannelSelection!, 1)
                    w.writeBits(0, 1)   // child EE
                    st367 = 2
                } else {
                    w.writeBits(2, 2)   // element EE
                    done367 = true
                }
            }
            else if (_c368 === 1) {
                if (msg.bPT_ChannelSelection != null) {
                    w.writeBits(0, 2)   // BPT_ChannelSelection
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.bPT_ChannelSelection!, 1)
                    w.writeBits(0, 1)   // child EE
                    st367 = 2
                } else {
                    w.writeBits(1, 2)   // element EE
                    done367 = true
                }
            }
            else if (_c368 === 2) {
                w.writeBits(0, 1)   // element EE
                done367 = true
            }
        }
    }
}

export function decodePowerDeliveryReq(r: BitReader): PowerDeliveryReq {
    r.readBits(1)   // SE
    const _header = decodeMessageHeaderType(r)
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _eVProcessing = exiEnum("Processing", ProcessingNames, r.readBits(2)) as Processing
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _chargeProgress = exiEnum("ChargeProgress", ChargeProgressNames, r.readBits(2)) as ChargeProgress
    r.readBits(1)   // child EE
    let _eVPowerProfile: EVPowerProfileType | null = null
    let _bPT_ChannelSelection: ChannelSelection | null = null
    let st369 = 0
    let done369 = false
    while (!done369) {
        { const _c370 = st369; if (false) {}   // dispatch
            else if (_c370 === 0) {
                { const _c371 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c371 === 0) {
                        _eVPowerProfile = decodeEVPowerProfileType(r)
                        st369 = 1
                    }
                    else if (_c371 === 1) {
                        r.readBits(1)   // value-start
                        _bPT_ChannelSelection = exiEnum("ChannelSelection", ChannelSelectionNames, r.readBits(1)) as ChannelSelection
                        r.readBits(1)   // child EE
                        st369 = 2
                    }
                    else if (_c371 === 2) done369 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c370 === 1) {
                { const _c372 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c372 === 0) {
                        r.readBits(1)   // value-start
                        _bPT_ChannelSelection = exiEnum("ChannelSelection", ChannelSelectionNames, r.readBits(1)) as ChannelSelection
                        r.readBits(1)   // child EE
                        st369 = 2
                    }
                    else if (_c372 === 1) done369 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c370 === 2) {
                { const _c373 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c373 === 0) done369 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new PowerDeliveryReq(_header, _eVProcessing, _chargeProgress, _eVPowerProfile, _bPT_ChannelSelection);
}

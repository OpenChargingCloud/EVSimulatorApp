// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";
import { PriceLevelScheduleEntryListType, encodePriceLevelScheduleEntryListType, decodePriceLevelScheduleEntryListType } from "./PriceLevelScheduleEntryListType.ts";
import { PriceScheduleType } from "./PriceScheduleType.ts";

export class PriceLevelScheduleType extends PriceScheduleType {
    readonly id: string | null;
    readonly numberOfPriceLevels: number;
    readonly priceLevelScheduleEntries: PriceLevelScheduleEntryListType;

    constructor(id: string | null, timeAnchor: bigint, priceScheduleID: number, priceScheduleDescription: string | null, numberOfPriceLevels: number, priceLevelScheduleEntries: PriceLevelScheduleEntryListType) {
        super(timeAnchor, priceScheduleID, priceScheduleDescription);
        this.id = id;
        this.numberOfPriceLevels = numberOfPriceLevels;
        this.priceLevelScheduleEntries = priceLevelScheduleEntries;
    }
}

export function encodePriceLevelScheduleType(w: BitWriter, msg: PriceLevelScheduleType): void {
    let st144 = 0
    let done144 = false
    while (!done144) {
        { const _c145 = st144; if (false) {}   // dispatch
            else if (_c145 === 0) {
                if (msg.id != null) {
                    w.writeBits(0, 2)   // Id
                    ExiPrimitives.writeStringValue(w, msg.id!)
                    st144 = 1
                } else {
                    w.writeBits(1, 2)   // SE(TimeAnchor)
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.timeAnchor);
                    w.writeBits(0, 1)   // child EE
                    done144 = true
                }
            }
            else if (_c145 === 1) {
                w.writeBits(0, 1)   // SE(TimeAnchor)
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, msg.timeAnchor);
                w.writeBits(0, 1)   // child EE
                done144 = true
            }
        }
    }
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.priceScheduleID));
    w.writeBits(0, 1)   // child EE
    let st146 = 0
    let done146 = false
    while (!done146) {
        { const _c147 = st146; if (false) {}   // dispatch
            else if (_c147 === 0) {
                if (msg.priceScheduleDescription != null) {
                    w.writeBits(0, 2)   // PriceScheduleDescription
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeStringValue(w, msg.priceScheduleDescription!)
                    w.writeBits(0, 1)   // child EE
                    st146 = 1
                } else {
                    w.writeBits(1, 2)   // SE(NumberOfPriceLevels)
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.numberOfPriceLevels, 8)
                    w.writeBits(0, 1)   // child EE
                    done146 = true
                }
            }
            else if (_c147 === 1) {
                w.writeBits(0, 1)   // SE(NumberOfPriceLevels)
                w.writeBits(0, 1)   // value-start
                w.writeBits(msg.numberOfPriceLevels, 8)
                w.writeBits(0, 1)   // child EE
                done146 = true
            }
        }
    }
    w.writeBits(0, 1)   // SE
    encodePriceLevelScheduleEntryListType(w, msg.priceLevelScheduleEntries)
    w.writeBits(0, 1)   // element EE
}

export function decodePriceLevelScheduleType(r: BitReader): PriceLevelScheduleType {
    let _id: string | null = null
    let _timeAnchor: bigint = null
    let st148 = 0
    let done148 = false
    while (!done148) {
        { const _c149 = st148; if (false) {}   // dispatch
            else if (_c149 === 0) {
                { const _c150 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c150 === 0) {
                        _id = ExiPrimitives.readStringValue(r, "Id")
                        st148 = 1
                    }
                    else if (_c150 === 1) {   // SE(TimeAnchor)
                        r.readBits(1)   // value-start
                        _timeAnchor = ExiPrimitives.readUnsignedInteger(r)
                        r.readBits(1)   // child EE
                        done148 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c149 === 1) {
                { const _c151 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c151 === 0) {   // SE(TimeAnchor)
                        r.readBits(1)   // value-start
                        _timeAnchor = ExiPrimitives.readUnsignedInteger(r)
                        r.readBits(1)   // child EE
                        done148 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _priceScheduleID = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    let _priceScheduleDescription: string | null = null
    let _numberOfPriceLevels: number = null
    let st152 = 0
    let done152 = false
    while (!done152) {
        { const _c153 = st152; if (false) {}   // dispatch
            else if (_c153 === 0) {
                { const _c154 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c154 === 0) {
                        r.readBits(1)   // value-start
                        _priceScheduleDescription = ExiPrimitives.readStringValue(r, "PriceScheduleDescription")
                        r.readBits(1)   // child EE
                        st152 = 1
                    }
                    else if (_c154 === 1) {   // SE(NumberOfPriceLevels)
                        r.readBits(1)   // value-start
                        _numberOfPriceLevels = r.readBits(8)
                        r.readBits(1)   // child EE
                        done152 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c153 === 1) {
                { const _c155 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c155 === 0) {   // SE(NumberOfPriceLevels)
                        r.readBits(1)   // value-start
                        _numberOfPriceLevels = r.readBits(8)
                        r.readBits(1)   // child EE
                        done152 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    r.readBits(1)   // SE
    const _priceLevelScheduleEntries = decodePriceLevelScheduleEntryListType(r)
    r.readBits(1)   // element EE
    return new PriceLevelScheduleType(_id, _timeAnchor!, _priceScheduleID, _priceScheduleDescription, _numberOfPriceLevels!, _priceLevelScheduleEntries);
}

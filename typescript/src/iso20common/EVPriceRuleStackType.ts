// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";
import { EVPriceRuleType, encodeEVPriceRuleType, decodeEVPriceRuleType } from "./EVPriceRuleType.ts";

export class EVPriceRuleStackType {
    readonly duration: number;
    readonly eVPriceRule: readonly EVPriceRuleType[];

    constructor(duration: number, eVPriceRule: readonly EVPriceRuleType[]) {
        this.duration = duration;
        this.eVPriceRule = eVPriceRule;
    }
}

export function encodeEVPriceRuleStackType(w: BitWriter, msg: EVPriceRuleStackType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.duration));
    w.writeBits(0, 1)   // child EE
    const eVPriceRuleList = msg.eVPriceRule
    if (eVPriceRuleList.length < 1 || eVPriceRuleList.length > 8) throw new RangeError("eVPriceRuleList: list size out of schema range");
    for (let i = 0; i < eVPriceRuleList.length; i++) {
        w.writeBits(0, i === 0 ? 1 : 2);   // SE(item)
        encodeEVPriceRuleType(w, eVPriceRuleList[i])
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

export function decodeEVPriceRuleStackType(r: BitReader): EVPriceRuleStackType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _duration = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    const eVPriceRuleList = new Array<EVPriceRuleType>()
    r.readBits(1)   // SE(item) first
    eVPriceRuleList.push(decodeEVPriceRuleType(r));
    while (true) {
        const ec = r.readBits(2)
        if (ec === 1) break;   // element EE
        if (!(ec === 0 && eVPriceRuleList.length < 8)) throw ExiError.invalidEventCode("repeating element");
        eVPriceRuleList.push(decodeEVPriceRuleType(r));
    }
    return new EVPriceRuleStackType(_duration, eVPriceRuleList);
}

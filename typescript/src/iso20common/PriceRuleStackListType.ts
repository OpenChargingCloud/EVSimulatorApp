// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { PriceRuleStackType, encodePriceRuleStackType, decodePriceRuleStackType } from "./PriceRuleStackType.ts";

export class PriceRuleStackListType {
    readonly priceRuleStack: readonly PriceRuleStackType[];

    constructor(priceRuleStack: readonly PriceRuleStackType[]) {
        this.priceRuleStack = priceRuleStack;
    }
}

export function encodePriceRuleStackListType(w: BitWriter, msg: PriceRuleStackListType): void {
    const list = msg.priceRuleStack
    if (list.length < 1 || list.length > 1024) throw new RangeError("list: list size out of schema range");
    for (let i = 0; i < list.length; i++) {
        w.writeBits(0, i === 0 ? 1 : 2);   // SE(item)
        encodePriceRuleStackType(w, list[i])
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

export function decodePriceRuleStackListType(r: BitReader): PriceRuleStackListType {
    const list = new Array<PriceRuleStackType>()
    r.readBits(1)   // SE(item) first
    list.push(decodePriceRuleStackType(r));
    while (true) {
        const ec = r.readBits(2)
        if (ec === 1) break;   // element EE
        if (!(ec === 0 && list.length < 1024)) throw ExiError.invalidEventCode("repeating element");
        list.push(decodePriceRuleStackType(r));
    }
    return new PriceRuleStackListType(list);
}

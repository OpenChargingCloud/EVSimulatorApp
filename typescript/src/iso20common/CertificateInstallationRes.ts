// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, exiEnum } from "../runtime/index.ts";
import { CertificateChainType, encodeCertificateChainType, decodeCertificateChainType } from "./CertificateChainType.ts";
import { MessageHeaderType, encodeMessageHeaderType, decodeMessageHeaderType } from "./MessageHeaderType.ts";
import { Processing, ProcessingNames } from "./Processing.ts";
import { ResponseCode, ResponseCodeNames } from "./ResponseCode.ts";
import { SignedInstallationDataType, encodeSignedInstallationDataType, decodeSignedInstallationDataType } from "./SignedInstallationDataType.ts";
import { V2GResponseType } from "./V2GResponseType.ts";

export class CertificateInstallationRes extends V2GResponseType {
    readonly eVSEProcessing: Processing;
    readonly cPSCertificateChain: CertificateChainType;
    readonly signedInstallationData: SignedInstallationDataType;
    readonly remainingContractCertificateChains: number;

    constructor(header: MessageHeaderType, responseCode: ResponseCode, eVSEProcessing: Processing, cPSCertificateChain: CertificateChainType, signedInstallationData: SignedInstallationDataType, remainingContractCertificateChains: number) {
        super(header, responseCode);
        this.eVSEProcessing = eVSEProcessing;
        this.cPSCertificateChain = cPSCertificateChain;
        this.signedInstallationData = signedInstallationData;
        this.remainingContractCertificateChains = remainingContractCertificateChains;
    }
}

export function encodeCertificateInstallationRes(w: BitWriter, msg: CertificateInstallationRes): void {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.responseCode, 6)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.eVSEProcessing, 2)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    encodeCertificateChainType(w, msg.cPSCertificateChain)
    w.writeBits(0, 1)   // SE
    encodeSignedInstallationDataType(w, msg.signedInstallationData)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.remainingContractCertificateChains, 8)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

export function decodeCertificateInstallationRes(r: BitReader): CertificateInstallationRes {
    r.readBits(1)   // SE
    const _header = decodeMessageHeaderType(r)
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _responseCode = exiEnum("ResponseCode", ResponseCodeNames, r.readBits(6)) as ResponseCode
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _eVSEProcessing = exiEnum("Processing", ProcessingNames, r.readBits(2)) as Processing
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    const _cPSCertificateChain = decodeCertificateChainType(r)
    r.readBits(1)   // SE
    const _signedInstallationData = decodeSignedInstallationDataType(r)
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _remainingContractCertificateChains = r.readBits(8)
    r.readBits(1)   // child EE
    r.readBits(1)   // element EE
    return new CertificateInstallationRes(_header, _responseCode, _eVSEProcessing, _cPSCertificateChain, _signedInstallationData, _remainingContractCertificateChains);
}

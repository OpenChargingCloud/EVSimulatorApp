// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { EVPriceRuleStackType, encodeEVPriceRuleStackType, decodeEVPriceRuleStackType } from "./EVPriceRuleStackType.ts";

export class EVPriceRuleStackListType {
    readonly eVPriceRuleStack: readonly EVPriceRuleStackType[];

    constructor(eVPriceRuleStack: readonly EVPriceRuleStackType[]) {
        this.eVPriceRuleStack = eVPriceRuleStack;
    }
}

export function encodeEVPriceRuleStackListType(w: BitWriter, msg: EVPriceRuleStackListType): void {
    const list = msg.eVPriceRuleStack
    if (list.length < 1 || list.length > 1024) throw new RangeError("list: list size out of schema range");
    for (let i = 0; i < list.length; i++) {
        w.writeBits(0, i === 0 ? 1 : 2);   // SE(item)
        encodeEVPriceRuleStackType(w, list[i])
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

export function decodeEVPriceRuleStackListType(r: BitReader): EVPriceRuleStackListType {
    const list = new Array<EVPriceRuleStackType>()
    r.readBits(1)   // SE(item) first
    list.push(decodeEVPriceRuleStackType(r));
    while (true) {
        const ec = r.readBits(2)
        if (ec === 1) break;   // element EE
        if (!(ec === 0 && list.length < 1024)) throw ExiError.invalidEventCode("repeating element");
        list.push(decodeEVPriceRuleStackType(r));
    }
    return new EVPriceRuleStackListType(list);
}

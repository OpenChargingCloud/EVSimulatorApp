// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { MessageHeaderType, encodeMessageHeaderType, decodeMessageHeaderType } from "./MessageHeaderType.ts";
import { ResponseCode, ResponseCodeNames } from "./ResponseCode.ts";
import { ServiceParameterListType, encodeServiceParameterListType, decodeServiceParameterListType } from "./ServiceParameterListType.ts";
import { V2GResponseType } from "./V2GResponseType.ts";

export class ServiceDetailRes extends V2GResponseType {
    readonly serviceID: number;
    readonly serviceParameterList: ServiceParameterListType;

    constructor(header: MessageHeaderType, responseCode: ResponseCode, serviceID: number, serviceParameterList: ServiceParameterListType) {
        super(header, responseCode);
        this.serviceID = serviceID;
        this.serviceParameterList = serviceParameterList;
    }
}

export function encodeServiceDetailRes(w: BitWriter, msg: ServiceDetailRes): void {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.responseCode, 6)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.serviceID));
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    encodeServiceParameterListType(w, msg.serviceParameterList)
    w.writeBits(0, 1)   // element EE
}

export function decodeServiceDetailRes(r: BitReader): ServiceDetailRes {
    r.readBits(1)   // SE
    const _header = decodeMessageHeaderType(r)
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _responseCode = exiEnum("ResponseCode", ResponseCodeNames, r.readBits(6)) as ResponseCode
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _serviceID = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    const _serviceParameterList = decodeServiceParameterListType(r)
    r.readBits(1)   // element EE
    return new ServiceDetailRes(_header, _responseCode, _serviceID, _serviceParameterList);
}

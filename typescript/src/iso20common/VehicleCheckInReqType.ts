// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { EvCheckInStatus, EvCheckInStatusNames } from "./EvCheckInStatus.ts";
import { MessageHeaderType, encodeMessageHeaderType, decodeMessageHeaderType } from "./MessageHeaderType.ts";
import { ParkingMethod, ParkingMethodNames } from "./ParkingMethod.ts";
import { V2GRequestType } from "./V2GRequestType.ts";

export class VehicleCheckInReqType extends V2GRequestType {
    readonly eVCheckInStatus: EvCheckInStatus;
    readonly parkingMethod: ParkingMethod;
    readonly vehicleFrame: number | null;
    readonly deviceOffset: number | null;
    readonly vehicleTravel: number | null;

    constructor(header: MessageHeaderType, eVCheckInStatus: EvCheckInStatus, parkingMethod: ParkingMethod, vehicleFrame: number | null, deviceOffset: number | null, vehicleTravel: number | null) {
        super(header);
        this.eVCheckInStatus = eVCheckInStatus;
        this.parkingMethod = parkingMethod;
        this.vehicleFrame = vehicleFrame;
        this.deviceOffset = deviceOffset;
        this.vehicleTravel = vehicleTravel;
    }
}

export function encodeVehicleCheckInReqType(w: BitWriter, msg: VehicleCheckInReqType): void {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.eVCheckInStatus, 2)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.parkingMethod, 2)
    w.writeBits(0, 1)   // child EE
    let st55 = 0
    let done55 = false
    while (!done55) {
        { const _c56 = st55; if (false) {}   // dispatch
            else if (_c56 === 0) {
                if (msg.vehicleFrame != null) {
                    w.writeBits(0, 3)   // VehicleFrame
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.vehicleFrame!));
                    w.writeBits(0, 1)   // child EE
                    st55 = 1
                } else if (msg.deviceOffset != null) {
                    w.writeBits(1, 3)   // DeviceOffset
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.deviceOffset!));
                    w.writeBits(0, 1)   // child EE
                    st55 = 2
                } else if (msg.vehicleTravel != null) {
                    w.writeBits(2, 3)   // VehicleTravel
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.vehicleTravel!));
                    w.writeBits(0, 1)   // child EE
                    st55 = 3
                } else {
                    w.writeBits(3, 3)   // element EE
                    done55 = true
                }
            }
            else if (_c56 === 1) {
                if (msg.deviceOffset != null) {
                    w.writeBits(0, 2)   // DeviceOffset
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.deviceOffset!));
                    w.writeBits(0, 1)   // child EE
                    st55 = 2
                } else if (msg.vehicleTravel != null) {
                    w.writeBits(1, 2)   // VehicleTravel
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.vehicleTravel!));
                    w.writeBits(0, 1)   // child EE
                    st55 = 3
                } else {
                    w.writeBits(2, 2)   // element EE
                    done55 = true
                }
            }
            else if (_c56 === 2) {
                if (msg.vehicleTravel != null) {
                    w.writeBits(0, 2)   // VehicleTravel
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.vehicleTravel!));
                    w.writeBits(0, 1)   // child EE
                    st55 = 3
                } else {
                    w.writeBits(1, 2)   // element EE
                    done55 = true
                }
            }
            else if (_c56 === 3) {
                w.writeBits(0, 1)   // element EE
                done55 = true
            }
        }
    }
}

export function decodeVehicleCheckInReqType(r: BitReader): VehicleCheckInReqType {
    r.readBits(1)   // SE
    const _header = decodeMessageHeaderType(r)
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _eVCheckInStatus = exiEnum("EvCheckInStatus", EvCheckInStatusNames, r.readBits(2)) as EvCheckInStatus
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _parkingMethod = exiEnum("ParkingMethod", ParkingMethodNames, r.readBits(2)) as ParkingMethod
    r.readBits(1)   // child EE
    let _vehicleFrame: number | null = null
    let _deviceOffset: number | null = null
    let _vehicleTravel: number | null = null
    let st57 = 0
    let done57 = false
    while (!done57) {
        { const _c58 = st57; if (false) {}   // dispatch
            else if (_c58 === 0) {
                { const _c59 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c59 === 0) {
                        r.readBits(1)   // value-start
                        _vehicleFrame = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st57 = 1
                    }
                    else if (_c59 === 1) {
                        r.readBits(1)   // value-start
                        _deviceOffset = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st57 = 2
                    }
                    else if (_c59 === 2) {
                        r.readBits(1)   // value-start
                        _vehicleTravel = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st57 = 3
                    }
                    else if (_c59 === 3) done57 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c58 === 1) {
                { const _c60 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c60 === 0) {
                        r.readBits(1)   // value-start
                        _deviceOffset = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st57 = 2
                    }
                    else if (_c60 === 1) {
                        r.readBits(1)   // value-start
                        _vehicleTravel = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st57 = 3
                    }
                    else if (_c60 === 2) done57 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c58 === 2) {
                { const _c61 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c61 === 0) {
                        r.readBits(1)   // value-start
                        _vehicleTravel = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st57 = 3
                    }
                    else if (_c61 === 1) done57 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c58 === 3) {
                { const _c62 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c62 === 0) done57 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new VehicleCheckInReqType(_header, _eVCheckInStatus, _parkingMethod, _vehicleFrame, _deviceOffset, _vehicleTravel);
}

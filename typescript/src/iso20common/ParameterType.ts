// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";
import { RationalNumberType, encodeRationalNumberType, decodeRationalNumberType } from "./RationalNumberType.ts";

export class ParameterType {
    readonly name: string;
    readonly boolValue: boolean | null;
    readonly byteValue: number | null;
    readonly shortValue: number | null;
    readonly intValue: number | null;
    readonly rationalNumber: RationalNumberType | null;
    readonly finiteString: string | null;

    constructor(name: string, boolValue: boolean | null, byteValue: number | null, shortValue: number | null, intValue: number | null, rationalNumber: RationalNumberType | null, finiteString: string | null) {
        this.name = name;
        this.boolValue = boolValue;
        this.byteValue = byteValue;
        this.shortValue = shortValue;
        this.intValue = intValue;
        this.rationalNumber = rationalNumber;
        this.finiteString = finiteString;
    }
}

export function encodeParameterType(w: BitWriter, msg: ParameterType): void {
    w.writeBits(0, 1)   // AT(required attribute)
    ExiPrimitives.writeStringValue(w, msg.name)
    if (msg.boolValue != null) {
        w.writeBits(0, 3)   // BoolValue
        w.writeBits(0, 1)   // value-start
        w.writeBits(msg.boolValue! ? 1 : 0, 1)
        w.writeBits(0, 1)   // child EE
    } else if (msg.byteValue != null) {
        w.writeBits(1, 3)   // ByteValue
        w.writeBits(0, 1)   // value-start
        w.writeBits((msg.byteValue! - -128), 8)
        w.writeBits(0, 1)   // child EE
    } else if (msg.shortValue != null) {
        w.writeBits(2, 3)   // ShortValue
        w.writeBits(0, 1)   // value-start
        ExiPrimitives.writeSignedInteger(w, BigInt(msg.shortValue!));
        w.writeBits(0, 1)   // child EE
    } else if (msg.intValue != null) {
        w.writeBits(3, 3)   // IntValue
        w.writeBits(0, 1)   // value-start
        ExiPrimitives.writeSignedInteger(w, BigInt(msg.intValue!));
        w.writeBits(0, 1)   // child EE
    } else if (msg.rationalNumber != null) {
        w.writeBits(4, 3)   // RationalNumber
        encodeRationalNumberType(w, msg.rationalNumber!)
    } else if (msg.finiteString != null) {
        w.writeBits(5, 3)   // FiniteString
        w.writeBits(0, 1)   // value-start
        ExiPrimitives.writeStringValue(w, msg.finiteString!)
        w.writeBits(0, 1)   // child EE
    } else {
        throw new TypeError("no choice alternative set for ParameterType")
    }
    w.writeBits(0, 1)   // element EE
}

export function decodeParameterType(r: BitReader): ParameterType {
    r.readBits(1)   // AT(required attribute)
    const _name = ExiPrimitives.readStringValue(r, "Name")
    let _boolValue: boolean | null = null
    let _byteValue: number | null = null
    let _shortValue: number | null = null
    let _intValue: number | null = null
    let _rationalNumber: RationalNumberType | null = null
    let _finiteString: string | null = null
    { const _c77 = r.readBits(3); if (false) {}   // dispatch
        else if (_c77 === 0) {   // BoolValue
            r.readBits(1)   // value-start
            _boolValue = r.readBits(1) !== 0
            r.readBits(1)   // child EE
        }
        else if (_c77 === 1) {   // ByteValue
            r.readBits(1)   // value-start
            _byteValue = (r.readBits(8) + -128)
            r.readBits(1)   // child EE
        }
        else if (_c77 === 2) {   // ShortValue
            r.readBits(1)   // value-start
            _shortValue = Number(ExiPrimitives.readSignedInteger(r))
            r.readBits(1)   // child EE
        }
        else if (_c77 === 3) {   // IntValue
            r.readBits(1)   // value-start
            _intValue = Number(ExiPrimitives.readSignedInteger(r))
            r.readBits(1)   // child EE
        }
        else if (_c77 === 4) {   // RationalNumber
            _rationalNumber = decodeRationalNumberType(r)
        }
        else if (_c77 === 5) {   // FiniteString
            r.readBits(1)   // value-start
            _finiteString = ExiPrimitives.readStringValue(r, "FiniteString")
            r.readBits(1)   // child EE
        }
        else throw ExiError.invalidEventCode("choice");
    }
    r.readBits(1)   // element EE
    return new ParameterType(_name, _boolValue, _byteValue, _shortValue, _intValue, _rationalNumber, _finiteString);
}

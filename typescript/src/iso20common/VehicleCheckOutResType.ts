// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, exiEnum } from "../runtime/index.ts";
import { EvseCheckOutStatus, EvseCheckOutStatusNames } from "./EvseCheckOutStatus.ts";
import { MessageHeaderType, encodeMessageHeaderType, decodeMessageHeaderType } from "./MessageHeaderType.ts";
import { ResponseCode, ResponseCodeNames } from "./ResponseCode.ts";
import { V2GResponseType } from "./V2GResponseType.ts";

export class VehicleCheckOutResType extends V2GResponseType {
    readonly eVSECheckOutStatus: EvseCheckOutStatus;

    constructor(header: MessageHeaderType, responseCode: ResponseCode, eVSECheckOutStatus: EvseCheckOutStatus) {
        super(header, responseCode);
        this.eVSECheckOutStatus = eVSECheckOutStatus;
    }
}

export function encodeVehicleCheckOutResType(w: BitWriter, msg: VehicleCheckOutResType): void {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.responseCode, 6)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.eVSECheckOutStatus, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

export function decodeVehicleCheckOutResType(r: BitReader): VehicleCheckOutResType {
    r.readBits(1)   // SE
    const _header = decodeMessageHeaderType(r)
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _responseCode = exiEnum("ResponseCode", ResponseCodeNames, r.readBits(6)) as ResponseCode
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _eVSECheckOutStatus = exiEnum("EvseCheckOutStatus", EvseCheckOutStatusNames, r.readBits(1)) as EvseCheckOutStatus
    r.readBits(1)   // child EE
    r.readBits(1)   // element EE
    return new VehicleCheckOutResType(_header, _responseCode, _eVSECheckOutStatus);
}

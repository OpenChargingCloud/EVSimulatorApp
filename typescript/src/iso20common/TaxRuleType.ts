// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";
import { RationalNumberType, encodeRationalNumberType, decodeRationalNumberType } from "./RationalNumberType.ts";

export class TaxRuleType {
    readonly taxRuleID: number;
    readonly taxRuleName: string | null;
    readonly taxRate: RationalNumberType;
    readonly taxIncludedInPrice: boolean | null;
    readonly appliesToEnergyFee: boolean;
    readonly appliesToParkingFee: boolean;
    readonly appliesToOverstayFee: boolean;
    readonly appliesMinimumMaximumCost: boolean;

    constructor(taxRuleID: number, taxRuleName: string | null, taxRate: RationalNumberType, taxIncludedInPrice: boolean | null, appliesToEnergyFee: boolean, appliesToParkingFee: boolean, appliesToOverstayFee: boolean, appliesMinimumMaximumCost: boolean) {
        this.taxRuleID = taxRuleID;
        this.taxRuleName = taxRuleName;
        this.taxRate = taxRate;
        this.taxIncludedInPrice = taxIncludedInPrice;
        this.appliesToEnergyFee = appliesToEnergyFee;
        this.appliesToParkingFee = appliesToParkingFee;
        this.appliesToOverstayFee = appliesToOverstayFee;
        this.appliesMinimumMaximumCost = appliesMinimumMaximumCost;
    }
}

export function encodeTaxRuleType(w: BitWriter, msg: TaxRuleType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.taxRuleID));
    w.writeBits(0, 1)   // child EE
    let st183 = 0
    let done183 = false
    while (!done183) {
        { const _c184 = st183; if (false) {}   // dispatch
            else if (_c184 === 0) {
                if (msg.taxRuleName != null) {
                    w.writeBits(0, 2)   // TaxRuleName
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeStringValue(w, msg.taxRuleName!)
                    w.writeBits(0, 1)   // child EE
                    st183 = 1
                } else {
                    w.writeBits(1, 2)   // SE(TaxRate)
                    encodeRationalNumberType(w, msg.taxRate)
                    done183 = true
                }
            }
            else if (_c184 === 1) {
                w.writeBits(0, 1)   // SE(TaxRate)
                encodeRationalNumberType(w, msg.taxRate)
                done183 = true
            }
        }
    }
    let st185 = 0
    let done185 = false
    while (!done185) {
        { const _c186 = st185; if (false) {}   // dispatch
            else if (_c186 === 0) {
                if (msg.taxIncludedInPrice != null) {
                    w.writeBits(0, 2)   // TaxIncludedInPrice
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.taxIncludedInPrice! ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    st185 = 1
                } else {
                    w.writeBits(1, 2)   // SE(AppliesToEnergyFee)
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.appliesToEnergyFee ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    done185 = true
                }
            }
            else if (_c186 === 1) {
                w.writeBits(0, 1)   // SE(AppliesToEnergyFee)
                w.writeBits(0, 1)   // value-start
                w.writeBits(msg.appliesToEnergyFee ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                done185 = true
            }
        }
    }
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.appliesToParkingFee ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.appliesToOverstayFee ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.appliesMinimumMaximumCost ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

export function decodeTaxRuleType(r: BitReader): TaxRuleType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _taxRuleID = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    let _taxRuleName: string | null = null
    let _taxRate: RationalNumberType = null
    let st187 = 0
    let done187 = false
    while (!done187) {
        { const _c188 = st187; if (false) {}   // dispatch
            else if (_c188 === 0) {
                { const _c189 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c189 === 0) {
                        r.readBits(1)   // value-start
                        _taxRuleName = ExiPrimitives.readStringValue(r, "TaxRuleName")
                        r.readBits(1)   // child EE
                        st187 = 1
                    }
                    else if (_c189 === 1) {   // SE(TaxRate)
                        _taxRate = decodeRationalNumberType(r)
                        done187 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c188 === 1) {
                { const _c190 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c190 === 0) {   // SE(TaxRate)
                        _taxRate = decodeRationalNumberType(r)
                        done187 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    let _taxIncludedInPrice: boolean | null = null
    let _appliesToEnergyFee: boolean = null
    let st191 = 0
    let done191 = false
    while (!done191) {
        { const _c192 = st191; if (false) {}   // dispatch
            else if (_c192 === 0) {
                { const _c193 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c193 === 0) {
                        r.readBits(1)   // value-start
                        _taxIncludedInPrice = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        st191 = 1
                    }
                    else if (_c193 === 1) {   // SE(AppliesToEnergyFee)
                        r.readBits(1)   // value-start
                        _appliesToEnergyFee = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        done191 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c192 === 1) {
                { const _c194 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c194 === 0) {   // SE(AppliesToEnergyFee)
                        r.readBits(1)   // value-start
                        _appliesToEnergyFee = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        done191 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _appliesToParkingFee = r.readBits(1) !== 0
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _appliesToOverstayFee = r.readBits(1) !== 0
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _appliesMinimumMaximumCost = r.readBits(1) !== 0
    r.readBits(1)   // child EE
    r.readBits(1)   // element EE
    return new TaxRuleType(_taxRuleID, _taxRuleName, _taxRate!, _taxIncludedInPrice, _appliesToEnergyFee!, _appliesToParkingFee, _appliesToOverstayFee, _appliesMinimumMaximumCost);
}

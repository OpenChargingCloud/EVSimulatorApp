// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { ContractCertificateChainType, encodeContractCertificateChainType, decodeContractCertificateChainType } from "./ContractCertificateChainType.ts";
import { EcdhCurve, EcdhCurveNames } from "./EcdhCurve.ts";

export class SignedInstallationDataType {
    readonly id: string;
    readonly contractCertificateChain: ContractCertificateChainType;
    readonly eCDHCurve: EcdhCurve;
    readonly dHPublicKey: Uint8Array;
    readonly sECP521_EncryptedPrivateKey: Uint8Array | null;
    readonly x448_EncryptedPrivateKey: Uint8Array | null;
    readonly tPM_EncryptedPrivateKey: Uint8Array | null;

    constructor(id: string, contractCertificateChain: ContractCertificateChainType, eCDHCurve: EcdhCurve, dHPublicKey: Uint8Array, sECP521_EncryptedPrivateKey: Uint8Array | null, x448_EncryptedPrivateKey: Uint8Array | null, tPM_EncryptedPrivateKey: Uint8Array | null) {
        this.id = id;
        this.contractCertificateChain = contractCertificateChain;
        this.eCDHCurve = eCDHCurve;
        this.dHPublicKey = dHPublicKey;
        this.sECP521_EncryptedPrivateKey = sECP521_EncryptedPrivateKey;
        this.x448_EncryptedPrivateKey = x448_EncryptedPrivateKey;
        this.tPM_EncryptedPrivateKey = tPM_EncryptedPrivateKey;
    }
}

export function encodeSignedInstallationDataType(w: BitWriter, msg: SignedInstallationDataType): void {
    w.writeBits(0, 1)   // AT(required attribute)
    ExiPrimitives.writeStringValue(w, msg.id)
    w.writeBits(0, 1)   // SE
    encodeContractCertificateChainType(w, msg.contractCertificateChain)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.eCDHCurve, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeBinary(w, msg.dHPublicKey)
    w.writeBits(0, 1)   // child EE
    if (msg.sECP521_EncryptedPrivateKey != null) {
        w.writeBits(0, 2)   // SECP521_EncryptedPrivateKey
        w.writeBits(0, 1)   // value-start
        ExiPrimitives.writeBinary(w, msg.sECP521_EncryptedPrivateKey!)
        w.writeBits(0, 1)   // child EE
    } else if (msg.x448_EncryptedPrivateKey != null) {
        w.writeBits(1, 2)   // X448_EncryptedPrivateKey
        w.writeBits(0, 1)   // value-start
        ExiPrimitives.writeBinary(w, msg.x448_EncryptedPrivateKey!)
        w.writeBits(0, 1)   // child EE
    } else if (msg.tPM_EncryptedPrivateKey != null) {
        w.writeBits(2, 2)   // TPM_EncryptedPrivateKey
        w.writeBits(0, 1)   // value-start
        ExiPrimitives.writeBinary(w, msg.tPM_EncryptedPrivateKey!)
        w.writeBits(0, 1)   // child EE
    } else {
        throw new TypeError("no choice alternative set");
    }
    w.writeBits(0, 1)   // element EE
}

export function decodeSignedInstallationDataType(r: BitReader): SignedInstallationDataType {
    r.readBits(1)   // AT(required attribute)
    const _id = ExiPrimitives.readStringValue(r, "Id")
    r.readBits(1)   // SE
    const _contractCertificateChain = decodeContractCertificateChainType(r)
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _eCDHCurve = exiEnum("EcdhCurve", EcdhCurveNames, r.readBits(1)) as EcdhCurve
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _dHPublicKey = ExiPrimitives.readBinary(r)
    r.readBits(1)   // child EE
    let _sECP521_EncryptedPrivateKey: Uint8Array = null
    let _x448_EncryptedPrivateKey: Uint8Array = null
    let _tPM_EncryptedPrivateKey: Uint8Array = null
    { const _c123 = r.readBits(2); if (false) {}   // dispatch
        else if (_c123 === 0) {   // SECP521_EncryptedPrivateKey
            r.readBits(1)   // value-start
            _sECP521_EncryptedPrivateKey = ExiPrimitives.readBinary(r)
            r.readBits(1)   // child EE
        }
        else if (_c123 === 1) {   // X448_EncryptedPrivateKey
            r.readBits(1)   // value-start
            _x448_EncryptedPrivateKey = ExiPrimitives.readBinary(r)
            r.readBits(1)   // child EE
        }
        else if (_c123 === 2) {   // TPM_EncryptedPrivateKey
            r.readBits(1)   // value-start
            _tPM_EncryptedPrivateKey = ExiPrimitives.readBinary(r)
            r.readBits(1)   // child EE
        }
        else throw ExiError.invalidEventCode("choice");
    }
    r.readBits(1)   // element EE
    return new SignedInstallationDataType(_id, _contractCertificateChain, _eCDHCurve, _dHPublicKey, _sECP521_EncryptedPrivateKey, _x448_EncryptedPrivateKey, _tPM_EncryptedPrivateKey);
}

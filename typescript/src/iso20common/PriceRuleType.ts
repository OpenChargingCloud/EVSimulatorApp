// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";
import { RationalNumberType, encodeRationalNumberType, decodeRationalNumberType } from "./RationalNumberType.ts";

export class PriceRuleType {
    readonly energyFee: RationalNumberType;
    readonly parkingFee: RationalNumberType | null;
    readonly parkingFeePeriod: number | null;
    readonly carbonDioxideEmission: number | null;
    readonly renewableGenerationPercentage: number | null;
    readonly powerRangeStart: RationalNumberType;

    constructor(energyFee: RationalNumberType, parkingFee: RationalNumberType | null, parkingFeePeriod: number | null, carbonDioxideEmission: number | null, renewableGenerationPercentage: number | null, powerRangeStart: RationalNumberType) {
        this.energyFee = energyFee;
        this.parkingFee = parkingFee;
        this.parkingFeePeriod = parkingFeePeriod;
        this.carbonDioxideEmission = carbonDioxideEmission;
        this.renewableGenerationPercentage = renewableGenerationPercentage;
        this.powerRangeStart = powerRangeStart;
    }
}

export function encodePriceRuleType(w: BitWriter, msg: PriceRuleType): void {
    w.writeBits(0, 1)   // SE
    encodeRationalNumberType(w, msg.energyFee)
    let st195 = 0
    let done195 = false
    while (!done195) {
        { const _c196 = st195; if (false) {}   // dispatch
            else if (_c196 === 0) {
                if (msg.parkingFee != null) {
                    w.writeBits(0, 3)   // ParkingFee
                    encodeRationalNumberType(w, msg.parkingFee!)
                    st195 = 1
                } else if (msg.parkingFeePeriod != null) {
                    w.writeBits(1, 3)   // ParkingFeePeriod
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.parkingFeePeriod!));
                    w.writeBits(0, 1)   // child EE
                    st195 = 2
                } else if (msg.carbonDioxideEmission != null) {
                    w.writeBits(2, 3)   // CarbonDioxideEmission
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.carbonDioxideEmission!));
                    w.writeBits(0, 1)   // child EE
                    st195 = 3
                } else if (msg.renewableGenerationPercentage != null) {
                    w.writeBits(3, 3)   // RenewableGenerationPercentage
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.renewableGenerationPercentage!, 8)
                    w.writeBits(0, 1)   // child EE
                    st195 = 4
                } else {
                    w.writeBits(4, 3)   // SE(PowerRangeStart)
                    encodeRationalNumberType(w, msg.powerRangeStart)
                    done195 = true
                }
            }
            else if (_c196 === 1) {
                if (msg.parkingFeePeriod != null) {
                    w.writeBits(0, 3)   // ParkingFeePeriod
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.parkingFeePeriod!));
                    w.writeBits(0, 1)   // child EE
                    st195 = 2
                } else if (msg.carbonDioxideEmission != null) {
                    w.writeBits(1, 3)   // CarbonDioxideEmission
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.carbonDioxideEmission!));
                    w.writeBits(0, 1)   // child EE
                    st195 = 3
                } else if (msg.renewableGenerationPercentage != null) {
                    w.writeBits(2, 3)   // RenewableGenerationPercentage
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.renewableGenerationPercentage!, 8)
                    w.writeBits(0, 1)   // child EE
                    st195 = 4
                } else {
                    w.writeBits(3, 3)   // SE(PowerRangeStart)
                    encodeRationalNumberType(w, msg.powerRangeStart)
                    done195 = true
                }
            }
            else if (_c196 === 2) {
                if (msg.carbonDioxideEmission != null) {
                    w.writeBits(0, 2)   // CarbonDioxideEmission
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.carbonDioxideEmission!));
                    w.writeBits(0, 1)   // child EE
                    st195 = 3
                } else if (msg.renewableGenerationPercentage != null) {
                    w.writeBits(1, 2)   // RenewableGenerationPercentage
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.renewableGenerationPercentage!, 8)
                    w.writeBits(0, 1)   // child EE
                    st195 = 4
                } else {
                    w.writeBits(2, 2)   // SE(PowerRangeStart)
                    encodeRationalNumberType(w, msg.powerRangeStart)
                    done195 = true
                }
            }
            else if (_c196 === 3) {
                if (msg.renewableGenerationPercentage != null) {
                    w.writeBits(0, 2)   // RenewableGenerationPercentage
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.renewableGenerationPercentage!, 8)
                    w.writeBits(0, 1)   // child EE
                    st195 = 4
                } else {
                    w.writeBits(1, 2)   // SE(PowerRangeStart)
                    encodeRationalNumberType(w, msg.powerRangeStart)
                    done195 = true
                }
            }
            else if (_c196 === 4) {
                w.writeBits(0, 1)   // SE(PowerRangeStart)
                encodeRationalNumberType(w, msg.powerRangeStart)
                done195 = true
            }
        }
    }
    w.writeBits(0, 1)   // element EE
}

export function decodePriceRuleType(r: BitReader): PriceRuleType {
    r.readBits(1)   // SE
    const _energyFee = decodeRationalNumberType(r)
    let _parkingFee: RationalNumberType | null = null
    let _parkingFeePeriod: number | null = null
    let _carbonDioxideEmission: number | null = null
    let _renewableGenerationPercentage: number | null = null
    let _powerRangeStart: RationalNumberType = null
    let st197 = 0
    let done197 = false
    while (!done197) {
        { const _c198 = st197; if (false) {}   // dispatch
            else if (_c198 === 0) {
                { const _c199 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c199 === 0) {
                        _parkingFee = decodeRationalNumberType(r)
                        st197 = 1
                    }
                    else if (_c199 === 1) {
                        r.readBits(1)   // value-start
                        _parkingFeePeriod = Number(ExiPrimitives.readUnsignedInteger(r))
                        r.readBits(1)   // child EE
                        st197 = 2
                    }
                    else if (_c199 === 2) {
                        r.readBits(1)   // value-start
                        _carbonDioxideEmission = Number(ExiPrimitives.readUnsignedInteger(r))
                        r.readBits(1)   // child EE
                        st197 = 3
                    }
                    else if (_c199 === 3) {
                        r.readBits(1)   // value-start
                        _renewableGenerationPercentage = r.readBits(8)
                        r.readBits(1)   // child EE
                        st197 = 4
                    }
                    else if (_c199 === 4) {   // SE(PowerRangeStart)
                        _powerRangeStart = decodeRationalNumberType(r)
                        done197 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c198 === 1) {
                { const _c200 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c200 === 0) {
                        r.readBits(1)   // value-start
                        _parkingFeePeriod = Number(ExiPrimitives.readUnsignedInteger(r))
                        r.readBits(1)   // child EE
                        st197 = 2
                    }
                    else if (_c200 === 1) {
                        r.readBits(1)   // value-start
                        _carbonDioxideEmission = Number(ExiPrimitives.readUnsignedInteger(r))
                        r.readBits(1)   // child EE
                        st197 = 3
                    }
                    else if (_c200 === 2) {
                        r.readBits(1)   // value-start
                        _renewableGenerationPercentage = r.readBits(8)
                        r.readBits(1)   // child EE
                        st197 = 4
                    }
                    else if (_c200 === 3) {   // SE(PowerRangeStart)
                        _powerRangeStart = decodeRationalNumberType(r)
                        done197 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c198 === 2) {
                { const _c201 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c201 === 0) {
                        r.readBits(1)   // value-start
                        _carbonDioxideEmission = Number(ExiPrimitives.readUnsignedInteger(r))
                        r.readBits(1)   // child EE
                        st197 = 3
                    }
                    else if (_c201 === 1) {
                        r.readBits(1)   // value-start
                        _renewableGenerationPercentage = r.readBits(8)
                        r.readBits(1)   // child EE
                        st197 = 4
                    }
                    else if (_c201 === 2) {   // SE(PowerRangeStart)
                        _powerRangeStart = decodeRationalNumberType(r)
                        done197 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c198 === 3) {
                { const _c202 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c202 === 0) {
                        r.readBits(1)   // value-start
                        _renewableGenerationPercentage = r.readBits(8)
                        r.readBits(1)   // child EE
                        st197 = 4
                    }
                    else if (_c202 === 1) {   // SE(PowerRangeStart)
                        _powerRangeStart = decodeRationalNumberType(r)
                        done197 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c198 === 4) {
                { const _c203 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c203 === 0) {   // SE(PowerRangeStart)
                        _powerRangeStart = decodeRationalNumberType(r)
                        done197 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    r.readBits(1)   // element EE
    return new PriceRuleType(_energyFee, _parkingFee, _parkingFeePeriod, _carbonDioxideEmission, _renewableGenerationPercentage, _powerRangeStart!);
}

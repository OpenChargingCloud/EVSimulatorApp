// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiPrimitives } from "../runtime/index.ts";

export class PriceLevelScheduleEntryType {
    readonly duration: number;
    readonly priceLevel: number;

    constructor(duration: number, priceLevel: number) {
        this.duration = duration;
        this.priceLevel = priceLevel;
    }
}

export function encodePriceLevelScheduleEntryType(w: BitWriter, msg: PriceLevelScheduleEntryType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.duration));
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.priceLevel, 8)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

export function decodePriceLevelScheduleEntryType(r: BitReader): PriceLevelScheduleEntryType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _duration = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _priceLevel = r.readBits(8)
    r.readBits(1)   // child EE
    r.readBits(1)   // element EE
    return new PriceLevelScheduleEntryType(_duration, _priceLevel);
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { TransformType, encodeTransformType, decodeTransformType } from "./TransformType.ts";

export class TransformsType {
    readonly transform: readonly TransformType[];

    constructor(transform: readonly TransformType[]) {
        this.transform = transform;
    }
}

export function encodeTransformsType(w: BitWriter, msg: TransformsType): void {
    const list = msg.transform
    if (list.length < 1 || list.length > 2147483647) throw new RangeError("list: list size out of schema range");
    for (let i = 0; i < list.length; i++) {
        w.writeBits(0, i === 0 ? 1 : 2);   // SE(item)
        encodeTransformType(w, list[i])
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

export function decodeTransformsType(r: BitReader): TransformsType {
    const list = new Array<TransformType>()
    r.readBits(1)   // SE(item) first
    list.push(decodeTransformType(r));
    while (true) {
        const ec = r.readBits(2)
        if (ec === 1) break;   // element EE
        if (!(ec === 0 && list.length < 2147483647)) throw ExiError.invalidEventCode("repeating element");
        list.push(decodeTransformType(r));
    }
    return new TransformsType(list);
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { MessageHeaderType, encodeMessageHeaderType, decodeMessageHeaderType } from "./MessageHeaderType.ts";
import { ResponseCode, ResponseCodeNames } from "./ResponseCode.ts";
import { V2GResponseType } from "./V2GResponseType.ts";

export class VehicleCheckInRes extends V2GResponseType {
    readonly parkingSpace: number | null;
    readonly deviceLocation: number | null;
    readonly targetDistance: number | null;

    constructor(header: MessageHeaderType, responseCode: ResponseCode, parkingSpace: number | null, deviceLocation: number | null, targetDistance: number | null) {
        super(header, responseCode);
        this.parkingSpace = parkingSpace;
        this.deviceLocation = deviceLocation;
        this.targetDistance = targetDistance;
    }
}

export function encodeVehicleCheckInRes(w: BitWriter, msg: VehicleCheckInRes): void {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.responseCode, 6)
    w.writeBits(0, 1)   // child EE
    let st401 = 0
    let done401 = false
    while (!done401) {
        { const _c402 = st401; if (false) {}   // dispatch
            else if (_c402 === 0) {
                if (msg.parkingSpace != null) {
                    w.writeBits(0, 3)   // ParkingSpace
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.parkingSpace!));
                    w.writeBits(0, 1)   // child EE
                    st401 = 1
                } else if (msg.deviceLocation != null) {
                    w.writeBits(1, 3)   // DeviceLocation
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.deviceLocation!));
                    w.writeBits(0, 1)   // child EE
                    st401 = 2
                } else if (msg.targetDistance != null) {
                    w.writeBits(2, 3)   // TargetDistance
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.targetDistance!));
                    w.writeBits(0, 1)   // child EE
                    st401 = 3
                } else {
                    w.writeBits(3, 3)   // element EE
                    done401 = true
                }
            }
            else if (_c402 === 1) {
                if (msg.deviceLocation != null) {
                    w.writeBits(0, 2)   // DeviceLocation
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.deviceLocation!));
                    w.writeBits(0, 1)   // child EE
                    st401 = 2
                } else if (msg.targetDistance != null) {
                    w.writeBits(1, 2)   // TargetDistance
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.targetDistance!));
                    w.writeBits(0, 1)   // child EE
                    st401 = 3
                } else {
                    w.writeBits(2, 2)   // element EE
                    done401 = true
                }
            }
            else if (_c402 === 2) {
                if (msg.targetDistance != null) {
                    w.writeBits(0, 2)   // TargetDistance
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.targetDistance!));
                    w.writeBits(0, 1)   // child EE
                    st401 = 3
                } else {
                    w.writeBits(1, 2)   // element EE
                    done401 = true
                }
            }
            else if (_c402 === 3) {
                w.writeBits(0, 1)   // element EE
                done401 = true
            }
        }
    }
}

export function decodeVehicleCheckInRes(r: BitReader): VehicleCheckInRes {
    r.readBits(1)   // SE
    const _header = decodeMessageHeaderType(r)
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _responseCode = exiEnum("ResponseCode", ResponseCodeNames, r.readBits(6)) as ResponseCode
    r.readBits(1)   // child EE
    let _parkingSpace: number | null = null
    let _deviceLocation: number | null = null
    let _targetDistance: number | null = null
    let st403 = 0
    let done403 = false
    while (!done403) {
        { const _c404 = st403; if (false) {}   // dispatch
            else if (_c404 === 0) {
                { const _c405 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c405 === 0) {
                        r.readBits(1)   // value-start
                        _parkingSpace = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st403 = 1
                    }
                    else if (_c405 === 1) {
                        r.readBits(1)   // value-start
                        _deviceLocation = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st403 = 2
                    }
                    else if (_c405 === 2) {
                        r.readBits(1)   // value-start
                        _targetDistance = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st403 = 3
                    }
                    else if (_c405 === 3) done403 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c404 === 1) {
                { const _c406 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c406 === 0) {
                        r.readBits(1)   // value-start
                        _deviceLocation = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st403 = 2
                    }
                    else if (_c406 === 1) {
                        r.readBits(1)   // value-start
                        _targetDistance = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st403 = 3
                    }
                    else if (_c406 === 2) done403 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c404 === 2) {
                { const _c407 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c407 === 0) {
                        r.readBits(1)   // value-start
                        _targetDistance = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st403 = 3
                    }
                    else if (_c407 === 1) done403 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c404 === 3) {
                { const _c408 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c408 === 0) done403 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new VehicleCheckInRes(_header, _responseCode, _parkingSpace, _deviceLocation, _targetDistance);
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { PriceLevelScheduleEntryType, encodePriceLevelScheduleEntryType, decodePriceLevelScheduleEntryType } from "./PriceLevelScheduleEntryType.ts";

export class PriceLevelScheduleEntryListType {
    readonly priceLevelScheduleEntry: readonly PriceLevelScheduleEntryType[];

    constructor(priceLevelScheduleEntry: readonly PriceLevelScheduleEntryType[]) {
        this.priceLevelScheduleEntry = priceLevelScheduleEntry;
    }
}

export function encodePriceLevelScheduleEntryListType(w: BitWriter, msg: PriceLevelScheduleEntryListType): void {
    const list = msg.priceLevelScheduleEntry
    if (list.length < 1 || list.length > 1024) throw new RangeError("list: list size out of schema range");
    for (let i = 0; i < list.length; i++) {
        w.writeBits(0, i === 0 ? 1 : 2);   // SE(item)
        encodePriceLevelScheduleEntryType(w, list[i])
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

export function decodePriceLevelScheduleEntryListType(r: BitReader): PriceLevelScheduleEntryListType {
    const list = new Array<PriceLevelScheduleEntryType>()
    r.readBits(1)   // SE(item) first
    list.push(decodePriceLevelScheduleEntryType(r));
    while (true) {
        const ec = r.readBits(2)
        if (ec === 1) break;   // element EE
        if (!(ec === 0 && list.length < 1024)) throw ExiError.invalidEventCode("repeating element");
        list.push(decodePriceLevelScheduleEntryType(r));
    }
    return new PriceLevelScheduleEntryListType(list);
}

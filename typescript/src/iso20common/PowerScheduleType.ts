// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";
import { PowerScheduleEntryListType, encodePowerScheduleEntryListType, decodePowerScheduleEntryListType } from "./PowerScheduleEntryListType.ts";
import { RationalNumberType, encodeRationalNumberType, decodeRationalNumberType } from "./RationalNumberType.ts";

export class PowerScheduleType {
    readonly timeAnchor: bigint;
    readonly availableEnergy: RationalNumberType | null;
    readonly powerTolerance: RationalNumberType | null;
    readonly powerScheduleEntries: PowerScheduleEntryListType;

    constructor(timeAnchor: bigint, availableEnergy: RationalNumberType | null, powerTolerance: RationalNumberType | null, powerScheduleEntries: PowerScheduleEntryListType) {
        this.timeAnchor = timeAnchor;
        this.availableEnergy = availableEnergy;
        this.powerTolerance = powerTolerance;
        this.powerScheduleEntries = powerScheduleEntries;
    }
}

export function encodePowerScheduleType(w: BitWriter, msg: PowerScheduleType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, msg.timeAnchor);
    w.writeBits(0, 1)   // child EE
    let st130 = 0
    let done130 = false
    while (!done130) {
        { const _c131 = st130; if (false) {}   // dispatch
            else if (_c131 === 0) {
                if (msg.availableEnergy != null) {
                    w.writeBits(0, 2)   // AvailableEnergy
                    encodeRationalNumberType(w, msg.availableEnergy!)
                    st130 = 1
                } else if (msg.powerTolerance != null) {
                    w.writeBits(1, 2)   // PowerTolerance
                    encodeRationalNumberType(w, msg.powerTolerance!)
                    st130 = 2
                } else {
                    w.writeBits(2, 2)   // SE(PowerScheduleEntries)
                    encodePowerScheduleEntryListType(w, msg.powerScheduleEntries)
                    done130 = true
                }
            }
            else if (_c131 === 1) {
                if (msg.powerTolerance != null) {
                    w.writeBits(0, 2)   // PowerTolerance
                    encodeRationalNumberType(w, msg.powerTolerance!)
                    st130 = 2
                } else {
                    w.writeBits(1, 2)   // SE(PowerScheduleEntries)
                    encodePowerScheduleEntryListType(w, msg.powerScheduleEntries)
                    done130 = true
                }
            }
            else if (_c131 === 2) {
                w.writeBits(0, 1)   // SE(PowerScheduleEntries)
                encodePowerScheduleEntryListType(w, msg.powerScheduleEntries)
                done130 = true
            }
        }
    }
    w.writeBits(0, 1)   // element EE
}

export function decodePowerScheduleType(r: BitReader): PowerScheduleType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _timeAnchor = ExiPrimitives.readUnsignedInteger(r)
    r.readBits(1)   // child EE
    let _availableEnergy: RationalNumberType | null = null
    let _powerTolerance: RationalNumberType | null = null
    let _powerScheduleEntries: PowerScheduleEntryListType = null
    let st132 = 0
    let done132 = false
    while (!done132) {
        { const _c133 = st132; if (false) {}   // dispatch
            else if (_c133 === 0) {
                { const _c134 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c134 === 0) {
                        _availableEnergy = decodeRationalNumberType(r)
                        st132 = 1
                    }
                    else if (_c134 === 1) {
                        _powerTolerance = decodeRationalNumberType(r)
                        st132 = 2
                    }
                    else if (_c134 === 2) {   // SE(PowerScheduleEntries)
                        _powerScheduleEntries = decodePowerScheduleEntryListType(r)
                        done132 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c133 === 1) {
                { const _c135 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c135 === 0) {
                        _powerTolerance = decodeRationalNumberType(r)
                        st132 = 2
                    }
                    else if (_c135 === 1) {   // SE(PowerScheduleEntries)
                        _powerScheduleEntries = decodePowerScheduleEntryListType(r)
                        done132 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c133 === 2) {
                { const _c136 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c136 === 0) {   // SE(PowerScheduleEntries)
                        _powerScheduleEntries = decodePowerScheduleEntryListType(r)
                        done132 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    r.readBits(1)   // element EE
    return new PowerScheduleType(_timeAnchor, _availableEnergy, _powerTolerance, _powerScheduleEntries!);
}

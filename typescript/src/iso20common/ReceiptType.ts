// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";
import { DetailedCostType, encodeDetailedCostType, decodeDetailedCostType } from "./DetailedCostType.ts";
import { DetailedTaxType, encodeDetailedTaxType, decodeDetailedTaxType } from "./DetailedTaxType.ts";

export class ReceiptType {
    readonly timeAnchor: bigint;
    readonly energyCosts: DetailedCostType | null;
    readonly occupancyCosts: DetailedCostType | null;
    readonly additionalServicesCosts: DetailedCostType | null;
    readonly overstayCosts: DetailedCostType | null;
    readonly taxCosts: readonly DetailedTaxType[];

    constructor(timeAnchor: bigint, energyCosts: DetailedCostType | null, occupancyCosts: DetailedCostType | null, additionalServicesCosts: DetailedCostType | null, overstayCosts: DetailedCostType | null, taxCosts: readonly DetailedTaxType[]) {
        this.timeAnchor = timeAnchor;
        this.energyCosts = energyCosts;
        this.occupancyCosts = occupancyCosts;
        this.additionalServicesCosts = additionalServicesCosts;
        this.overstayCosts = overstayCosts;
        this.taxCosts = taxCosts;
    }
}

export function encodeReceiptType(w: BitWriter, msg: ReceiptType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, msg.timeAnchor);
    w.writeBits(0, 1)   // child EE
    let st268 = 0
    let done268 = false
    while (!done268) {
        { const _c269 = st268; if (false) {}   // dispatch
            else if (_c269 === 0) {
                if (msg.energyCosts != null) {
                    w.writeBits(0, 3)   // EnergyCosts
                    encodeDetailedCostType(w, msg.energyCosts!)
                    st268 = 1
                } else if (msg.occupancyCosts != null) {
                    w.writeBits(1, 3)   // OccupancyCosts
                    encodeDetailedCostType(w, msg.occupancyCosts!)
                    st268 = 2
                } else if (msg.additionalServicesCosts != null) {
                    w.writeBits(2, 3)   // AdditionalServicesCosts
                    encodeDetailedCostType(w, msg.additionalServicesCosts!)
                    st268 = 3
                } else if (msg.overstayCosts != null) {
                    w.writeBits(3, 3)   // OverstayCosts
                    encodeDetailedCostType(w, msg.overstayCosts!)
                    st268 = 4
                } else if (msg.taxCosts.length > 0) {
                    if (msg.taxCosts.length < 0 || msg.taxCosts.length > 10) throw new RangeError("msg.taxCosts: list size out of schema range");
                    w.writeBits(4, 3)   // TaxCosts
                    encodeDetailedTaxType(w, msg.taxCosts[0])
                    for (let ci = 1; ci < msg.taxCosts.length; ci++) {
                        w.writeBits(0, 2)   // TaxCosts
                        encodeDetailedTaxType(w, msg.taxCosts[ci])
                    }
                    w.writeBits(1, 2)   // element EE (list end)
                    done268 = true
                } else {
                    w.writeBits(5, 3)   // element EE
                    done268 = true
                }
            }
            else if (_c269 === 1) {
                if (msg.occupancyCosts != null) {
                    w.writeBits(0, 3)   // OccupancyCosts
                    encodeDetailedCostType(w, msg.occupancyCosts!)
                    st268 = 2
                } else if (msg.additionalServicesCosts != null) {
                    w.writeBits(1, 3)   // AdditionalServicesCosts
                    encodeDetailedCostType(w, msg.additionalServicesCosts!)
                    st268 = 3
                } else if (msg.overstayCosts != null) {
                    w.writeBits(2, 3)   // OverstayCosts
                    encodeDetailedCostType(w, msg.overstayCosts!)
                    st268 = 4
                } else if (msg.taxCosts.length > 0) {
                    if (msg.taxCosts.length < 0 || msg.taxCosts.length > 10) throw new RangeError("msg.taxCosts: list size out of schema range");
                    w.writeBits(3, 3)   // TaxCosts
                    encodeDetailedTaxType(w, msg.taxCosts[0])
                    for (let ci = 1; ci < msg.taxCosts.length; ci++) {
                        w.writeBits(0, 2)   // TaxCosts
                        encodeDetailedTaxType(w, msg.taxCosts[ci])
                    }
                    w.writeBits(1, 2)   // element EE (list end)
                    done268 = true
                } else {
                    w.writeBits(4, 3)   // element EE
                    done268 = true
                }
            }
            else if (_c269 === 2) {
                if (msg.additionalServicesCosts != null) {
                    w.writeBits(0, 3)   // AdditionalServicesCosts
                    encodeDetailedCostType(w, msg.additionalServicesCosts!)
                    st268 = 3
                } else if (msg.overstayCosts != null) {
                    w.writeBits(1, 3)   // OverstayCosts
                    encodeDetailedCostType(w, msg.overstayCosts!)
                    st268 = 4
                } else if (msg.taxCosts.length > 0) {
                    if (msg.taxCosts.length < 0 || msg.taxCosts.length > 10) throw new RangeError("msg.taxCosts: list size out of schema range");
                    w.writeBits(2, 3)   // TaxCosts
                    encodeDetailedTaxType(w, msg.taxCosts[0])
                    for (let ci = 1; ci < msg.taxCosts.length; ci++) {
                        w.writeBits(0, 2)   // TaxCosts
                        encodeDetailedTaxType(w, msg.taxCosts[ci])
                    }
                    w.writeBits(1, 2)   // element EE (list end)
                    done268 = true
                } else {
                    w.writeBits(3, 3)   // element EE
                    done268 = true
                }
            }
            else if (_c269 === 3) {
                if (msg.overstayCosts != null) {
                    w.writeBits(0, 2)   // OverstayCosts
                    encodeDetailedCostType(w, msg.overstayCosts!)
                    st268 = 4
                } else if (msg.taxCosts.length > 0) {
                    if (msg.taxCosts.length < 0 || msg.taxCosts.length > 10) throw new RangeError("msg.taxCosts: list size out of schema range");
                    w.writeBits(1, 2)   // TaxCosts
                    encodeDetailedTaxType(w, msg.taxCosts[0])
                    for (let ci = 1; ci < msg.taxCosts.length; ci++) {
                        w.writeBits(0, 2)   // TaxCosts
                        encodeDetailedTaxType(w, msg.taxCosts[ci])
                    }
                    w.writeBits(1, 2)   // element EE (list end)
                    done268 = true
                } else {
                    w.writeBits(2, 2)   // element EE
                    done268 = true
                }
            }
            else if (_c269 === 4) {
                if (msg.taxCosts.length > 0) {
                    if (msg.taxCosts.length < 0 || msg.taxCosts.length > 10) throw new RangeError("msg.taxCosts: list size out of schema range");
                    w.writeBits(0, 2)   // TaxCosts
                    encodeDetailedTaxType(w, msg.taxCosts[0])
                    for (let ci = 1; ci < msg.taxCosts.length; ci++) {
                        w.writeBits(0, 2)   // TaxCosts
                        encodeDetailedTaxType(w, msg.taxCosts[ci])
                    }
                    w.writeBits(1, 2)   // element EE (list end)
                    done268 = true
                } else {
                    w.writeBits(1, 2)   // element EE
                    done268 = true
                }
            }
            else if (_c269 === 5) {
                w.writeBits(0, 1)   // element EE
                done268 = true
            }
        }
    }
}

export function decodeReceiptType(r: BitReader): ReceiptType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _timeAnchor = ExiPrimitives.readUnsignedInteger(r)
    r.readBits(1)   // child EE
    let _energyCosts: DetailedCostType | null = null
    let _occupancyCosts: DetailedCostType | null = null
    let _additionalServicesCosts: DetailedCostType | null = null
    let _overstayCosts: DetailedCostType | null = null
    const taxCostsList = new Array<DetailedTaxType>()
    let st270 = 0
    let done270 = false
    while (!done270) {
        { const _c271 = st270; if (false) {}   // dispatch
            else if (_c271 === 0) {
                { const _c272 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c272 === 0) {
                        _energyCosts = decodeDetailedCostType(r)
                        st270 = 1
                    }
                    else if (_c272 === 1) {
                        _occupancyCosts = decodeDetailedCostType(r)
                        st270 = 2
                    }
                    else if (_c272 === 2) {
                        _additionalServicesCosts = decodeDetailedCostType(r)
                        st270 = 3
                    }
                    else if (_c272 === 3) {
                        _overstayCosts = decodeDetailedCostType(r)
                        st270 = 4
                    }
                    else if (_c272 === 4) {   // TaxCosts
                        taxCostsList.push(decodeDetailedTaxType(r));
                        while (true) {
                            const lc = r.readBits(2)
                            if (lc === 1) break;   // element EE (list end)
                            if (!(lc === 0 && taxCostsList.length < 10)) throw ExiError.invalidEventCode("repeating element");
                            taxCostsList.push(decodeDetailedTaxType(r));
                        }
                        done270 = true
                    }
                    else if (_c272 === 5) done270 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c271 === 1) {
                { const _c273 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c273 === 0) {
                        _occupancyCosts = decodeDetailedCostType(r)
                        st270 = 2
                    }
                    else if (_c273 === 1) {
                        _additionalServicesCosts = decodeDetailedCostType(r)
                        st270 = 3
                    }
                    else if (_c273 === 2) {
                        _overstayCosts = decodeDetailedCostType(r)
                        st270 = 4
                    }
                    else if (_c273 === 3) {   // TaxCosts
                        taxCostsList.push(decodeDetailedTaxType(r));
                        while (true) {
                            const lc = r.readBits(2)
                            if (lc === 1) break;   // element EE (list end)
                            if (!(lc === 0 && taxCostsList.length < 10)) throw ExiError.invalidEventCode("repeating element");
                            taxCostsList.push(decodeDetailedTaxType(r));
                        }
                        done270 = true
                    }
                    else if (_c273 === 4) done270 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c271 === 2) {
                { const _c274 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c274 === 0) {
                        _additionalServicesCosts = decodeDetailedCostType(r)
                        st270 = 3
                    }
                    else if (_c274 === 1) {
                        _overstayCosts = decodeDetailedCostType(r)
                        st270 = 4
                    }
                    else if (_c274 === 2) {   // TaxCosts
                        taxCostsList.push(decodeDetailedTaxType(r));
                        while (true) {
                            const lc = r.readBits(2)
                            if (lc === 1) break;   // element EE (list end)
                            if (!(lc === 0 && taxCostsList.length < 10)) throw ExiError.invalidEventCode("repeating element");
                            taxCostsList.push(decodeDetailedTaxType(r));
                        }
                        done270 = true
                    }
                    else if (_c274 === 3) done270 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c271 === 3) {
                { const _c275 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c275 === 0) {
                        _overstayCosts = decodeDetailedCostType(r)
                        st270 = 4
                    }
                    else if (_c275 === 1) {   // TaxCosts
                        taxCostsList.push(decodeDetailedTaxType(r));
                        while (true) {
                            const lc = r.readBits(2)
                            if (lc === 1) break;   // element EE (list end)
                            if (!(lc === 0 && taxCostsList.length < 10)) throw ExiError.invalidEventCode("repeating element");
                            taxCostsList.push(decodeDetailedTaxType(r));
                        }
                        done270 = true
                    }
                    else if (_c275 === 2) done270 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c271 === 4) {
                { const _c276 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c276 === 0) {   // TaxCosts
                        taxCostsList.push(decodeDetailedTaxType(r));
                        while (true) {
                            const lc = r.readBits(2)
                            if (lc === 1) break;   // element EE (list end)
                            if (!(lc === 0 && taxCostsList.length < 10)) throw ExiError.invalidEventCode("repeating element");
                            taxCostsList.push(decodeDetailedTaxType(r));
                        }
                        done270 = true
                    }
                    else if (_c276 === 1) done270 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c271 === 5) {
                { const _c277 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c277 === 0) done270 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new ReceiptType(_timeAnchor, _energyCosts, _occupancyCosts, _additionalServicesCosts, _overstayCosts, taxCostsList);
}

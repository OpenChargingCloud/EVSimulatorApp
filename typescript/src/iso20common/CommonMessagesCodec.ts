// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { AbsolutePriceScheduleType, encodeAbsolutePriceScheduleType, decodeAbsolutePriceScheduleType } from "./AbsolutePriceScheduleType.ts";
import { AuthorizationReq, encodeAuthorizationReq, decodeAuthorizationReq } from "./AuthorizationReq.ts";
import { AuthorizationRes, encodeAuthorizationRes, decodeAuthorizationRes } from "./AuthorizationRes.ts";
import { AuthorizationSetupReq, encodeAuthorizationSetupReq, decodeAuthorizationSetupReq } from "./AuthorizationSetupReq.ts";
import { AuthorizationSetupRes, encodeAuthorizationSetupRes, decodeAuthorizationSetupRes } from "./AuthorizationSetupRes.ts";
import { CertificateInstallationReq, encodeCertificateInstallationReq, decodeCertificateInstallationReq } from "./CertificateInstallationReq.ts";
import { CertificateInstallationReqType, encodeCertificateInstallationReqType, decodeCertificateInstallationReqType } from "./CertificateInstallationReqType.ts";
import { CertificateInstallationRes, encodeCertificateInstallationRes, decodeCertificateInstallationRes } from "./CertificateInstallationRes.ts";
import { MeteringConfirmationReq, encodeMeteringConfirmationReq, decodeMeteringConfirmationReq } from "./MeteringConfirmationReq.ts";
import { MeteringConfirmationReqType, encodeMeteringConfirmationReqType, decodeMeteringConfirmationReqType } from "./MeteringConfirmationReqType.ts";
import { MeteringConfirmationRes, encodeMeteringConfirmationRes, decodeMeteringConfirmationRes } from "./MeteringConfirmationRes.ts";
import { PnC_AReqAuthorizationModeType, encodePnC_AReqAuthorizationModeType, decodePnC_AReqAuthorizationModeType } from "./PnC_AReqAuthorizationModeType.ts";
import { PowerDeliveryReq, encodePowerDeliveryReq, decodePowerDeliveryReq } from "./PowerDeliveryReq.ts";
import { PowerDeliveryRes, encodePowerDeliveryRes, decodePowerDeliveryRes } from "./PowerDeliveryRes.ts";
import { ScheduleExchangeReq, encodeScheduleExchangeReq, decodeScheduleExchangeReq } from "./ScheduleExchangeReq.ts";
import { ScheduleExchangeRes, encodeScheduleExchangeRes, decodeScheduleExchangeRes } from "./ScheduleExchangeRes.ts";
import { ServiceDetailReq, encodeServiceDetailReq, decodeServiceDetailReq } from "./ServiceDetailReq.ts";
import { ServiceDetailRes, encodeServiceDetailRes, decodeServiceDetailRes } from "./ServiceDetailRes.ts";
import { ServiceDiscoveryReq, encodeServiceDiscoveryReq, decodeServiceDiscoveryReq } from "./ServiceDiscoveryReq.ts";
import { ServiceDiscoveryRes, encodeServiceDiscoveryRes, decodeServiceDiscoveryRes } from "./ServiceDiscoveryRes.ts";
import { ServiceSelectionReq, encodeServiceSelectionReq, decodeServiceSelectionReq } from "./ServiceSelectionReq.ts";
import { ServiceSelectionRes, encodeServiceSelectionRes, decodeServiceSelectionRes } from "./ServiceSelectionRes.ts";
import { SessionSetupReq, encodeSessionSetupReq, decodeSessionSetupReq } from "./SessionSetupReq.ts";
import { SessionSetupRes, encodeSessionSetupRes, decodeSessionSetupRes } from "./SessionSetupRes.ts";
import { SessionStopReq, encodeSessionStopReq, decodeSessionStopReq } from "./SessionStopReq.ts";
import { SessionStopRes, encodeSessionStopRes, decodeSessionStopRes } from "./SessionStopRes.ts";
import { SignedCertificateChainType, encodeSignedCertificateChainType, decodeSignedCertificateChainType } from "./SignedCertificateChainType.ts";
import { SignedInfoType, encodeSignedInfoType, decodeSignedInfoType } from "./SignedInfoType.ts";
import { SignedInstallationData, encodeSignedInstallationData, decodeSignedInstallationData } from "./SignedInstallationData.ts";
import { SignedInstallationDataType, encodeSignedInstallationDataType, decodeSignedInstallationDataType } from "./SignedInstallationDataType.ts";
import { SignedMeteringData, encodeSignedMeteringData, decodeSignedMeteringData } from "./SignedMeteringData.ts";
import { VehicleCheckInReq, encodeVehicleCheckInReq, decodeVehicleCheckInReq } from "./VehicleCheckInReq.ts";
import { VehicleCheckInRes, encodeVehicleCheckInRes, decodeVehicleCheckInRes } from "./VehicleCheckInRes.ts";
import { VehicleCheckOutReq, encodeVehicleCheckOutReq, decodeVehicleCheckOutReq } from "./VehicleCheckOutReq.ts";
import { VehicleCheckOutRes, encodeVehicleCheckOutRes, decodeVehicleCheckOutRes } from "./VehicleCheckOutRes.ts";

export const CommonMessagesCodec = {

    exiHeader: 0x80,

    encodeSessionSetupReq(msg: SessionSetupReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(35, 6);   // document element selector
        encodeSessionSetupReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeSessionSetupRes(msg: SessionSetupRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(36, 6);   // document element selector
        encodeSessionSetupRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeAuthorizationSetupReq(msg: AuthorizationSetupReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(2, 6);   // document element selector
        encodeAuthorizationSetupReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeAuthorizationSetupRes(msg: AuthorizationSetupRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(3, 6);   // document element selector
        encodeAuthorizationSetupRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeAuthorizationReq(msg: AuthorizationReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(0, 6);   // document element selector
        encodeAuthorizationReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeAuthorizationRes(msg: AuthorizationRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(1, 6);   // document element selector
        encodeAuthorizationRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeServiceDiscoveryReq(msg: ServiceDiscoveryReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(31, 6);   // document element selector
        encodeServiceDiscoveryReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeServiceDiscoveryRes(msg: ServiceDiscoveryRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(32, 6);   // document element selector
        encodeServiceDiscoveryRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeServiceDetailReq(msg: ServiceDetailReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(29, 6);   // document element selector
        encodeServiceDetailReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeServiceDetailRes(msg: ServiceDetailRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(30, 6);   // document element selector
        encodeServiceDetailRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeServiceSelectionReq(msg: ServiceSelectionReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(33, 6);   // document element selector
        encodeServiceSelectionReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeServiceSelectionRes(msg: ServiceSelectionRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(34, 6);   // document element selector
        encodeServiceSelectionRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeScheduleExchangeReq(msg: ScheduleExchangeReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(27, 6);   // document element selector
        encodeScheduleExchangeReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeScheduleExchangeRes(msg: ScheduleExchangeRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(28, 6);   // document element selector
        encodeScheduleExchangeRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodePowerDeliveryReq(msg: PowerDeliveryReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(21, 6);   // document element selector
        encodePowerDeliveryReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodePowerDeliveryRes(msg: PowerDeliveryRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(22, 6);   // document element selector
        encodePowerDeliveryRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeMeteringConfirmationReq(msg: MeteringConfirmationReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(16, 6);   // document element selector
        encodeMeteringConfirmationReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeMeteringConfirmationRes(msg: MeteringConfirmationRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(17, 6);   // document element selector
        encodeMeteringConfirmationRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeSessionStopReq(msg: SessionStopReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(37, 6);   // document element selector
        encodeSessionStopReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeSessionStopRes(msg: SessionStopRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(38, 6);   // document element selector
        encodeSessionStopRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeCertificateInstallationReq(msg: CertificateInstallationReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(7, 6);   // document element selector
        encodeCertificateInstallationReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeCertificateInstallationRes(msg: CertificateInstallationRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(8, 6);   // document element selector
        encodeCertificateInstallationRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeVehicleCheckInReq(msg: VehicleCheckInReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(49, 6);   // document element selector
        encodeVehicleCheckInReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeVehicleCheckInRes(msg: VehicleCheckInRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(50, 6);   // document element selector
        encodeVehicleCheckInRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeVehicleCheckOutReq(msg: VehicleCheckOutReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(51, 6);   // document element selector
        encodeVehicleCheckOutReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeVehicleCheckOutRes(msg: VehicleCheckOutRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(52, 6);   // document element selector
        encodeVehicleCheckOutRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeSignedInstallationData(msg: SignedInstallationData): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(45, 6);   // document element selector
        encodeSignedInstallationData(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeSignedMeteringData(msg: SignedMeteringData): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(46, 6);   // document element selector
        encodeSignedMeteringData(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    /** Encodes any document element of this message set, dispatching on its runtime type. */
    encodeAny(msg: unknown): Uint8Array {
        if (msg instanceof AuthorizationReq) return this.encodeAuthorizationReq(msg);
        if (msg instanceof AuthorizationRes) return this.encodeAuthorizationRes(msg);
        if (msg instanceof AuthorizationSetupReq) return this.encodeAuthorizationSetupReq(msg);
        if (msg instanceof AuthorizationSetupRes) return this.encodeAuthorizationSetupRes(msg);
        if (msg instanceof CertificateInstallationReq) return this.encodeCertificateInstallationReq(msg);
        if (msg instanceof CertificateInstallationRes) return this.encodeCertificateInstallationRes(msg);
        if (msg instanceof MeteringConfirmationReq) return this.encodeMeteringConfirmationReq(msg);
        if (msg instanceof MeteringConfirmationRes) return this.encodeMeteringConfirmationRes(msg);
        if (msg instanceof PowerDeliveryReq) return this.encodePowerDeliveryReq(msg);
        if (msg instanceof PowerDeliveryRes) return this.encodePowerDeliveryRes(msg);
        if (msg instanceof ScheduleExchangeReq) return this.encodeScheduleExchangeReq(msg);
        if (msg instanceof ScheduleExchangeRes) return this.encodeScheduleExchangeRes(msg);
        if (msg instanceof ServiceDetailReq) return this.encodeServiceDetailReq(msg);
        if (msg instanceof ServiceDetailRes) return this.encodeServiceDetailRes(msg);
        if (msg instanceof ServiceDiscoveryReq) return this.encodeServiceDiscoveryReq(msg);
        if (msg instanceof ServiceDiscoveryRes) return this.encodeServiceDiscoveryRes(msg);
        if (msg instanceof ServiceSelectionReq) return this.encodeServiceSelectionReq(msg);
        if (msg instanceof ServiceSelectionRes) return this.encodeServiceSelectionRes(msg);
        if (msg instanceof SessionSetupReq) return this.encodeSessionSetupReq(msg);
        if (msg instanceof SessionSetupRes) return this.encodeSessionSetupRes(msg);
        if (msg instanceof SessionStopReq) return this.encodeSessionStopReq(msg);
        if (msg instanceof SessionStopRes) return this.encodeSessionStopRes(msg);
        if (msg instanceof VehicleCheckInReq) return this.encodeVehicleCheckInReq(msg);
        if (msg instanceof VehicleCheckInRes) return this.encodeVehicleCheckInRes(msg);
        if (msg instanceof VehicleCheckOutReq) return this.encodeVehicleCheckOutReq(msg);
        if (msg instanceof VehicleCheckOutRes) return this.encodeVehicleCheckOutRes(msg);
        if (msg instanceof SignedInstallationData) return this.encodeSignedInstallationData(msg);
        if (msg instanceof SignedMeteringData) return this.encodeSignedMeteringData(msg);
        throw new TypeError(
            `${(msg as object)?.constructor?.name} is not a document element of this message set.`);
    },

    decodeAny(src: Uint8Array): unknown {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        const selector = r.readBits(6);
        switch (selector) {
            case 35: return decodeSessionSetupReq(r);
            case 36: return decodeSessionSetupRes(r);
            case 2: return decodeAuthorizationSetupReq(r);
            case 3: return decodeAuthorizationSetupRes(r);
            case 0: return decodeAuthorizationReq(r);
            case 1: return decodeAuthorizationRes(r);
            case 31: return decodeServiceDiscoveryReq(r);
            case 32: return decodeServiceDiscoveryRes(r);
            case 29: return decodeServiceDetailReq(r);
            case 30: return decodeServiceDetailRes(r);
            case 33: return decodeServiceSelectionReq(r);
            case 34: return decodeServiceSelectionRes(r);
            case 27: return decodeScheduleExchangeReq(r);
            case 28: return decodeScheduleExchangeRes(r);
            case 21: return decodePowerDeliveryReq(r);
            case 22: return decodePowerDeliveryRes(r);
            case 16: return decodeMeteringConfirmationReq(r);
            case 17: return decodeMeteringConfirmationRes(r);
            case 37: return decodeSessionStopReq(r);
            case 38: return decodeSessionStopRes(r);
            case 7: return decodeCertificateInstallationReq(r);
            case 8: return decodeCertificateInstallationRes(r);
            case 49: return decodeVehicleCheckInReq(r);
            case 50: return decodeVehicleCheckInRes(r);
            case 51: return decodeVehicleCheckOutReq(r);
            case 52: return decodeVehicleCheckOutRes(r);
            case 45: return decodeSignedInstallationData(r);
            case 46: return decodeSignedMeteringData(r);
            default: throw ExiError.unknownDocumentIndex(selector);
        }
    },

    encodeFragment_AbsolutePriceSchedule(content: AbsolutePriceScheduleType): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(0, 9);   // fragment SE(AbsolutePriceSchedule)
        encodeAbsolutePriceScheduleType(w, content);
        w.writeBits(282, 9);   // End Fragment (ED)
        w.alignToByte();
        return w.bytes;
    },

    decodeFragment_AbsolutePriceSchedule(src: Uint8Array): AbsolutePriceScheduleType {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        if (r.readBits(9) !== 0) throw ExiError.invalidEventCode("not a AbsolutePriceSchedule fragment");
        const result = decodeAbsolutePriceScheduleType(r);
        if (r.readBits(9) !== 282) throw ExiError.invalidEventCode("missing End Fragment");
        return result;
    },

    encodeFragment_CertificateInstallationReq(content: CertificateInstallationReqType): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(27, 9);   // fragment SE(CertificateInstallationReq)
        encodeCertificateInstallationReqType(w, content);
        w.writeBits(282, 9);   // End Fragment (ED)
        w.alignToByte();
        return w.bytes;
    },

    decodeFragment_CertificateInstallationReq(src: Uint8Array): CertificateInstallationReqType {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        if (r.readBits(9) !== 27) throw ExiError.invalidEventCode("not a CertificateInstallationReq fragment");
        const result = decodeCertificateInstallationReqType(r);
        if (r.readBits(9) !== 282) throw ExiError.invalidEventCode("missing End Fragment");
        return result;
    },

    encodeFragment_MeteringConfirmationReq(content: MeteringConfirmationReqType): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(119, 9);   // fragment SE(MeteringConfirmationReq)
        encodeMeteringConfirmationReqType(w, content);
        w.writeBits(282, 9);   // End Fragment (ED)
        w.alignToByte();
        return w.bytes;
    },

    decodeFragment_MeteringConfirmationReq(src: Uint8Array): MeteringConfirmationReqType {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        if (r.readBits(9) !== 119) throw ExiError.invalidEventCode("not a MeteringConfirmationReq fragment");
        const result = decodeMeteringConfirmationReqType(r);
        if (r.readBits(9) !== 282) throw ExiError.invalidEventCode("missing End Fragment");
        return result;
    },

    encodeFragment_OEMProvisioningCertificateChain(content: SignedCertificateChainType): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(128, 9);   // fragment SE(OEMProvisioningCertificateChain)
        encodeSignedCertificateChainType(w, content);
        w.writeBits(282, 9);   // End Fragment (ED)
        w.alignToByte();
        return w.bytes;
    },

    decodeFragment_OEMProvisioningCertificateChain(src: Uint8Array): SignedCertificateChainType {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        if (r.readBits(9) !== 128) throw ExiError.invalidEventCode("not a OEMProvisioningCertificateChain fragment");
        const result = decodeSignedCertificateChainType(r);
        if (r.readBits(9) !== 282) throw ExiError.invalidEventCode("missing End Fragment");
        return result;
    },

    encodeFragment_PnC_AReqAuthorizationMode(content: PnC_AReqAuthorizationModeType): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(151, 9);   // fragment SE(PnC_AReqAuthorizationMode)
        encodePnC_AReqAuthorizationModeType(w, content);
        w.writeBits(282, 9);   // End Fragment (ED)
        w.alignToByte();
        return w.bytes;
    },

    decodeFragment_PnC_AReqAuthorizationMode(src: Uint8Array): PnC_AReqAuthorizationModeType {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        if (r.readBits(9) !== 151) throw ExiError.invalidEventCode("not a PnC_AReqAuthorizationMode fragment");
        const result = decodePnC_AReqAuthorizationModeType(r);
        if (r.readBits(9) !== 282) throw ExiError.invalidEventCode("missing End Fragment");
        return result;
    },

    encodeFragment_SignedInstallationData(content: SignedInstallationDataType): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(231, 9);   // fragment SE(SignedInstallationData)
        encodeSignedInstallationDataType(w, content);
        w.writeBits(282, 9);   // End Fragment (ED)
        w.alignToByte();
        return w.bytes;
    },

    decodeFragment_SignedInstallationData(src: Uint8Array): SignedInstallationDataType {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        if (r.readBits(9) !== 231) throw ExiError.invalidEventCode("not a SignedInstallationData fragment");
        const result = decodeSignedInstallationDataType(r);
        if (r.readBits(9) !== 282) throw ExiError.invalidEventCode("missing End Fragment");
        return result;
    },

    encodeFragment_SignedInfo(content: SignedInfoType): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(230, 9);   // fragment SE(SignedInfo)
        encodeSignedInfoType(w, content);
        w.writeBits(282, 9);   // End Fragment (ED)
        w.alignToByte();
        return w.bytes;
    },

    decodeFragment_SignedInfo(src: Uint8Array): SignedInfoType {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        if (r.readBits(9) !== 230) throw ExiError.invalidEventCode("not a SignedInfo fragment");
        const result = decodeSignedInfoType(r);
        if (r.readBits(9) !== 282) throw ExiError.invalidEventCode("missing End Fragment");
        return result;
    },

};

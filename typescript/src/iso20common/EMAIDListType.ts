// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";

export class EMAIDListType {
    readonly eMAID: readonly string[];

    constructor(eMAID: readonly string[]) {
        this.eMAID = eMAID;
    }
}

export function encodeEMAIDListType(w: BitWriter, msg: EMAIDListType): void {
    const list = msg.eMAID
    if (list.length < 1 || list.length > 8) throw new RangeError("list: list size out of schema range");
    for (let i = 0; i < list.length; i++) {
        w.writeBits(0, i === 0 ? 1 : 2);   // SE(item)
        w.writeBits(0, 1)   // value-start
        ExiPrimitives.writeStringValue(w, list[i])
        w.writeBits(0, 1)   // child EE
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

export function decodeEMAIDListType(r: BitReader): EMAIDListType {
    const list = new Array<string>()
    r.readBits(1)   // SE(item) first
    r.readBits(1)   // value-start
    const listFirst = ExiPrimitives.readStringValue(r, "EMAID")
    r.readBits(1)   // child EE
    list.push(listFirst);
    while (true) {
        const ec = r.readBits(2)
        if (ec === 1) break;   // element EE
        if (!(ec === 0 && list.length < 8)) throw ExiError.invalidEventCode("repeating element");
        r.readBits(1)   // value-start
        const listNext = ExiPrimitives.readStringValue(r, "EMAID")
        r.readBits(1)   // child EE
        list.push(listNext);
    }
    return new EMAIDListType(list);
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";
import { Dynamic_SMDTControlModeType, encodeDynamic_SMDTControlModeType, decodeDynamic_SMDTControlModeType } from "./Dynamic_SMDTControlModeType.ts";
import { MeterInfoType, encodeMeterInfoType, decodeMeterInfoType } from "./MeterInfoType.ts";
import { ReceiptType, encodeReceiptType, decodeReceiptType } from "./ReceiptType.ts";
import { Scheduled_SMDTControlModeType, encodeScheduled_SMDTControlModeType, decodeScheduled_SMDTControlModeType } from "./Scheduled_SMDTControlModeType.ts";

export class SignedMeteringDataType {
    readonly id: string;
    readonly sessionID: Uint8Array;
    readonly meterInfo: MeterInfoType;
    readonly receipt: ReceiptType | null;
    readonly dynamic_SMDTControlMode: Dynamic_SMDTControlModeType | null;
    readonly scheduled_SMDTControlMode: Scheduled_SMDTControlModeType | null;

    constructor(id: string, sessionID: Uint8Array, meterInfo: MeterInfoType, receipt: ReceiptType | null, dynamic_SMDTControlMode: Dynamic_SMDTControlModeType | null, scheduled_SMDTControlMode: Scheduled_SMDTControlModeType | null) {
        this.id = id;
        this.sessionID = sessionID;
        this.meterInfo = meterInfo;
        this.receipt = receipt;
        this.dynamic_SMDTControlMode = dynamic_SMDTControlMode;
        this.scheduled_SMDTControlMode = scheduled_SMDTControlMode;
    }
}

export function encodeSignedMeteringDataType(w: BitWriter, msg: SignedMeteringDataType): void {
    w.writeBits(0, 1)   // AT(required attribute)
    ExiPrimitives.writeStringValue(w, msg.id)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeBinary(w, msg.sessionID)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    encodeMeterInfoType(w, msg.meterInfo)
    let st124 = 0
    let done124 = false
    while (!done124) {
        { const _c125 = st124; if (false) {}   // dispatch
            else if (_c125 === 0) {
                if (msg.receipt != null) {
                    w.writeBits(0, 2)   // Receipt
                    encodeReceiptType(w, msg.receipt!)
                    st124 = 1
                } else if (msg.dynamic_SMDTControlMode != null) {
                    w.writeBits(1, 2)   // Dynamic_SMDTControlMode
                    encodeDynamic_SMDTControlModeType(w, msg.dynamic_SMDTControlMode!)
                    done124 = true
                } else if (msg.scheduled_SMDTControlMode != null) {
                    w.writeBits(2, 2)   // Scheduled_SMDTControlMode
                    encodeScheduled_SMDTControlModeType(w, msg.scheduled_SMDTControlMode!)
                    done124 = true
                } else {
                    throw new TypeError("no value set for \$InlineChoice")
                }
            }
            else if (_c125 === 1) {
                if (msg.dynamic_SMDTControlMode != null) {
                    w.writeBits(0, 2)   // Dynamic_SMDTControlMode
                    encodeDynamic_SMDTControlModeType(w, msg.dynamic_SMDTControlMode!)
                    done124 = true
                } else if (msg.scheduled_SMDTControlMode != null) {
                    w.writeBits(1, 2)   // Scheduled_SMDTControlMode
                    encodeScheduled_SMDTControlModeType(w, msg.scheduled_SMDTControlMode!)
                    done124 = true
                } else {
                    throw new TypeError("no value set for \$InlineChoice")
                }
            }
        }
    }
    w.writeBits(0, 1)   // element EE
}

export function decodeSignedMeteringDataType(r: BitReader): SignedMeteringDataType {
    r.readBits(1)   // AT(required attribute)
    const _id = ExiPrimitives.readStringValue(r, "Id")
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _sessionID = ExiPrimitives.readBinary(r)
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    const _meterInfo = decodeMeterInfoType(r)
    let _receipt: ReceiptType | null = null
    let _dynamic_SMDTControlMode: Dynamic_SMDTControlModeType = null
    let _scheduled_SMDTControlMode: Scheduled_SMDTControlModeType = null
    let st126 = 0
    let done126 = false
    while (!done126) {
        { const _c127 = st126; if (false) {}   // dispatch
            else if (_c127 === 0) {
                { const _c128 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c128 === 0) {
                        _receipt = decodeReceiptType(r)
                        st126 = 1
                    }
                    else if (_c128 === 1) {   // Dynamic_SMDTControlMode
                        _dynamic_SMDTControlMode = decodeDynamic_SMDTControlModeType(r)
                        done126 = true
                    }
                    else if (_c128 === 2) {   // Scheduled_SMDTControlMode
                        _scheduled_SMDTControlMode = decodeScheduled_SMDTControlModeType(r)
                        done126 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c127 === 1) {
                { const _c129 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c129 === 0) {   // Dynamic_SMDTControlMode
                        _dynamic_SMDTControlMode = decodeDynamic_SMDTControlModeType(r)
                        done126 = true
                    }
                    else if (_c129 === 1) {   // Scheduled_SMDTControlMode
                        _scheduled_SMDTControlMode = decodeScheduled_SMDTControlModeType(r)
                        done126 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    r.readBits(1)   // element EE
    return new SignedMeteringDataType(_id, _sessionID, _meterInfo, _receipt, _dynamic_SMDTControlMode, _scheduled_SMDTControlMode);
}

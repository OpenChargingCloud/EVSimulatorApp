// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiPrimitives } from "../runtime/index.ts";
import { MessageHeaderType, encodeMessageHeaderType, decodeMessageHeaderType } from "./MessageHeaderType.ts";
import { V2GRequestType } from "./V2GRequestType.ts";

export class ServiceDetailReqType extends V2GRequestType {
    readonly serviceID: number;

    constructor(header: MessageHeaderType, serviceID: number) {
        super(header);
        this.serviceID = serviceID;
    }
}

export function encodeServiceDetailReqType(w: BitWriter, msg: ServiceDetailReqType): void {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.serviceID));
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

export function decodeServiceDetailReqType(r: BitReader): ServiceDetailReqType {
    r.readBits(1)   // SE
    const _header = decodeMessageHeaderType(r)
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _serviceID = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    r.readBits(1)   // element EE
    return new ServiceDetailReqType(_header, _serviceID);
}

// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiPrimitives } from "../runtime/index.ts";

export class TargetPositionType {
    readonly targetOffsetX: number;
    readonly targetOffsetY: number;

    constructor(targetOffsetX: number, targetOffsetY: number) {
        this.targetOffsetX = targetOffsetX;
        this.targetOffsetY = targetOffsetY;
    }
}

export function encodeTargetPositionType(w: BitWriter, msg: TargetPositionType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.targetOffsetX));
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.targetOffsetY));
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

export function decodeTargetPositionType(r: BitReader): TargetPositionType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _targetOffsetX = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _targetOffsetY = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    r.readBits(1)   // element EE
    return new TargetPositionType(_targetOffsetX, _targetOffsetY);
}

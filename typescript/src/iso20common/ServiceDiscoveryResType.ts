// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, exiEnum } from "../runtime/index.ts";
import { MessageHeaderType, encodeMessageHeaderType, decodeMessageHeaderType } from "./MessageHeaderType.ts";
import { ResponseCode, ResponseCodeNames } from "./ResponseCode.ts";
import { ServiceListType, encodeServiceListType, decodeServiceListType } from "./ServiceListType.ts";
import { V2GResponseType } from "./V2GResponseType.ts";

export class ServiceDiscoveryResType extends V2GResponseType {
    readonly serviceRenegotiationSupported: boolean;
    readonly energyTransferServiceList: ServiceListType;
    readonly vASList: ServiceListType | null;

    constructor(header: MessageHeaderType, responseCode: ResponseCode, serviceRenegotiationSupported: boolean, energyTransferServiceList: ServiceListType, vASList: ServiceListType | null) {
        super(header, responseCode);
        this.serviceRenegotiationSupported = serviceRenegotiationSupported;
        this.energyTransferServiceList = energyTransferServiceList;
        this.vASList = vASList;
    }
}

export function encodeServiceDiscoveryResType(w: BitWriter, msg: ServiceDiscoveryResType): void {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.responseCode, 6)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.serviceRenegotiationSupported ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    encodeServiceListType(w, msg.energyTransferServiceList)
    let st10 = 0
    let done10 = false
    while (!done10) {
        { const _c11 = st10; if (false) {}   // dispatch
            else if (_c11 === 0) {
                if (msg.vASList != null) {
                    w.writeBits(0, 2)   // VASList
                    encodeServiceListType(w, msg.vASList!)
                    st10 = 1
                } else {
                    w.writeBits(1, 2)   // element EE
                    done10 = true
                }
            }
            else if (_c11 === 1) {
                w.writeBits(0, 1)   // element EE
                done10 = true
            }
        }
    }
}

export function decodeServiceDiscoveryResType(r: BitReader): ServiceDiscoveryResType {
    r.readBits(1)   // SE
    const _header = decodeMessageHeaderType(r)
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _responseCode = exiEnum("ResponseCode", ResponseCodeNames, r.readBits(6)) as ResponseCode
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _serviceRenegotiationSupported = r.readBits(1) !== 0
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    const _energyTransferServiceList = decodeServiceListType(r)
    let _vASList: ServiceListType | null = null
    let st12 = 0
    let done12 = false
    while (!done12) {
        { const _c13 = st12; if (false) {}   // dispatch
            else if (_c13 === 0) {
                { const _c14 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c14 === 0) {
                        _vASList = decodeServiceListType(r)
                        st12 = 1
                    }
                    else if (_c14 === 1) done12 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c13 === 1) {
                { const _c15 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c15 === 0) done12 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new ServiceDiscoveryResType(_header, _responseCode, _serviceRenegotiationSupported, _energyTransferServiceList, _vASList);
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { AdditionalServiceType, encodeAdditionalServiceType, decodeAdditionalServiceType } from "./AdditionalServiceType.ts";

export class AdditionalServiceListType {
    readonly additionalService: readonly AdditionalServiceType[];

    constructor(additionalService: readonly AdditionalServiceType[]) {
        this.additionalService = additionalService;
    }
}

export function encodeAdditionalServiceListType(w: BitWriter, msg: AdditionalServiceListType): void {
    const list = msg.additionalService
    if (list.length < 1 || list.length > 5) throw new RangeError("list: list size out of schema range");
    for (let i = 0; i < list.length; i++) {
        w.writeBits(0, i === 0 ? 1 : 2);   // SE(item)
        encodeAdditionalServiceType(w, list[i])
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

export function decodeAdditionalServiceListType(r: BitReader): AdditionalServiceListType {
    const list = new Array<AdditionalServiceType>()
    r.readBits(1)   // SE(item) first
    list.push(decodeAdditionalServiceType(r));
    while (true) {
        const ec = r.readBits(2)
        if (ec === 1) break;   // element EE
        if (!(ec === 0 && list.length < 5)) throw ExiError.invalidEventCode("repeating element");
        list.push(decodeAdditionalServiceType(r));
    }
    return new AdditionalServiceListType(list);
}

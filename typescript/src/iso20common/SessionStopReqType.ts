// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiError, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { ChargingSession, ChargingSessionNames } from "./ChargingSession.ts";
import { MessageHeaderType, encodeMessageHeaderType, decodeMessageHeaderType } from "./MessageHeaderType.ts";
import { V2GRequestType } from "./V2GRequestType.ts";

export class SessionStopReqType extends V2GRequestType {
    readonly chargingSession: ChargingSession;
    readonly eVTerminationCode: string | null;
    readonly eVTerminationExplanation: string | null;

    constructor(header: MessageHeaderType, chargingSession: ChargingSession, eVTerminationCode: string | null, eVTerminationExplanation: string | null) {
        super(header);
        this.chargingSession = chargingSession;
        this.eVTerminationCode = eVTerminationCode;
        this.eVTerminationExplanation = eVTerminationExplanation;
    }
}

export function encodeSessionStopReqType(w: BitWriter, msg: SessionStopReqType): void {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.chargingSession, 2)
    w.writeBits(0, 1)   // child EE
    let st42 = 0
    let done42 = false
    while (!done42) {
        { const _c43 = st42; if (false) {}   // dispatch
            else if (_c43 === 0) {
                if (msg.eVTerminationCode != null) {
                    w.writeBits(0, 2)   // EVTerminationCode
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeStringValue(w, msg.eVTerminationCode!)
                    w.writeBits(0, 1)   // child EE
                    st42 = 1
                } else if (msg.eVTerminationExplanation != null) {
                    w.writeBits(1, 2)   // EVTerminationExplanation
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeStringValue(w, msg.eVTerminationExplanation!)
                    w.writeBits(0, 1)   // child EE
                    st42 = 2
                } else {
                    w.writeBits(2, 2)   // element EE
                    done42 = true
                }
            }
            else if (_c43 === 1) {
                if (msg.eVTerminationExplanation != null) {
                    w.writeBits(0, 2)   // EVTerminationExplanation
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeStringValue(w, msg.eVTerminationExplanation!)
                    w.writeBits(0, 1)   // child EE
                    st42 = 2
                } else {
                    w.writeBits(1, 2)   // element EE
                    done42 = true
                }
            }
            else if (_c43 === 2) {
                w.writeBits(0, 1)   // element EE
                done42 = true
            }
        }
    }
}

export function decodeSessionStopReqType(r: BitReader): SessionStopReqType {
    r.readBits(1)   // SE
    const _header = decodeMessageHeaderType(r)
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _chargingSession = exiEnum("ChargingSession", ChargingSessionNames, r.readBits(2)) as ChargingSession
    r.readBits(1)   // child EE
    let _eVTerminationCode: string | null = null
    let _eVTerminationExplanation: string | null = null
    let st44 = 0
    let done44 = false
    while (!done44) {
        { const _c45 = st44; if (false) {}   // dispatch
            else if (_c45 === 0) {
                { const _c46 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c46 === 0) {
                        r.readBits(1)   // value-start
                        _eVTerminationCode = ExiPrimitives.readStringValue(r, "EVTerminationCode")
                        r.readBits(1)   // child EE
                        st44 = 1
                    }
                    else if (_c46 === 1) {
                        r.readBits(1)   // value-start
                        _eVTerminationExplanation = ExiPrimitives.readStringValue(r, "EVTerminationExplanation")
                        r.readBits(1)   // child EE
                        st44 = 2
                    }
                    else if (_c46 === 2) done44 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c45 === 1) {
                { const _c47 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c47 === 0) {
                        r.readBits(1)   // value-start
                        _eVTerminationExplanation = ExiPrimitives.readStringValue(r, "EVTerminationExplanation")
                        r.readBits(1)   // child EE
                        st44 = 2
                    }
                    else if (_c47 === 1) done44 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c45 === 2) {
                { const _c48 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c48 === 0) done44 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new SessionStopReqType(_header, _chargingSession, _eVTerminationCode, _eVTerminationExplanation);
}

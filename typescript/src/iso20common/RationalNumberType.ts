// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiPrimitives } from "../runtime/index.ts";

export class RationalNumberType {
    readonly exponent: number;
    readonly value: number;

    constructor(exponent: number, value: number) {
        this.exponent = exponent;
        this.value = value;
    }
}

export function encodeRationalNumberType(w: BitWriter, msg: RationalNumberType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits((msg.exponent - -128), 8)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeSignedInteger(w, BigInt(msg.value));
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

export function decodeRationalNumberType(r: BitReader): RationalNumberType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _exponent = (r.readBits(8) + -128)
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _value = Number(ExiPrimitives.readSignedInteger(r))
    r.readBits(1)   // child EE
    r.readBits(1)   // element EE
    return new RationalNumberType(_exponent, _value);
}

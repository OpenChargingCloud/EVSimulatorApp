// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import { BitReader, BitWriter, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { EvCheckOutStatus, EvCheckOutStatusNames } from "./EvCheckOutStatus.ts";
import { MessageHeaderType, encodeMessageHeaderType, decodeMessageHeaderType } from "./MessageHeaderType.ts";
import { V2GRequestType } from "./V2GRequestType.ts";

export class VehicleCheckOutReqType extends V2GRequestType {
    readonly eVCheckOutStatus: EvCheckOutStatus;
    readonly checkOutTime: bigint;

    constructor(header: MessageHeaderType, eVCheckOutStatus: EvCheckOutStatus, checkOutTime: bigint) {
        super(header);
        this.eVCheckOutStatus = eVCheckOutStatus;
        this.checkOutTime = checkOutTime;
    }
}

export function encodeVehicleCheckOutReqType(w: BitWriter, msg: VehicleCheckOutReqType): void {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.eVCheckOutStatus, 2)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, msg.checkOutTime);
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

export function decodeVehicleCheckOutReqType(r: BitReader): VehicleCheckOutReqType {
    r.readBits(1)   // SE
    const _header = decodeMessageHeaderType(r)
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _eVCheckOutStatus = exiEnum("EvCheckOutStatus", EvCheckOutStatusNames, r.readBits(2)) as EvCheckOutStatus
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _checkOutTime = ExiPrimitives.readUnsignedInteger(r)
    r.readBits(1)   // child EE
    r.readBits(1)   // element EE
    return new VehicleCheckOutReqType(_header, _eVCheckOutStatus, _checkOutTime);
}

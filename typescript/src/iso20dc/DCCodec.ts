// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.dc

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { DC_CPDReqEnergyTransferMode, encodeDC_CPDReqEnergyTransferMode, decodeDC_CPDReqEnergyTransferMode } from "./DC_CPDReqEnergyTransferMode.ts";
import { DC_CPDResEnergyTransferMode, encodeDC_CPDResEnergyTransferMode, decodeDC_CPDResEnergyTransferMode } from "./DC_CPDResEnergyTransferMode.ts";
import { DC_CableCheckReq, encodeDC_CableCheckReq, decodeDC_CableCheckReq } from "./DC_CableCheckReq.ts";
import { DC_CableCheckRes, encodeDC_CableCheckRes, decodeDC_CableCheckRes } from "./DC_CableCheckRes.ts";
import { DC_ChargeLoopReq, encodeDC_ChargeLoopReq, decodeDC_ChargeLoopReq } from "./DC_ChargeLoopReq.ts";
import { DC_ChargeLoopRes, encodeDC_ChargeLoopRes, decodeDC_ChargeLoopRes } from "./DC_ChargeLoopRes.ts";
import { DC_ChargeParameterDiscoveryReq, encodeDC_ChargeParameterDiscoveryReq, decodeDC_ChargeParameterDiscoveryReq } from "./DC_ChargeParameterDiscoveryReq.ts";
import { DC_ChargeParameterDiscoveryRes, encodeDC_ChargeParameterDiscoveryRes, decodeDC_ChargeParameterDiscoveryRes } from "./DC_ChargeParameterDiscoveryRes.ts";
import { DC_ChargeParameterDiscoveryResType, encodeDC_ChargeParameterDiscoveryResType, decodeDC_ChargeParameterDiscoveryResType } from "./DC_ChargeParameterDiscoveryResType.ts";
import { DC_PreChargeReq, encodeDC_PreChargeReq, decodeDC_PreChargeReq } from "./DC_PreChargeReq.ts";
import { DC_PreChargeRes, encodeDC_PreChargeRes, decodeDC_PreChargeRes } from "./DC_PreChargeRes.ts";
import { DC_WeldingDetectionReq, encodeDC_WeldingDetectionReq, decodeDC_WeldingDetectionReq } from "./DC_WeldingDetectionReq.ts";
import { DC_WeldingDetectionRes, encodeDC_WeldingDetectionRes, decodeDC_WeldingDetectionRes } from "./DC_WeldingDetectionRes.ts";
import { SignedInfoType, encodeSignedInfoType, decodeSignedInfoType } from "./SignedInfoType.ts";

export const DCCodec = {

    exiHeader: 0x80,

    encodeDC_ChargeParameterDiscoveryReq(msg: DC_ChargeParameterDiscoveryReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(15, 6);   // document element selector
        encodeDC_ChargeParameterDiscoveryReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeDC_ChargeParameterDiscoveryRes(msg: DC_ChargeParameterDiscoveryRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(16, 6);   // document element selector
        encodeDC_ChargeParameterDiscoveryRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeDC_CableCheckReq(msg: DC_CableCheckReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(11, 6);   // document element selector
        encodeDC_CableCheckReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeDC_CableCheckRes(msg: DC_CableCheckRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(12, 6);   // document element selector
        encodeDC_CableCheckRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeDC_PreChargeReq(msg: DC_PreChargeReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(17, 6);   // document element selector
        encodeDC_PreChargeReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeDC_PreChargeRes(msg: DC_PreChargeRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(18, 6);   // document element selector
        encodeDC_PreChargeRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeDC_ChargeLoopReq(msg: DC_ChargeLoopReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(13, 6);   // document element selector
        encodeDC_ChargeLoopReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeDC_ChargeLoopRes(msg: DC_ChargeLoopRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(14, 6);   // document element selector
        encodeDC_ChargeLoopRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeDC_WeldingDetectionReq(msg: DC_WeldingDetectionReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(19, 6);   // document element selector
        encodeDC_WeldingDetectionReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeDC_WeldingDetectionRes(msg: DC_WeldingDetectionRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(20, 6);   // document element selector
        encodeDC_WeldingDetectionRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeDC_CPDReqEnergyTransferMode(msg: DC_CPDReqEnergyTransferMode): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(9, 6);   // document element selector
        encodeDC_CPDReqEnergyTransferMode(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeDC_CPDResEnergyTransferMode(msg: DC_CPDResEnergyTransferMode): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(10, 6);   // document element selector
        encodeDC_CPDResEnergyTransferMode(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    /** Encodes any document element of this message set, dispatching on its runtime type. */
    encodeAny(msg: unknown): Uint8Array {
        if (msg instanceof DC_ChargeLoopReq) return this.encodeDC_ChargeLoopReq(msg);
        if (msg instanceof DC_ChargeLoopRes) return this.encodeDC_ChargeLoopRes(msg);
        if (msg instanceof DC_ChargeParameterDiscoveryReq) return this.encodeDC_ChargeParameterDiscoveryReq(msg);
        if (msg instanceof DC_ChargeParameterDiscoveryRes) return this.encodeDC_ChargeParameterDiscoveryRes(msg);
        if (msg instanceof DC_CableCheckReq) return this.encodeDC_CableCheckReq(msg);
        if (msg instanceof DC_CableCheckRes) return this.encodeDC_CableCheckRes(msg);
        if (msg instanceof DC_PreChargeReq) return this.encodeDC_PreChargeReq(msg);
        if (msg instanceof DC_PreChargeRes) return this.encodeDC_PreChargeRes(msg);
        if (msg instanceof DC_WeldingDetectionReq) return this.encodeDC_WeldingDetectionReq(msg);
        if (msg instanceof DC_WeldingDetectionRes) return this.encodeDC_WeldingDetectionRes(msg);
        if (msg instanceof DC_CPDReqEnergyTransferMode) return this.encodeDC_CPDReqEnergyTransferMode(msg);
        if (msg instanceof DC_CPDResEnergyTransferMode) return this.encodeDC_CPDResEnergyTransferMode(msg);
        throw new TypeError(
            `${(msg as object)?.constructor?.name} is not a document element of this message set.`);
    },

    decodeAny(src: Uint8Array): unknown {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        const selector = r.readBits(6);
        switch (selector) {
            case 15: return decodeDC_ChargeParameterDiscoveryReq(r);
            case 16: return decodeDC_ChargeParameterDiscoveryRes(r);
            case 11: return decodeDC_CableCheckReq(r);
            case 12: return decodeDC_CableCheckRes(r);
            case 17: return decodeDC_PreChargeReq(r);
            case 18: return decodeDC_PreChargeRes(r);
            case 13: return decodeDC_ChargeLoopReq(r);
            case 14: return decodeDC_ChargeLoopRes(r);
            case 19: return decodeDC_WeldingDetectionReq(r);
            case 20: return decodeDC_WeldingDetectionRes(r);
            case 9: return decodeDC_CPDReqEnergyTransferMode(r);
            case 10: return decodeDC_CPDResEnergyTransferMode(r);
            default: throw ExiError.unknownDocumentIndex(selector);
        }
    },

    encodeFragment_DC_ChargeParameterDiscoveryRes(content: DC_ChargeParameterDiscoveryResType): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(26, 8);   // fragment SE(DC_ChargeParameterDiscoveryRes)
        encodeDC_ChargeParameterDiscoveryResType(w, content);
        w.writeBits(150, 8);   // End Fragment (ED)
        w.alignToByte();
        return w.bytes;
    },

    decodeFragment_DC_ChargeParameterDiscoveryRes(src: Uint8Array): DC_ChargeParameterDiscoveryResType {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        if (r.readBits(8) !== 26) throw ExiError.invalidEventCode("not a DC_ChargeParameterDiscoveryRes fragment");
        const result = decodeDC_ChargeParameterDiscoveryResType(r);
        if (r.readBits(8) !== 150) throw ExiError.invalidEventCode("missing End Fragment");
        return result;
    },

    encodeFragment_SignedInfo(content: SignedInfoType): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(129, 8);   // fragment SE(SignedInfo)
        encodeSignedInfoType(w, content);
        w.writeBits(150, 8);   // End Fragment (ED)
        w.alignToByte();
        return w.bytes;
    },

    decodeFragment_SignedInfo(src: Uint8Array): SignedInfoType {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        if (r.readBits(8) !== 129) throw ExiError.invalidEventCode("not a SignedInfo fragment");
        const result = decodeSignedInfoType(r);
        if (r.readBits(8) !== 150) throw ExiError.invalidEventCode("missing End Fragment");
        return result;
    },

};

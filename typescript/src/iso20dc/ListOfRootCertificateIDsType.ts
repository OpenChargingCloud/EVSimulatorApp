// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.dc

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { X509IssuerSerialType, encodeX509IssuerSerialType, decodeX509IssuerSerialType } from "./X509IssuerSerialType.ts";

export class ListOfRootCertificateIDsType {
    readonly rootCertificateID: readonly X509IssuerSerialType[];

    constructor(rootCertificateID: readonly X509IssuerSerialType[]) {
        this.rootCertificateID = rootCertificateID;
    }
}

export function encodeListOfRootCertificateIDsType(w: BitWriter, msg: ListOfRootCertificateIDsType): void {
    const list = msg.rootCertificateID
    if (list.length < 1 || list.length > 20) throw new RangeError("list: list size out of schema range");
    for (let i = 0; i < list.length; i++) {
        w.writeBits(0, i === 0 ? 1 : 2);   // SE(item)
        encodeX509IssuerSerialType(w, list[i])
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

export function decodeListOfRootCertificateIDsType(r: BitReader): ListOfRootCertificateIDsType {
    const list = new Array<X509IssuerSerialType>()
    r.readBits(1)   // SE(item) first
    list.push(decodeX509IssuerSerialType(r));
    while (true) {
        const ec = r.readBits(2)
        if (ec === 1) break;   // element EE
        if (!(ec === 0 && list.length < 20)) throw ExiError.invalidEventCode("repeating element");
        list.push(decodeX509IssuerSerialType(r));
    }
    return new ListOfRootCertificateIDsType(list);
}

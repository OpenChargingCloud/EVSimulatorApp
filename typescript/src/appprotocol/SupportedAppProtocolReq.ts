// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.appprotocol

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { AppProtocolType, encodeAppProtocolType, decodeAppProtocolType } from "./AppProtocolType.ts";

export class SupportedAppProtocolReq {
    readonly appProtocol: readonly AppProtocolType[];

    constructor(appProtocol: readonly AppProtocolType[]) {
        this.appProtocol = appProtocol;
    }
}

export function encodeSupportedAppProtocolReq(w: BitWriter, msg: SupportedAppProtocolReq): void {
    const list = msg.appProtocol
    if (list.length < 1 || list.length > 20) throw new RangeError("list: list size out of schema range");
    for (let i = 0; i < list.length; i++) {
        w.writeBits(0, i === 0 ? 1 : 2);   // SE(item)
        encodeAppProtocolType(w, list[i])
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

export function decodeSupportedAppProtocolReq(r: BitReader): SupportedAppProtocolReq {
    const list = new Array<AppProtocolType>()
    r.readBits(1)   // SE(item) first
    list.push(decodeAppProtocolType(r));
    while (true) {
        const ec = r.readBits(2)
        if (ec === 1) break;   // element EE
        if (!(ec === 0 && list.length < 20)) throw ExiError.invalidEventCode("repeating element");
        list.push(decodeAppProtocolType(r));
    }
    return new SupportedAppProtocolReq(list);
}

// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.appprotocol

import { BitReader, BitWriter, ExiPrimitives } from "../runtime/index.ts";

export class AppProtocolType {
    readonly protocolNamespace: string;
    readonly versionNumberMajor: number;
    readonly versionNumberMinor: number;
    readonly schemaID: number;
    readonly priority: number;

    constructor(protocolNamespace: string, versionNumberMajor: number, versionNumberMinor: number, schemaID: number, priority: number) {
        this.protocolNamespace = protocolNamespace;
        this.versionNumberMajor = versionNumberMajor;
        this.versionNumberMinor = versionNumberMinor;
        this.schemaID = schemaID;
        this.priority = priority;
    }
}

export function encodeAppProtocolType(w: BitWriter, msg: AppProtocolType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeStringValue(w, msg.protocolNamespace)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.versionNumberMajor));
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.versionNumberMinor));
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.schemaID, 8)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits((msg.priority - 1), 5)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

export function decodeAppProtocolType(r: BitReader): AppProtocolType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _protocolNamespace = ExiPrimitives.readStringValue(r, "ProtocolNamespace")
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _versionNumberMajor = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _versionNumberMinor = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _schemaID = r.readBits(8)
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _priority = (r.readBits(5) + 1)
    r.readBits(1)   // child EE
    r.readBits(1)   // element EE
    return new AppProtocolType(_protocolNamespace, _versionNumberMajor, _versionNumberMinor, _schemaID, _priority);
}

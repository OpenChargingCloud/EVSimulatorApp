// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { CostType, encodeCostType, decodeCostType } from "./CostType.ts";
import { PhysicalValueType, encodePhysicalValueType, decodePhysicalValueType } from "./PhysicalValueType.ts";

export class ConsumptionCostType {
    readonly startValue: PhysicalValueType;
    readonly cost: readonly CostType[];

    constructor(startValue: PhysicalValueType, cost: readonly CostType[]) {
        this.startValue = startValue;
        this.cost = cost;
    }
}

export function encodeConsumptionCostType(w: BitWriter, msg: ConsumptionCostType): void {
    w.writeBits(0, 1)   // SE
    encodePhysicalValueType(w, msg.startValue)
    const costList = msg.cost
    if (costList.length < 1 || costList.length > 3) throw new RangeError("costList: list size out of schema range");
    for (let i = 0; i < costList.length; i++) {
        w.writeBits(0, i === 0 ? 1 : 2);   // SE(item)
        encodeCostType(w, costList[i])
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

export function decodeConsumptionCostType(r: BitReader): ConsumptionCostType {
    r.readBits(1)   // SE
    const _startValue = decodePhysicalValueType(r)
    const costList = new Array<CostType>()
    r.readBits(1)   // SE(item) first
    costList.push(decodeCostType(r));
    while (true) {
        const ec = r.readBits(2)
        if (ec === 1) break;   // element EE
        if (!(ec === 0 && costList.length < 3)) throw ExiError.invalidEventCode("repeating element");
        costList.push(decodeCostType(r));
    }
    return new ConsumptionCostType(_startValue, costList);
}

// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, exiEnum } from "../runtime/index.ts";
import { EnergyTransferMode, EnergyTransferModeNames } from "./EnergyTransferMode.ts";

export class SupportedEnergyTransferModeType {
    readonly energyTransferMode: readonly EnergyTransferMode[];

    constructor(energyTransferMode: readonly EnergyTransferMode[]) {
        this.energyTransferMode = energyTransferMode;
    }
}

export function encodeSupportedEnergyTransferModeType(w: BitWriter, msg: SupportedEnergyTransferModeType): void {
    const list = msg.energyTransferMode
    if (list.length < 1 || list.length > 6) throw new RangeError("list: list size out of schema range");
    for (let i = 0; i < list.length; i++) {
        w.writeBits(0, i === 0 ? 1 : 2);   // SE(item)
        w.writeBits(0, 1)   // value-start
        w.writeBits(list[i], 3)
        w.writeBits(0, 1)   // child EE
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

export function decodeSupportedEnergyTransferModeType(r: BitReader): SupportedEnergyTransferModeType {
    const list = new Array<EnergyTransferMode>()
    r.readBits(1)   // SE(item) first
    r.readBits(1)   // value-start
    const listFirst = exiEnum("EnergyTransferMode", EnergyTransferModeNames, r.readBits(3)) as EnergyTransferMode
    r.readBits(1)   // child EE
    list.push(listFirst);
    while (true) {
        const ec = r.readBits(2)
        if (ec === 1) break;   // element EE
        if (!(ec === 0 && list.length < 6)) throw ExiError.invalidEventCode("repeating element");
        r.readBits(1)   // value-start
        const listNext = exiEnum("EnergyTransferMode", EnergyTransferModeNames, r.readBits(3)) as EnergyTransferMode
        r.readBits(1)   // child EE
        list.push(listNext);
    }
    return new SupportedEnergyTransferModeType(list);
}

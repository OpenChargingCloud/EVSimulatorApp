// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { CostKind, CostKindNames } from "./CostKind.ts";

export class CostType {
    readonly costKind: CostKind;
    readonly amount: number;
    readonly amountMultiplier: number | null;

    constructor(costKind: CostKind, amount: number, amountMultiplier: number | null) {
        this.costKind = costKind;
        this.amount = amount;
        this.amountMultiplier = amountMultiplier;
    }
}

export function encodeCostType(w: BitWriter, msg: CostType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.costKind, 2)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.amount));
    w.writeBits(0, 1)   // child EE
    let st209 = 0
    let done209 = false
    while (!done209) {
        { const _c210 = st209; if (false) {}   // dispatch
            else if (_c210 === 0) {
                if (msg.amountMultiplier != null) {
                    w.writeBits(0, 2)   // AmountMultiplier
                    w.writeBits(0, 1)   // value-start
                    w.writeBits((msg.amountMultiplier! - -3), 3)
                    w.writeBits(0, 1)   // child EE
                    st209 = 1
                } else {
                    w.writeBits(1, 2)   // element EE
                    done209 = true
                }
            }
            else if (_c210 === 1) {
                w.writeBits(0, 1)   // element EE
                done209 = true
            }
        }
    }
}

export function decodeCostType(r: BitReader): CostType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _costKind = exiEnum("CostKind", CostKindNames, r.readBits(2)) as CostKind
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _amount = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    let _amountMultiplier: number | null = null
    let st211 = 0
    let done211 = false
    while (!done211) {
        { const _c212 = st211; if (false) {}   // dispatch
            else if (_c212 === 0) {
                { const _c213 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c213 === 0) {
                        r.readBits(1)   // value-start
                        _amountMultiplier = (r.readBits(3) + -3)
                        r.readBits(1)   // child EE
                        st211 = 1
                    }
                    else if (_c213 === 1) done211 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c212 === 1) {
                { const _c214 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c214 === 0) done211 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new CostType(_costKind, _amount, _amountMultiplier);
}

// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { AC_EVSEStatusType, encodeAC_EVSEStatusType, decodeAC_EVSEStatusType } from "./AC_EVSEStatusType.ts";
import { BodyBaseType } from "./BodyBaseType.ts";
import { MeterInfoType, encodeMeterInfoType, decodeMeterInfoType } from "./MeterInfoType.ts";
import { PhysicalValueType, encodePhysicalValueType, decodePhysicalValueType } from "./PhysicalValueType.ts";
import { ResponseCode, ResponseCodeNames } from "./ResponseCode.ts";

export class ChargingStatusResType extends BodyBaseType {
    readonly responseCode: ResponseCode;
    readonly eVSEID: string;
    readonly sAScheduleTupleID: number;
    readonly eVSEMaxCurrent: PhysicalValueType | null;
    readonly meterInfo: MeterInfoType | null;
    readonly receiptRequired: boolean | null;
    readonly aC_EVSEStatus: AC_EVSEStatusType;

    constructor(responseCode: ResponseCode, eVSEID: string, sAScheduleTupleID: number, eVSEMaxCurrent: PhysicalValueType | null, meterInfo: MeterInfoType | null, receiptRequired: boolean | null, aC_EVSEStatus: AC_EVSEStatusType) {
        super();
        this.responseCode = responseCode;
        this.eVSEID = eVSEID;
        this.sAScheduleTupleID = sAScheduleTupleID;
        this.eVSEMaxCurrent = eVSEMaxCurrent;
        this.meterInfo = meterInfo;
        this.receiptRequired = receiptRequired;
        this.aC_EVSEStatus = aC_EVSEStatus;
    }
}

export function encodeChargingStatusResType(w: BitWriter, msg: ChargingStatusResType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.responseCode, 5)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeStringValue(w, msg.eVSEID)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits((msg.sAScheduleTupleID - 1), 8)
    w.writeBits(0, 1)   // child EE
    let st78 = 0
    let done78 = false
    while (!done78) {
        { const _c79 = st78; if (false) {}   // dispatch
            else if (_c79 === 0) {
                if (msg.eVSEMaxCurrent != null) {
                    w.writeBits(0, 3)   // EVSEMaxCurrent
                    encodePhysicalValueType(w, msg.eVSEMaxCurrent!)
                    st78 = 1
                } else if (msg.meterInfo != null) {
                    w.writeBits(1, 3)   // MeterInfo
                    encodeMeterInfoType(w, msg.meterInfo!)
                    st78 = 2
                } else if (msg.receiptRequired != null) {
                    w.writeBits(2, 3)   // ReceiptRequired
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.receiptRequired! ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    st78 = 3
                } else {
                    w.writeBits(3, 3)   // SE(AC_EVSEStatus)
                    encodeAC_EVSEStatusType(w, msg.aC_EVSEStatus)
                    done78 = true
                }
            }
            else if (_c79 === 1) {
                if (msg.meterInfo != null) {
                    w.writeBits(0, 2)   // MeterInfo
                    encodeMeterInfoType(w, msg.meterInfo!)
                    st78 = 2
                } else if (msg.receiptRequired != null) {
                    w.writeBits(1, 2)   // ReceiptRequired
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.receiptRequired! ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    st78 = 3
                } else {
                    w.writeBits(2, 2)   // SE(AC_EVSEStatus)
                    encodeAC_EVSEStatusType(w, msg.aC_EVSEStatus)
                    done78 = true
                }
            }
            else if (_c79 === 2) {
                if (msg.receiptRequired != null) {
                    w.writeBits(0, 2)   // ReceiptRequired
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.receiptRequired! ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    st78 = 3
                } else {
                    w.writeBits(1, 2)   // SE(AC_EVSEStatus)
                    encodeAC_EVSEStatusType(w, msg.aC_EVSEStatus)
                    done78 = true
                }
            }
            else if (_c79 === 3) {
                w.writeBits(0, 1)   // SE(AC_EVSEStatus)
                encodeAC_EVSEStatusType(w, msg.aC_EVSEStatus)
                done78 = true
            }
        }
    }
    w.writeBits(0, 1)   // element EE
}

export function decodeChargingStatusResType(r: BitReader): ChargingStatusResType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _responseCode = exiEnum("ResponseCode", ResponseCodeNames, r.readBits(5)) as ResponseCode
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _eVSEID = ExiPrimitives.readStringValue(r, "EVSEID")
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _sAScheduleTupleID = (r.readBits(8) + 1)
    r.readBits(1)   // child EE
    let _eVSEMaxCurrent: PhysicalValueType | null = null
    let _meterInfo: MeterInfoType | null = null
    let _receiptRequired: boolean | null = null
    let _aC_EVSEStatus: AC_EVSEStatusType = null
    let st80 = 0
    let done80 = false
    while (!done80) {
        { const _c81 = st80; if (false) {}   // dispatch
            else if (_c81 === 0) {
                { const _c82 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c82 === 0) {
                        _eVSEMaxCurrent = decodePhysicalValueType(r)
                        st80 = 1
                    }
                    else if (_c82 === 1) {
                        _meterInfo = decodeMeterInfoType(r)
                        st80 = 2
                    }
                    else if (_c82 === 2) {
                        r.readBits(1)   // value-start
                        _receiptRequired = r.readBits(1)
                        r.readBits(1)   // child EE
                        st80 = 3
                    }
                    else if (_c82 === 3) {   // SE(AC_EVSEStatus)
                        _aC_EVSEStatus = decodeAC_EVSEStatusType(r)
                        done80 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c81 === 1) {
                { const _c83 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c83 === 0) {
                        _meterInfo = decodeMeterInfoType(r)
                        st80 = 2
                    }
                    else if (_c83 === 1) {
                        r.readBits(1)   // value-start
                        _receiptRequired = r.readBits(1)
                        r.readBits(1)   // child EE
                        st80 = 3
                    }
                    else if (_c83 === 2) {   // SE(AC_EVSEStatus)
                        _aC_EVSEStatus = decodeAC_EVSEStatusType(r)
                        done80 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c81 === 2) {
                { const _c84 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c84 === 0) {
                        r.readBits(1)   // value-start
                        _receiptRequired = r.readBits(1)
                        r.readBits(1)   // child EE
                        st80 = 3
                    }
                    else if (_c84 === 1) {   // SE(AC_EVSEStatus)
                        _aC_EVSEStatus = decodeAC_EVSEStatusType(r)
                        done80 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c81 === 3) {
                { const _c85 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c85 === 0) {   // SE(AC_EVSEStatus)
                        _aC_EVSEStatus = decodeAC_EVSEStatusType(r)
                        done80 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    r.readBits(1)   // element EE
    return new ChargingStatusResType(_responseCode, _eVSEID, _sAScheduleTupleID, _eVSEMaxCurrent, _meterInfo, _receiptRequired, _aC_EVSEStatus!);
}

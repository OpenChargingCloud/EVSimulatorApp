// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { ServiceCategory, ServiceCategoryNames } from "./ServiceCategory.ts";

export class ServiceType {
    readonly serviceID: number;
    readonly serviceName: string | null;
    readonly serviceCategory: ServiceCategory;
    readonly serviceScope: string | null;
    readonly freeService: boolean;

    constructor(serviceID: number, serviceName: string | null, serviceCategory: ServiceCategory, serviceScope: string | null, freeService: boolean) {
        this.serviceID = serviceID;
        this.serviceName = serviceName;
        this.serviceCategory = serviceCategory;
        this.serviceScope = serviceScope;
        this.freeService = freeService;
    }
}

export function encodeServiceType(w: BitWriter, msg: ServiceType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.serviceID));
    w.writeBits(0, 1)   // child EE
    let st117 = 0
    let done117 = false
    while (!done117) {
        { const _c118 = st117; if (false) {}   // dispatch
            else if (_c118 === 0) {
                if (msg.serviceName != null) {
                    w.writeBits(0, 2)   // ServiceName
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeStringValue(w, msg.serviceName!)
                    w.writeBits(0, 1)   // child EE
                    st117 = 1
                } else {
                    w.writeBits(1, 2)   // SE(ServiceCategory)
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.serviceCategory, 2)
                    w.writeBits(0, 1)   // child EE
                    done117 = true
                }
            }
            else if (_c118 === 1) {
                w.writeBits(0, 1)   // SE(ServiceCategory)
                w.writeBits(0, 1)   // value-start
                w.writeBits(msg.serviceCategory, 2)
                w.writeBits(0, 1)   // child EE
                done117 = true
            }
        }
    }
    let st119 = 0
    let done119 = false
    while (!done119) {
        { const _c120 = st119; if (false) {}   // dispatch
            else if (_c120 === 0) {
                if (msg.serviceScope != null) {
                    w.writeBits(0, 2)   // ServiceScope
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeStringValue(w, msg.serviceScope!)
                    w.writeBits(0, 1)   // child EE
                    st119 = 1
                } else {
                    w.writeBits(1, 2)   // SE(FreeService)
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.freeService ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    done119 = true
                }
            }
            else if (_c120 === 1) {
                w.writeBits(0, 1)   // SE(FreeService)
                w.writeBits(0, 1)   // value-start
                w.writeBits(msg.freeService ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                done119 = true
            }
        }
    }
    w.writeBits(0, 1)   // element EE
}

export function decodeServiceType(r: BitReader): ServiceType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _serviceID = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    let _serviceName: string | null = null
    let _serviceCategory: ServiceCategory = null
    let st121 = 0
    let done121 = false
    while (!done121) {
        { const _c122 = st121; if (false) {}   // dispatch
            else if (_c122 === 0) {
                { const _c123 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c123 === 0) {
                        r.readBits(1)   // value-start
                        _serviceName = ExiPrimitives.readStringValue(r, "ServiceName")
                        r.readBits(1)   // child EE
                        st121 = 1
                    }
                    else if (_c123 === 1) {   // SE(ServiceCategory)
                        r.readBits(1)   // value-start
                        _serviceCategory = exiEnum("ServiceCategory", ServiceCategoryNames, r.readBits(2)) as ServiceCategory
                        r.readBits(1)   // child EE
                        done121 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c122 === 1) {
                { const _c124 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c124 === 0) {   // SE(ServiceCategory)
                        r.readBits(1)   // value-start
                        _serviceCategory = exiEnum("ServiceCategory", ServiceCategoryNames, r.readBits(2)) as ServiceCategory
                        r.readBits(1)   // child EE
                        done121 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    let _serviceScope: string | null = null
    let _freeService: boolean = null
    let st125 = 0
    let done125 = false
    while (!done125) {
        { const _c126 = st125; if (false) {}   // dispatch
            else if (_c126 === 0) {
                { const _c127 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c127 === 0) {
                        r.readBits(1)   // value-start
                        _serviceScope = ExiPrimitives.readStringValue(r, "ServiceScope")
                        r.readBits(1)   // child EE
                        st125 = 1
                    }
                    else if (_c127 === 1) {   // SE(FreeService)
                        r.readBits(1)   // value-start
                        _freeService = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        done125 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c126 === 1) {
                { const _c128 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c128 === 0) {   // SE(FreeService)
                        r.readBits(1)   // value-start
                        _freeService = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        done125 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    r.readBits(1)   // element EE
    return new ServiceType(_serviceID, _serviceName, _serviceCategory!, _serviceScope, _freeService!);
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { PMaxScheduleEntryType, encodePMaxScheduleEntryType, decodePMaxScheduleEntryType } from "./PMaxScheduleEntryType.ts";

export class PMaxScheduleType {
    readonly pMaxScheduleEntry: readonly PMaxScheduleEntryType[];

    constructor(pMaxScheduleEntry: readonly PMaxScheduleEntryType[]) {
        this.pMaxScheduleEntry = pMaxScheduleEntry;
    }
}

export function encodePMaxScheduleType(w: BitWriter, msg: PMaxScheduleType): void {
    const list = msg.pMaxScheduleEntry
    if (list.length < 1 || list.length > 1024) throw new RangeError("list: list size out of schema range");
    for (let i = 0; i < list.length; i++) {
        w.writeBits(0, i === 0 ? 1 : 2);   // SE(item)
        encodePMaxScheduleEntryType(w, list[i])
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

export function decodePMaxScheduleType(r: BitReader): PMaxScheduleType {
    const list = new Array<PMaxScheduleEntryType>()
    r.readBits(1)   // SE(item) first
    list.push(decodePMaxScheduleEntryType(r));
    while (true) {
        const ec = r.readBits(2)
        if (ec === 1) break;   // element EE
        if (!(ec === 0 && list.length < 1024)) throw ExiError.invalidEventCode("repeating element");
        list.push(decodePMaxScheduleEntryType(r));
    }
    return new PMaxScheduleType(list);
}

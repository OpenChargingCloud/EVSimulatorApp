// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { ConsumptionCostType, encodeConsumptionCostType, decodeConsumptionCostType } from "./ConsumptionCostType.ts";
import { EntryType } from "./EntryType.ts";
import { IntervalType } from "./IntervalType.ts";
import { RelativeTimeIntervalType, encodeRelativeTimeIntervalType, decodeRelativeTimeIntervalType } from "./RelativeTimeIntervalType.ts";

export class SalesTariffEntryType extends EntryType {
    readonly ePriceLevel: number | null;
    readonly consumptionCost: readonly ConsumptionCostType[];

    constructor(timeInterval: IntervalType, ePriceLevel: number | null, consumptionCost: readonly ConsumptionCostType[]) {
        super(timeInterval);
        this.ePriceLevel = ePriceLevel;
        this.consumptionCost = consumptionCost;
    }
}

export function encodeSalesTariffEntryType(w: BitWriter, msg: SalesTariffEntryType): void {
    { const v = msg.timeInterval; if (false) {}   // dispatch
        else if (v instanceof RelativeTimeIntervalType) {
            w.writeBits(0, 2)   // RelativeTimeInterval
            encodeRelativeTimeIntervalType(w, v)
        }
        else throw new TypeError("unsupported substitution member for TimeInterval");
    }
    let st194 = 0
    let done194 = false
    while (!done194) {
        { const _c195 = st194; if (false) {}   // dispatch
            else if (_c195 === 0) {
                if (msg.ePriceLevel != null) {
                    w.writeBits(0, 2)   // EPriceLevel
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.ePriceLevel!, 8)
                    w.writeBits(0, 1)   // child EE
                    st194 = 1
                } else if (msg.consumptionCost.length > 0) {
                    if (msg.consumptionCost.length < 0 || msg.consumptionCost.length > 3) throw new RangeError("msg.consumptionCost: list size out of schema range");
                    w.writeBits(1, 2)   // ConsumptionCost
                    encodeConsumptionCostType(w, msg.consumptionCost[0])
                    for (let ci = 1; ci < msg.consumptionCost.length; ci++) {
                        w.writeBits(0, 2)   // ConsumptionCost
                        encodeConsumptionCostType(w, msg.consumptionCost[ci])
                    }
                    w.writeBits(1, 2)   // element EE (list end)
                    done194 = true
                } else {
                    w.writeBits(2, 2)   // element EE
                    done194 = true
                }
            }
            else if (_c195 === 1) {
                if (msg.consumptionCost.length > 0) {
                    if (msg.consumptionCost.length < 0 || msg.consumptionCost.length > 3) throw new RangeError("msg.consumptionCost: list size out of schema range");
                    w.writeBits(0, 2)   // ConsumptionCost
                    encodeConsumptionCostType(w, msg.consumptionCost[0])
                    for (let ci = 1; ci < msg.consumptionCost.length; ci++) {
                        w.writeBits(0, 2)   // ConsumptionCost
                        encodeConsumptionCostType(w, msg.consumptionCost[ci])
                    }
                    w.writeBits(1, 2)   // element EE (list end)
                    done194 = true
                } else {
                    w.writeBits(1, 2)   // element EE
                    done194 = true
                }
            }
            else if (_c195 === 2) {
                w.writeBits(0, 1)   // element EE
                done194 = true
            }
        }
    }
}

export function decodeSalesTariffEntryType(r: BitReader): SalesTariffEntryType {
    let _timeInterval!: IntervalType;
    { const _c196 = r.readBits(2); if (false) {}   // dispatch
        else if (_c196 === 0) _timeInterval = decodeRelativeTimeIntervalType(r);
        else if (_c196 === 1) throw ExiError.invalidEventCode("abstract substitution head");
        else throw ExiError.invalidEventCode("substitution index");
    }
    let _ePriceLevel: number | null = null
    const consumptionCostList = new Array<ConsumptionCostType>()
    let st197 = 0
    let done197 = false
    while (!done197) {
        { const _c198 = st197; if (false) {}   // dispatch
            else if (_c198 === 0) {
                { const _c199 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c199 === 0) {
                        r.readBits(1)   // value-start
                        _ePriceLevel = r.readBits(8)
                        r.readBits(1)   // child EE
                        st197 = 1
                    }
                    else if (_c199 === 1) {   // ConsumptionCost
                        consumptionCostList.push(decodeConsumptionCostType(r));
                        while (true) {
                            const lc = r.readBits(2)
                            if (lc === 1) break;   // element EE (list end)
                            if (!(lc === 0 && consumptionCostList.length < 3)) throw ExiError.invalidEventCode("repeating element");
                            consumptionCostList.push(decodeConsumptionCostType(r));
                        }
                        done197 = true
                    }
                    else if (_c199 === 2) done197 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c198 === 1) {
                { const _c200 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c200 === 0) {   // ConsumptionCost
                        consumptionCostList.push(decodeConsumptionCostType(r));
                        while (true) {
                            const lc = r.readBits(2)
                            if (lc === 1) break;   // element EE (list end)
                            if (!(lc === 0 && consumptionCostList.length < 3)) throw ExiError.invalidEventCode("repeating element");
                            consumptionCostList.push(decodeConsumptionCostType(r));
                        }
                        done197 = true
                    }
                    else if (_c200 === 1) done197 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c198 === 2) {
                { const _c201 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c201 === 0) done197 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new SalesTariffEntryType(_timeInterval, _ePriceLevel, consumptionCostList);
}

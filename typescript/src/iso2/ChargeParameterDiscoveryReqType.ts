// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { AC_EVChargeParameterType, encodeAC_EVChargeParameterType, decodeAC_EVChargeParameterType } from "./AC_EVChargeParameterType.ts";
import { BodyBaseType } from "./BodyBaseType.ts";
import { DC_EVChargeParameterType, encodeDC_EVChargeParameterType, decodeDC_EVChargeParameterType } from "./DC_EVChargeParameterType.ts";
import { EVChargeParameterType } from "./EVChargeParameterType.ts";
import { EnergyTransferMode, EnergyTransferModeNames } from "./EnergyTransferMode.ts";

export class ChargeParameterDiscoveryReqType extends BodyBaseType {
    readonly maxEntriesSAScheduleTuple: number | null;
    readonly requestedEnergyTransferMode: EnergyTransferMode;
    readonly eVChargeParameter: EVChargeParameterType;

    constructor(maxEntriesSAScheduleTuple: number | null, requestedEnergyTransferMode: EnergyTransferMode, eVChargeParameter: EVChargeParameterType) {
        super();
        this.maxEntriesSAScheduleTuple = maxEntriesSAScheduleTuple;
        this.requestedEnergyTransferMode = requestedEnergyTransferMode;
        this.eVChargeParameter = eVChargeParameter;
    }
}

export function encodeChargeParameterDiscoveryReqType(w: BitWriter, msg: ChargeParameterDiscoveryReqType): void {
    let st38 = 0
    let done38 = false
    while (!done38) {
        { const _c39 = st38; if (false) {}   // dispatch
            else if (_c39 === 0) {
                if (msg.maxEntriesSAScheduleTuple != null) {
                    w.writeBits(0, 2)   // MaxEntriesSAScheduleTuple
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.maxEntriesSAScheduleTuple!));
                    w.writeBits(0, 1)   // child EE
                    st38 = 1
                } else {
                    w.writeBits(1, 2)   // SE(RequestedEnergyTransferMode)
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.requestedEnergyTransferMode, 3)
                    w.writeBits(0, 1)   // child EE
                    done38 = true
                }
            }
            else if (_c39 === 1) {
                w.writeBits(0, 1)   // SE(RequestedEnergyTransferMode)
                w.writeBits(0, 1)   // value-start
                w.writeBits(msg.requestedEnergyTransferMode, 3)
                w.writeBits(0, 1)   // child EE
                done38 = true
            }
        }
    }
    { const v = msg.eVChargeParameter; if (false) {}   // dispatch
        else if (v instanceof AC_EVChargeParameterType) {
            w.writeBits(0, 2)   // AC_EVChargeParameter
            encodeAC_EVChargeParameterType(w, v)
        }
        else if (v instanceof DC_EVChargeParameterType) {
            w.writeBits(1, 2)   // DC_EVChargeParameter
            encodeDC_EVChargeParameterType(w, v)
        }
        else throw new TypeError("unsupported substitution member for EVChargeParameter");
    }
    w.writeBits(0, 1)   // element EE
}

export function decodeChargeParameterDiscoveryReqType(r: BitReader): ChargeParameterDiscoveryReqType {
    let _maxEntriesSAScheduleTuple: number | null = null
    let _requestedEnergyTransferMode: EnergyTransferMode = null
    let st40 = 0
    let done40 = false
    while (!done40) {
        { const _c41 = st40; if (false) {}   // dispatch
            else if (_c41 === 0) {
                { const _c42 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c42 === 0) {
                        r.readBits(1)   // value-start
                        _maxEntriesSAScheduleTuple = Number(ExiPrimitives.readUnsignedInteger(r))
                        r.readBits(1)   // child EE
                        st40 = 1
                    }
                    else if (_c42 === 1) {   // SE(RequestedEnergyTransferMode)
                        r.readBits(1)   // value-start
                        _requestedEnergyTransferMode = exiEnum("EnergyTransferMode", EnergyTransferModeNames, r.readBits(3)) as EnergyTransferMode
                        r.readBits(1)   // child EE
                        done40 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c41 === 1) {
                { const _c43 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c43 === 0) {   // SE(RequestedEnergyTransferMode)
                        r.readBits(1)   // value-start
                        _requestedEnergyTransferMode = exiEnum("EnergyTransferMode", EnergyTransferModeNames, r.readBits(3)) as EnergyTransferMode
                        r.readBits(1)   // child EE
                        done40 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    let _eVChargeParameter!: EVChargeParameterType;
    { const _c44 = r.readBits(2); if (false) {}   // dispatch
        else if (_c44 === 0) _eVChargeParameter = decodeAC_EVChargeParameterType(r);
        else if (_c44 === 1) _eVChargeParameter = decodeDC_EVChargeParameterType(r);
        else if (_c44 === 2) throw ExiError.invalidEventCode("abstract substitution head");
        else throw ExiError.invalidEventCode("substitution index");
    }
    r.readBits(1)   // element EE
    return new ChargeParameterDiscoveryReqType(_maxEntriesSAScheduleTuple, _requestedEnergyTransferMode!, _eVChargeParameter);
}

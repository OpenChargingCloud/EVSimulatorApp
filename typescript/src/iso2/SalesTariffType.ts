// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";
import { SalesTariffEntryType, encodeSalesTariffEntryType, decodeSalesTariffEntryType } from "./SalesTariffEntryType.ts";

export class SalesTariffType {
    readonly id: string | null;
    readonly salesTariffID: number;
    readonly salesTariffDescription: string | null;
    readonly numEPriceLevels: number | null;
    readonly salesTariffEntry: readonly SalesTariffEntryType[];

    constructor(id: string | null, salesTariffID: number, salesTariffDescription: string | null, numEPriceLevels: number | null, salesTariffEntry: readonly SalesTariffEntryType[]) {
        this.id = id;
        this.salesTariffID = salesTariffID;
        this.salesTariffDescription = salesTariffDescription;
        this.numEPriceLevels = numEPriceLevels;
        this.salesTariffEntry = salesTariffEntry;
    }
}

export function encodeSalesTariffType(w: BitWriter, msg: SalesTariffType): void {
    let st181 = 0
    let done181 = false
    while (!done181) {
        { const _c182 = st181; if (false) {}   // dispatch
            else if (_c182 === 0) {
                if (msg.id != null) {
                    w.writeBits(0, 2)   // Id
                    ExiPrimitives.writeStringValue(w, msg.id!)
                    st181 = 1
                } else {
                    w.writeBits(1, 2)   // SE(SalesTariffID)
                    w.writeBits(0, 1)   // value-start
                    w.writeBits((msg.salesTariffID - 1), 8)
                    w.writeBits(0, 1)   // child EE
                    done181 = true
                }
            }
            else if (_c182 === 1) {
                w.writeBits(0, 1)   // SE(SalesTariffID)
                w.writeBits(0, 1)   // value-start
                w.writeBits((msg.salesTariffID - 1), 8)
                w.writeBits(0, 1)   // child EE
                done181 = true
            }
        }
    }
    let st183 = 0
    let done183 = false
    while (!done183) {
        { const _c184 = st183; if (false) {}   // dispatch
            else if (_c184 === 0) {
                if (msg.salesTariffDescription != null) {
                    w.writeBits(0, 2)   // SalesTariffDescription
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeStringValue(w, msg.salesTariffDescription!)
                    w.writeBits(0, 1)   // child EE
                    st183 = 1
                } else if (msg.numEPriceLevels != null) {
                    w.writeBits(1, 2)   // NumEPriceLevels
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.numEPriceLevels!, 8)
                    w.writeBits(0, 1)   // child EE
                    st183 = 2
                } else {
                    if (msg.salesTariffEntry.length < 1 || msg.salesTariffEntry.length > 1024) throw new RangeError("msg.salesTariffEntry: list size out of schema range");
                    w.writeBits(2, 2)   // SalesTariffEntry
                    encodeSalesTariffEntryType(w, msg.salesTariffEntry[0])
                    for (let ci = 1; ci < msg.salesTariffEntry.length; ci++) {
                        w.writeBits(0, 2)   // SalesTariffEntry
                        encodeSalesTariffEntryType(w, msg.salesTariffEntry[ci])
                    }
                    w.writeBits(1, 2)   // element EE (list end)
                    done183 = true
                }
            }
            else if (_c184 === 1) {
                if (msg.numEPriceLevels != null) {
                    w.writeBits(0, 2)   // NumEPriceLevels
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.numEPriceLevels!, 8)
                    w.writeBits(0, 1)   // child EE
                    st183 = 2
                } else {
                    if (msg.salesTariffEntry.length < 1 || msg.salesTariffEntry.length > 1024) throw new RangeError("msg.salesTariffEntry: list size out of schema range");
                    w.writeBits(1, 2)   // SalesTariffEntry
                    encodeSalesTariffEntryType(w, msg.salesTariffEntry[0])
                    for (let ci = 1; ci < msg.salesTariffEntry.length; ci++) {
                        w.writeBits(0, 2)   // SalesTariffEntry
                        encodeSalesTariffEntryType(w, msg.salesTariffEntry[ci])
                    }
                    w.writeBits(1, 2)   // element EE (list end)
                    done183 = true
                }
            }
            else if (_c184 === 2) {
                if (msg.salesTariffEntry.length < 1 || msg.salesTariffEntry.length > 1024) throw new RangeError("msg.salesTariffEntry: list size out of schema range");
                w.writeBits(0, 1)   // SalesTariffEntry
                encodeSalesTariffEntryType(w, msg.salesTariffEntry[0])
                for (let ci = 1; ci < msg.salesTariffEntry.length; ci++) {
                    w.writeBits(0, 2)   // SalesTariffEntry
                    encodeSalesTariffEntryType(w, msg.salesTariffEntry[ci])
                }
                w.writeBits(1, 2)   // element EE (list end)
                done183 = true
            }
        }
    }
}

export function decodeSalesTariffType(r: BitReader): SalesTariffType {
    let _id: string | null = null
    let _salesTariffID: number = null
    let st185 = 0
    let done185 = false
    while (!done185) {
        { const _c186 = st185; if (false) {}   // dispatch
            else if (_c186 === 0) {
                { const _c187 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c187 === 0) {
                        _id = ExiPrimitives.readStringValue(r, "Id")
                        st185 = 1
                    }
                    else if (_c187 === 1) {   // SE(SalesTariffID)
                        r.readBits(1)   // value-start
                        _salesTariffID = (r.readBits(8) + 1)
                        r.readBits(1)   // child EE
                        done185 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c186 === 1) {
                { const _c188 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c188 === 0) {   // SE(SalesTariffID)
                        r.readBits(1)   // value-start
                        _salesTariffID = (r.readBits(8) + 1)
                        r.readBits(1)   // child EE
                        done185 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    let _salesTariffDescription: string | null = null
    let _numEPriceLevels: number | null = null
    const salesTariffEntryList = new Array<SalesTariffEntryType>()
    let st189 = 0
    let done189 = false
    while (!done189) {
        { const _c190 = st189; if (false) {}   // dispatch
            else if (_c190 === 0) {
                { const _c191 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c191 === 0) {
                        r.readBits(1)   // value-start
                        _salesTariffDescription = ExiPrimitives.readStringValue(r, "SalesTariffDescription")
                        r.readBits(1)   // child EE
                        st189 = 1
                    }
                    else if (_c191 === 1) {
                        r.readBits(1)   // value-start
                        _numEPriceLevels = r.readBits(8)
                        r.readBits(1)   // child EE
                        st189 = 2
                    }
                    else if (_c191 === 2) {   // SalesTariffEntry
                        salesTariffEntryList.push(decodeSalesTariffEntryType(r));
                        while (true) {
                            const lc = r.readBits(2)
                            if (lc === 1) break;   // element EE (list end)
                            if (!(lc === 0 && salesTariffEntryList.length < 1024)) throw ExiError.invalidEventCode("repeating element");
                            salesTariffEntryList.push(decodeSalesTariffEntryType(r));
                        }
                        done189 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c190 === 1) {
                { const _c192 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c192 === 0) {
                        r.readBits(1)   // value-start
                        _numEPriceLevels = r.readBits(8)
                        r.readBits(1)   // child EE
                        st189 = 2
                    }
                    else if (_c192 === 1) {   // SalesTariffEntry
                        salesTariffEntryList.push(decodeSalesTariffEntryType(r));
                        while (true) {
                            const lc = r.readBits(2)
                            if (lc === 1) break;   // element EE (list end)
                            if (!(lc === 0 && salesTariffEntryList.length < 1024)) throw ExiError.invalidEventCode("repeating element");
                            salesTariffEntryList.push(decodeSalesTariffEntryType(r));
                        }
                        done189 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c190 === 2) {
                { const _c193 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c193 === 0) {   // SalesTariffEntry
                        salesTariffEntryList.push(decodeSalesTariffEntryType(r));
                        while (true) {
                            const lc = r.readBits(2)
                            if (lc === 1) break;   // element EE (list end)
                            if (!(lc === 0 && salesTariffEntryList.length < 1024)) throw ExiError.invalidEventCode("repeating element");
                            salesTariffEntryList.push(decodeSalesTariffEntryType(r));
                        }
                        done189 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new SalesTariffType(_id, _salesTariffID!, _salesTariffDescription, _numEPriceLevels, salesTariffEntryList);
}

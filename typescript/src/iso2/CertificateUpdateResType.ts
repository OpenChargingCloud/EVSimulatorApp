// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { BodyBaseType } from "./BodyBaseType.ts";
import { CertificateChainType, encodeCertificateChainType, decodeCertificateChainType } from "./CertificateChainType.ts";
import { ContractSignatureEncryptedPrivateKeyType, encodeContractSignatureEncryptedPrivateKeyType, decodeContractSignatureEncryptedPrivateKeyType } from "./ContractSignatureEncryptedPrivateKeyType.ts";
import { DiffieHellmanPublickeyType, encodeDiffieHellmanPublickeyType, decodeDiffieHellmanPublickeyType } from "./DiffieHellmanPublickeyType.ts";
import { EMAIDType, encodeEMAIDType, decodeEMAIDType } from "./EMAIDType.ts";
import { ResponseCode, ResponseCodeNames } from "./ResponseCode.ts";

export class CertificateUpdateResType extends BodyBaseType {
    readonly responseCode: ResponseCode;
    readonly sAProvisioningCertificateChain: CertificateChainType;
    readonly contractSignatureCertChain: CertificateChainType;
    readonly contractSignatureEncryptedPrivateKey: ContractSignatureEncryptedPrivateKeyType;
    readonly dHpublickey: DiffieHellmanPublickeyType;
    readonly eMAID: EMAIDType;
    readonly retryCounter: number | null;

    constructor(responseCode: ResponseCode, sAProvisioningCertificateChain: CertificateChainType, contractSignatureCertChain: CertificateChainType, contractSignatureEncryptedPrivateKey: ContractSignatureEncryptedPrivateKeyType, dHpublickey: DiffieHellmanPublickeyType, eMAID: EMAIDType, retryCounter: number | null) {
        super();
        this.responseCode = responseCode;
        this.sAProvisioningCertificateChain = sAProvisioningCertificateChain;
        this.contractSignatureCertChain = contractSignatureCertChain;
        this.contractSignatureEncryptedPrivateKey = contractSignatureEncryptedPrivateKey;
        this.dHpublickey = dHpublickey;
        this.eMAID = eMAID;
        this.retryCounter = retryCounter;
    }
}

export function encodeCertificateUpdateResType(w: BitWriter, msg: CertificateUpdateResType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.responseCode, 5)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    encodeCertificateChainType(w, msg.sAProvisioningCertificateChain)
    w.writeBits(0, 1)   // SE
    encodeCertificateChainType(w, msg.contractSignatureCertChain)
    w.writeBits(0, 1)   // SE
    encodeContractSignatureEncryptedPrivateKeyType(w, msg.contractSignatureEncryptedPrivateKey)
    w.writeBits(0, 1)   // SE
    encodeDiffieHellmanPublickeyType(w, msg.dHpublickey)
    w.writeBits(0, 1)   // SE
    encodeEMAIDType(w, msg.eMAID)
    let st72 = 0
    let done72 = false
    while (!done72) {
        { const _c73 = st72; if (false) {}   // dispatch
            else if (_c73 === 0) {
                if (msg.retryCounter != null) {
                    w.writeBits(0, 2)   // RetryCounter
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.retryCounter!));
                    w.writeBits(0, 1)   // child EE
                    st72 = 1
                } else {
                    w.writeBits(1, 2)   // element EE
                    done72 = true
                }
            }
            else if (_c73 === 1) {
                w.writeBits(0, 1)   // element EE
                done72 = true
            }
        }
    }
}

export function decodeCertificateUpdateResType(r: BitReader): CertificateUpdateResType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _responseCode = exiEnum("ResponseCode", ResponseCodeNames, r.readBits(5)) as ResponseCode
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    const _sAProvisioningCertificateChain = decodeCertificateChainType(r)
    r.readBits(1)   // SE
    const _contractSignatureCertChain = decodeCertificateChainType(r)
    r.readBits(1)   // SE
    const _contractSignatureEncryptedPrivateKey = decodeContractSignatureEncryptedPrivateKeyType(r)
    r.readBits(1)   // SE
    const _dHpublickey = decodeDiffieHellmanPublickeyType(r)
    r.readBits(1)   // SE
    const _eMAID = decodeEMAIDType(r)
    let _retryCounter: number | null = null
    let st74 = 0
    let done74 = false
    while (!done74) {
        { const _c75 = st74; if (false) {}   // dispatch
            else if (_c75 === 0) {
                { const _c76 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c76 === 0) {
                        r.readBits(1)   // value-start
                        _retryCounter = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st74 = 1
                    }
                    else if (_c76 === 1) done74 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c75 === 1) {
                { const _c77 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c77 === 0) done74 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new CertificateUpdateResType(_responseCode, _sAProvisioningCertificateChain, _contractSignatureCertChain, _contractSignatureEncryptedPrivateKey, _dHpublickey, _eMAID, _retryCounter);
}

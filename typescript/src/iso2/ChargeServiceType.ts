// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { ServiceCategory, ServiceCategoryNames } from "./ServiceCategory.ts";
import { ServiceType } from "./ServiceType.ts";
import { SupportedEnergyTransferModeType, encodeSupportedEnergyTransferModeType, decodeSupportedEnergyTransferModeType } from "./SupportedEnergyTransferModeType.ts";

export class ChargeServiceType extends ServiceType {
    readonly supportedEnergyTransferMode: SupportedEnergyTransferModeType;

    constructor(serviceID: number, serviceName: string | null, serviceCategory: ServiceCategory, serviceScope: string | null, freeService: boolean, supportedEnergyTransferMode: SupportedEnergyTransferModeType) {
        super(serviceID, serviceName, serviceCategory, serviceScope, freeService);
        this.supportedEnergyTransferMode = supportedEnergyTransferMode;
    }
}

export function encodeChargeServiceType(w: BitWriter, msg: ChargeServiceType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.serviceID));
    w.writeBits(0, 1)   // child EE
    let st136 = 0
    let done136 = false
    while (!done136) {
        { const _c137 = st136; if (false) {}   // dispatch
            else if (_c137 === 0) {
                if (msg.serviceName != null) {
                    w.writeBits(0, 2)   // ServiceName
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeStringValue(w, msg.serviceName!)
                    w.writeBits(0, 1)   // child EE
                    st136 = 1
                } else {
                    w.writeBits(1, 2)   // SE(ServiceCategory)
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.serviceCategory, 2)
                    w.writeBits(0, 1)   // child EE
                    done136 = true
                }
            }
            else if (_c137 === 1) {
                w.writeBits(0, 1)   // SE(ServiceCategory)
                w.writeBits(0, 1)   // value-start
                w.writeBits(msg.serviceCategory, 2)
                w.writeBits(0, 1)   // child EE
                done136 = true
            }
        }
    }
    let st138 = 0
    let done138 = false
    while (!done138) {
        { const _c139 = st138; if (false) {}   // dispatch
            else if (_c139 === 0) {
                if (msg.serviceScope != null) {
                    w.writeBits(0, 2)   // ServiceScope
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeStringValue(w, msg.serviceScope!)
                    w.writeBits(0, 1)   // child EE
                    st138 = 1
                } else {
                    w.writeBits(1, 2)   // SE(FreeService)
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.freeService ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    done138 = true
                }
            }
            else if (_c139 === 1) {
                w.writeBits(0, 1)   // SE(FreeService)
                w.writeBits(0, 1)   // value-start
                w.writeBits(msg.freeService ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                done138 = true
            }
        }
    }
    w.writeBits(0, 1)   // SE
    encodeSupportedEnergyTransferModeType(w, msg.supportedEnergyTransferMode)
    w.writeBits(0, 1)   // element EE
}

export function decodeChargeServiceType(r: BitReader): ChargeServiceType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _serviceID = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    let _serviceName: string | null = null
    let _serviceCategory: ServiceCategory = null
    let st140 = 0
    let done140 = false
    while (!done140) {
        { const _c141 = st140; if (false) {}   // dispatch
            else if (_c141 === 0) {
                { const _c142 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c142 === 0) {
                        r.readBits(1)   // value-start
                        _serviceName = ExiPrimitives.readStringValue(r, "ServiceName")
                        r.readBits(1)   // child EE
                        st140 = 1
                    }
                    else if (_c142 === 1) {   // SE(ServiceCategory)
                        r.readBits(1)   // value-start
                        _serviceCategory = exiEnum("ServiceCategory", ServiceCategoryNames, r.readBits(2)) as ServiceCategory
                        r.readBits(1)   // child EE
                        done140 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c141 === 1) {
                { const _c143 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c143 === 0) {   // SE(ServiceCategory)
                        r.readBits(1)   // value-start
                        _serviceCategory = exiEnum("ServiceCategory", ServiceCategoryNames, r.readBits(2)) as ServiceCategory
                        r.readBits(1)   // child EE
                        done140 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    let _serviceScope: string | null = null
    let _freeService: boolean = null
    let st144 = 0
    let done144 = false
    while (!done144) {
        { const _c145 = st144; if (false) {}   // dispatch
            else if (_c145 === 0) {
                { const _c146 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c146 === 0) {
                        r.readBits(1)   // value-start
                        _serviceScope = ExiPrimitives.readStringValue(r, "ServiceScope")
                        r.readBits(1)   // child EE
                        st144 = 1
                    }
                    else if (_c146 === 1) {   // SE(FreeService)
                        r.readBits(1)   // value-start
                        _freeService = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        done144 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c145 === 1) {
                { const _c147 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c147 === 0) {   // SE(FreeService)
                        r.readBits(1)   // value-start
                        _freeService = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        done144 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    r.readBits(1)   // SE
    const _supportedEnergyTransferMode = decodeSupportedEnergyTransferModeType(r)
    r.readBits(1)   // element EE
    return new ChargeServiceType(_serviceID, _serviceName, _serviceCategory!, _serviceScope, _freeService!, _supportedEnergyTransferMode);
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { SAScheduleTupleType, encodeSAScheduleTupleType, decodeSAScheduleTupleType } from "./SAScheduleTupleType.ts";
import { SASchedulesType } from "./SASchedulesType.ts";

export class SAScheduleListType extends SASchedulesType {
    readonly sAScheduleTuple: readonly SAScheduleTupleType[];

    constructor(sAScheduleTuple: readonly SAScheduleTupleType[]) {
        super();
        this.sAScheduleTuple = sAScheduleTuple;
    }
}

export function encodeSAScheduleListType(w: BitWriter, msg: SAScheduleListType): void {
    const list = msg.sAScheduleTuple
    if (list.length < 1 || list.length > 3) throw new RangeError("list: list size out of schema range");
    for (let i = 0; i < list.length; i++) {
        w.writeBits(0, i === 0 ? 1 : 2);   // SE(item)
        encodeSAScheduleTupleType(w, list[i])
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

export function decodeSAScheduleListType(r: BitReader): SAScheduleListType {
    const list = new Array<SAScheduleTupleType>()
    r.readBits(1)   // SE(item) first
    list.push(decodeSAScheduleTupleType(r));
    while (true) {
        const ec = r.readBits(2)
        if (ec === 1) break;   // element EE
        if (!(ec === 0 && list.length < 3)) throw ExiError.invalidEventCode("repeating element");
        list.push(decodeSAScheduleTupleType(r));
    }
    return new SAScheduleListType(list);
}

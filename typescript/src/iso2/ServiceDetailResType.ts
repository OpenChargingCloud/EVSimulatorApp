// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { BodyBaseType } from "./BodyBaseType.ts";
import { ResponseCode, ResponseCodeNames } from "./ResponseCode.ts";
import { ServiceParameterListType, encodeServiceParameterListType, decodeServiceParameterListType } from "./ServiceParameterListType.ts";

export class ServiceDetailResType extends BodyBaseType {
    readonly responseCode: ResponseCode;
    readonly serviceID: number;
    readonly serviceParameterList: ServiceParameterListType | null;

    constructor(responseCode: ResponseCode, serviceID: number, serviceParameterList: ServiceParameterListType | null) {
        super();
        this.responseCode = responseCode;
        this.serviceID = serviceID;
        this.serviceParameterList = serviceParameterList;
    }
}

export function encodeServiceDetailResType(w: BitWriter, msg: ServiceDetailResType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.responseCode, 5)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.serviceID));
    w.writeBits(0, 1)   // child EE
    let st25 = 0
    let done25 = false
    while (!done25) {
        { const _c26 = st25; if (false) {}   // dispatch
            else if (_c26 === 0) {
                if (msg.serviceParameterList != null) {
                    w.writeBits(0, 2)   // ServiceParameterList
                    encodeServiceParameterListType(w, msg.serviceParameterList!)
                    st25 = 1
                } else {
                    w.writeBits(1, 2)   // element EE
                    done25 = true
                }
            }
            else if (_c26 === 1) {
                w.writeBits(0, 1)   // element EE
                done25 = true
            }
        }
    }
}

export function decodeServiceDetailResType(r: BitReader): ServiceDetailResType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _responseCode = exiEnum("ResponseCode", ResponseCodeNames, r.readBits(5)) as ResponseCode
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _serviceID = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    let _serviceParameterList: ServiceParameterListType | null = null
    let st27 = 0
    let done27 = false
    while (!done27) {
        { const _c28 = st27; if (false) {}   // dispatch
            else if (_c28 === 0) {
                { const _c29 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c29 === 0) {
                        _serviceParameterList = decodeServiceParameterListType(r)
                        st27 = 1
                    }
                    else if (_c29 === 1) done27 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c28 === 1) {
                { const _c30 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c30 === 0) done27 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new ServiceDetailResType(_responseCode, _serviceID, _serviceParameterList);
}

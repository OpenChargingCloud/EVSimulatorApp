// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, exiEnum } from "../runtime/index.ts";
import { BodyBaseType } from "./BodyBaseType.ts";
import { ChargeProgress, ChargeProgressNames } from "./ChargeProgress.ts";
import { ChargingProfileType, encodeChargingProfileType, decodeChargingProfileType } from "./ChargingProfileType.ts";
import { DC_EVPowerDeliveryParameterType, encodeDC_EVPowerDeliveryParameterType, decodeDC_EVPowerDeliveryParameterType } from "./DC_EVPowerDeliveryParameterType.ts";
import { EVPowerDeliveryParameterType } from "./EVPowerDeliveryParameterType.ts";

export class PowerDeliveryReqType extends BodyBaseType {
    readonly chargeProgress: ChargeProgress;
    readonly sAScheduleTupleID: number;
    readonly chargingProfile: ChargingProfileType | null;
    readonly eVPowerDeliveryParameter: EVPowerDeliveryParameterType | null;

    constructor(chargeProgress: ChargeProgress, sAScheduleTupleID: number, chargingProfile: ChargingProfileType | null, eVPowerDeliveryParameter: EVPowerDeliveryParameterType | null) {
        super();
        this.chargeProgress = chargeProgress;
        this.sAScheduleTupleID = sAScheduleTupleID;
        this.chargingProfile = chargingProfile;
        this.eVPowerDeliveryParameter = eVPowerDeliveryParameter;
    }
}

export function encodePowerDeliveryReqType(w: BitWriter, msg: PowerDeliveryReqType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.chargeProgress, 2)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits((msg.sAScheduleTupleID - 1), 8)
    w.writeBits(0, 1)   // child EE
    let st51 = 0
    let done51 = false
    while (!done51) {
        { const _c52 = st51; if (false) {}   // dispatch
            else if (_c52 === 0) {
                const sub_eVPowerDeliveryParameter = msg.eVPowerDeliveryParameter
                if (msg.chargingProfile != null) {
                    w.writeBits(0, 3)   // ChargingProfile
                    encodeChargingProfileType(w, msg.chargingProfile!)
                    st51 = 1
                } else if (sub_eVPowerDeliveryParameter instanceof DC_EVPowerDeliveryParameterType) {
                    w.writeBits(1, 3)   // DC_EVPowerDeliveryParameter
                    encodeDC_EVPowerDeliveryParameterType(w, sub_eVPowerDeliveryParameter)
                    st51 = 2
                } else {
                    w.writeBits(3, 3)   // element EE
                    done51 = true
                }
            }
            else if (_c52 === 1) {
                const sub_eVPowerDeliveryParameter = msg.eVPowerDeliveryParameter
                if (sub_eVPowerDeliveryParameter instanceof DC_EVPowerDeliveryParameterType) {
                    w.writeBits(0, 2)   // DC_EVPowerDeliveryParameter
                    encodeDC_EVPowerDeliveryParameterType(w, sub_eVPowerDeliveryParameter)
                    st51 = 2
                } else {
                    w.writeBits(2, 2)   // element EE
                    done51 = true
                }
            }
            else if (_c52 === 2) {
                w.writeBits(0, 1)   // element EE
                done51 = true
            }
        }
    }
}

export function decodePowerDeliveryReqType(r: BitReader): PowerDeliveryReqType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _chargeProgress = exiEnum("ChargeProgress", ChargeProgressNames, r.readBits(2)) as ChargeProgress
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _sAScheduleTupleID = (r.readBits(8) + 1)
    r.readBits(1)   // child EE
    let _chargingProfile: ChargingProfileType | null = null
    let _eVPowerDeliveryParameter: EVPowerDeliveryParameterType | null = null
    let st53 = 0
    let done53 = false
    while (!done53) {
        { const _c54 = st53; if (false) {}   // dispatch
            else if (_c54 === 0) {
                { const _c55 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c55 === 0) {
                        _chargingProfile = decodeChargingProfileType(r)
                        st53 = 1
                    }
                    else if (_c55 === 1) {
                        _eVPowerDeliveryParameter = decodeDC_EVPowerDeliveryParameterType(r)
                        st53 = 2
                    }
                    else if (_c55 === 2) throw ExiError.invalidEventCode("abstract substitution head");
                    else if (_c55 === 3) done53 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c54 === 1) {
                { const _c56 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c56 === 0) {
                        _eVPowerDeliveryParameter = decodeDC_EVPowerDeliveryParameterType(r)
                        st53 = 2
                    }
                    else if (_c56 === 1) throw ExiError.invalidEventCode("abstract substitution head");
                    else if (_c56 === 2) done53 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c54 === 2) {
                { const _c57 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c57 === 0) done53 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new PowerDeliveryReqType(_chargeProgress, _sAScheduleTupleID, _chargingProfile, _eVPowerDeliveryParameter);
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { DC_EVSEStatusType, encodeDC_EVSEStatusType, decodeDC_EVSEStatusType } from "./DC_EVSEStatusType.ts";
import { EVSEChargeParameterType } from "./EVSEChargeParameterType.ts";
import { PhysicalValueType, encodePhysicalValueType, decodePhysicalValueType } from "./PhysicalValueType.ts";

export class DC_EVSEChargeParameterType extends EVSEChargeParameterType {
    readonly dC_EVSEStatus: DC_EVSEStatusType;
    readonly eVSEMaximumCurrentLimit: PhysicalValueType;
    readonly eVSEMaximumPowerLimit: PhysicalValueType;
    readonly eVSEMaximumVoltageLimit: PhysicalValueType;
    readonly eVSEMinimumCurrentLimit: PhysicalValueType;
    readonly eVSEMinimumVoltageLimit: PhysicalValueType;
    readonly eVSECurrentRegulationTolerance: PhysicalValueType | null;
    readonly eVSEPeakCurrentRipple: PhysicalValueType;
    readonly eVSEEnergyToBeDelivered: PhysicalValueType | null;

    constructor(dC_EVSEStatus: DC_EVSEStatusType, eVSEMaximumCurrentLimit: PhysicalValueType, eVSEMaximumPowerLimit: PhysicalValueType, eVSEMaximumVoltageLimit: PhysicalValueType, eVSEMinimumCurrentLimit: PhysicalValueType, eVSEMinimumVoltageLimit: PhysicalValueType, eVSECurrentRegulationTolerance: PhysicalValueType | null, eVSEPeakCurrentRipple: PhysicalValueType, eVSEEnergyToBeDelivered: PhysicalValueType | null) {
        super();
        this.dC_EVSEStatus = dC_EVSEStatus;
        this.eVSEMaximumCurrentLimit = eVSEMaximumCurrentLimit;
        this.eVSEMaximumPowerLimit = eVSEMaximumPowerLimit;
        this.eVSEMaximumVoltageLimit = eVSEMaximumVoltageLimit;
        this.eVSEMinimumCurrentLimit = eVSEMinimumCurrentLimit;
        this.eVSEMinimumVoltageLimit = eVSEMinimumVoltageLimit;
        this.eVSECurrentRegulationTolerance = eVSECurrentRegulationTolerance;
        this.eVSEPeakCurrentRipple = eVSEPeakCurrentRipple;
        this.eVSEEnergyToBeDelivered = eVSEEnergyToBeDelivered;
    }
}

export function encodeDC_EVSEChargeParameterType(w: BitWriter, msg: DC_EVSEChargeParameterType): void {
    w.writeBits(0, 1)   // SE
    encodeDC_EVSEStatusType(w, msg.dC_EVSEStatus)
    w.writeBits(0, 1)   // SE
    encodePhysicalValueType(w, msg.eVSEMaximumCurrentLimit)
    w.writeBits(0, 1)   // SE
    encodePhysicalValueType(w, msg.eVSEMaximumPowerLimit)
    w.writeBits(0, 1)   // SE
    encodePhysicalValueType(w, msg.eVSEMaximumVoltageLimit)
    w.writeBits(0, 1)   // SE
    encodePhysicalValueType(w, msg.eVSEMinimumCurrentLimit)
    w.writeBits(0, 1)   // SE
    encodePhysicalValueType(w, msg.eVSEMinimumVoltageLimit)
    let st248 = 0
    let done248 = false
    while (!done248) {
        { const _c249 = st248; if (false) {}   // dispatch
            else if (_c249 === 0) {
                if (msg.eVSECurrentRegulationTolerance != null) {
                    w.writeBits(0, 2)   // EVSECurrentRegulationTolerance
                    encodePhysicalValueType(w, msg.eVSECurrentRegulationTolerance!)
                    st248 = 1
                } else {
                    w.writeBits(1, 2)   // SE(EVSEPeakCurrentRipple)
                    encodePhysicalValueType(w, msg.eVSEPeakCurrentRipple)
                    done248 = true
                }
            }
            else if (_c249 === 1) {
                w.writeBits(0, 1)   // SE(EVSEPeakCurrentRipple)
                encodePhysicalValueType(w, msg.eVSEPeakCurrentRipple)
                done248 = true
            }
        }
    }
    let st250 = 0
    let done250 = false
    while (!done250) {
        { const _c251 = st250; if (false) {}   // dispatch
            else if (_c251 === 0) {
                if (msg.eVSEEnergyToBeDelivered != null) {
                    w.writeBits(0, 2)   // EVSEEnergyToBeDelivered
                    encodePhysicalValueType(w, msg.eVSEEnergyToBeDelivered!)
                    st250 = 1
                } else {
                    w.writeBits(1, 2)   // element EE
                    done250 = true
                }
            }
            else if (_c251 === 1) {
                w.writeBits(0, 1)   // element EE
                done250 = true
            }
        }
    }
}

export function decodeDC_EVSEChargeParameterType(r: BitReader): DC_EVSEChargeParameterType {
    r.readBits(1)   // SE
    const _dC_EVSEStatus = decodeDC_EVSEStatusType(r)
    r.readBits(1)   // SE
    const _eVSEMaximumCurrentLimit = decodePhysicalValueType(r)
    r.readBits(1)   // SE
    const _eVSEMaximumPowerLimit = decodePhysicalValueType(r)
    r.readBits(1)   // SE
    const _eVSEMaximumVoltageLimit = decodePhysicalValueType(r)
    r.readBits(1)   // SE
    const _eVSEMinimumCurrentLimit = decodePhysicalValueType(r)
    r.readBits(1)   // SE
    const _eVSEMinimumVoltageLimit = decodePhysicalValueType(r)
    let _eVSECurrentRegulationTolerance: PhysicalValueType | null = null
    let _eVSEPeakCurrentRipple: PhysicalValueType = null
    let st252 = 0
    let done252 = false
    while (!done252) {
        { const _c253 = st252; if (false) {}   // dispatch
            else if (_c253 === 0) {
                { const _c254 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c254 === 0) {
                        _eVSECurrentRegulationTolerance = decodePhysicalValueType(r)
                        st252 = 1
                    }
                    else if (_c254 === 1) {   // SE(EVSEPeakCurrentRipple)
                        _eVSEPeakCurrentRipple = decodePhysicalValueType(r)
                        done252 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c253 === 1) {
                { const _c255 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c255 === 0) {   // SE(EVSEPeakCurrentRipple)
                        _eVSEPeakCurrentRipple = decodePhysicalValueType(r)
                        done252 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    let _eVSEEnergyToBeDelivered: PhysicalValueType | null = null
    let st256 = 0
    let done256 = false
    while (!done256) {
        { const _c257 = st256; if (false) {}   // dispatch
            else if (_c257 === 0) {
                { const _c258 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c258 === 0) {
                        _eVSEEnergyToBeDelivered = decodePhysicalValueType(r)
                        st256 = 1
                    }
                    else if (_c258 === 1) done256 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c257 === 1) {
                { const _c259 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c259 === 0) done256 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new DC_EVSEChargeParameterType(_dC_EVSEStatus, _eVSEMaximumCurrentLimit, _eVSEMaximumPowerLimit, _eVSEMaximumVoltageLimit, _eVSEMinimumCurrentLimit, _eVSEMinimumVoltageLimit, _eVSECurrentRegulationTolerance, _eVSEPeakCurrentRipple!, _eVSEEnergyToBeDelivered);
}

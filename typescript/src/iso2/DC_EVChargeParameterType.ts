// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";
import { DC_EVStatusType, encodeDC_EVStatusType, decodeDC_EVStatusType } from "./DC_EVStatusType.ts";
import { EVChargeParameterType } from "./EVChargeParameterType.ts";
import { PhysicalValueType, encodePhysicalValueType, decodePhysicalValueType } from "./PhysicalValueType.ts";

export class DC_EVChargeParameterType extends EVChargeParameterType {
    readonly dC_EVStatus: DC_EVStatusType;
    readonly eVMaximumCurrentLimit: PhysicalValueType;
    readonly eVMaximumPowerLimit: PhysicalValueType | null;
    readonly eVMaximumVoltageLimit: PhysicalValueType;
    readonly eVEnergyCapacity: PhysicalValueType | null;
    readonly eVEnergyRequest: PhysicalValueType | null;
    readonly fullSOC: number | null;
    readonly bulkSOC: number | null;

    constructor(departureTime: number | null, dC_EVStatus: DC_EVStatusType, eVMaximumCurrentLimit: PhysicalValueType, eVMaximumPowerLimit: PhysicalValueType | null, eVMaximumVoltageLimit: PhysicalValueType, eVEnergyCapacity: PhysicalValueType | null, eVEnergyRequest: PhysicalValueType | null, fullSOC: number | null, bulkSOC: number | null) {
        super(departureTime);
        this.dC_EVStatus = dC_EVStatus;
        this.eVMaximumCurrentLimit = eVMaximumCurrentLimit;
        this.eVMaximumPowerLimit = eVMaximumPowerLimit;
        this.eVMaximumVoltageLimit = eVMaximumVoltageLimit;
        this.eVEnergyCapacity = eVEnergyCapacity;
        this.eVEnergyRequest = eVEnergyRequest;
        this.fullSOC = fullSOC;
        this.bulkSOC = bulkSOC;
    }
}

export function encodeDC_EVChargeParameterType(w: BitWriter, msg: DC_EVChargeParameterType): void {
    let st227 = 0
    let done227 = false
    while (!done227) {
        { const _c228 = st227; if (false) {}   // dispatch
            else if (_c228 === 0) {
                if (msg.departureTime != null) {
                    w.writeBits(0, 2)   // DepartureTime
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.departureTime!));
                    w.writeBits(0, 1)   // child EE
                    st227 = 1
                } else {
                    w.writeBits(1, 2)   // SE(DC_EVStatus)
                    encodeDC_EVStatusType(w, msg.dC_EVStatus)
                    done227 = true
                }
            }
            else if (_c228 === 1) {
                w.writeBits(0, 1)   // SE(DC_EVStatus)
                encodeDC_EVStatusType(w, msg.dC_EVStatus)
                done227 = true
            }
        }
    }
    w.writeBits(0, 1)   // SE
    encodePhysicalValueType(w, msg.eVMaximumCurrentLimit)
    let st229 = 0
    let done229 = false
    while (!done229) {
        { const _c230 = st229; if (false) {}   // dispatch
            else if (_c230 === 0) {
                if (msg.eVMaximumPowerLimit != null) {
                    w.writeBits(0, 2)   // EVMaximumPowerLimit
                    encodePhysicalValueType(w, msg.eVMaximumPowerLimit!)
                    st229 = 1
                } else {
                    w.writeBits(1, 2)   // SE(EVMaximumVoltageLimit)
                    encodePhysicalValueType(w, msg.eVMaximumVoltageLimit)
                    done229 = true
                }
            }
            else if (_c230 === 1) {
                w.writeBits(0, 1)   // SE(EVMaximumVoltageLimit)
                encodePhysicalValueType(w, msg.eVMaximumVoltageLimit)
                done229 = true
            }
        }
    }
    let st231 = 0
    let done231 = false
    while (!done231) {
        { const _c232 = st231; if (false) {}   // dispatch
            else if (_c232 === 0) {
                if (msg.eVEnergyCapacity != null) {
                    w.writeBits(0, 3)   // EVEnergyCapacity
                    encodePhysicalValueType(w, msg.eVEnergyCapacity!)
                    st231 = 1
                } else if (msg.eVEnergyRequest != null) {
                    w.writeBits(1, 3)   // EVEnergyRequest
                    encodePhysicalValueType(w, msg.eVEnergyRequest!)
                    st231 = 2
                } else if (msg.fullSOC != null) {
                    w.writeBits(2, 3)   // FullSOC
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.fullSOC!, 7)
                    w.writeBits(0, 1)   // child EE
                    st231 = 3
                } else if (msg.bulkSOC != null) {
                    w.writeBits(3, 3)   // BulkSOC
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.bulkSOC!, 7)
                    w.writeBits(0, 1)   // child EE
                    st231 = 4
                } else {
                    w.writeBits(4, 3)   // element EE
                    done231 = true
                }
            }
            else if (_c232 === 1) {
                if (msg.eVEnergyRequest != null) {
                    w.writeBits(0, 3)   // EVEnergyRequest
                    encodePhysicalValueType(w, msg.eVEnergyRequest!)
                    st231 = 2
                } else if (msg.fullSOC != null) {
                    w.writeBits(1, 3)   // FullSOC
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.fullSOC!, 7)
                    w.writeBits(0, 1)   // child EE
                    st231 = 3
                } else if (msg.bulkSOC != null) {
                    w.writeBits(2, 3)   // BulkSOC
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.bulkSOC!, 7)
                    w.writeBits(0, 1)   // child EE
                    st231 = 4
                } else {
                    w.writeBits(3, 3)   // element EE
                    done231 = true
                }
            }
            else if (_c232 === 2) {
                if (msg.fullSOC != null) {
                    w.writeBits(0, 2)   // FullSOC
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.fullSOC!, 7)
                    w.writeBits(0, 1)   // child EE
                    st231 = 3
                } else if (msg.bulkSOC != null) {
                    w.writeBits(1, 2)   // BulkSOC
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.bulkSOC!, 7)
                    w.writeBits(0, 1)   // child EE
                    st231 = 4
                } else {
                    w.writeBits(2, 2)   // element EE
                    done231 = true
                }
            }
            else if (_c232 === 3) {
                if (msg.bulkSOC != null) {
                    w.writeBits(0, 2)   // BulkSOC
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.bulkSOC!, 7)
                    w.writeBits(0, 1)   // child EE
                    st231 = 4
                } else {
                    w.writeBits(1, 2)   // element EE
                    done231 = true
                }
            }
            else if (_c232 === 4) {
                w.writeBits(0, 1)   // element EE
                done231 = true
            }
        }
    }
}

export function decodeDC_EVChargeParameterType(r: BitReader): DC_EVChargeParameterType {
    let _departureTime: number | null = null
    let _dC_EVStatus: DC_EVStatusType = null
    let st233 = 0
    let done233 = false
    while (!done233) {
        { const _c234 = st233; if (false) {}   // dispatch
            else if (_c234 === 0) {
                { const _c235 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c235 === 0) {
                        r.readBits(1)   // value-start
                        _departureTime = Number(ExiPrimitives.readUnsignedInteger(r))
                        r.readBits(1)   // child EE
                        st233 = 1
                    }
                    else if (_c235 === 1) {   // SE(DC_EVStatus)
                        _dC_EVStatus = decodeDC_EVStatusType(r)
                        done233 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c234 === 1) {
                { const _c236 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c236 === 0) {   // SE(DC_EVStatus)
                        _dC_EVStatus = decodeDC_EVStatusType(r)
                        done233 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    r.readBits(1)   // SE
    const _eVMaximumCurrentLimit = decodePhysicalValueType(r)
    let _eVMaximumPowerLimit: PhysicalValueType | null = null
    let _eVMaximumVoltageLimit: PhysicalValueType = null
    let st237 = 0
    let done237 = false
    while (!done237) {
        { const _c238 = st237; if (false) {}   // dispatch
            else if (_c238 === 0) {
                { const _c239 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c239 === 0) {
                        _eVMaximumPowerLimit = decodePhysicalValueType(r)
                        st237 = 1
                    }
                    else if (_c239 === 1) {   // SE(EVMaximumVoltageLimit)
                        _eVMaximumVoltageLimit = decodePhysicalValueType(r)
                        done237 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c238 === 1) {
                { const _c240 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c240 === 0) {   // SE(EVMaximumVoltageLimit)
                        _eVMaximumVoltageLimit = decodePhysicalValueType(r)
                        done237 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    let _eVEnergyCapacity: PhysicalValueType | null = null
    let _eVEnergyRequest: PhysicalValueType | null = null
    let _fullSOC: number | null = null
    let _bulkSOC: number | null = null
    let st241 = 0
    let done241 = false
    while (!done241) {
        { const _c242 = st241; if (false) {}   // dispatch
            else if (_c242 === 0) {
                { const _c243 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c243 === 0) {
                        _eVEnergyCapacity = decodePhysicalValueType(r)
                        st241 = 1
                    }
                    else if (_c243 === 1) {
                        _eVEnergyRequest = decodePhysicalValueType(r)
                        st241 = 2
                    }
                    else if (_c243 === 2) {
                        r.readBits(1)   // value-start
                        _fullSOC = r.readBits(7)
                        r.readBits(1)   // child EE
                        st241 = 3
                    }
                    else if (_c243 === 3) {
                        r.readBits(1)   // value-start
                        _bulkSOC = r.readBits(7)
                        r.readBits(1)   // child EE
                        st241 = 4
                    }
                    else if (_c243 === 4) done241 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c242 === 1) {
                { const _c244 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c244 === 0) {
                        _eVEnergyRequest = decodePhysicalValueType(r)
                        st241 = 2
                    }
                    else if (_c244 === 1) {
                        r.readBits(1)   // value-start
                        _fullSOC = r.readBits(7)
                        r.readBits(1)   // child EE
                        st241 = 3
                    }
                    else if (_c244 === 2) {
                        r.readBits(1)   // value-start
                        _bulkSOC = r.readBits(7)
                        r.readBits(1)   // child EE
                        st241 = 4
                    }
                    else if (_c244 === 3) done241 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c242 === 2) {
                { const _c245 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c245 === 0) {
                        r.readBits(1)   // value-start
                        _fullSOC = r.readBits(7)
                        r.readBits(1)   // child EE
                        st241 = 3
                    }
                    else if (_c245 === 1) {
                        r.readBits(1)   // value-start
                        _bulkSOC = r.readBits(7)
                        r.readBits(1)   // child EE
                        st241 = 4
                    }
                    else if (_c245 === 2) done241 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c242 === 3) {
                { const _c246 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c246 === 0) {
                        r.readBits(1)   // value-start
                        _bulkSOC = r.readBits(7)
                        r.readBits(1)   // child EE
                        st241 = 4
                    }
                    else if (_c246 === 1) done241 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c242 === 4) {
                { const _c247 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c247 === 0) done241 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new DC_EVChargeParameterType(_departureTime, _dC_EVStatus!, _eVMaximumCurrentLimit, _eVMaximumPowerLimit, _eVMaximumVoltageLimit!, _eVEnergyCapacity, _eVEnergyRequest, _fullSOC, _bulkSOC);
}

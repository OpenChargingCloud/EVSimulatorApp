// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { EVSENotification, EVSENotificationNames } from "./EVSENotification.ts";
import { EVSEStatusType } from "./EVSEStatusType.ts";

export class AC_EVSEStatusType extends EVSEStatusType {
    readonly rCD: boolean;

    constructor(notificationMaxDelay: number, eVSENotification: EVSENotification, rCD: boolean) {
        super(notificationMaxDelay, eVSENotification);
        this.rCD = rCD;
    }
}

export function encodeAC_EVSEStatusType(w: BitWriter, msg: AC_EVSEStatusType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.notificationMaxDelay));
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.eVSENotification, 2)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.rCD ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

export function decodeAC_EVSEStatusType(r: BitReader): AC_EVSEStatusType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _notificationMaxDelay = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _eVSENotification = exiEnum("EVSENotification", EVSENotificationNames, r.readBits(2)) as EVSENotification
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _rCD = r.readBits(1) !== 0
    r.readBits(1)   // child EE
    r.readBits(1)   // element EE
    return new AC_EVSEStatusType(_notificationMaxDelay, _eVSENotification, _rCD);
}

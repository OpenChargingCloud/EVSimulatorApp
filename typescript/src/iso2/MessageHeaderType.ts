// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";
import { NotificationType, encodeNotificationType, decodeNotificationType } from "./NotificationType.ts";
import { SignatureType, encodeSignatureType, decodeSignatureType } from "./SignatureType.ts";

export class MessageHeaderType {
    readonly sessionID: Uint8Array;
    readonly notification: NotificationType | null;
    readonly signature: SignatureType | null;

    constructor(sessionID: Uint8Array, notification: NotificationType | null, signature: SignatureType | null) {
        this.sessionID = sessionID;
        this.notification = notification;
        this.signature = signature;
    }
}

export function encodeMessageHeaderType(w: BitWriter, msg: MessageHeaderType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeBinary(w, msg.sessionID)
    w.writeBits(0, 1)   // child EE
    let st272 = 0
    let done272 = false
    while (!done272) {
        { const _c273 = st272; if (false) {}   // dispatch
            else if (_c273 === 0) {
                if (msg.notification != null) {
                    w.writeBits(0, 2)   // Notification
                    encodeNotificationType(w, msg.notification!)
                    st272 = 1
                } else if (msg.signature != null) {
                    w.writeBits(1, 2)   // Signature
                    encodeSignatureType(w, msg.signature!)
                    st272 = 2
                } else {
                    w.writeBits(2, 2)   // element EE
                    done272 = true
                }
            }
            else if (_c273 === 1) {
                if (msg.signature != null) {
                    w.writeBits(0, 2)   // Signature
                    encodeSignatureType(w, msg.signature!)
                    st272 = 2
                } else {
                    w.writeBits(1, 2)   // element EE
                    done272 = true
                }
            }
            else if (_c273 === 2) {
                w.writeBits(0, 1)   // element EE
                done272 = true
            }
        }
    }
}

export function decodeMessageHeaderType(r: BitReader): MessageHeaderType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _sessionID = ExiPrimitives.readBinary(r)
    r.readBits(1)   // child EE
    let _notification: NotificationType | null = null
    let _signature: SignatureType | null = null
    let st274 = 0
    let done274 = false
    while (!done274) {
        { const _c275 = st274; if (false) {}   // dispatch
            else if (_c275 === 0) {
                { const _c276 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c276 === 0) {
                        _notification = decodeNotificationType(r)
                        st274 = 1
                    }
                    else if (_c276 === 1) {
                        _signature = decodeSignatureType(r)
                        st274 = 2
                    }
                    else if (_c276 === 2) done274 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c275 === 1) {
                { const _c277 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c277 === 0) {
                        _signature = decodeSignatureType(r)
                        st274 = 2
                    }
                    else if (_c277 === 1) done274 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c275 === 2) {
                { const _c278 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c278 === 0) done274 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new MessageHeaderType(_sessionID, _notification, _signature);
}

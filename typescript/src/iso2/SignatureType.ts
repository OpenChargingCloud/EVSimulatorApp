// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives, exiUnsupported } from "../runtime/index.ts";
import { KeyInfo } from "./KeyInfo.ts";
import { Object } from "./Object.ts";
import { SignatureValueType, encodeSignatureValueType, decodeSignatureValueType } from "./SignatureValueType.ts";
import { SignedInfoType, encodeSignedInfoType, decodeSignedInfoType } from "./SignedInfoType.ts";

export class SignatureType {
    readonly id: string | null;
    readonly signedInfo: SignedInfoType;
    readonly signatureValue: SignatureValueType;
    readonly keyInfo: KeyInfo | null;
    readonly object_: Object | null;

    constructor(id: string | null, signedInfo: SignedInfoType, signatureValue: SignatureValueType, keyInfo: KeyInfo | null, object_: Object | null) {
        this.id = id;
        this.signedInfo = signedInfo;
        this.signatureValue = signatureValue;
        this.keyInfo = keyInfo;
        this.object_ = object_;
    }
}

export function encodeSignatureType(w: BitWriter, msg: SignatureType): void {
    let st279 = 0
    let done279 = false
    while (!done279) {
        { const _c280 = st279; if (false) {}   // dispatch
            else if (_c280 === 0) {
                if (msg.id != null) {
                    w.writeBits(0, 2)   // Id
                    ExiPrimitives.writeStringValue(w, msg.id!)
                    st279 = 1
                } else {
                    w.writeBits(1, 2)   // SE(SignedInfo)
                    encodeSignedInfoType(w, msg.signedInfo)
                    done279 = true
                }
            }
            else if (_c280 === 1) {
                w.writeBits(0, 1)   // SE(SignedInfo)
                encodeSignedInfoType(w, msg.signedInfo)
                done279 = true
            }
        }
    }
    w.writeBits(0, 1)   // SE
    encodeSignatureValueType(w, msg.signatureValue)
    let st281 = 0
    let done281 = false
    while (!done281) {
        { const _c282 = st281; if (false) {}   // dispatch
            else if (_c282 === 0) {
                if (msg.keyInfo != null) {
                    w.writeBits(0, 2)   // KeyInfo
                    exiUnsupported("encoding a present KeyInfo (XMLDSig)");
                    st281 = 1
                } else if (msg.object_ != null) {
                    w.writeBits(1, 2)   // Object
                    exiUnsupported("encoding a present Object (XMLDSig)");
                    st281 = 2
                } else {
                    w.writeBits(2, 2)   // element EE
                    done281 = true
                }
            }
            else if (_c282 === 1) {
                if (msg.object_ != null) {
                    w.writeBits(0, 2)   // Object
                    exiUnsupported("encoding a present Object (XMLDSig)");
                    st281 = 2
                } else {
                    w.writeBits(1, 2)   // element EE
                    done281 = true
                }
            }
            else if (_c282 === 2) {
                w.writeBits(0, 1)   // element EE
                done281 = true
            }
        }
    }
}

export function decodeSignatureType(r: BitReader): SignatureType {
    let _id: string | null = null
    let _signedInfo: SignedInfoType = null
    let st283 = 0
    let done283 = false
    while (!done283) {
        { const _c284 = st283; if (false) {}   // dispatch
            else if (_c284 === 0) {
                { const _c285 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c285 === 0) {
                        _id = ExiPrimitives.readStringValue(r, "Id")
                        st283 = 1
                    }
                    else if (_c285 === 1) {   // SE(SignedInfo)
                        _signedInfo = decodeSignedInfoType(r)
                        done283 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c284 === 1) {
                { const _c286 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c286 === 0) {   // SE(SignedInfo)
                        _signedInfo = decodeSignedInfoType(r)
                        done283 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    r.readBits(1)   // SE
    const _signatureValue = decodeSignatureValueType(r)
    let _keyInfo: KeyInfo | null = null
    let _object: Object | null = null
    let st287 = 0
    let done287 = false
    while (!done287) {
        { const _c288 = st287; if (false) {}   // dispatch
            else if (_c288 === 0) {
                { const _c289 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c289 === 0) {
                        _keyInfo = exiUnsupported("decoding a present KeyInfo (XMLDSig)")
                        st287 = 1
                    }
                    else if (_c289 === 1) {
                        _object = exiUnsupported("decoding a present Object (XMLDSig)")
                        st287 = 2
                    }
                    else if (_c289 === 2) done287 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c288 === 1) {
                { const _c290 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c290 === 0) {
                        _object = exiUnsupported("decoding a present Object (XMLDSig)")
                        st287 = 2
                    }
                    else if (_c290 === 1) done287 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c288 === 2) {
                { const _c291 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c291 === 0) done287 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new SignatureType(_id, _signedInfo!, _signatureValue, _keyInfo, _object);
}

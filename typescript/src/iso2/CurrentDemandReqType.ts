// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { BodyBaseType } from "./BodyBaseType.ts";
import { DC_EVStatusType, encodeDC_EVStatusType, decodeDC_EVStatusType } from "./DC_EVStatusType.ts";
import { PhysicalValueType, encodePhysicalValueType, decodePhysicalValueType } from "./PhysicalValueType.ts";

export class CurrentDemandReqType extends BodyBaseType {
    readonly dC_EVStatus: DC_EVStatusType;
    readonly eVTargetCurrent: PhysicalValueType;
    readonly eVMaximumVoltageLimit: PhysicalValueType | null;
    readonly eVMaximumCurrentLimit: PhysicalValueType | null;
    readonly eVMaximumPowerLimit: PhysicalValueType | null;
    readonly bulkChargingComplete: boolean | null;
    readonly chargingComplete: boolean;
    readonly remainingTimeToFullSoC: PhysicalValueType | null;
    readonly remainingTimeToBulkSoC: PhysicalValueType | null;
    readonly eVTargetVoltage: PhysicalValueType;

    constructor(dC_EVStatus: DC_EVStatusType, eVTargetCurrent: PhysicalValueType, eVMaximumVoltageLimit: PhysicalValueType | null, eVMaximumCurrentLimit: PhysicalValueType | null, eVMaximumPowerLimit: PhysicalValueType | null, bulkChargingComplete: boolean | null, chargingComplete: boolean, remainingTimeToFullSoC: PhysicalValueType | null, remainingTimeToBulkSoC: PhysicalValueType | null, eVTargetVoltage: PhysicalValueType) {
        super();
        this.dC_EVStatus = dC_EVStatus;
        this.eVTargetCurrent = eVTargetCurrent;
        this.eVMaximumVoltageLimit = eVMaximumVoltageLimit;
        this.eVMaximumCurrentLimit = eVMaximumCurrentLimit;
        this.eVMaximumPowerLimit = eVMaximumPowerLimit;
        this.bulkChargingComplete = bulkChargingComplete;
        this.chargingComplete = chargingComplete;
        this.remainingTimeToFullSoC = remainingTimeToFullSoC;
        this.remainingTimeToBulkSoC = remainingTimeToBulkSoC;
        this.eVTargetVoltage = eVTargetVoltage;
    }
}

export function encodeCurrentDemandReqType(w: BitWriter, msg: CurrentDemandReqType): void {
    w.writeBits(0, 1)   // SE
    encodeDC_EVStatusType(w, msg.dC_EVStatus)
    w.writeBits(0, 1)   // SE
    encodePhysicalValueType(w, msg.eVTargetCurrent)
    let st86 = 0
    let done86 = false
    while (!done86) {
        { const _c87 = st86; if (false) {}   // dispatch
            else if (_c87 === 0) {
                if (msg.eVMaximumVoltageLimit != null) {
                    w.writeBits(0, 3)   // EVMaximumVoltageLimit
                    encodePhysicalValueType(w, msg.eVMaximumVoltageLimit!)
                    st86 = 1
                } else if (msg.eVMaximumCurrentLimit != null) {
                    w.writeBits(1, 3)   // EVMaximumCurrentLimit
                    encodePhysicalValueType(w, msg.eVMaximumCurrentLimit!)
                    st86 = 2
                } else if (msg.eVMaximumPowerLimit != null) {
                    w.writeBits(2, 3)   // EVMaximumPowerLimit
                    encodePhysicalValueType(w, msg.eVMaximumPowerLimit!)
                    st86 = 3
                } else if (msg.bulkChargingComplete != null) {
                    w.writeBits(3, 3)   // BulkChargingComplete
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.bulkChargingComplete! ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    st86 = 4
                } else {
                    w.writeBits(4, 3)   // SE(ChargingComplete)
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.chargingComplete ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    done86 = true
                }
            }
            else if (_c87 === 1) {
                if (msg.eVMaximumCurrentLimit != null) {
                    w.writeBits(0, 3)   // EVMaximumCurrentLimit
                    encodePhysicalValueType(w, msg.eVMaximumCurrentLimit!)
                    st86 = 2
                } else if (msg.eVMaximumPowerLimit != null) {
                    w.writeBits(1, 3)   // EVMaximumPowerLimit
                    encodePhysicalValueType(w, msg.eVMaximumPowerLimit!)
                    st86 = 3
                } else if (msg.bulkChargingComplete != null) {
                    w.writeBits(2, 3)   // BulkChargingComplete
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.bulkChargingComplete! ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    st86 = 4
                } else {
                    w.writeBits(3, 3)   // SE(ChargingComplete)
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.chargingComplete ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    done86 = true
                }
            }
            else if (_c87 === 2) {
                if (msg.eVMaximumPowerLimit != null) {
                    w.writeBits(0, 2)   // EVMaximumPowerLimit
                    encodePhysicalValueType(w, msg.eVMaximumPowerLimit!)
                    st86 = 3
                } else if (msg.bulkChargingComplete != null) {
                    w.writeBits(1, 2)   // BulkChargingComplete
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.bulkChargingComplete! ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    st86 = 4
                } else {
                    w.writeBits(2, 2)   // SE(ChargingComplete)
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.chargingComplete ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    done86 = true
                }
            }
            else if (_c87 === 3) {
                if (msg.bulkChargingComplete != null) {
                    w.writeBits(0, 2)   // BulkChargingComplete
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.bulkChargingComplete! ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    st86 = 4
                } else {
                    w.writeBits(1, 2)   // SE(ChargingComplete)
                    w.writeBits(0, 1)   // value-start
                    w.writeBits(msg.chargingComplete ? 1 : 0, 1)
                    w.writeBits(0, 1)   // child EE
                    done86 = true
                }
            }
            else if (_c87 === 4) {
                w.writeBits(0, 1)   // SE(ChargingComplete)
                w.writeBits(0, 1)   // value-start
                w.writeBits(msg.chargingComplete ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                done86 = true
            }
        }
    }
    let st88 = 0
    let done88 = false
    while (!done88) {
        { const _c89 = st88; if (false) {}   // dispatch
            else if (_c89 === 0) {
                if (msg.remainingTimeToFullSoC != null) {
                    w.writeBits(0, 2)   // RemainingTimeToFullSoC
                    encodePhysicalValueType(w, msg.remainingTimeToFullSoC!)
                    st88 = 1
                } else if (msg.remainingTimeToBulkSoC != null) {
                    w.writeBits(1, 2)   // RemainingTimeToBulkSoC
                    encodePhysicalValueType(w, msg.remainingTimeToBulkSoC!)
                    st88 = 2
                } else {
                    w.writeBits(2, 2)   // SE(EVTargetVoltage)
                    encodePhysicalValueType(w, msg.eVTargetVoltage)
                    done88 = true
                }
            }
            else if (_c89 === 1) {
                if (msg.remainingTimeToBulkSoC != null) {
                    w.writeBits(0, 2)   // RemainingTimeToBulkSoC
                    encodePhysicalValueType(w, msg.remainingTimeToBulkSoC!)
                    st88 = 2
                } else {
                    w.writeBits(1, 2)   // SE(EVTargetVoltage)
                    encodePhysicalValueType(w, msg.eVTargetVoltage)
                    done88 = true
                }
            }
            else if (_c89 === 2) {
                w.writeBits(0, 1)   // SE(EVTargetVoltage)
                encodePhysicalValueType(w, msg.eVTargetVoltage)
                done88 = true
            }
        }
    }
    w.writeBits(0, 1)   // element EE
}

export function decodeCurrentDemandReqType(r: BitReader): CurrentDemandReqType {
    r.readBits(1)   // SE
    const _dC_EVStatus = decodeDC_EVStatusType(r)
    r.readBits(1)   // SE
    const _eVTargetCurrent = decodePhysicalValueType(r)
    let _eVMaximumVoltageLimit: PhysicalValueType | null = null
    let _eVMaximumCurrentLimit: PhysicalValueType | null = null
    let _eVMaximumPowerLimit: PhysicalValueType | null = null
    let _bulkChargingComplete: boolean | null = null
    let _chargingComplete: boolean = null
    let st90 = 0
    let done90 = false
    while (!done90) {
        { const _c91 = st90; if (false) {}   // dispatch
            else if (_c91 === 0) {
                { const _c92 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c92 === 0) {
                        _eVMaximumVoltageLimit = decodePhysicalValueType(r)
                        st90 = 1
                    }
                    else if (_c92 === 1) {
                        _eVMaximumCurrentLimit = decodePhysicalValueType(r)
                        st90 = 2
                    }
                    else if (_c92 === 2) {
                        _eVMaximumPowerLimit = decodePhysicalValueType(r)
                        st90 = 3
                    }
                    else if (_c92 === 3) {
                        r.readBits(1)   // value-start
                        _bulkChargingComplete = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        st90 = 4
                    }
                    else if (_c92 === 4) {   // SE(ChargingComplete)
                        r.readBits(1)   // value-start
                        _chargingComplete = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        done90 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c91 === 1) {
                { const _c93 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c93 === 0) {
                        _eVMaximumCurrentLimit = decodePhysicalValueType(r)
                        st90 = 2
                    }
                    else if (_c93 === 1) {
                        _eVMaximumPowerLimit = decodePhysicalValueType(r)
                        st90 = 3
                    }
                    else if (_c93 === 2) {
                        r.readBits(1)   // value-start
                        _bulkChargingComplete = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        st90 = 4
                    }
                    else if (_c93 === 3) {   // SE(ChargingComplete)
                        r.readBits(1)   // value-start
                        _chargingComplete = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        done90 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c91 === 2) {
                { const _c94 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c94 === 0) {
                        _eVMaximumPowerLimit = decodePhysicalValueType(r)
                        st90 = 3
                    }
                    else if (_c94 === 1) {
                        r.readBits(1)   // value-start
                        _bulkChargingComplete = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        st90 = 4
                    }
                    else if (_c94 === 2) {   // SE(ChargingComplete)
                        r.readBits(1)   // value-start
                        _chargingComplete = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        done90 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c91 === 3) {
                { const _c95 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c95 === 0) {
                        r.readBits(1)   // value-start
                        _bulkChargingComplete = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        st90 = 4
                    }
                    else if (_c95 === 1) {   // SE(ChargingComplete)
                        r.readBits(1)   // value-start
                        _chargingComplete = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        done90 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c91 === 4) {
                { const _c96 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c96 === 0) {   // SE(ChargingComplete)
                        r.readBits(1)   // value-start
                        _chargingComplete = r.readBits(1) !== 0
                        r.readBits(1)   // child EE
                        done90 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    let _remainingTimeToFullSoC: PhysicalValueType | null = null
    let _remainingTimeToBulkSoC: PhysicalValueType | null = null
    let _eVTargetVoltage: PhysicalValueType = null
    let st97 = 0
    let done97 = false
    while (!done97) {
        { const _c98 = st97; if (false) {}   // dispatch
            else if (_c98 === 0) {
                { const _c99 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c99 === 0) {
                        _remainingTimeToFullSoC = decodePhysicalValueType(r)
                        st97 = 1
                    }
                    else if (_c99 === 1) {
                        _remainingTimeToBulkSoC = decodePhysicalValueType(r)
                        st97 = 2
                    }
                    else if (_c99 === 2) {   // SE(EVTargetVoltage)
                        _eVTargetVoltage = decodePhysicalValueType(r)
                        done97 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c98 === 1) {
                { const _c100 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c100 === 0) {
                        _remainingTimeToBulkSoC = decodePhysicalValueType(r)
                        st97 = 2
                    }
                    else if (_c100 === 1) {   // SE(EVTargetVoltage)
                        _eVTargetVoltage = decodePhysicalValueType(r)
                        done97 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c98 === 2) {
                { const _c101 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c101 === 0) {   // SE(EVTargetVoltage)
                        _eVTargetVoltage = decodePhysicalValueType(r)
                        done97 = true
                    }
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    r.readBits(1)   // element EE
    return new CurrentDemandReqType(_dC_EVStatus, _eVTargetCurrent, _eVMaximumVoltageLimit, _eVMaximumCurrentLimit, _eVMaximumPowerLimit, _bulkChargingComplete, _chargingComplete!, _remainingTimeToFullSoC, _remainingTimeToBulkSoC, _eVTargetVoltage!);
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { PMaxScheduleType, encodePMaxScheduleType, decodePMaxScheduleType } from "./PMaxScheduleType.ts";
import { SalesTariffType, encodeSalesTariffType, decodeSalesTariffType } from "./SalesTariffType.ts";

export class SAScheduleTupleType {
    readonly sAScheduleTupleID: number;
    readonly pMaxSchedule: PMaxScheduleType;
    readonly salesTariff: SalesTariffType | null;

    constructor(sAScheduleTupleID: number, pMaxSchedule: PMaxScheduleType, salesTariff: SalesTariffType | null) {
        this.sAScheduleTupleID = sAScheduleTupleID;
        this.pMaxSchedule = pMaxSchedule;
        this.salesTariff = salesTariff;
    }
}

export function encodeSAScheduleTupleType(w: BitWriter, msg: SAScheduleTupleType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits((msg.sAScheduleTupleID - 1), 8)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    encodePMaxScheduleType(w, msg.pMaxSchedule)
    let st175 = 0
    let done175 = false
    while (!done175) {
        { const _c176 = st175; if (false) {}   // dispatch
            else if (_c176 === 0) {
                if (msg.salesTariff != null) {
                    w.writeBits(0, 2)   // SalesTariff
                    encodeSalesTariffType(w, msg.salesTariff!)
                    st175 = 1
                } else {
                    w.writeBits(1, 2)   // element EE
                    done175 = true
                }
            }
            else if (_c176 === 1) {
                w.writeBits(0, 1)   // element EE
                done175 = true
            }
        }
    }
}

export function decodeSAScheduleTupleType(r: BitReader): SAScheduleTupleType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _sAScheduleTupleID = (r.readBits(8) + 1)
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    const _pMaxSchedule = decodePMaxScheduleType(r)
    let _salesTariff: SalesTariffType | null = null
    let st177 = 0
    let done177 = false
    while (!done177) {
        { const _c178 = st177; if (false) {}   // dispatch
            else if (_c178 === 0) {
                { const _c179 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c179 === 0) {
                        _salesTariff = decodeSalesTariffType(r)
                        st177 = 1
                    }
                    else if (_c179 === 1) done177 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c178 === 1) {
                { const _c180 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c180 === 0) done177 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new SAScheduleTupleType(_sAScheduleTupleID, _pMaxSchedule, _salesTariff);
}

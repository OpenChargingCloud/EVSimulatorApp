// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";

export class SelectedServiceType {
    readonly serviceID: number;
    readonly parameterSetID: number | null;

    constructor(serviceID: number, parameterSetID: number | null) {
        this.serviceID = serviceID;
        this.parameterSetID = parameterSetID;
    }
}

export function encodeSelectedServiceType(w: BitWriter, msg: SelectedServiceType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, BigInt(msg.serviceID));
    w.writeBits(0, 1)   // child EE
    let st129 = 0
    let done129 = false
    while (!done129) {
        { const _c130 = st129; if (false) {}   // dispatch
            else if (_c130 === 0) {
                if (msg.parameterSetID != null) {
                    w.writeBits(0, 2)   // ParameterSetID
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.parameterSetID!));
                    w.writeBits(0, 1)   // child EE
                    st129 = 1
                } else {
                    w.writeBits(1, 2)   // element EE
                    done129 = true
                }
            }
            else if (_c130 === 1) {
                w.writeBits(0, 1)   // element EE
                done129 = true
            }
        }
    }
}

export function decodeSelectedServiceType(r: BitReader): SelectedServiceType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _serviceID = Number(ExiPrimitives.readUnsignedInteger(r))
    r.readBits(1)   // child EE
    let _parameterSetID: number | null = null
    let st131 = 0
    let done131 = false
    while (!done131) {
        { const _c132 = st131; if (false) {}   // dispatch
            else if (_c132 === 0) {
                { const _c133 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c133 === 0) {
                        r.readBits(1)   // value-start
                        _parameterSetID = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st131 = 1
                    }
                    else if (_c133 === 1) done131 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c132 === 1) {
                { const _c134 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c134 === 0) done131 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new SelectedServiceType(_serviceID, _parameterSetID);
}

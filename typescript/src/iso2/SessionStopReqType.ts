// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, exiEnum } from "../runtime/index.ts";
import { BodyBaseType } from "./BodyBaseType.ts";
import { ChargingSession, ChargingSessionNames } from "./ChargingSession.ts";

export class SessionStopReqType extends BodyBaseType {
    readonly chargingSession: ChargingSession;

    constructor(chargingSession: ChargingSession) {
        super();
        this.chargingSession = chargingSession;
    }
}

export function encodeSessionStopReqType(w: BitWriter, msg: SessionStopReqType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.chargingSession, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

export function decodeSessionStopReqType(r: BitReader): SessionStopReqType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _chargingSession = exiEnum("ChargingSession", ChargingSessionNames, r.readBits(1)) as ChargingSession
    r.readBits(1)   // child EE
    r.readBits(1)   // element EE
    return new SessionStopReqType(_chargingSession);
}

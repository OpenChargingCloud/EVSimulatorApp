// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { FaultCode, FaultCodeNames } from "./FaultCode.ts";

export class NotificationType {
    readonly faultCode: FaultCode;
    readonly faultMsg: string | null;

    constructor(faultCode: FaultCode, faultMsg: string | null) {
        this.faultCode = faultCode;
        this.faultMsg = faultMsg;
    }
}

export function encodeNotificationType(w: BitWriter, msg: NotificationType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.faultCode, 2)
    w.writeBits(0, 1)   // child EE
    let st169 = 0
    let done169 = false
    while (!done169) {
        { const _c170 = st169; if (false) {}   // dispatch
            else if (_c170 === 0) {
                if (msg.faultMsg != null) {
                    w.writeBits(0, 2)   // FaultMsg
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeStringValue(w, msg.faultMsg!)
                    w.writeBits(0, 1)   // child EE
                    st169 = 1
                } else {
                    w.writeBits(1, 2)   // element EE
                    done169 = true
                }
            }
            else if (_c170 === 1) {
                w.writeBits(0, 1)   // element EE
                done169 = true
            }
        }
    }
}

export function decodeNotificationType(r: BitReader): NotificationType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _faultCode = exiEnum("FaultCode", FaultCodeNames, r.readBits(2)) as FaultCode
    r.readBits(1)   // child EE
    let _faultMsg: string | null = null
    let st171 = 0
    let done171 = false
    while (!done171) {
        { const _c172 = st171; if (false) {}   // dispatch
            else if (_c172 === 0) {
                { const _c173 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c173 === 0) {
                        r.readBits(1)   // value-start
                        _faultMsg = ExiPrimitives.readStringValue(r, "FaultMsg")
                        r.readBits(1)   // child EE
                        st171 = 1
                    }
                    else if (_c173 === 1) done171 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c172 === 1) {
                { const _c174 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c174 === 0) done171 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new NotificationType(_faultCode, _faultMsg);
}

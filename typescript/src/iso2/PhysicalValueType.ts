// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiPrimitives, exiEnum } from "../runtime/index.ts";
import { UnitSymbol, UnitSymbolNames } from "./UnitSymbol.ts";

export class PhysicalValueType {
    readonly multiplier: number;
    readonly unit: UnitSymbol;
    readonly value: number;

    constructor(multiplier: number, unit: UnitSymbol, value: number) {
        this.multiplier = multiplier;
        this.unit = unit;
        this.value = value;
    }
}

export function encodePhysicalValueType(w: BitWriter, msg: PhysicalValueType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits((msg.multiplier - -3), 3)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.unit, 3)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeSignedInteger(w, BigInt(msg.value));
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

export function decodePhysicalValueType(r: BitReader): PhysicalValueType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _multiplier = (r.readBits(3) + -3)
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _unit = exiEnum("UnitSymbol", UnitSymbolNames, r.readBits(3)) as UnitSymbol
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _value = Number(ExiPrimitives.readSignedInteger(r))
    r.readBits(1)   // child EE
    r.readBits(1)   // element EE
    return new PhysicalValueType(_multiplier, _unit, _value);
}

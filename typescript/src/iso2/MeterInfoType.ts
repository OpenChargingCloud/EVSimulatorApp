// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, ExiError, ExiPrimitives } from "../runtime/index.ts";

export class MeterInfoType {
    readonly meterID: string;
    readonly meterReading: bigint | null;
    readonly sigMeterReading: Uint8Array | null;
    readonly meterStatus: number | null;
    readonly tMeter: bigint | null;

    constructor(meterID: string, meterReading: bigint | null, sigMeterReading: Uint8Array | null, meterStatus: number | null, tMeter: bigint | null) {
        this.meterID = meterID;
        this.meterReading = meterReading;
        this.sigMeterReading = sigMeterReading;
        this.meterStatus = meterStatus;
        this.tMeter = tMeter;
    }
}

export function encodeMeterInfoType(w: BitWriter, msg: MeterInfoType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeStringValue(w, msg.meterID)
    w.writeBits(0, 1)   // child EE
    let st160 = 0
    let done160 = false
    while (!done160) {
        { const _c161 = st160; if (false) {}   // dispatch
            else if (_c161 === 0) {
                if (msg.meterReading != null) {
                    w.writeBits(0, 3)   // MeterReading
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.meterReading!);
                    w.writeBits(0, 1)   // child EE
                    st160 = 1
                } else if (msg.sigMeterReading != null) {
                    w.writeBits(1, 3)   // SigMeterReading
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeBinary(w, msg.sigMeterReading!)
                    w.writeBits(0, 1)   // child EE
                    st160 = 2
                } else if (msg.meterStatus != null) {
                    w.writeBits(2, 3)   // MeterStatus
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.meterStatus!));
                    w.writeBits(0, 1)   // child EE
                    st160 = 3
                } else if (msg.tMeter != null) {
                    w.writeBits(3, 3)   // TMeter
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, msg.tMeter!);
                    w.writeBits(0, 1)   // child EE
                    st160 = 4
                } else {
                    w.writeBits(4, 3)   // element EE
                    done160 = true
                }
            }
            else if (_c161 === 1) {
                if (msg.sigMeterReading != null) {
                    w.writeBits(0, 3)   // SigMeterReading
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeBinary(w, msg.sigMeterReading!)
                    w.writeBits(0, 1)   // child EE
                    st160 = 2
                } else if (msg.meterStatus != null) {
                    w.writeBits(1, 3)   // MeterStatus
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.meterStatus!));
                    w.writeBits(0, 1)   // child EE
                    st160 = 3
                } else if (msg.tMeter != null) {
                    w.writeBits(2, 3)   // TMeter
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, msg.tMeter!);
                    w.writeBits(0, 1)   // child EE
                    st160 = 4
                } else {
                    w.writeBits(3, 3)   // element EE
                    done160 = true
                }
            }
            else if (_c161 === 2) {
                if (msg.meterStatus != null) {
                    w.writeBits(0, 2)   // MeterStatus
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, BigInt(msg.meterStatus!));
                    w.writeBits(0, 1)   // child EE
                    st160 = 3
                } else if (msg.tMeter != null) {
                    w.writeBits(1, 2)   // TMeter
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, msg.tMeter!);
                    w.writeBits(0, 1)   // child EE
                    st160 = 4
                } else {
                    w.writeBits(2, 2)   // element EE
                    done160 = true
                }
            }
            else if (_c161 === 3) {
                if (msg.tMeter != null) {
                    w.writeBits(0, 2)   // TMeter
                    w.writeBits(0, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, msg.tMeter!);
                    w.writeBits(0, 1)   // child EE
                    st160 = 4
                } else {
                    w.writeBits(1, 2)   // element EE
                    done160 = true
                }
            }
            else if (_c161 === 4) {
                w.writeBits(0, 1)   // element EE
                done160 = true
            }
        }
    }
}

export function decodeMeterInfoType(r: BitReader): MeterInfoType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _meterID = ExiPrimitives.readStringValue(r, "MeterID")
    r.readBits(1)   // child EE
    let _meterReading: bigint | null = null
    let _sigMeterReading: Uint8Array | null = null
    let _meterStatus: number | null = null
    let _tMeter: bigint | null = null
    let st162 = 0
    let done162 = false
    while (!done162) {
        { const _c163 = st162; if (false) {}   // dispatch
            else if (_c163 === 0) {
                { const _c164 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c164 === 0) {
                        r.readBits(1)   // value-start
                        _meterReading = ExiPrimitives.readUnsignedInteger(r)
                        r.readBits(1)   // child EE
                        st162 = 1
                    }
                    else if (_c164 === 1) {
                        r.readBits(1)   // value-start
                        _sigMeterReading = ExiPrimitives.readBinary(r)
                        r.readBits(1)   // child EE
                        st162 = 2
                    }
                    else if (_c164 === 2) {
                        r.readBits(1)   // value-start
                        _meterStatus = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st162 = 3
                    }
                    else if (_c164 === 3) {
                        r.readBits(1)   // value-start
                        _tMeter = ExiPrimitives.readSignedInteger(r)
                        r.readBits(1)   // child EE
                        st162 = 4
                    }
                    else if (_c164 === 4) done162 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c163 === 1) {
                { const _c165 = r.readBits(3); if (false) {}   // dispatch
                    else if (_c165 === 0) {
                        r.readBits(1)   // value-start
                        _sigMeterReading = ExiPrimitives.readBinary(r)
                        r.readBits(1)   // child EE
                        st162 = 2
                    }
                    else if (_c165 === 1) {
                        r.readBits(1)   // value-start
                        _meterStatus = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st162 = 3
                    }
                    else if (_c165 === 2) {
                        r.readBits(1)   // value-start
                        _tMeter = ExiPrimitives.readSignedInteger(r)
                        r.readBits(1)   // child EE
                        st162 = 4
                    }
                    else if (_c165 === 3) done162 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c163 === 2) {
                { const _c166 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c166 === 0) {
                        r.readBits(1)   // value-start
                        _meterStatus = Number(ExiPrimitives.readSignedInteger(r))
                        r.readBits(1)   // child EE
                        st162 = 3
                    }
                    else if (_c166 === 1) {
                        r.readBits(1)   // value-start
                        _tMeter = ExiPrimitives.readSignedInteger(r)
                        r.readBits(1)   // child EE
                        st162 = 4
                    }
                    else if (_c166 === 2) done162 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c163 === 3) {
                { const _c167 = r.readBits(2); if (false) {}   // dispatch
                    else if (_c167 === 0) {
                        r.readBits(1)   // value-start
                        _tMeter = ExiPrimitives.readSignedInteger(r)
                        r.readBits(1)   // child EE
                        st162 = 4
                    }
                    else if (_c167 === 1) done162 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
            else if (_c163 === 4) {
                { const _c168 = r.readBits(1); if (false) {}   // dispatch
                    else if (_c168 === 0) done162 = true;   // element EE
                    else throw ExiError.invalidEventCode("optional run");
                }
            }
        }
    }
    return new MeterInfoType(_meterID, _meterReading, _sigMeterReading, _meterStatus, _tMeter);
}

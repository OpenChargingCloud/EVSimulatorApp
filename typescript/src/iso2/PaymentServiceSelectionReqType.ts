// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import { BitReader, BitWriter, exiEnum } from "../runtime/index.ts";
import { BodyBaseType } from "./BodyBaseType.ts";
import { PaymentOption, PaymentOptionNames } from "./PaymentOption.ts";
import { SelectedServiceListType, encodeSelectedServiceListType, decodeSelectedServiceListType } from "./SelectedServiceListType.ts";

export class PaymentServiceSelectionReqType extends BodyBaseType {
    readonly selectedPaymentOption: PaymentOption;
    readonly selectedServiceList: SelectedServiceListType;

    constructor(selectedPaymentOption: PaymentOption, selectedServiceList: SelectedServiceListType) {
        super();
        this.selectedPaymentOption = selectedPaymentOption;
        this.selectedServiceList = selectedServiceList;
    }
}

export function encodePaymentServiceSelectionReqType(w: BitWriter, msg: PaymentServiceSelectionReqType): void {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.selectedPaymentOption, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    encodeSelectedServiceListType(w, msg.selectedServiceList)
    w.writeBits(0, 1)   // element EE
}

export function decodePaymentServiceSelectionReqType(r: BitReader): PaymentServiceSelectionReqType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    const _selectedPaymentOption = exiEnum("PaymentOption", PaymentOptionNames, r.readBits(1)) as PaymentOption
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    const _selectedServiceList = decodeSelectedServiceListType(r)
    r.readBits(1)   // element EE
    return new PaymentServiceSelectionReqType(_selectedPaymentOption, _selectedServiceList);
}

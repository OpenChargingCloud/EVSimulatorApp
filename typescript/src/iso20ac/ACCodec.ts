// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (TypeScript back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.ac

import { BitReader, BitWriter, ExiError } from "../runtime/index.ts";
import { AC_CPDReqEnergyTransferMode, encodeAC_CPDReqEnergyTransferMode, decodeAC_CPDReqEnergyTransferMode } from "./AC_CPDReqEnergyTransferMode.ts";
import { AC_CPDResEnergyTransferMode, encodeAC_CPDResEnergyTransferMode, decodeAC_CPDResEnergyTransferMode } from "./AC_CPDResEnergyTransferMode.ts";
import { AC_ChargeLoopReq, encodeAC_ChargeLoopReq, decodeAC_ChargeLoopReq } from "./AC_ChargeLoopReq.ts";
import { AC_ChargeLoopRes, encodeAC_ChargeLoopRes, decodeAC_ChargeLoopRes } from "./AC_ChargeLoopRes.ts";
import { AC_ChargeParameterDiscoveryReq, encodeAC_ChargeParameterDiscoveryReq, decodeAC_ChargeParameterDiscoveryReq } from "./AC_ChargeParameterDiscoveryReq.ts";
import { AC_ChargeParameterDiscoveryRes, encodeAC_ChargeParameterDiscoveryRes, decodeAC_ChargeParameterDiscoveryRes } from "./AC_ChargeParameterDiscoveryRes.ts";
import { AC_ChargeParameterDiscoveryResType, encodeAC_ChargeParameterDiscoveryResType, decodeAC_ChargeParameterDiscoveryResType } from "./AC_ChargeParameterDiscoveryResType.ts";
import { SignedInfoType, encodeSignedInfoType, decodeSignedInfoType } from "./SignedInfoType.ts";

export const ACCodec = {

    exiHeader: 0x80,

    encodeAC_ChargeParameterDiscoveryReq(msg: AC_ChargeParameterDiscoveryReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(4, 6);   // document element selector
        encodeAC_ChargeParameterDiscoveryReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeAC_ChargeParameterDiscoveryRes(msg: AC_ChargeParameterDiscoveryRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(5, 6);   // document element selector
        encodeAC_ChargeParameterDiscoveryRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeAC_ChargeLoopReq(msg: AC_ChargeLoopReq): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(2, 6);   // document element selector
        encodeAC_ChargeLoopReq(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeAC_ChargeLoopRes(msg: AC_ChargeLoopRes): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(3, 6);   // document element selector
        encodeAC_ChargeLoopRes(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeAC_CPDReqEnergyTransferMode(msg: AC_CPDReqEnergyTransferMode): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(0, 6);   // document element selector
        encodeAC_CPDReqEnergyTransferMode(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    encodeAC_CPDResEnergyTransferMode(msg: AC_CPDResEnergyTransferMode): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(1, 6);   // document element selector
        encodeAC_CPDResEnergyTransferMode(w, msg);
        w.alignToByte();
        return w.bytes;
    },

    /** Encodes any document element of this message set, dispatching on its runtime type. */
    encodeAny(msg: unknown): Uint8Array {
        if (msg instanceof AC_ChargeLoopReq) return this.encodeAC_ChargeLoopReq(msg);
        if (msg instanceof AC_ChargeLoopRes) return this.encodeAC_ChargeLoopRes(msg);
        if (msg instanceof AC_ChargeParameterDiscoveryReq) return this.encodeAC_ChargeParameterDiscoveryReq(msg);
        if (msg instanceof AC_ChargeParameterDiscoveryRes) return this.encodeAC_ChargeParameterDiscoveryRes(msg);
        if (msg instanceof AC_CPDReqEnergyTransferMode) return this.encodeAC_CPDReqEnergyTransferMode(msg);
        if (msg instanceof AC_CPDResEnergyTransferMode) return this.encodeAC_CPDResEnergyTransferMode(msg);
        throw new TypeError(
            `${(msg as object)?.constructor?.name} is not a document element of this message set.`);
    },

    decodeAny(src: Uint8Array): unknown {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        const selector = r.readBits(6);
        switch (selector) {
            case 4: return decodeAC_ChargeParameterDiscoveryReq(r);
            case 5: return decodeAC_ChargeParameterDiscoveryRes(r);
            case 2: return decodeAC_ChargeLoopReq(r);
            case 3: return decodeAC_ChargeLoopRes(r);
            case 0: return decodeAC_CPDReqEnergyTransferMode(r);
            case 1: return decodeAC_CPDResEnergyTransferMode(r);
            default: throw ExiError.unknownDocumentIndex(selector);
        }
    },

    encodeFragment_AC_ChargeParameterDiscoveryRes(content: AC_ChargeParameterDiscoveryResType): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(5, 8);   // fragment SE(AC_ChargeParameterDiscoveryRes)
        encodeAC_ChargeParameterDiscoveryResType(w, content);
        w.writeBits(155, 8);   // End Fragment (ED)
        w.alignToByte();
        return w.bytes;
    },

    decodeFragment_AC_ChargeParameterDiscoveryRes(src: Uint8Array): AC_ChargeParameterDiscoveryResType {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        if (r.readBits(8) !== 5) throw ExiError.invalidEventCode("not a AC_ChargeParameterDiscoveryRes fragment");
        const result = decodeAC_ChargeParameterDiscoveryResType(r);
        if (r.readBits(8) !== 155) throw ExiError.invalidEventCode("missing End Fragment");
        return result;
    },

    encodeFragment_SignedInfo(content: SignedInfoType): Uint8Array {
        const w = new BitWriter();
        w.writeBits(0x80, 8);
        w.writeBits(135, 8);   // fragment SE(SignedInfo)
        encodeSignedInfoType(w, content);
        w.writeBits(155, 8);   // End Fragment (ED)
        w.alignToByte();
        return w.bytes;
    },

    decodeFragment_SignedInfo(src: Uint8Array): SignedInfoType {
        if (src.length === 0 || src[0] !== 0x80) throw ExiError.invalidHeader();
        const r = new BitReader(src, 1);
        if (r.readBits(8) !== 135) throw ExiError.invalidEventCode("not a SignedInfo fragment");
        const result = decodeSignedInfoType(r);
        if (r.readBits(8) !== 155) throw ExiError.invalidEventCode("missing End Fragment");
        return result;
    },

};

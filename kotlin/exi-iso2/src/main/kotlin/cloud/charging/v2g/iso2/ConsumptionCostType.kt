// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Kotlin back end). Do not edit by hand.

package cloud.charging.v2g.iso2

import cloud.charging.v2g.exi.BitReader
import cloud.charging.v2g.exi.BitWriter

data class ConsumptionCostType(
    val startValue: PhysicalValueType,
    val cost: List<CostType>
)

internal fun encodeConsumptionCostType(w: BitWriter, msg: ConsumptionCostType) {
    w.writeBits(0u, 1)   // SE
    encodePhysicalValueType(w, msg.startValue)
    val costList = msg.cost
    require(costList.size in 1..3) { "list size out of schema range" }
    for (i in costList.indices) {
        w.writeBits(0u, if (i == 0) 1 else 2)   // SE(item)
        encodeCostType(w, costList[i])
    }
    w.writeBits(1u, 2)   // list terminator / element EE
}

internal fun decodeConsumptionCostType(r: BitReader): ConsumptionCostType {
    r.readBits(1)   // SE
    val _startValue = decodePhysicalValueType(r)
    val costList = ArrayList<CostType>()
    r.readBits(1)   // SE(item) first
    costList.add(decodeCostType(r))
    while (true) {
        val ec = r.readBits(2)
        if (ec == 1u) break   // element EE
        require(ec == 0u && costList.size < 3) { "invalid repeating-element event code" }
        costList.add(decodeCostType(r))
    }
    return ConsumptionCostType(_startValue, costList)
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Kotlin back end). Do not edit by hand.

package cloud.charging.v2g.iso2

import cloud.charging.v2g.exi.BitReader
import cloud.charging.v2g.exi.BitWriter
import cloud.charging.v2g.exi.ExiPrimitives

data class SalesTariffType(
    val id: String?,
    val salesTariffID: UByte,
    val salesTariffDescription: String?,
    val numEPriceLevels: UByte?,
    val salesTariffEntry: List<SalesTariffEntryType>
)

internal fun encodeSalesTariffType(w: BitWriter, msg: SalesTariffType) {
    var st54 = 0
    var done54 = false
    while (!done54) {
        when (st54) {
            0 -> {
                if (msg.id != null) {
                    w.writeBits(0u, 2)   // Id
                    ExiPrimitives.writeStringValue(w, msg.id!!)
                    st54 = 1
                } else {
                    w.writeBits(1u, 2)   // SE(SalesTariffID)
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits((msg.salesTariffID.toLong() - 1L).toUInt(), 8)
                    w.writeBits(0u, 1)   // child EE
                    done54 = true
                }
            }
            1 -> {
                w.writeBits(0u, 1)   // SE(SalesTariffID)
                w.writeBits(0u, 1)   // value-start
                w.writeBits((msg.salesTariffID.toLong() - 1L).toUInt(), 8)
                w.writeBits(0u, 1)   // child EE
                done54 = true
            }
        }
    }
    var st55 = 0
    var done55 = false
    while (!done55) {
        when (st55) {
            0 -> {
                if (msg.salesTariffDescription != null) {
                    w.writeBits(0u, 2)   // SalesTariffDescription
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeStringValue(w, msg.salesTariffDescription!!)
                    w.writeBits(0u, 1)   // child EE
                    st55 = 1
                } else if (msg.numEPriceLevels != null) {
                    w.writeBits(1u, 2)   // NumEPriceLevels
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits(msg.numEPriceLevels!!.toLong().toUInt(), 8)
                    w.writeBits(0u, 1)   // child EE
                    st55 = 2
                } else {
                    require(msg.salesTariffEntry.size in 1..1024) { "list size out of schema range" }
                    w.writeBits(2u, 2)   // SalesTariffEntry
                    encodeSalesTariffEntryType(w, msg.salesTariffEntry[0])
                    for (ci in 1 until msg.salesTariffEntry.size) {
                        w.writeBits(0u, 2)   // SalesTariffEntry
                        encodeSalesTariffEntryType(w, msg.salesTariffEntry[ci])
                    }
                    w.writeBits(1u, 2)   // element EE (list end)
                    done55 = true
                }
            }
            1 -> {
                if (msg.numEPriceLevels != null) {
                    w.writeBits(0u, 2)   // NumEPriceLevels
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits(msg.numEPriceLevels!!.toLong().toUInt(), 8)
                    w.writeBits(0u, 1)   // child EE
                    st55 = 2
                } else {
                    require(msg.salesTariffEntry.size in 1..1024) { "list size out of schema range" }
                    w.writeBits(1u, 2)   // SalesTariffEntry
                    encodeSalesTariffEntryType(w, msg.salesTariffEntry[0])
                    for (ci in 1 until msg.salesTariffEntry.size) {
                        w.writeBits(0u, 2)   // SalesTariffEntry
                        encodeSalesTariffEntryType(w, msg.salesTariffEntry[ci])
                    }
                    w.writeBits(1u, 2)   // element EE (list end)
                    done55 = true
                }
            }
            2 -> {
                require(msg.salesTariffEntry.size in 1..1024) { "list size out of schema range" }
                w.writeBits(0u, 1)   // SalesTariffEntry
                encodeSalesTariffEntryType(w, msg.salesTariffEntry[0])
                for (ci in 1 until msg.salesTariffEntry.size) {
                    w.writeBits(0u, 2)   // SalesTariffEntry
                    encodeSalesTariffEntryType(w, msg.salesTariffEntry[ci])
                }
                w.writeBits(1u, 2)   // element EE (list end)
                done55 = true
            }
        }
    }
}

internal fun decodeSalesTariffType(r: BitReader): SalesTariffType {
    var _id: String? = null
    var _salesTariffID: UByte? = null
    var st56 = 0
    var done56 = false
    while (!done56) {
        when (st56) {
            0 -> {
                when (r.readBits(2)) {
                    0u -> {
                        _id = ExiPrimitives.readStringValue(r, "Id")
                        st56 = 1
                    }
                    1u -> {   // SE(SalesTariffID)
                        r.readBits(1)   // value-start
                        _salesTariffID = (r.readBits(8).toLong() + 1L).toUByte()
                        r.readBits(1)   // child EE
                        done56 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(1)) {
                    0u -> {   // SE(SalesTariffID)
                        r.readBits(1)   // value-start
                        _salesTariffID = (r.readBits(8).toLong() + 1L).toUByte()
                        r.readBits(1)   // child EE
                        done56 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    var _salesTariffDescription: String? = null
    var _numEPriceLevels: UByte? = null
    val salesTariffEntryList = ArrayList<SalesTariffEntryType>()
    var st57 = 0
    var done57 = false
    while (!done57) {
        when (st57) {
            0 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _salesTariffDescription = ExiPrimitives.readStringValue(r, "SalesTariffDescription")
                        r.readBits(1)   // child EE
                        st57 = 1
                    }
                    1u -> {
                        r.readBits(1)   // value-start
                        _numEPriceLevels = r.readBits(8).toUByte()
                        r.readBits(1)   // child EE
                        st57 = 2
                    }
                    2u -> {   // SalesTariffEntry
                        salesTariffEntryList.add(decodeSalesTariffEntryType(r))
                        while (true) {
                            val lc = r.readBits(2)
                            if (lc == 1u) break   // element EE (list end)
                            require(lc == 0u && salesTariffEntryList.size < 1024) { "invalid repeating-element event code" }
                            salesTariffEntryList.add(decodeSalesTariffEntryType(r))
                        }
                        done57 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _numEPriceLevels = r.readBits(8).toUByte()
                        r.readBits(1)   // child EE
                        st57 = 2
                    }
                    1u -> {   // SalesTariffEntry
                        salesTariffEntryList.add(decodeSalesTariffEntryType(r))
                        while (true) {
                            val lc = r.readBits(2)
                            if (lc == 1u) break   // element EE (list end)
                            require(lc == 0u && salesTariffEntryList.size < 1024) { "invalid repeating-element event code" }
                            salesTariffEntryList.add(decodeSalesTariffEntryType(r))
                        }
                        done57 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            2 -> {
                when (r.readBits(1)) {
                    0u -> {   // SalesTariffEntry
                        salesTariffEntryList.add(decodeSalesTariffEntryType(r))
                        while (true) {
                            val lc = r.readBits(2)
                            if (lc == 1u) break   // element EE (list end)
                            require(lc == 0u && salesTariffEntryList.size < 1024) { "invalid repeating-element event code" }
                            salesTariffEntryList.add(decodeSalesTariffEntryType(r))
                        }
                        done57 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    return SalesTariffType(_id, _salesTariffID!!, _salesTariffDescription, _numEPriceLevels, salesTariffEntryList)
}

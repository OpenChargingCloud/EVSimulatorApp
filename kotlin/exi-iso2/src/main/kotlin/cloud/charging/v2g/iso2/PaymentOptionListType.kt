// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Kotlin back end). Do not edit by hand.

package cloud.charging.v2g.iso2

import cloud.charging.v2g.exi.BitReader
import cloud.charging.v2g.exi.BitWriter

data class PaymentOptionListType(
    val paymentOption: List<PaymentOption>
)

internal fun encodePaymentOptionListType(w: BitWriter, msg: PaymentOptionListType) {
    val list = msg.paymentOption
    require(list.size in 1..2) { "list size out of schema range" }
    for (i in list.indices) {
        w.writeBits(0u, if (i == 0) 1 else 2)   // SE(item)
        w.writeBits(0u, 1)   // value-start
        w.writeBits(list[i].ordinal.toUInt(), 1)
        w.writeBits(0u, 1)   // child EE
    }
    if (list.size >= 2) w.writeBits(0u, 1)   // element EE (list at max)
    else w.writeBits(1u, 2)   // element EE
}

internal fun decodePaymentOptionListType(r: BitReader): PaymentOptionListType {
    val list = ArrayList<PaymentOption>()
    r.readBits(1)   // SE(item) first
    r.readBits(1)   // value-start
    val listFirst = PaymentOption.entries[r.readBits(1).toInt()]
    r.readBits(1)   // child EE
    list.add(listFirst)
    while (true) {
        if (list.size >= 2) { r.readBits(1); break }   // element EE (list at max)
        val ec = r.readBits(2)
        if (ec == 1u) break   // element EE
        require(ec == 0u && list.size < 2) { "invalid repeating-element event code" }
        r.readBits(1)   // value-start
        val listNext = PaymentOption.entries[r.readBits(1).toInt()]
        r.readBits(1)   // child EE
        list.add(listNext)
    }
    return PaymentOptionListType(list)
}

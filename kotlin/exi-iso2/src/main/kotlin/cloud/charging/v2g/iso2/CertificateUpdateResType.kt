// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Kotlin back end). Do not edit by hand.

package cloud.charging.v2g.iso2

import cloud.charging.v2g.exi.BitReader
import cloud.charging.v2g.exi.BitWriter
import cloud.charging.v2g.exi.ExiPrimitives

class CertificateUpdateResType(
    val responseCode: ResponseCode,
    val sAProvisioningCertificateChain: CertificateChainType,
    val contractSignatureCertChain: CertificateChainType,
    val contractSignatureEncryptedPrivateKey: ContractSignatureEncryptedPrivateKeyType,
    val dHpublickey: DiffieHellmanPublickeyType,
    val eMAID: EMAIDType,
    val retryCounter: Short?
) : BodyBaseType()

internal fun encodeCertificateUpdateResType(w: BitWriter, msg: CertificateUpdateResType) {
    w.writeBits(0u, 1)   // SE
    w.writeBits(0u, 1)   // value-start
    w.writeBits(msg.responseCode.ordinal.toUInt(), 5)
    w.writeBits(0u, 1)   // child EE
    w.writeBits(0u, 1)   // SE
    encodeCertificateChainType(w, msg.sAProvisioningCertificateChain)
    w.writeBits(0u, 1)   // SE
    encodeCertificateChainType(w, msg.contractSignatureCertChain)
    w.writeBits(0u, 1)   // SE
    encodeContractSignatureEncryptedPrivateKeyType(w, msg.contractSignatureEncryptedPrivateKey)
    w.writeBits(0u, 1)   // SE
    encodeDiffieHellmanPublickeyType(w, msg.dHpublickey)
    w.writeBits(0u, 1)   // SE
    encodeEMAIDType(w, msg.eMAID)
    var st22 = 0
    var done22 = false
    while (!done22) {
        when (st22) {
            0 -> {
                if (msg.retryCounter != null) {
                    w.writeBits(0u, 2)   // RetryCounter
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeSignedInteger(w, msg.retryCounter!!.toLong())
                    w.writeBits(0u, 1)   // child EE
                    st22 = 1
                } else {
                    w.writeBits(1u, 2)   // element EE
                    done22 = true
                }
            }
            1 -> {
                w.writeBits(0u, 1)   // element EE
                done22 = true
            }
        }
    }
}

internal fun decodeCertificateUpdateResType(r: BitReader): CertificateUpdateResType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    val _responseCode = ResponseCode.entries[r.readBits(5).toInt()]
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    val _sAProvisioningCertificateChain = decodeCertificateChainType(r)
    r.readBits(1)   // SE
    val _contractSignatureCertChain = decodeCertificateChainType(r)
    r.readBits(1)   // SE
    val _contractSignatureEncryptedPrivateKey = decodeContractSignatureEncryptedPrivateKeyType(r)
    r.readBits(1)   // SE
    val _dHpublickey = decodeDiffieHellmanPublickeyType(r)
    r.readBits(1)   // SE
    val _eMAID = decodeEMAIDType(r)
    var _retryCounter: Short? = null
    var st23 = 0
    var done23 = false
    while (!done23) {
        when (st23) {
            0 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _retryCounter = ExiPrimitives.readSignedInteger(r).toShort()
                        r.readBits(1)   // child EE
                        st23 = 1
                    }
                    1u -> done23 = true   // element EE
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(1)) {
                    0u -> done23 = true   // element EE
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    return CertificateUpdateResType(_responseCode, _sAProvisioningCertificateChain, _contractSignatureCertChain, _contractSignatureEncryptedPrivateKey, _dHpublickey, _eMAID, _retryCounter)
}

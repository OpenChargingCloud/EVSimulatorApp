// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Kotlin back end). Do not edit by hand.

package cloud.charging.v2g.iso2

import cloud.charging.v2g.exi.BitReader
import cloud.charging.v2g.exi.BitWriter

class SalesTariffEntryType(
    override val timeInterval: IntervalType,
    val ePriceLevel: UByte?,
    val consumptionCost: List<ConsumptionCostType>
) : EntryType(timeInterval)

internal fun encodeSalesTariffEntryType(w: BitWriter, msg: SalesTariffEntryType) {
    when (val v = msg.timeInterval) {
        is RelativeTimeIntervalType -> {
            w.writeBits(0u, 2)   // RelativeTimeInterval
            encodeRelativeTimeIntervalType(w, v)
        }
        else -> throw IllegalArgumentException("unsupported substitution member for TimeInterval")
    }
    var st58 = 0
    var done58 = false
    while (!done58) {
        when (st58) {
            0 -> {
                if (msg.ePriceLevel != null) {
                    w.writeBits(0u, 2)   // EPriceLevel
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits(msg.ePriceLevel!!.toLong().toUInt(), 8)
                    w.writeBits(0u, 1)   // child EE
                    st58 = 1
                } else if (msg.consumptionCost.isNotEmpty()) {
                    require(msg.consumptionCost.size <= 3) { "list size out of schema range" }
                    w.writeBits(1u, 2)   // ConsumptionCost
                    encodeConsumptionCostType(w, msg.consumptionCost[0])
                    for (ci in 1 until msg.consumptionCost.size) {
                        w.writeBits(0u, 2)   // ConsumptionCost
                        encodeConsumptionCostType(w, msg.consumptionCost[ci])
                    }
                    w.writeBits(1u, 2)   // element EE (list end)
                    done58 = true
                } else {
                    w.writeBits(2u, 2)   // element EE
                    done58 = true
                }
            }
            1 -> {
                if (msg.consumptionCost.isNotEmpty()) {
                    require(msg.consumptionCost.size <= 3) { "list size out of schema range" }
                    w.writeBits(0u, 2)   // ConsumptionCost
                    encodeConsumptionCostType(w, msg.consumptionCost[0])
                    for (ci in 1 until msg.consumptionCost.size) {
                        w.writeBits(0u, 2)   // ConsumptionCost
                        encodeConsumptionCostType(w, msg.consumptionCost[ci])
                    }
                    w.writeBits(1u, 2)   // element EE (list end)
                    done58 = true
                } else {
                    w.writeBits(1u, 2)   // element EE
                    done58 = true
                }
            }
            2 -> {
                w.writeBits(0u, 1)   // element EE
                done58 = true
            }
        }
    }
}

internal fun decodeSalesTariffEntryType(r: BitReader): SalesTariffEntryType {
    val _timeInterval: IntervalType = when (r.readBits(2)) {
        0u -> decodeRelativeTimeIntervalType(r)
        1u -> throw IllegalArgumentException("abstract substitution head cannot be decoded")
        else -> throw IllegalArgumentException("unknown substitution index")
    }
    var _ePriceLevel: UByte? = null
    val consumptionCostList = ArrayList<ConsumptionCostType>()
    var st59 = 0
    var done59 = false
    while (!done59) {
        when (st59) {
            0 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _ePriceLevel = r.readBits(8).toUByte()
                        r.readBits(1)   // child EE
                        st59 = 1
                    }
                    1u -> {   // ConsumptionCost
                        consumptionCostList.add(decodeConsumptionCostType(r))
                        while (true) {
                            val lc = r.readBits(2)
                            if (lc == 1u) break   // element EE (list end)
                            require(lc == 0u && consumptionCostList.size < 3) { "invalid repeating-element event code" }
                            consumptionCostList.add(decodeConsumptionCostType(r))
                        }
                        done59 = true
                    }
                    2u -> done59 = true   // element EE
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(2)) {
                    0u -> {   // ConsumptionCost
                        consumptionCostList.add(decodeConsumptionCostType(r))
                        while (true) {
                            val lc = r.readBits(2)
                            if (lc == 1u) break   // element EE (list end)
                            require(lc == 0u && consumptionCostList.size < 3) { "invalid repeating-element event code" }
                            consumptionCostList.add(decodeConsumptionCostType(r))
                        }
                        done59 = true
                    }
                    1u -> done59 = true   // element EE
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            2 -> {
                when (r.readBits(1)) {
                    0u -> done59 = true   // element EE
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    return SalesTariffEntryType(_timeInterval, _ePriceLevel, consumptionCostList)
}

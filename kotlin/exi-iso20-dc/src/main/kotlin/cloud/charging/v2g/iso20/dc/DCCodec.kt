// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Kotlin back end). Do not edit by hand.

package cloud.charging.v2g.iso20.dc

import cloud.charging.v2g.exi.BitReader
import cloud.charging.v2g.exi.BitWriter

object DCCodec {

    const val EXI_HEADER: Byte = 0x80.toByte()

    /** Generous upper bound; encode() returns a right-sized copy. */
    private const val MAX_MESSAGE_BYTES = 65536

    fun encode(msg: DC_ChargeParameterDiscoveryReq): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(15u, 6)   // document element selector
        encodeDC_ChargeParameterDiscoveryReq(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: DC_ChargeParameterDiscoveryRes): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(16u, 6)   // document element selector
        encodeDC_ChargeParameterDiscoveryRes(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: DC_CableCheckReq): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(11u, 6)   // document element selector
        encodeDC_CableCheckReq(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: DC_CableCheckRes): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(12u, 6)   // document element selector
        encodeDC_CableCheckRes(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: DC_PreChargeReq): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(17u, 6)   // document element selector
        encodeDC_PreChargeReq(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: DC_PreChargeRes): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(18u, 6)   // document element selector
        encodeDC_PreChargeRes(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: DC_ChargeLoopReq): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(13u, 6)   // document element selector
        encodeDC_ChargeLoopReq(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: DC_ChargeLoopRes): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(14u, 6)   // document element selector
        encodeDC_ChargeLoopRes(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: DC_WeldingDetectionReq): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(19u, 6)   // document element selector
        encodeDC_WeldingDetectionReq(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: DC_WeldingDetectionRes): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(20u, 6)   // document element selector
        encodeDC_WeldingDetectionRes(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: DC_CPDReqEnergyTransferMode): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(9u, 6)   // document element selector
        encodeDC_CPDReqEnergyTransferMode(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: DC_CPDResEnergyTransferMode): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(10u, 6)   // document element selector
        encodeDC_CPDResEnergyTransferMode(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun decodeAny(src: ByteArray): Any {
        require(src.isNotEmpty() && src[0] == EXI_HEADER) { "Invalid EXI header." }
        val r = BitReader(src, 1)
        return when (val sel = r.readBits(6)) {
            15u -> decodeDC_ChargeParameterDiscoveryReq(r)
            16u -> decodeDC_ChargeParameterDiscoveryRes(r)
            11u -> decodeDC_CableCheckReq(r)
            12u -> decodeDC_CableCheckRes(r)
            17u -> decodeDC_PreChargeReq(r)
            18u -> decodeDC_PreChargeRes(r)
            13u -> decodeDC_ChargeLoopReq(r)
            14u -> decodeDC_ChargeLoopRes(r)
            19u -> decodeDC_WeldingDetectionReq(r)
            20u -> decodeDC_WeldingDetectionRes(r)
            9u -> decodeDC_CPDReqEnergyTransferMode(r)
            10u -> decodeDC_CPDResEnergyTransferMode(r)
            else -> throw IllegalArgumentException("Unknown document index $sel.")
        }
    }

    fun encodeFragment_DC_ChargeParameterDiscoveryRes(content: DC_ChargeParameterDiscoveryResType): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(26u, 8)   // fragment SE(DC_ChargeParameterDiscoveryRes)
        encodeDC_ChargeParameterDiscoveryResType(w, content)
        w.writeBits(150u, 8)   // End Fragment (ED)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun decodeFragment_DC_ChargeParameterDiscoveryRes(src: ByteArray): DC_ChargeParameterDiscoveryResType {
        require(src.isNotEmpty() && src[0] == EXI_HEADER) { "Invalid EXI header." }
        val r = BitReader(src, 1)
        require(r.readBits(8) == 26u) { "Not a DC_ChargeParameterDiscoveryRes fragment." }
        val result = decodeDC_ChargeParameterDiscoveryResType(r)
        require(r.readBits(8) == 150u) { "missing End Fragment." }
        return result
    }

    fun encodeFragment_SignedInfo(content: SignedInfoType): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(129u, 8)   // fragment SE(SignedInfo)
        encodeSignedInfoType(w, content)
        w.writeBits(150u, 8)   // End Fragment (ED)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun decodeFragment_SignedInfo(src: ByteArray): SignedInfoType {
        require(src.isNotEmpty() && src[0] == EXI_HEADER) { "Invalid EXI header." }
        val r = BitReader(src, 1)
        require(r.readBits(8) == 129u) { "Not a SignedInfo fragment." }
        val result = decodeSignedInfoType(r)
        require(r.readBits(8) == 150u) { "missing End Fragment." }
        return result
    }

}

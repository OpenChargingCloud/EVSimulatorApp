// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Kotlin back end). Do not edit by hand.

package cloud.charging.v2g.iso20.acderiec

import cloud.charging.v2g.exi.BitReader
import cloud.charging.v2g.exi.BitWriter

data class ActivePowerSupportType(
    val underFrequencyWatt: FrequencyWattType?,
    val overFrequencyWatt: FrequencyWattType?,
    val voltWatt: VoltWattType?
)

internal fun encodeActivePowerSupportType(w: BitWriter, msg: ActivePowerSupportType) {
    var st64 = 0
    var done64 = false
    while (!done64) {
        when (st64) {
            0 -> {
                if (msg.underFrequencyWatt != null) {
                    w.writeBits(0u, 3)   // UnderFrequencyWatt
                    encodeFrequencyWattType(w, msg.underFrequencyWatt!!)
                    st64 = 1
                } else if (msg.overFrequencyWatt != null) {
                    w.writeBits(1u, 3)   // OverFrequencyWatt
                    encodeFrequencyWattType(w, msg.overFrequencyWatt!!)
                    st64 = 2
                } else if (msg.voltWatt != null) {
                    w.writeBits(2u, 3)   // VoltWatt
                    encodeVoltWattType(w, msg.voltWatt!!)
                    st64 = 3
                } else {
                    w.writeBits(3u, 3)   // element EE
                    done64 = true
                }
            }
            1 -> {
                if (msg.overFrequencyWatt != null) {
                    w.writeBits(0u, 2)   // OverFrequencyWatt
                    encodeFrequencyWattType(w, msg.overFrequencyWatt!!)
                    st64 = 2
                } else if (msg.voltWatt != null) {
                    w.writeBits(1u, 2)   // VoltWatt
                    encodeVoltWattType(w, msg.voltWatt!!)
                    st64 = 3
                } else {
                    w.writeBits(2u, 2)   // element EE
                    done64 = true
                }
            }
            2 -> {
                if (msg.voltWatt != null) {
                    w.writeBits(0u, 2)   // VoltWatt
                    encodeVoltWattType(w, msg.voltWatt!!)
                    st64 = 3
                } else {
                    w.writeBits(1u, 2)   // element EE
                    done64 = true
                }
            }
            3 -> {
                w.writeBits(0u, 1)   // element EE
                done64 = true
            }
        }
    }
}

internal fun decodeActivePowerSupportType(r: BitReader): ActivePowerSupportType {
    var _underFrequencyWatt: FrequencyWattType? = null
    var _overFrequencyWatt: FrequencyWattType? = null
    var _voltWatt: VoltWattType? = null
    var st65 = 0
    var done65 = false
    while (!done65) {
        when (st65) {
            0 -> {
                when (r.readBits(3)) {
                    0u -> {
                        _underFrequencyWatt = decodeFrequencyWattType(r)
                        st65 = 1
                    }
                    1u -> {
                        _overFrequencyWatt = decodeFrequencyWattType(r)
                        st65 = 2
                    }
                    2u -> {
                        _voltWatt = decodeVoltWattType(r)
                        st65 = 3
                    }
                    3u -> done65 = true   // element EE
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(2)) {
                    0u -> {
                        _overFrequencyWatt = decodeFrequencyWattType(r)
                        st65 = 2
                    }
                    1u -> {
                        _voltWatt = decodeVoltWattType(r)
                        st65 = 3
                    }
                    2u -> done65 = true   // element EE
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            2 -> {
                when (r.readBits(2)) {
                    0u -> {
                        _voltWatt = decodeVoltWattType(r)
                        st65 = 3
                    }
                    1u -> done65 = true   // element EE
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            3 -> {
                when (r.readBits(1)) {
                    0u -> done65 = true   // element EE
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    return ActivePowerSupportType(_underFrequencyWatt, _overFrequencyWatt, _voltWatt)
}

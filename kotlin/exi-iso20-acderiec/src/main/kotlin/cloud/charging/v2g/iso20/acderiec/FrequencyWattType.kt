// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Kotlin back end). Do not edit by hand.

package cloud.charging.v2g.iso20.acderiec

import cloud.charging.v2g.exi.BitReader
import cloud.charging.v2g.exi.BitWriter
import cloud.charging.v2g.exi.ExiPrimitives

data class FrequencyWattType(
    val fstart: RationalNumberType,
    val fstop: RationalNumberType,
    val intentionalDelayFstop: UShort?,
    val slope: RationalNumberType,
    val deactivationTime: UShort?,
    val intentionalDelayPowerControl: UShort?,
    val powerReference: PowerReference,
    val hysteresisControl: Boolean,
    val powerUpRamp: UShort?,
    val pT1ResponseActivePower: Boolean,
    val stepResponseTimeConstantActivePower: RationalNumberType
)

internal fun encodeFrequencyWattType(w: BitWriter, msg: FrequencyWattType) {
    w.writeBits(0u, 1)   // SE
    encodeRationalNumberType(w, msg.fstart)
    w.writeBits(0u, 1)   // SE
    encodeRationalNumberType(w, msg.fstop)
    var st72 = 0
    var done72 = false
    while (!done72) {
        when (st72) {
            0 -> {
                if (msg.intentionalDelayFstop != null) {
                    w.writeBits(0u, 2)   // IntentionalDelayFstop
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.intentionalDelayFstop!!.toULong())
                    w.writeBits(0u, 1)   // child EE
                    st72 = 1
                } else {
                    w.writeBits(1u, 2)   // SE(Slope)
                    encodeRationalNumberType(w, msg.slope)
                    done72 = true
                }
            }
            1 -> {
                w.writeBits(0u, 1)   // SE(Slope)
                encodeRationalNumberType(w, msg.slope)
                done72 = true
            }
        }
    }
    var st73 = 0
    var done73 = false
    while (!done73) {
        when (st73) {
            0 -> {
                if (msg.deactivationTime != null) {
                    w.writeBits(0u, 2)   // DeactivationTime
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.deactivationTime!!.toULong())
                    w.writeBits(0u, 1)   // child EE
                    st73 = 1
                } else if (msg.intentionalDelayPowerControl != null) {
                    w.writeBits(1u, 2)   // IntentionalDelayPowerControl
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.intentionalDelayPowerControl!!.toULong())
                    w.writeBits(0u, 1)   // child EE
                    st73 = 2
                } else {
                    w.writeBits(2u, 2)   // SE(PowerReference)
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits(msg.powerReference.ordinal.toUInt(), 1)
                    w.writeBits(0u, 1)   // child EE
                    done73 = true
                }
            }
            1 -> {
                if (msg.intentionalDelayPowerControl != null) {
                    w.writeBits(0u, 2)   // IntentionalDelayPowerControl
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.intentionalDelayPowerControl!!.toULong())
                    w.writeBits(0u, 1)   // child EE
                    st73 = 2
                } else {
                    w.writeBits(1u, 2)   // SE(PowerReference)
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits(msg.powerReference.ordinal.toUInt(), 1)
                    w.writeBits(0u, 1)   // child EE
                    done73 = true
                }
            }
            2 -> {
                w.writeBits(0u, 1)   // SE(PowerReference)
                w.writeBits(0u, 1)   // value-start
                w.writeBits(msg.powerReference.ordinal.toUInt(), 1)
                w.writeBits(0u, 1)   // child EE
                done73 = true
            }
        }
    }
    w.writeBits(0u, 1)   // SE
    w.writeBits(0u, 1)   // value-start
    w.writeBits(if (msg.hysteresisControl) 1u else 0u, 1)
    w.writeBits(0u, 1)   // child EE
    var st74 = 0
    var done74 = false
    while (!done74) {
        when (st74) {
            0 -> {
                if (msg.powerUpRamp != null) {
                    w.writeBits(0u, 2)   // PowerUpRamp
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.powerUpRamp!!.toULong())
                    w.writeBits(0u, 1)   // child EE
                    st74 = 1
                } else {
                    w.writeBits(1u, 2)   // SE(PT1ResponseActivePower)
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits(if (msg.pT1ResponseActivePower) 1u else 0u, 1)
                    w.writeBits(0u, 1)   // child EE
                    done74 = true
                }
            }
            1 -> {
                w.writeBits(0u, 1)   // SE(PT1ResponseActivePower)
                w.writeBits(0u, 1)   // value-start
                w.writeBits(if (msg.pT1ResponseActivePower) 1u else 0u, 1)
                w.writeBits(0u, 1)   // child EE
                done74 = true
            }
        }
    }
    w.writeBits(0u, 1)   // SE
    encodeRationalNumberType(w, msg.stepResponseTimeConstantActivePower)
    w.writeBits(0u, 1)   // element EE
}

internal fun decodeFrequencyWattType(r: BitReader): FrequencyWattType {
    r.readBits(1)   // SE
    val _fstart = decodeRationalNumberType(r)
    r.readBits(1)   // SE
    val _fstop = decodeRationalNumberType(r)
    var _intentionalDelayFstop: UShort? = null
    var _slope: RationalNumberType? = null
    var st75 = 0
    var done75 = false
    while (!done75) {
        when (st75) {
            0 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _intentionalDelayFstop = ExiPrimitives.readUnsignedInteger(r).toUShort()
                        r.readBits(1)   // child EE
                        st75 = 1
                    }
                    1u -> {   // SE(Slope)
                        _slope = decodeRationalNumberType(r)
                        done75 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(1)) {
                    0u -> {   // SE(Slope)
                        _slope = decodeRationalNumberType(r)
                        done75 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    var _deactivationTime: UShort? = null
    var _intentionalDelayPowerControl: UShort? = null
    var _powerReference: PowerReference? = null
    var st76 = 0
    var done76 = false
    while (!done76) {
        when (st76) {
            0 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _deactivationTime = ExiPrimitives.readUnsignedInteger(r).toUShort()
                        r.readBits(1)   // child EE
                        st76 = 1
                    }
                    1u -> {
                        r.readBits(1)   // value-start
                        _intentionalDelayPowerControl = ExiPrimitives.readUnsignedInteger(r).toUShort()
                        r.readBits(1)   // child EE
                        st76 = 2
                    }
                    2u -> {   // SE(PowerReference)
                        r.readBits(1)   // value-start
                        _powerReference = PowerReference.entries[r.readBits(1).toInt()]
                        r.readBits(1)   // child EE
                        done76 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _intentionalDelayPowerControl = ExiPrimitives.readUnsignedInteger(r).toUShort()
                        r.readBits(1)   // child EE
                        st76 = 2
                    }
                    1u -> {   // SE(PowerReference)
                        r.readBits(1)   // value-start
                        _powerReference = PowerReference.entries[r.readBits(1).toInt()]
                        r.readBits(1)   // child EE
                        done76 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            2 -> {
                when (r.readBits(1)) {
                    0u -> {   // SE(PowerReference)
                        r.readBits(1)   // value-start
                        _powerReference = PowerReference.entries[r.readBits(1).toInt()]
                        r.readBits(1)   // child EE
                        done76 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    val _hysteresisControl = r.readBits(1).toInt() != 0
    r.readBits(1)   // child EE
    var _powerUpRamp: UShort? = null
    var _pT1ResponseActivePower: Boolean? = null
    var st77 = 0
    var done77 = false
    while (!done77) {
        when (st77) {
            0 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _powerUpRamp = ExiPrimitives.readUnsignedInteger(r).toUShort()
                        r.readBits(1)   // child EE
                        st77 = 1
                    }
                    1u -> {   // SE(PT1ResponseActivePower)
                        r.readBits(1)   // value-start
                        _pT1ResponseActivePower = r.readBits(1).toInt() != 0
                        r.readBits(1)   // child EE
                        done77 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(1)) {
                    0u -> {   // SE(PT1ResponseActivePower)
                        r.readBits(1)   // value-start
                        _pT1ResponseActivePower = r.readBits(1).toInt() != 0
                        r.readBits(1)   // child EE
                        done77 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    r.readBits(1)   // SE
    val _stepResponseTimeConstantActivePower = decodeRationalNumberType(r)
    r.readBits(1)   // element EE
    return FrequencyWattType(_fstart, _fstop, _intentionalDelayFstop, _slope!!, _deactivationTime, _intentionalDelayPowerControl, _powerReference!!, _hysteresisControl, _powerUpRamp, _pT1ResponseActivePower!!, _stepResponseTimeConstantActivePower)
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Kotlin back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

package cloud.charging.v2g.iso20.common

import cloud.charging.v2g.exi.BitReader
import cloud.charging.v2g.exi.BitWriter
import cloud.charging.v2g.exi.ExiPrimitives

data class PriceRuleType(
    val energyFee: RationalNumberType,
    val parkingFee: RationalNumberType?,
    val parkingFeePeriod: UInt?,
    val carbonDioxideEmission: UShort?,
    val renewableGenerationPercentage: UByte?,
    val powerRangeStart: RationalNumberType
)

internal fun encodePriceRuleType(w: BitWriter, msg: PriceRuleType) {
    w.writeBits(0u, 1)   // SE
    encodeRationalNumberType(w, msg.energyFee)
    var st56 = 0
    var done56 = false
    while (!done56) {
        when (st56) {
            0 -> {
                if (msg.parkingFee != null) {
                    w.writeBits(0u, 3)   // ParkingFee
                    encodeRationalNumberType(w, msg.parkingFee!!)
                    st56 = 1
                } else if (msg.parkingFeePeriod != null) {
                    w.writeBits(1u, 3)   // ParkingFeePeriod
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.parkingFeePeriod!!.toULong())
                    w.writeBits(0u, 1)   // child EE
                    st56 = 2
                } else if (msg.carbonDioxideEmission != null) {
                    w.writeBits(2u, 3)   // CarbonDioxideEmission
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.carbonDioxideEmission!!.toULong())
                    w.writeBits(0u, 1)   // child EE
                    st56 = 3
                } else if (msg.renewableGenerationPercentage != null) {
                    w.writeBits(3u, 3)   // RenewableGenerationPercentage
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits(msg.renewableGenerationPercentage!!.toLong().toUInt(), 8)
                    w.writeBits(0u, 1)   // child EE
                    st56 = 4
                } else {
                    w.writeBits(4u, 3)   // SE(PowerRangeStart)
                    encodeRationalNumberType(w, msg.powerRangeStart)
                    done56 = true
                }
            }
            1 -> {
                if (msg.parkingFeePeriod != null) {
                    w.writeBits(0u, 3)   // ParkingFeePeriod
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.parkingFeePeriod!!.toULong())
                    w.writeBits(0u, 1)   // child EE
                    st56 = 2
                } else if (msg.carbonDioxideEmission != null) {
                    w.writeBits(1u, 3)   // CarbonDioxideEmission
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.carbonDioxideEmission!!.toULong())
                    w.writeBits(0u, 1)   // child EE
                    st56 = 3
                } else if (msg.renewableGenerationPercentage != null) {
                    w.writeBits(2u, 3)   // RenewableGenerationPercentage
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits(msg.renewableGenerationPercentage!!.toLong().toUInt(), 8)
                    w.writeBits(0u, 1)   // child EE
                    st56 = 4
                } else {
                    w.writeBits(3u, 3)   // SE(PowerRangeStart)
                    encodeRationalNumberType(w, msg.powerRangeStart)
                    done56 = true
                }
            }
            2 -> {
                if (msg.carbonDioxideEmission != null) {
                    w.writeBits(0u, 2)   // CarbonDioxideEmission
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.carbonDioxideEmission!!.toULong())
                    w.writeBits(0u, 1)   // child EE
                    st56 = 3
                } else if (msg.renewableGenerationPercentage != null) {
                    w.writeBits(1u, 2)   // RenewableGenerationPercentage
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits(msg.renewableGenerationPercentage!!.toLong().toUInt(), 8)
                    w.writeBits(0u, 1)   // child EE
                    st56 = 4
                } else {
                    w.writeBits(2u, 2)   // SE(PowerRangeStart)
                    encodeRationalNumberType(w, msg.powerRangeStart)
                    done56 = true
                }
            }
            3 -> {
                if (msg.renewableGenerationPercentage != null) {
                    w.writeBits(0u, 2)   // RenewableGenerationPercentage
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits(msg.renewableGenerationPercentage!!.toLong().toUInt(), 8)
                    w.writeBits(0u, 1)   // child EE
                    st56 = 4
                } else {
                    w.writeBits(1u, 2)   // SE(PowerRangeStart)
                    encodeRationalNumberType(w, msg.powerRangeStart)
                    done56 = true
                }
            }
            4 -> {
                w.writeBits(0u, 1)   // SE(PowerRangeStart)
                encodeRationalNumberType(w, msg.powerRangeStart)
                done56 = true
            }
        }
    }
    w.writeBits(0u, 1)   // element EE
}

internal fun decodePriceRuleType(r: BitReader): PriceRuleType {
    r.readBits(1)   // SE
    val _energyFee = decodeRationalNumberType(r)
    var _parkingFee: RationalNumberType? = null
    var _parkingFeePeriod: UInt? = null
    var _carbonDioxideEmission: UShort? = null
    var _renewableGenerationPercentage: UByte? = null
    var _powerRangeStart: RationalNumberType? = null
    var st57 = 0
    var done57 = false
    while (!done57) {
        when (st57) {
            0 -> {
                when (r.readBits(3)) {
                    0u -> {
                        _parkingFee = decodeRationalNumberType(r)
                        st57 = 1
                    }
                    1u -> {
                        r.readBits(1)   // value-start
                        _parkingFeePeriod = ExiPrimitives.readUnsignedInteger(r).toUInt()
                        r.readBits(1)   // child EE
                        st57 = 2
                    }
                    2u -> {
                        r.readBits(1)   // value-start
                        _carbonDioxideEmission = ExiPrimitives.readUnsignedInteger(r).toUShort()
                        r.readBits(1)   // child EE
                        st57 = 3
                    }
                    3u -> {
                        r.readBits(1)   // value-start
                        _renewableGenerationPercentage = r.readBits(8).toUByte()
                        r.readBits(1)   // child EE
                        st57 = 4
                    }
                    4u -> {   // SE(PowerRangeStart)
                        _powerRangeStart = decodeRationalNumberType(r)
                        done57 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(3)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _parkingFeePeriod = ExiPrimitives.readUnsignedInteger(r).toUInt()
                        r.readBits(1)   // child EE
                        st57 = 2
                    }
                    1u -> {
                        r.readBits(1)   // value-start
                        _carbonDioxideEmission = ExiPrimitives.readUnsignedInteger(r).toUShort()
                        r.readBits(1)   // child EE
                        st57 = 3
                    }
                    2u -> {
                        r.readBits(1)   // value-start
                        _renewableGenerationPercentage = r.readBits(8).toUByte()
                        r.readBits(1)   // child EE
                        st57 = 4
                    }
                    3u -> {   // SE(PowerRangeStart)
                        _powerRangeStart = decodeRationalNumberType(r)
                        done57 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            2 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _carbonDioxideEmission = ExiPrimitives.readUnsignedInteger(r).toUShort()
                        r.readBits(1)   // child EE
                        st57 = 3
                    }
                    1u -> {
                        r.readBits(1)   // value-start
                        _renewableGenerationPercentage = r.readBits(8).toUByte()
                        r.readBits(1)   // child EE
                        st57 = 4
                    }
                    2u -> {   // SE(PowerRangeStart)
                        _powerRangeStart = decodeRationalNumberType(r)
                        done57 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            3 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _renewableGenerationPercentage = r.readBits(8).toUByte()
                        r.readBits(1)   // child EE
                        st57 = 4
                    }
                    1u -> {   // SE(PowerRangeStart)
                        _powerRangeStart = decodeRationalNumberType(r)
                        done57 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            4 -> {
                when (r.readBits(1)) {
                    0u -> {   // SE(PowerRangeStart)
                        _powerRangeStart = decodeRationalNumberType(r)
                        done57 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    r.readBits(1)   // element EE
    return PriceRuleType(_energyFee, _parkingFee, _parkingFeePeriod, _carbonDioxideEmission, _renewableGenerationPercentage, _powerRangeStart!!)
}

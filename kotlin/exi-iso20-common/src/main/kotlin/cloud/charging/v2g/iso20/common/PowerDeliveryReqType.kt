// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Kotlin back end). Do not edit by hand.

package cloud.charging.v2g.iso20.common

import cloud.charging.v2g.exi.BitReader
import cloud.charging.v2g.exi.BitWriter

class PowerDeliveryReqType(
    override val header: MessageHeaderType,
    val eVProcessing: Processing,
    val chargeProgress: ChargeProgress,
    val eVPowerProfile: EVPowerProfileType?,
    val bPT_ChannelSelection: ChannelSelection?
) : V2GRequestType(header)

internal fun encodePowerDeliveryReqType(w: BitWriter, msg: PowerDeliveryReqType) {
    w.writeBits(0u, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0u, 1)   // SE
    w.writeBits(0u, 1)   // value-start
    w.writeBits(msg.eVProcessing.ordinal.toUInt(), 2)
    w.writeBits(0u, 1)   // child EE
    w.writeBits(0u, 1)   // SE
    w.writeBits(0u, 1)   // value-start
    w.writeBits(msg.chargeProgress.ordinal.toUInt(), 2)
    w.writeBits(0u, 1)   // child EE
    var st8 = 0
    var done8 = false
    while (!done8) {
        when (st8) {
            0 -> {
                if (msg.eVPowerProfile != null) {
                    w.writeBits(0u, 2)   // EVPowerProfile
                    encodeEVPowerProfileType(w, msg.eVPowerProfile!!)
                    st8 = 1
                } else if (msg.bPT_ChannelSelection != null) {
                    w.writeBits(1u, 2)   // BPT_ChannelSelection
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits(msg.bPT_ChannelSelection!!.ordinal.toUInt(), 1)
                    w.writeBits(0u, 1)   // child EE
                    st8 = 2
                } else {
                    w.writeBits(2u, 2)   // element EE
                    done8 = true
                }
            }
            1 -> {
                if (msg.bPT_ChannelSelection != null) {
                    w.writeBits(0u, 2)   // BPT_ChannelSelection
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits(msg.bPT_ChannelSelection!!.ordinal.toUInt(), 1)
                    w.writeBits(0u, 1)   // child EE
                    st8 = 2
                } else {
                    w.writeBits(1u, 2)   // element EE
                    done8 = true
                }
            }
            2 -> {
                w.writeBits(0u, 1)   // element EE
                done8 = true
            }
        }
    }
}

internal fun decodePowerDeliveryReqType(r: BitReader): PowerDeliveryReqType {
    r.readBits(1)   // SE
    val _header = decodeMessageHeaderType(r)
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    val _eVProcessing = Processing.entries[r.readBits(2).toInt()]
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    val _chargeProgress = ChargeProgress.entries[r.readBits(2).toInt()]
    r.readBits(1)   // child EE
    var _eVPowerProfile: EVPowerProfileType? = null
    var _bPT_ChannelSelection: ChannelSelection? = null
    var st9 = 0
    var done9 = false
    while (!done9) {
        when (st9) {
            0 -> {
                when (r.readBits(2)) {
                    0u -> {
                        _eVPowerProfile = decodeEVPowerProfileType(r)
                        st9 = 1
                    }
                    1u -> {
                        r.readBits(1)   // value-start
                        _bPT_ChannelSelection = ChannelSelection.entries[r.readBits(1).toInt()]
                        r.readBits(1)   // child EE
                        st9 = 2
                    }
                    2u -> done9 = true   // element EE
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _bPT_ChannelSelection = ChannelSelection.entries[r.readBits(1).toInt()]
                        r.readBits(1)   // child EE
                        st9 = 2
                    }
                    1u -> done9 = true   // element EE
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            2 -> {
                when (r.readBits(1)) {
                    0u -> done9 = true   // element EE
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    return PowerDeliveryReqType(_header, _eVProcessing, _chargeProgress, _eVPowerProfile, _bPT_ChannelSelection)
}

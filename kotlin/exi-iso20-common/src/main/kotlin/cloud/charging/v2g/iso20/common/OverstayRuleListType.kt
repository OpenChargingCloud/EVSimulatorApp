// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Kotlin back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

package cloud.charging.v2g.iso20.common

import cloud.charging.v2g.exi.BitReader
import cloud.charging.v2g.exi.BitWriter
import cloud.charging.v2g.exi.ExiPrimitives

data class OverstayRuleListType(
    val overstayTimeThreshold: UInt?,
    val overstayPowerThreshold: RationalNumberType?,
    val overstayRule: List<OverstayRuleType>
)

internal fun encodeOverstayRuleListType(w: BitWriter, msg: OverstayRuleListType) {
    var st58 = 0
    var done58 = false
    while (!done58) {
        when (st58) {
            0 -> {
                if (msg.overstayTimeThreshold != null) {
                    w.writeBits(0u, 2)   // OverstayTimeThreshold
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.overstayTimeThreshold!!.toULong())
                    w.writeBits(0u, 1)   // child EE
                    st58 = 1
                } else if (msg.overstayPowerThreshold != null) {
                    w.writeBits(1u, 2)   // OverstayPowerThreshold
                    encodeRationalNumberType(w, msg.overstayPowerThreshold!!)
                    st58 = 2
                } else {
                    require(msg.overstayRule.size in 1..5) { "list size out of schema range" }
                    w.writeBits(2u, 2)   // OverstayRule
                    encodeOverstayRuleType(w, msg.overstayRule[0])
                    for (ci in 1 until msg.overstayRule.size) {
                        w.writeBits(0u, 2)   // OverstayRule
                        encodeOverstayRuleType(w, msg.overstayRule[ci])
                    }
                    w.writeBits(1u, 2)   // element EE (list end)
                    done58 = true
                }
            }
            1 -> {
                if (msg.overstayPowerThreshold != null) {
                    w.writeBits(0u, 2)   // OverstayPowerThreshold
                    encodeRationalNumberType(w, msg.overstayPowerThreshold!!)
                    st58 = 2
                } else {
                    require(msg.overstayRule.size in 1..5) { "list size out of schema range" }
                    w.writeBits(1u, 2)   // OverstayRule
                    encodeOverstayRuleType(w, msg.overstayRule[0])
                    for (ci in 1 until msg.overstayRule.size) {
                        w.writeBits(0u, 2)   // OverstayRule
                        encodeOverstayRuleType(w, msg.overstayRule[ci])
                    }
                    w.writeBits(1u, 2)   // element EE (list end)
                    done58 = true
                }
            }
            2 -> {
                require(msg.overstayRule.size in 1..5) { "list size out of schema range" }
                w.writeBits(0u, 1)   // OverstayRule
                encodeOverstayRuleType(w, msg.overstayRule[0])
                for (ci in 1 until msg.overstayRule.size) {
                    w.writeBits(0u, 2)   // OverstayRule
                    encodeOverstayRuleType(w, msg.overstayRule[ci])
                }
                w.writeBits(1u, 2)   // element EE (list end)
                done58 = true
            }
        }
    }
}

internal fun decodeOverstayRuleListType(r: BitReader): OverstayRuleListType {
    var _overstayTimeThreshold: UInt? = null
    var _overstayPowerThreshold: RationalNumberType? = null
    val overstayRuleList = ArrayList<OverstayRuleType>()
    var st59 = 0
    var done59 = false
    while (!done59) {
        when (st59) {
            0 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _overstayTimeThreshold = ExiPrimitives.readUnsignedInteger(r).toUInt()
                        r.readBits(1)   // child EE
                        st59 = 1
                    }
                    1u -> {
                        _overstayPowerThreshold = decodeRationalNumberType(r)
                        st59 = 2
                    }
                    2u -> {   // OverstayRule
                        overstayRuleList.add(decodeOverstayRuleType(r))
                        while (true) {
                            val lc = r.readBits(2)
                            if (lc == 1u) break   // element EE (list end)
                            require(lc == 0u && overstayRuleList.size < 5) { "invalid repeating-element event code" }
                            overstayRuleList.add(decodeOverstayRuleType(r))
                        }
                        done59 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(2)) {
                    0u -> {
                        _overstayPowerThreshold = decodeRationalNumberType(r)
                        st59 = 2
                    }
                    1u -> {   // OverstayRule
                        overstayRuleList.add(decodeOverstayRuleType(r))
                        while (true) {
                            val lc = r.readBits(2)
                            if (lc == 1u) break   // element EE (list end)
                            require(lc == 0u && overstayRuleList.size < 5) { "invalid repeating-element event code" }
                            overstayRuleList.add(decodeOverstayRuleType(r))
                        }
                        done59 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            2 -> {
                when (r.readBits(1)) {
                    0u -> {   // OverstayRule
                        overstayRuleList.add(decodeOverstayRuleType(r))
                        while (true) {
                            val lc = r.readBits(2)
                            if (lc == 1u) break   // element EE (list end)
                            require(lc == 0u && overstayRuleList.size < 5) { "invalid repeating-element event code" }
                            overstayRuleList.add(decodeOverstayRuleType(r))
                        }
                        done59 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    return OverstayRuleListType(_overstayTimeThreshold, _overstayPowerThreshold, overstayRuleList)
}

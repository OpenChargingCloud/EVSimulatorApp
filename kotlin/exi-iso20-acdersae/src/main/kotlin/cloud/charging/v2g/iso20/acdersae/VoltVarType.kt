// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Kotlin back end). Do not edit by hand.

package cloud.charging.v2g.iso20.acdersae

import cloud.charging.v2g.exi.BitReader
import cloud.charging.v2g.exi.BitWriter
import cloud.charging.v2g.exi.ExiPrimitives

data class VoltVarType(
    val enable: Boolean,
    val priority: UShort?,
    val xUnit: DerUnit,
    val yUnit: DerUnit,
    val curveDataPoints: CurveDataPointsListType,
    val curveDataPoints_L2: CurveDataPointsListType?,
    val curveDataPoints_L3: CurveDataPointsListType?,
    val openLoopResponseTime: RationalNumberType,
    val timeConstantPT1: UInt?,
    val referenceVoltage: RationalNumberType,
    val autonomousReferenceVoltageAdjustmentEnable: Boolean,
    val referenceVoltageAdjustmentTimeConstant: UInt
)

internal fun encodeVoltVarType(w: BitWriter, msg: VoltVarType) {
    w.writeBits(0u, 1)   // SE
    w.writeBits(0u, 1)   // value-start
    w.writeBits(if (msg.enable) 1u else 0u, 1)
    w.writeBits(0u, 1)   // child EE
    var st88 = 0
    var done88 = false
    while (!done88) {
        when (st88) {
            0 -> {
                if (msg.priority != null) {
                    w.writeBits(0u, 2)   // Priority
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.priority!!.toULong())
                    w.writeBits(0u, 1)   // child EE
                    st88 = 1
                } else {
                    w.writeBits(1u, 2)   // SE(XUnit)
                    w.writeBits(0u, 1)   // value-start
                    w.writeBits(msg.xUnit.ordinal.toUInt(), 4)
                    w.writeBits(0u, 1)   // child EE
                    done88 = true
                }
            }
            1 -> {
                w.writeBits(0u, 1)   // SE(XUnit)
                w.writeBits(0u, 1)   // value-start
                w.writeBits(msg.xUnit.ordinal.toUInt(), 4)
                w.writeBits(0u, 1)   // child EE
                done88 = true
            }
        }
    }
    w.writeBits(0u, 1)   // SE
    w.writeBits(0u, 1)   // value-start
    w.writeBits(msg.yUnit.ordinal.toUInt(), 4)
    w.writeBits(0u, 1)   // child EE
    w.writeBits(0u, 1)   // SE
    encodeCurveDataPointsListType(w, msg.curveDataPoints)
    var st89 = 0
    var done89 = false
    while (!done89) {
        when (st89) {
            0 -> {
                if (msg.curveDataPoints_L2 != null) {
                    w.writeBits(0u, 2)   // CurveDataPoints_L2
                    encodeCurveDataPointsListType(w, msg.curveDataPoints_L2!!)
                    st89 = 1
                } else if (msg.curveDataPoints_L3 != null) {
                    w.writeBits(1u, 2)   // CurveDataPoints_L3
                    encodeCurveDataPointsListType(w, msg.curveDataPoints_L3!!)
                    st89 = 2
                } else {
                    w.writeBits(2u, 2)   // SE(OpenLoopResponseTime)
                    encodeRationalNumberType(w, msg.openLoopResponseTime)
                    done89 = true
                }
            }
            1 -> {
                if (msg.curveDataPoints_L3 != null) {
                    w.writeBits(0u, 2)   // CurveDataPoints_L3
                    encodeCurveDataPointsListType(w, msg.curveDataPoints_L3!!)
                    st89 = 2
                } else {
                    w.writeBits(1u, 2)   // SE(OpenLoopResponseTime)
                    encodeRationalNumberType(w, msg.openLoopResponseTime)
                    done89 = true
                }
            }
            2 -> {
                w.writeBits(0u, 1)   // SE(OpenLoopResponseTime)
                encodeRationalNumberType(w, msg.openLoopResponseTime)
                done89 = true
            }
        }
    }
    var st90 = 0
    var done90 = false
    while (!done90) {
        when (st90) {
            0 -> {
                if (msg.timeConstantPT1 != null) {
                    w.writeBits(0u, 2)   // TimeConstantPT1
                    w.writeBits(0u, 1)   // value-start
                    ExiPrimitives.writeUnsignedInteger(w, msg.timeConstantPT1!!.toULong())
                    w.writeBits(0u, 1)   // child EE
                    st90 = 1
                } else {
                    w.writeBits(1u, 2)   // SE(ReferenceVoltage)
                    encodeRationalNumberType(w, msg.referenceVoltage)
                    done90 = true
                }
            }
            1 -> {
                w.writeBits(0u, 1)   // SE(ReferenceVoltage)
                encodeRationalNumberType(w, msg.referenceVoltage)
                done90 = true
            }
        }
    }
    w.writeBits(0u, 1)   // SE
    w.writeBits(0u, 1)   // value-start
    w.writeBits(if (msg.autonomousReferenceVoltageAdjustmentEnable) 1u else 0u, 1)
    w.writeBits(0u, 1)   // child EE
    w.writeBits(0u, 1)   // SE
    w.writeBits(0u, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, msg.referenceVoltageAdjustmentTimeConstant.toULong())
    w.writeBits(0u, 1)   // child EE
    w.writeBits(0u, 1)   // element EE
}

internal fun decodeVoltVarType(r: BitReader): VoltVarType {
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    val _enable = r.readBits(1).toInt() != 0
    r.readBits(1)   // child EE
    var _priority: UShort? = null
    var _xUnit: DerUnit? = null
    var st91 = 0
    var done91 = false
    while (!done91) {
        when (st91) {
            0 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _priority = ExiPrimitives.readUnsignedInteger(r).toUShort()
                        r.readBits(1)   // child EE
                        st91 = 1
                    }
                    1u -> {   // SE(XUnit)
                        r.readBits(1)   // value-start
                        _xUnit = DerUnit.entries[r.readBits(4).toInt()]
                        r.readBits(1)   // child EE
                        done91 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(1)) {
                    0u -> {   // SE(XUnit)
                        r.readBits(1)   // value-start
                        _xUnit = DerUnit.entries[r.readBits(4).toInt()]
                        r.readBits(1)   // child EE
                        done91 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    val _yUnit = DerUnit.entries[r.readBits(4).toInt()]
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    val _curveDataPoints = decodeCurveDataPointsListType(r)
    var _curveDataPoints_L2: CurveDataPointsListType? = null
    var _curveDataPoints_L3: CurveDataPointsListType? = null
    var _openLoopResponseTime: RationalNumberType? = null
    var st92 = 0
    var done92 = false
    while (!done92) {
        when (st92) {
            0 -> {
                when (r.readBits(2)) {
                    0u -> {
                        _curveDataPoints_L2 = decodeCurveDataPointsListType(r)
                        st92 = 1
                    }
                    1u -> {
                        _curveDataPoints_L3 = decodeCurveDataPointsListType(r)
                        st92 = 2
                    }
                    2u -> {   // SE(OpenLoopResponseTime)
                        _openLoopResponseTime = decodeRationalNumberType(r)
                        done92 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(2)) {
                    0u -> {
                        _curveDataPoints_L3 = decodeCurveDataPointsListType(r)
                        st92 = 2
                    }
                    1u -> {   // SE(OpenLoopResponseTime)
                        _openLoopResponseTime = decodeRationalNumberType(r)
                        done92 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            2 -> {
                when (r.readBits(1)) {
                    0u -> {   // SE(OpenLoopResponseTime)
                        _openLoopResponseTime = decodeRationalNumberType(r)
                        done92 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    var _timeConstantPT1: UInt? = null
    var _referenceVoltage: RationalNumberType? = null
    var st93 = 0
    var done93 = false
    while (!done93) {
        when (st93) {
            0 -> {
                when (r.readBits(2)) {
                    0u -> {
                        r.readBits(1)   // value-start
                        _timeConstantPT1 = ExiPrimitives.readUnsignedInteger(r).toUInt()
                        r.readBits(1)   // child EE
                        st93 = 1
                    }
                    1u -> {   // SE(ReferenceVoltage)
                        _referenceVoltage = decodeRationalNumberType(r)
                        done93 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
            1 -> {
                when (r.readBits(1)) {
                    0u -> {   // SE(ReferenceVoltage)
                        _referenceVoltage = decodeRationalNumberType(r)
                        done93 = true
                    }
                    else -> throw IllegalArgumentException("invalid optional-run event code")
                }
            }
        }
    }
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    val _autonomousReferenceVoltageAdjustmentEnable = r.readBits(1).toInt() != 0
    r.readBits(1)   // child EE
    r.readBits(1)   // SE
    r.readBits(1)   // value-start
    val _referenceVoltageAdjustmentTimeConstant = ExiPrimitives.readUnsignedInteger(r).toUInt()
    r.readBits(1)   // child EE
    r.readBits(1)   // element EE
    return VoltVarType(_enable, _priority, _xUnit!!, _yUnit, _curveDataPoints, _curveDataPoints_L2, _curveDataPoints_L3, _openLoopResponseTime!!, _timeConstantPT1, _referenceVoltage!!, _autonomousReferenceVoltageAdjustmentEnable, _referenceVoltageAdjustmentTimeConstant)
}

// The hand-written EXI bit layer: BitWriter / BitReader / ExiPrimitives.
// A faithful port of the C# runtime — the generated codecs sit on top of it.

dependencies {
    testImplementation(kotlin("test"))
}

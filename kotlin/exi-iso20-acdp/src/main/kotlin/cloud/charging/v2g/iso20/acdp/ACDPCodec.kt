// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Kotlin back end). Do not edit by hand.

package cloud.charging.v2g.iso20.acdp

import cloud.charging.v2g.exi.BitReader
import cloud.charging.v2g.exi.BitWriter

object ACDPCodec {

    const val EXI_HEADER: Byte = 0x80.toByte()

    /** Generous upper bound; encode() returns a right-sized copy. */
    private const val MAX_MESSAGE_BYTES = 65536

    fun encode(msg: ACDP_VehiclePositioningReq): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(6u, 6)   // document element selector
        encodeACDP_VehiclePositioningReq(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: ACDP_VehiclePositioningRes): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(7u, 6)   // document element selector
        encodeACDP_VehiclePositioningRes(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: ACDP_ConnectReq): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(0u, 6)   // document element selector
        encodeACDP_ConnectReq(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: ACDP_ConnectRes): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(2u, 6)   // document element selector
        encodeACDP_ConnectRes(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: ACDP_DisconnectReq): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(1u, 6)   // document element selector
        encodeACDP_DisconnectReq(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: ACDP_DisconnectRes): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(3u, 6)   // document element selector
        encodeACDP_DisconnectRes(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: ACDP_SystemStatusReq): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(4u, 6)   // document element selector
        encodeACDP_SystemStatusReq(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    fun encode(msg: ACDP_SystemStatusRes): ByteArray {
        val buf = ByteArray(MAX_MESSAGE_BYTES)
        buf[0] = EXI_HEADER
        val w = BitWriter(buf, 1)
        w.writeBits(5u, 6)   // document element selector
        encodeACDP_SystemStatusRes(w, msg)
        w.alignToByte()
        return buf.copyOf(1 + w.bytesWritten)
    }

    /** Encodes any document element of this message set, dispatching on its runtime type. */
    fun encodeAny(msg: Any): ByteArray =
        when (msg) {
            is ACDP_ConnectReq -> encode(msg)
            is ACDP_DisconnectReq -> encode(msg)
            is ACDP_ConnectRes -> encode(msg)
            is ACDP_DisconnectRes -> encode(msg)
            is ACDP_SystemStatusReq -> encode(msg)
            is ACDP_SystemStatusRes -> encode(msg)
            is ACDP_VehiclePositioningReq -> encode(msg)
            is ACDP_VehiclePositioningRes -> encode(msg)

            else -> throw IllegalArgumentException(
                "${msg::class.simpleName} is not a document element of this message set.")
        }

    fun decodeAny(src: ByteArray): Any {
        require(src.isNotEmpty() && src[0] == EXI_HEADER) { "Invalid EXI header." }
        val r = BitReader(src, 1)
        return when (val sel = r.readBits(6)) {
            6u -> decodeACDP_VehiclePositioningReq(r)
            7u -> decodeACDP_VehiclePositioningRes(r)
            0u -> decodeACDP_ConnectReq(r)
            2u -> decodeACDP_ConnectRes(r)
            1u -> decodeACDP_DisconnectReq(r)
            3u -> decodeACDP_DisconnectRes(r)
            4u -> decodeACDP_SystemStatusReq(r)
            5u -> decodeACDP_SystemStatusRes(r)
            else -> throw IllegalArgumentException("Unknown document index $sel.")
        }
    }

}

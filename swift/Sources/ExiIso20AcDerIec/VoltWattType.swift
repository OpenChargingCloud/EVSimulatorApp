// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.acderiec

import ExiRuntime

public final class VoltWattType {
    public var powerReference: PowerReference
    public var uStart: RationalNumberType
    public var uStop: RationalNumberType
    public var pT1ResponseActivePower: Bool
    public var stepResponseTimeConstantActivePower: RationalNumberType
    public var intentionalDelayPowerControl: UInt32?

    public init(powerReference: PowerReference, uStart: RationalNumberType, uStop: RationalNumberType, pT1ResponseActivePower: Bool, stepResponseTimeConstantActivePower: RationalNumberType, intentionalDelayPowerControl: UInt32? = nil) {
        self.powerReference = powerReference
        self.uStart = uStart
        self.uStop = uStop
        self.pT1ResponseActivePower = pT1ResponseActivePower
        self.stepResponseTimeConstantActivePower = stepResponseTimeConstantActivePower
        self.intentionalDelayPowerControl = intentionalDelayPowerControl
    }
}

internal func encodeVoltWattType(_ w: BitWriter, _ msg: VoltWattType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.powerReference.rawValue), 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    encodeRationalNumberType(w, msg.uStart)
    w.writeBits(0, 1)   // SE
    encodeRationalNumberType(w, msg.uStop)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.pT1ResponseActivePower ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    encodeRationalNumberType(w, msg.stepResponseTimeConstantActivePower)
    var st78 = 0
    var done78 = false
    while !done78 {
        switch st78 {
        case 0:
            if let v = msg.intentionalDelayPowerControl {
                w.writeBits(0, 2)   // IntentionalDelayPowerControl
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st78 = 1
            } else {
                w.writeBits(1, 2)   // element EE
                done78 = true
            }
        case 1:
            w.writeBits(0, 1)   // element EE
            done78 = true
        default:
            done78 = true
        }
    }
}

internal func decodeVoltWattType(_ r: BitReader) throws -> VoltWattType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _powerReference = try exiEnum(PowerReference.self, try r.readBits(1))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    let _uStart = try decodeRationalNumberType(r)
    _ = try r.readBits(1)   // SE
    let _uStop = try decodeRationalNumberType(r)
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _pT1ResponseActivePower = try r.readBits(1) != 0
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    let _stepResponseTimeConstantActivePower = try decodeRationalNumberType(r)
    var _intentionalDelayPowerControl: UInt32? = nil
    var st79 = 0
    var done79 = false
    while !done79 {
        switch st79 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // IntentionalDelayPowerControl
                _ = try r.readBits(1)   // value-start
                _intentionalDelayPowerControl = UInt32(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st79 = 1
            case 1:   // element EE
                done79 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // element EE
                done79 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done79 = true
        }
    }
    return VoltWattType(powerReference: _powerReference, uStart: _uStart, uStop: _uStop, pT1ResponseActivePower: _pT1ResponseActivePower, stepResponseTimeConstantActivePower: _stepResponseTimeConstantActivePower, intentionalDelayPowerControl: _intentionalDelayPowerControl)
}

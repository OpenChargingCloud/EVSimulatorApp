// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.acderiec

import ExiRuntime

public enum AcDerIecCodec {

    public static let exiHeader: UInt8 = 0x80

    public static func encode(_ msg: AC_ChargeParameterDiscoveryReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(4, 6)   // document element selector
        encodeAC_ChargeParameterDiscoveryReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: AC_ChargeParameterDiscoveryRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(5, 6)   // document element selector
        encodeAC_ChargeParameterDiscoveryRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: AC_ChargeLoopReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(2, 6)   // document element selector
        encodeAC_ChargeLoopReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: AC_ChargeLoopRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(3, 6)   // document element selector
        encodeAC_ChargeLoopRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: AC_CPDReqEnergyTransferMode) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(0, 6)   // document element selector
        encodeAC_CPDReqEnergyTransferMode(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: AC_CPDResEnergyTransferMode) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(1, 6)   // document element selector
        encodeAC_CPDResEnergyTransferMode(w, msg)
        w.alignToByte()
        return w.bytes
    }

    /// Encodes any document element of this message set, dispatching on its runtime type.
    public static func encodeAny(_ msg: Any) throws -> [UInt8] {
        switch msg {
        case let m as AC_ChargeLoopReq: return encode(m)
        case let m as AC_ChargeLoopRes: return encode(m)
        case let m as AC_ChargeParameterDiscoveryReq: return encode(m)
        case let m as AC_ChargeParameterDiscoveryRes: return encode(m)
        case let m as AC_CPDReqEnergyTransferMode: return encode(m)
        case let m as AC_CPDResEnergyTransferMode: return encode(m)
        default: throw ExiError.invalidHeader
        }
    }

    public static func decodeAny(_ src: [UInt8]) throws -> Any {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        let sel = try r.readBits(6)
        switch sel {
        case 0: return try decodeAC_CPDReqEnergyTransferMode(r)
        case 1: return try decodeAC_CPDResEnergyTransferMode(r)
        case 2: return try decodeAC_ChargeLoopReq(r)
        case 3: return try decodeAC_ChargeLoopRes(r)
        case 4: return try decodeAC_ChargeParameterDiscoveryReq(r)
        case 5: return try decodeAC_ChargeParameterDiscoveryRes(r)
        default: throw ExiError.unknownDocumentIndex(sel)
        }
    }

    public static func encodeFragment_AC_ChargeParameterDiscoveryRes(_ content: AC_ChargeParameterDiscoveryResType) -> [UInt8] {
        let w = BitWriter(capacity: 512)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(5, 9)   // fragment SE(AC_ChargeParameterDiscoveryRes)
        encodeAC_ChargeParameterDiscoveryResType(w, content)
        w.writeBits(260, 9)   // End Fragment (ED)
        w.alignToByte()
        return w.bytes
    }

    public static func decodeFragment_AC_ChargeParameterDiscoveryRes(_ src: [UInt8]) throws -> AC_ChargeParameterDiscoveryResType {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        guard try r.readBits(9) == 5 else {
            throw ExiError.invalidEventCode("not a AC_ChargeParameterDiscoveryRes fragment")
        }
        let result = try decodeAC_ChargeParameterDiscoveryResType(r)
        guard try r.readBits(9) == 260 else {
            throw ExiError.invalidEventCode("missing End Fragment")
        }
        return result
    }

    public static func encodeFragment_SignedInfo(_ content: SignedInfoType) -> [UInt8] {
        let w = BitWriter(capacity: 512)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(217, 9)   // fragment SE(SignedInfo)
        encodeSignedInfoType(w, content)
        w.writeBits(260, 9)   // End Fragment (ED)
        w.alignToByte()
        return w.bytes
    }

    public static func decodeFragment_SignedInfo(_ src: [UInt8]) throws -> SignedInfoType {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        guard try r.readBits(9) == 217 else {
            throw ExiError.invalidEventCode("not a SignedInfo fragment")
        }
        let result = try decodeSignedInfoType(r)
        guard try r.readBits(9) == 260 else {
            throw ExiError.invalidEventCode("missing End Fragment")
        }
        return result
    }
}

// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.acderiec

import ExiRuntime

public final class FrequencyWattType {
    public var fstart: RationalNumberType
    public var fstop: RationalNumberType
    public var intentionalDelayFstop: UInt16?
    public var slope: RationalNumberType
    public var deactivationTime: UInt16?
    public var intentionalDelayPowerControl: UInt16?
    public var powerReference: PowerReference
    public var hysteresisControl: Bool
    public var powerUpRamp: UInt16?
    public var pT1ResponseActivePower: Bool
    public var stepResponseTimeConstantActivePower: RationalNumberType

    public init(fstart: RationalNumberType, fstop: RationalNumberType, intentionalDelayFstop: UInt16? = nil, slope: RationalNumberType, deactivationTime: UInt16? = nil, intentionalDelayPowerControl: UInt16? = nil, powerReference: PowerReference, hysteresisControl: Bool, powerUpRamp: UInt16? = nil, pT1ResponseActivePower: Bool, stepResponseTimeConstantActivePower: RationalNumberType) {
        self.fstart = fstart
        self.fstop = fstop
        self.intentionalDelayFstop = intentionalDelayFstop
        self.slope = slope
        self.deactivationTime = deactivationTime
        self.intentionalDelayPowerControl = intentionalDelayPowerControl
        self.powerReference = powerReference
        self.hysteresisControl = hysteresisControl
        self.powerUpRamp = powerUpRamp
        self.pT1ResponseActivePower = pT1ResponseActivePower
        self.stepResponseTimeConstantActivePower = stepResponseTimeConstantActivePower
    }
}

internal func encodeFrequencyWattType(_ w: BitWriter, _ msg: FrequencyWattType) {
    w.writeBits(0, 1)   // SE
    encodeRationalNumberType(w, msg.fstart)
    w.writeBits(0, 1)   // SE
    encodeRationalNumberType(w, msg.fstop)
    var st72 = 0
    var done72 = false
    while !done72 {
        switch st72 {
        case 0:
            if let v = msg.intentionalDelayFstop {
                w.writeBits(0, 2)   // IntentionalDelayFstop
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st72 = 1
            } else {
                w.writeBits(1, 2)   // SE(Slope)
                encodeRationalNumberType(w, msg.slope)
                done72 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(Slope)
            encodeRationalNumberType(w, msg.slope)
            done72 = true
        default:
            done72 = true
        }
    }
    var st73 = 0
    var done73 = false
    while !done73 {
        switch st73 {
        case 0:
            if let v = msg.deactivationTime {
                w.writeBits(0, 2)   // DeactivationTime
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st73 = 1
            } else if let v = msg.intentionalDelayPowerControl {
                w.writeBits(1, 2)   // IntentionalDelayPowerControl
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st73 = 2
            } else {
                w.writeBits(2, 2)   // SE(PowerReference)
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(msg.powerReference.rawValue), 1)
                w.writeBits(0, 1)   // child EE
                done73 = true
            }
        case 1:
            if let v = msg.intentionalDelayPowerControl {
                w.writeBits(0, 2)   // IntentionalDelayPowerControl
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st73 = 2
            } else {
                w.writeBits(1, 2)   // SE(PowerReference)
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(msg.powerReference.rawValue), 1)
                w.writeBits(0, 1)   // child EE
                done73 = true
            }
        case 2:
            w.writeBits(0, 1)   // SE(PowerReference)
            w.writeBits(0, 1)   // value-start
            w.writeBits(UInt32(msg.powerReference.rawValue), 1)
            w.writeBits(0, 1)   // child EE
            done73 = true
        default:
            done73 = true
        }
    }
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.hysteresisControl ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    var st74 = 0
    var done74 = false
    while !done74 {
        switch st74 {
        case 0:
            if let v = msg.powerUpRamp {
                w.writeBits(0, 2)   // PowerUpRamp
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st74 = 1
            } else {
                w.writeBits(1, 2)   // SE(PT1ResponseActivePower)
                w.writeBits(0, 1)   // value-start
                w.writeBits(msg.pT1ResponseActivePower ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                done74 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(PT1ResponseActivePower)
            w.writeBits(0, 1)   // value-start
            w.writeBits(msg.pT1ResponseActivePower ? 1 : 0, 1)
            w.writeBits(0, 1)   // child EE
            done74 = true
        default:
            done74 = true
        }
    }
    w.writeBits(0, 1)   // SE
    encodeRationalNumberType(w, msg.stepResponseTimeConstantActivePower)
    w.writeBits(0, 1)   // element EE
}

internal func decodeFrequencyWattType(_ r: BitReader) throws -> FrequencyWattType {
    _ = try r.readBits(1)   // SE
    let _fstart = try decodeRationalNumberType(r)
    _ = try r.readBits(1)   // SE
    let _fstop = try decodeRationalNumberType(r)
    var _intentionalDelayFstop: UInt16? = nil
    var _slope: RationalNumberType? = nil
    var st75 = 0
    var done75 = false
    while !done75 {
        switch st75 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // IntentionalDelayFstop
                _ = try r.readBits(1)   // value-start
                _intentionalDelayFstop = UInt16(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st75 = 1
            case 1:   // SE(Slope)
                _slope = try decodeRationalNumberType(r)
                done75 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(Slope)
                _slope = try decodeRationalNumberType(r)
                done75 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done75 = true
        }
    }
    guard let _slope else {
        throw ExiError.invalidEventCode("Slope is required but the element ended")
    }
    var _deactivationTime: UInt16? = nil
    var _intentionalDelayPowerControl: UInt16? = nil
    var _powerReference: PowerReference? = nil
    var st76 = 0
    var done76 = false
    while !done76 {
        switch st76 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // DeactivationTime
                _ = try r.readBits(1)   // value-start
                _deactivationTime = UInt16(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st76 = 1
            case 1:   // IntentionalDelayPowerControl
                _ = try r.readBits(1)   // value-start
                _intentionalDelayPowerControl = UInt16(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st76 = 2
            case 2:   // SE(PowerReference)
                _ = try r.readBits(1)   // value-start
                _powerReference = try exiEnum(PowerReference.self, try r.readBits(1))
                _ = try r.readBits(1)   // child EE
                done76 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // IntentionalDelayPowerControl
                _ = try r.readBits(1)   // value-start
                _intentionalDelayPowerControl = UInt16(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st76 = 2
            case 1:   // SE(PowerReference)
                _ = try r.readBits(1)   // value-start
                _powerReference = try exiEnum(PowerReference.self, try r.readBits(1))
                _ = try r.readBits(1)   // child EE
                done76 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(1) {
            case 0:   // SE(PowerReference)
                _ = try r.readBits(1)   // value-start
                _powerReference = try exiEnum(PowerReference.self, try r.readBits(1))
                _ = try r.readBits(1)   // child EE
                done76 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done76 = true
        }
    }
    guard let _powerReference else {
        throw ExiError.invalidEventCode("PowerReference is required but the element ended")
    }
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _hysteresisControl = try r.readBits(1) != 0
    _ = try r.readBits(1)   // child EE
    var _powerUpRamp: UInt16? = nil
    var _pT1ResponseActivePower: Bool? = nil
    var st77 = 0
    var done77 = false
    while !done77 {
        switch st77 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // PowerUpRamp
                _ = try r.readBits(1)   // value-start
                _powerUpRamp = UInt16(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st77 = 1
            case 1:   // SE(PT1ResponseActivePower)
                _ = try r.readBits(1)   // value-start
                _pT1ResponseActivePower = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                done77 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(PT1ResponseActivePower)
                _ = try r.readBits(1)   // value-start
                _pT1ResponseActivePower = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                done77 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done77 = true
        }
    }
    guard let _pT1ResponseActivePower else {
        throw ExiError.invalidEventCode("PT1ResponseActivePower is required but the element ended")
    }
    _ = try r.readBits(1)   // SE
    let _stepResponseTimeConstantActivePower = try decodeRationalNumberType(r)
    _ = try r.readBits(1)   // element EE
    return FrequencyWattType(fstart: _fstart, fstop: _fstop, intentionalDelayFstop: _intentionalDelayFstop, slope: _slope, deactivationTime: _deactivationTime, intentionalDelayPowerControl: _intentionalDelayPowerControl, powerReference: _powerReference, hysteresisControl: _hysteresisControl, powerUpRamp: _powerUpRamp, pT1ResponseActivePower: _pT1ResponseActivePower, stepResponseTimeConstantActivePower: _stepResponseTimeConstantActivePower)
}

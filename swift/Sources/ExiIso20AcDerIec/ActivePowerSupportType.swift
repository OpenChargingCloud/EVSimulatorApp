// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.acderiec

import ExiRuntime

public final class ActivePowerSupportType {
    public var underFrequencyWatt: FrequencyWattType?
    public var overFrequencyWatt: FrequencyWattType?
    public var voltWatt: VoltWattType?

    public init(underFrequencyWatt: FrequencyWattType? = nil, overFrequencyWatt: FrequencyWattType? = nil, voltWatt: VoltWattType? = nil) {
        self.underFrequencyWatt = underFrequencyWatt
        self.overFrequencyWatt = overFrequencyWatt
        self.voltWatt = voltWatt
    }
}

internal func encodeActivePowerSupportType(_ w: BitWriter, _ msg: ActivePowerSupportType) {
    var st64 = 0
    var done64 = false
    while !done64 {
        switch st64 {
        case 0:
            if let v = msg.underFrequencyWatt {
                w.writeBits(0, 3)   // UnderFrequencyWatt
                encodeFrequencyWattType(w, v)
                st64 = 1
            } else if let v = msg.overFrequencyWatt {
                w.writeBits(1, 3)   // OverFrequencyWatt
                encodeFrequencyWattType(w, v)
                st64 = 2
            } else if let v = msg.voltWatt {
                w.writeBits(2, 3)   // VoltWatt
                encodeVoltWattType(w, v)
                st64 = 3
            } else {
                w.writeBits(3, 3)   // element EE
                done64 = true
            }
        case 1:
            if let v = msg.overFrequencyWatt {
                w.writeBits(0, 2)   // OverFrequencyWatt
                encodeFrequencyWattType(w, v)
                st64 = 2
            } else if let v = msg.voltWatt {
                w.writeBits(1, 2)   // VoltWatt
                encodeVoltWattType(w, v)
                st64 = 3
            } else {
                w.writeBits(2, 2)   // element EE
                done64 = true
            }
        case 2:
            if let v = msg.voltWatt {
                w.writeBits(0, 2)   // VoltWatt
                encodeVoltWattType(w, v)
                st64 = 3
            } else {
                w.writeBits(1, 2)   // element EE
                done64 = true
            }
        case 3:
            w.writeBits(0, 1)   // element EE
            done64 = true
        default:
            done64 = true
        }
    }
}

internal func decodeActivePowerSupportType(_ r: BitReader) throws -> ActivePowerSupportType {
    var _underFrequencyWatt: FrequencyWattType? = nil
    var _overFrequencyWatt: FrequencyWattType? = nil
    var _voltWatt: VoltWattType? = nil
    var st65 = 0
    var done65 = false
    while !done65 {
        switch st65 {
        case 0:
            switch try r.readBits(3) {
            case 0:   // UnderFrequencyWatt
                _underFrequencyWatt = try decodeFrequencyWattType(r)
                st65 = 1
            case 1:   // OverFrequencyWatt
                _overFrequencyWatt = try decodeFrequencyWattType(r)
                st65 = 2
            case 2:   // VoltWatt
                _voltWatt = try decodeVoltWattType(r)
                st65 = 3
            case 3:   // element EE
                done65 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // OverFrequencyWatt
                _overFrequencyWatt = try decodeFrequencyWattType(r)
                st65 = 2
            case 1:   // VoltWatt
                _voltWatt = try decodeVoltWattType(r)
                st65 = 3
            case 2:   // element EE
                done65 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(2) {
            case 0:   // VoltWatt
                _voltWatt = try decodeVoltWattType(r)
                st65 = 3
            case 1:   // element EE
                done65 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 3:
            switch try r.readBits(1) {
            case 0:   // element EE
                done65 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done65 = true
        }
    }
    return ActivePowerSupportType(underFrequencyWatt: _underFrequencyWatt, overFrequencyWatt: _overFrequencyWatt, voltWatt: _voltWatt)
}

// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import ExiRuntime

public final class SupportedEnergyTransferModeType {
    public var energyTransferMode: [EnergyTransferMode]

    public init(energyTransferMode: [EnergyTransferMode]) {
        self.energyTransferMode = energyTransferMode
    }
}

internal func encodeSupportedEnergyTransferModeType(_ w: BitWriter, _ msg: SupportedEnergyTransferModeType) {
    let list = msg.energyTransferMode
    precondition((1...6).contains(list.count), "list size out of schema range")
    for (i, item) in list.enumerated() {
        w.writeBits(0, i == 0 ? 1 : 2)   // SE(item)
        w.writeBits(0, 1)   // value-start
        w.writeBits(UInt32(item.rawValue), 3)
        w.writeBits(0, 1)   // child EE
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

internal func decodeSupportedEnergyTransferModeType(_ r: BitReader) throws -> SupportedEnergyTransferModeType {
    var list = [EnergyTransferMode]()
    _ = try r.readBits(1)   // SE(item) first
    _ = try r.readBits(1)   // value-start
    let item = try exiEnum(EnergyTransferMode.self, try r.readBits(3))
    _ = try r.readBits(1)   // child EE
    list.append(item)
    while true {
        let ec = try r.readBits(2)
        if ec == 1 { break }   // element EE
        guard ec == 0, list.count < 6 else {
            throw ExiError.invalidEventCode("repeating element")
        }
        _ = try r.readBits(1)   // value-start
        let item = try exiEnum(EnergyTransferMode.self, try r.readBits(3))
        _ = try r.readBits(1)   // child EE
        list.append(item)
    }
    return SupportedEnergyTransferModeType(energyTransferMode: list)
}

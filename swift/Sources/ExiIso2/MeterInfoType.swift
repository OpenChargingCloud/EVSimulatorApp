// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import ExiRuntime

public final class MeterInfoType {
    public var meterID: String
    public var meterReading: UInt64?
    public var sigMeterReading: [UInt8]?
    public var meterStatus: Int16?
    public var tMeter: Int64?

    public init(meterID: String, meterReading: UInt64? = nil, sigMeterReading: [UInt8]? = nil, meterStatus: Int16? = nil, tMeter: Int64? = nil) {
        self.meterID = meterID
        self.meterReading = meterReading
        self.sigMeterReading = sigMeterReading
        self.meterStatus = meterStatus
        self.tMeter = tMeter
    }
}

internal func encodeMeterInfoType(_ w: BitWriter, _ msg: MeterInfoType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeStringValue(w, msg.meterID)
    w.writeBits(0, 1)   // child EE
    var st48 = 0
    var done48 = false
    while !done48 {
        switch st48 {
        case 0:
            if let v = msg.meterReading {
                w.writeBits(0, 3)   // MeterReading
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st48 = 1
            } else if let v = msg.sigMeterReading {
                w.writeBits(1, 3)   // SigMeterReading
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeBinary(w, v)
                w.writeBits(0, 1)   // child EE
                st48 = 2
            } else if let v = msg.meterStatus {
                w.writeBits(2, 3)   // MeterStatus
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st48 = 3
            } else if let v = msg.tMeter {
                w.writeBits(3, 3)   // TMeter
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st48 = 4
            } else {
                w.writeBits(4, 3)   // element EE
                done48 = true
            }
        case 1:
            if let v = msg.sigMeterReading {
                w.writeBits(0, 3)   // SigMeterReading
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeBinary(w, v)
                w.writeBits(0, 1)   // child EE
                st48 = 2
            } else if let v = msg.meterStatus {
                w.writeBits(1, 3)   // MeterStatus
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st48 = 3
            } else if let v = msg.tMeter {
                w.writeBits(2, 3)   // TMeter
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st48 = 4
            } else {
                w.writeBits(3, 3)   // element EE
                done48 = true
            }
        case 2:
            if let v = msg.meterStatus {
                w.writeBits(0, 2)   // MeterStatus
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st48 = 3
            } else if let v = msg.tMeter {
                w.writeBits(1, 2)   // TMeter
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st48 = 4
            } else {
                w.writeBits(2, 2)   // element EE
                done48 = true
            }
        case 3:
            if let v = msg.tMeter {
                w.writeBits(0, 2)   // TMeter
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st48 = 4
            } else {
                w.writeBits(1, 2)   // element EE
                done48 = true
            }
        case 4:
            w.writeBits(0, 1)   // element EE
            done48 = true
        default:
            done48 = true
        }
    }
}

internal func decodeMeterInfoType(_ r: BitReader) throws -> MeterInfoType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _meterID = try ExiPrimitives.readStringValue(r, slot: "MeterID")
    _ = try r.readBits(1)   // child EE
    var _meterReading: UInt64? = nil
    var _sigMeterReading: [UInt8]? = nil
    var _meterStatus: Int16? = nil
    var _tMeter: Int64? = nil
    var st49 = 0
    var done49 = false
    while !done49 {
        switch st49 {
        case 0:
            switch try r.readBits(3) {
            case 0:   // MeterReading
                _ = try r.readBits(1)   // value-start
                _meterReading = UInt64(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st49 = 1
            case 1:   // SigMeterReading
                _ = try r.readBits(1)   // value-start
                _sigMeterReading = try ExiPrimitives.readBinary(r)
                _ = try r.readBits(1)   // child EE
                st49 = 2
            case 2:   // MeterStatus
                _ = try r.readBits(1)   // value-start
                _meterStatus = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st49 = 3
            case 3:   // TMeter
                _ = try r.readBits(1)   // value-start
                _tMeter = Int64(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st49 = 4
            case 4:   // element EE
                done49 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(3) {
            case 0:   // SigMeterReading
                _ = try r.readBits(1)   // value-start
                _sigMeterReading = try ExiPrimitives.readBinary(r)
                _ = try r.readBits(1)   // child EE
                st49 = 2
            case 1:   // MeterStatus
                _ = try r.readBits(1)   // value-start
                _meterStatus = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st49 = 3
            case 2:   // TMeter
                _ = try r.readBits(1)   // value-start
                _tMeter = Int64(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st49 = 4
            case 3:   // element EE
                done49 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(2) {
            case 0:   // MeterStatus
                _ = try r.readBits(1)   // value-start
                _meterStatus = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st49 = 3
            case 1:   // TMeter
                _ = try r.readBits(1)   // value-start
                _tMeter = Int64(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st49 = 4
            case 2:   // element EE
                done49 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 3:
            switch try r.readBits(2) {
            case 0:   // TMeter
                _ = try r.readBits(1)   // value-start
                _tMeter = Int64(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st49 = 4
            case 1:   // element EE
                done49 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 4:
            switch try r.readBits(1) {
            case 0:   // element EE
                done49 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done49 = true
        }
    }
    return MeterInfoType(meterID: _meterID, meterReading: _meterReading, sigMeterReading: _sigMeterReading, meterStatus: _meterStatus, tMeter: _tMeter)
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso2

import ExiRuntime

public final class SalesTariffEntryType: EntryType {
    public var ePriceLevel: UInt8?
    public var consumptionCost: [ConsumptionCostType]

    public init(timeInterval: IntervalType, ePriceLevel: UInt8? = nil, consumptionCost: [ConsumptionCostType]) {
        self.ePriceLevel = ePriceLevel
        self.consumptionCost = consumptionCost
        super.init(timeInterval: timeInterval)
    }
}

internal func encodeSalesTariffEntryType(_ w: BitWriter, _ msg: SalesTariffEntryType) {
    switch msg.timeInterval {
    case let v as RelativeTimeIntervalType:
        w.writeBits(0, 2)   // RelativeTimeInterval
        encodeRelativeTimeIntervalType(w, v)
    default:
        preconditionFailure("unsupported substitution member for TimeInterval")
    }
    var st58 = 0
    var done58 = false
    while !done58 {
        switch st58 {
        case 0:
            if let v = msg.ePriceLevel {
                w.writeBits(0, 2)   // EPriceLevel
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 8)
                w.writeBits(0, 1)   // child EE
                st58 = 1
            } else if !msg.consumptionCost.isEmpty {
                precondition(msg.consumptionCost.count <= 3, "list size out of schema range")
                w.writeBits(1, 2)   // ConsumptionCost
                encodeConsumptionCostType(w, msg.consumptionCost[0])
                for ci in 1..<msg.consumptionCost.count {
                    w.writeBits(0, 2)   // ConsumptionCost
                    encodeConsumptionCostType(w, msg.consumptionCost[ci])
                }
                w.writeBits(1, 2)   // element EE (list end)
                done58 = true
            } else {
                w.writeBits(2, 2)   // element EE
                done58 = true
            }
        case 1:
            if !msg.consumptionCost.isEmpty {
                precondition(msg.consumptionCost.count <= 3, "list size out of schema range")
                w.writeBits(0, 2)   // ConsumptionCost
                encodeConsumptionCostType(w, msg.consumptionCost[0])
                for ci in 1..<msg.consumptionCost.count {
                    w.writeBits(0, 2)   // ConsumptionCost
                    encodeConsumptionCostType(w, msg.consumptionCost[ci])
                }
                w.writeBits(1, 2)   // element EE (list end)
                done58 = true
            } else {
                w.writeBits(1, 2)   // element EE
                done58 = true
            }
        case 2:
            w.writeBits(0, 1)   // element EE
            done58 = true
        default:
            done58 = true
        }
    }
}

internal func decodeSalesTariffEntryType(_ r: BitReader) throws -> SalesTariffEntryType {
    let _timeInterval: IntervalType
    switch try r.readBits(2) {
    case 0:   // RelativeTimeInterval
        _timeInterval = try decodeRelativeTimeIntervalType(r)
    default:
        throw ExiError.invalidEventCode("TimeInterval substitution")
    }
    var _ePriceLevel: UInt8? = nil
    var consumptionCostList = [ConsumptionCostType]()
    var st59 = 0
    var done59 = false
    while !done59 {
        switch st59 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // EPriceLevel
                _ = try r.readBits(1)   // value-start
                _ePriceLevel = UInt8(try r.readBits(8))
                _ = try r.readBits(1)   // child EE
                st59 = 1
            case 1:   // ConsumptionCost
                consumptionCostList.append(try decodeConsumptionCostType(r))
                while true {
                    let ec = try r.readBits(2)
                    if ec == 1 { break }   // element EE (list end)
                    guard ec == 0, consumptionCostList.count < 3 else {
                        throw ExiError.invalidEventCode("repeating element")
                    }
                    consumptionCostList.append(try decodeConsumptionCostType(r))
                }
                done59 = true
            case 2:   // element EE
                done59 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // ConsumptionCost
                consumptionCostList.append(try decodeConsumptionCostType(r))
                while true {
                    let ec = try r.readBits(2)
                    if ec == 1 { break }   // element EE (list end)
                    guard ec == 0, consumptionCostList.count < 3 else {
                        throw ExiError.invalidEventCode("repeating element")
                    }
                    consumptionCostList.append(try decodeConsumptionCostType(r))
                }
                done59 = true
            case 1:   // element EE
                done59 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(1) {
            case 0:   // element EE
                done59 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done59 = true
        }
    }
    return SalesTariffEntryType(timeInterval: _timeInterval, ePriceLevel: _ePriceLevel, consumptionCost: consumptionCostList)
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso2

import ExiRuntime

public class ServiceType {
    public var serviceID: UInt16
    public var serviceName: String?
    public var serviceCategory: ServiceCategory
    public var serviceScope: String?
    public var freeService: Bool

    public init(serviceID: UInt16, serviceName: String? = nil, serviceCategory: ServiceCategory, serviceScope: String? = nil, freeService: Bool) {
        self.serviceID = serviceID
        self.serviceName = serviceName
        self.serviceCategory = serviceCategory
        self.serviceScope = serviceScope
        self.freeService = freeService
    }
}

internal func encodeServiceType(_ w: BitWriter, _ msg: ServiceType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, UInt64(msg.serviceID))
    w.writeBits(0, 1)   // child EE
    var st34 = 0
    var done34 = false
    while !done34 {
        switch st34 {
        case 0:
            if let v = msg.serviceName {
                w.writeBits(0, 2)   // ServiceName
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeStringValue(w, v)
                w.writeBits(0, 1)   // child EE
                st34 = 1
            } else {
                w.writeBits(1, 2)   // SE(ServiceCategory)
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(msg.serviceCategory.rawValue), 2)
                w.writeBits(0, 1)   // child EE
                done34 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(ServiceCategory)
            w.writeBits(0, 1)   // value-start
            w.writeBits(UInt32(msg.serviceCategory.rawValue), 2)
            w.writeBits(0, 1)   // child EE
            done34 = true
        default:
            done34 = true
        }
    }
    var st35 = 0
    var done35 = false
    while !done35 {
        switch st35 {
        case 0:
            if let v = msg.serviceScope {
                w.writeBits(0, 2)   // ServiceScope
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeStringValue(w, v)
                w.writeBits(0, 1)   // child EE
                st35 = 1
            } else {
                w.writeBits(1, 2)   // SE(FreeService)
                w.writeBits(0, 1)   // value-start
                w.writeBits(msg.freeService ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                done35 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(FreeService)
            w.writeBits(0, 1)   // value-start
            w.writeBits(msg.freeService ? 1 : 0, 1)
            w.writeBits(0, 1)   // child EE
            done35 = true
        default:
            done35 = true
        }
    }
    w.writeBits(0, 1)   // element EE
}

internal func decodeServiceType(_ r: BitReader) throws -> ServiceType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _serviceID = UInt16(try ExiPrimitives.readUnsignedInteger(r))
    _ = try r.readBits(1)   // child EE
    var _serviceName: String? = nil
    var _serviceCategory: ServiceCategory? = nil
    var st36 = 0
    var done36 = false
    while !done36 {
        switch st36 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // ServiceName
                _ = try r.readBits(1)   // value-start
                _serviceName = try ExiPrimitives.readStringValue(r, slot: "ServiceName")
                _ = try r.readBits(1)   // child EE
                st36 = 1
            case 1:   // SE(ServiceCategory)
                _ = try r.readBits(1)   // value-start
                _serviceCategory = try exiEnum(ServiceCategory.self, try r.readBits(2))
                _ = try r.readBits(1)   // child EE
                done36 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(ServiceCategory)
                _ = try r.readBits(1)   // value-start
                _serviceCategory = try exiEnum(ServiceCategory.self, try r.readBits(2))
                _ = try r.readBits(1)   // child EE
                done36 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done36 = true
        }
    }
    guard let _serviceCategory else {
        throw ExiError.invalidEventCode("ServiceCategory is required but the element ended")
    }
    var _serviceScope: String? = nil
    var _freeService: Bool? = nil
    var st37 = 0
    var done37 = false
    while !done37 {
        switch st37 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // ServiceScope
                _ = try r.readBits(1)   // value-start
                _serviceScope = try ExiPrimitives.readStringValue(r, slot: "ServiceScope")
                _ = try r.readBits(1)   // child EE
                st37 = 1
            case 1:   // SE(FreeService)
                _ = try r.readBits(1)   // value-start
                _freeService = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                done37 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(FreeService)
                _ = try r.readBits(1)   // value-start
                _freeService = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                done37 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done37 = true
        }
    }
    guard let _freeService else {
        throw ExiError.invalidEventCode("FreeService is required but the element ended")
    }
    _ = try r.readBits(1)   // element EE
    return ServiceType(serviceID: _serviceID, serviceName: _serviceName, serviceCategory: _serviceCategory, serviceScope: _serviceScope, freeService: _freeService)
}

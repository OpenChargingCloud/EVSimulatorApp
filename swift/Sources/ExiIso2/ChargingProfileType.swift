// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import ExiRuntime

public final class ChargingProfileType {
    public var profileEntry: [ProfileEntryType]

    public init(profileEntry: [ProfileEntryType]) {
        self.profileEntry = profileEntry
    }
}

internal func encodeChargingProfileType(_ w: BitWriter, _ msg: ChargingProfileType) {
    let list = msg.profileEntry
    precondition((1...24).contains(list.count), "list size out of schema range")
    for (i, item) in list.enumerated() {
        w.writeBits(0, i == 0 ? 1 : 2)   // SE(item)
        encodeProfileEntryType(w, item)
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

internal func decodeChargingProfileType(_ r: BitReader) throws -> ChargingProfileType {
    var list = [ProfileEntryType]()
    _ = try r.readBits(1)   // SE(item) first
    list.append(try decodeProfileEntryType(r))
    while true {
        let ec = try r.readBits(2)
        if ec == 1 { break }   // element EE
        guard ec == 0, list.count < 24 else {
            throw ExiError.invalidEventCode("repeating element")
        }
        list.append(try decodeProfileEntryType(r))
    }
    return ChargingProfileType(profileEntry: list)
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import ExiRuntime

public final class MeteringReceiptReqType: BodyBaseType {
    public var id: String?
    public var sessionID: [UInt8]
    public var sAScheduleTupleID: UInt8?
    public var meterInfo: MeterInfoType

    public init(id: String? = nil, sessionID: [UInt8], sAScheduleTupleID: UInt8? = nil, meterInfo: MeterInfoType) {
        self.id = id
        self.sessionID = sessionID
        self.sAScheduleTupleID = sAScheduleTupleID
        self.meterInfo = meterInfo
        super.init()
    }
}

internal func encodeMeteringReceiptReqType(_ w: BitWriter, _ msg: MeteringReceiptReqType) {
    var st18 = 0
    var done18 = false
    while !done18 {
        switch st18 {
        case 0:
            if let v = msg.id {
                w.writeBits(0, 2)   // Id
                ExiPrimitives.writeStringValue(w, v)
                st18 = 1
            } else {
                w.writeBits(1, 2)   // SE(SessionID)
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeBinary(w, msg.sessionID)
                w.writeBits(0, 1)   // child EE
                done18 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(SessionID)
            w.writeBits(0, 1)   // value-start
            ExiPrimitives.writeBinary(w, msg.sessionID)
            w.writeBits(0, 1)   // child EE
            done18 = true
        default:
            done18 = true
        }
    }
    var st19 = 0
    var done19 = false
    while !done19 {
        switch st19 {
        case 0:
            if let v = msg.sAScheduleTupleID {
                w.writeBits(0, 2)   // SAScheduleTupleID
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(Int64(v) - 1), 8)
                w.writeBits(0, 1)   // child EE
                st19 = 1
            } else {
                w.writeBits(1, 2)   // SE(MeterInfo)
                encodeMeterInfoType(w, msg.meterInfo)
                done19 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(MeterInfo)
            encodeMeterInfoType(w, msg.meterInfo)
            done19 = true
        default:
            done19 = true
        }
    }
    w.writeBits(0, 1)   // element EE
}

internal func decodeMeteringReceiptReqType(_ r: BitReader) throws -> MeteringReceiptReqType {
    var _id: String? = nil
    var _sessionID: [UInt8]? = nil
    var st20 = 0
    var done20 = false
    while !done20 {
        switch st20 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // Id
                _id = try ExiPrimitives.readStringValue(r, slot: "Id")
                st20 = 1
            case 1:   // SE(SessionID)
                _ = try r.readBits(1)   // value-start
                _sessionID = try ExiPrimitives.readBinary(r)
                _ = try r.readBits(1)   // child EE
                done20 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(SessionID)
                _ = try r.readBits(1)   // value-start
                _sessionID = try ExiPrimitives.readBinary(r)
                _ = try r.readBits(1)   // child EE
                done20 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done20 = true
        }
    }
    guard let _sessionID else {
        throw ExiError.invalidEventCode("SessionID is required but the element ended")
    }
    var _sAScheduleTupleID: UInt8? = nil
    var _meterInfo: MeterInfoType? = nil
    var st21 = 0
    var done21 = false
    while !done21 {
        switch st21 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // SAScheduleTupleID
                _ = try r.readBits(1)   // value-start
                _sAScheduleTupleID = UInt8(Int64(try r.readBits(8)) + 1)
                _ = try r.readBits(1)   // child EE
                st21 = 1
            case 1:   // SE(MeterInfo)
                _meterInfo = try decodeMeterInfoType(r)
                done21 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(MeterInfo)
                _meterInfo = try decodeMeterInfoType(r)
                done21 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done21 = true
        }
    }
    guard let _meterInfo else {
        throw ExiError.invalidEventCode("MeterInfo is required but the element ended")
    }
    _ = try r.readBits(1)   // element EE
    return MeteringReceiptReqType(id: _id, sessionID: _sessionID, sAScheduleTupleID: _sAScheduleTupleID, meterInfo: _meterInfo)
}

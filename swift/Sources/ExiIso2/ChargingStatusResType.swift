// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import ExiRuntime

public final class ChargingStatusResType: BodyBaseType {
    public var responseCode: ResponseCode
    public var eVSEID: String
    public var sAScheduleTupleID: UInt8
    public var eVSEMaxCurrent: PhysicalValueType?
    public var meterInfo: MeterInfoType?
    public var receiptRequired: Bool?
    public var aC_EVSEStatus: AC_EVSEStatusType

    public init(responseCode: ResponseCode, eVSEID: String, sAScheduleTupleID: UInt8, eVSEMaxCurrent: PhysicalValueType? = nil, meterInfo: MeterInfoType? = nil, receiptRequired: Bool? = nil, aC_EVSEStatus: AC_EVSEStatusType) {
        self.responseCode = responseCode
        self.eVSEID = eVSEID
        self.sAScheduleTupleID = sAScheduleTupleID
        self.eVSEMaxCurrent = eVSEMaxCurrent
        self.meterInfo = meterInfo
        self.receiptRequired = receiptRequired
        self.aC_EVSEStatus = aC_EVSEStatus
        super.init()
    }
}

internal func encodeChargingStatusResType(_ w: BitWriter, _ msg: ChargingStatusResType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.responseCode.rawValue), 5)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeStringValue(w, msg.eVSEID)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(Int64(msg.sAScheduleTupleID) - 1), 8)
    w.writeBits(0, 1)   // child EE
    var st24 = 0
    var done24 = false
    while !done24 {
        switch st24 {
        case 0:
            if let v = msg.eVSEMaxCurrent {
                w.writeBits(0, 3)   // EVSEMaxCurrent
                encodePhysicalValueType(w, v)
                st24 = 1
            } else if let v = msg.meterInfo {
                w.writeBits(1, 3)   // MeterInfo
                encodeMeterInfoType(w, v)
                st24 = 2
            } else if let v = msg.receiptRequired {
                w.writeBits(2, 3)   // ReceiptRequired
                w.writeBits(0, 1)   // value-start
                w.writeBits(v ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                st24 = 3
            } else {
                w.writeBits(3, 3)   // SE(AC_EVSEStatus)
                encodeAC_EVSEStatusType(w, msg.aC_EVSEStatus)
                done24 = true
            }
        case 1:
            if let v = msg.meterInfo {
                w.writeBits(0, 2)   // MeterInfo
                encodeMeterInfoType(w, v)
                st24 = 2
            } else if let v = msg.receiptRequired {
                w.writeBits(1, 2)   // ReceiptRequired
                w.writeBits(0, 1)   // value-start
                w.writeBits(v ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                st24 = 3
            } else {
                w.writeBits(2, 2)   // SE(AC_EVSEStatus)
                encodeAC_EVSEStatusType(w, msg.aC_EVSEStatus)
                done24 = true
            }
        case 2:
            if let v = msg.receiptRequired {
                w.writeBits(0, 2)   // ReceiptRequired
                w.writeBits(0, 1)   // value-start
                w.writeBits(v ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                st24 = 3
            } else {
                w.writeBits(1, 2)   // SE(AC_EVSEStatus)
                encodeAC_EVSEStatusType(w, msg.aC_EVSEStatus)
                done24 = true
            }
        case 3:
            w.writeBits(0, 1)   // SE(AC_EVSEStatus)
            encodeAC_EVSEStatusType(w, msg.aC_EVSEStatus)
            done24 = true
        default:
            done24 = true
        }
    }
    w.writeBits(0, 1)   // element EE
}

internal func decodeChargingStatusResType(_ r: BitReader) throws -> ChargingStatusResType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _responseCode = try exiEnum(ResponseCode.self, try r.readBits(5))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _eVSEID = try ExiPrimitives.readStringValue(r, slot: "EVSEID")
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _sAScheduleTupleID = UInt8(Int64(try r.readBits(8)) + 1)
    _ = try r.readBits(1)   // child EE
    var _eVSEMaxCurrent: PhysicalValueType? = nil
    var _meterInfo: MeterInfoType? = nil
    var _receiptRequired: Bool? = nil
    var _aC_EVSEStatus: AC_EVSEStatusType? = nil
    var st25 = 0
    var done25 = false
    while !done25 {
        switch st25 {
        case 0:
            switch try r.readBits(3) {
            case 0:   // EVSEMaxCurrent
                _eVSEMaxCurrent = try decodePhysicalValueType(r)
                st25 = 1
            case 1:   // MeterInfo
                _meterInfo = try decodeMeterInfoType(r)
                st25 = 2
            case 2:   // ReceiptRequired
                _ = try r.readBits(1)   // value-start
                _receiptRequired = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                st25 = 3
            case 3:   // SE(AC_EVSEStatus)
                _aC_EVSEStatus = try decodeAC_EVSEStatusType(r)
                done25 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // MeterInfo
                _meterInfo = try decodeMeterInfoType(r)
                st25 = 2
            case 1:   // ReceiptRequired
                _ = try r.readBits(1)   // value-start
                _receiptRequired = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                st25 = 3
            case 2:   // SE(AC_EVSEStatus)
                _aC_EVSEStatus = try decodeAC_EVSEStatusType(r)
                done25 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(2) {
            case 0:   // ReceiptRequired
                _ = try r.readBits(1)   // value-start
                _receiptRequired = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                st25 = 3
            case 1:   // SE(AC_EVSEStatus)
                _aC_EVSEStatus = try decodeAC_EVSEStatusType(r)
                done25 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 3:
            switch try r.readBits(1) {
            case 0:   // SE(AC_EVSEStatus)
                _aC_EVSEStatus = try decodeAC_EVSEStatusType(r)
                done25 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done25 = true
        }
    }
    guard let _aC_EVSEStatus else {
        throw ExiError.invalidEventCode("AC_EVSEStatus is required but the element ended")
    }
    _ = try r.readBits(1)   // element EE
    return ChargingStatusResType(responseCode: _responseCode, eVSEID: _eVSEID, sAScheduleTupleID: _sAScheduleTupleID, eVSEMaxCurrent: _eVSEMaxCurrent, meterInfo: _meterInfo, receiptRequired: _receiptRequired, aC_EVSEStatus: _aC_EVSEStatus)
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import ExiRuntime

public final class MessageHeaderType {
    public var sessionID: [UInt8]
    public var notification: NotificationType?
    public var signature: SignatureType?

    public init(sessionID: [UInt8], notification: NotificationType? = nil, signature: SignatureType? = nil) {
        self.sessionID = sessionID
        self.notification = notification
        self.signature = signature
    }
}

internal func encodeMessageHeaderType(_ w: BitWriter, _ msg: MessageHeaderType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeBinary(w, msg.sessionID)
    w.writeBits(0, 1)   // child EE
    var st82 = 0
    var done82 = false
    while !done82 {
        switch st82 {
        case 0:
            if let v = msg.notification {
                w.writeBits(0, 2)   // Notification
                encodeNotificationType(w, v)
                st82 = 1
            } else if let v = msg.signature {
                w.writeBits(1, 2)   // Signature
                encodeSignatureType(w, v)
                st82 = 2
            } else {
                w.writeBits(2, 2)   // element EE
                done82 = true
            }
        case 1:
            if let v = msg.signature {
                w.writeBits(0, 2)   // Signature
                encodeSignatureType(w, v)
                st82 = 2
            } else {
                w.writeBits(1, 2)   // element EE
                done82 = true
            }
        case 2:
            w.writeBits(0, 1)   // element EE
            done82 = true
        default:
            done82 = true
        }
    }
}

internal func decodeMessageHeaderType(_ r: BitReader) throws -> MessageHeaderType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _sessionID = try ExiPrimitives.readBinary(r)
    _ = try r.readBits(1)   // child EE
    var _notification: NotificationType? = nil
    var _signature: SignatureType? = nil
    var st83 = 0
    var done83 = false
    while !done83 {
        switch st83 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // Notification
                _notification = try decodeNotificationType(r)
                st83 = 1
            case 1:   // Signature
                _signature = try decodeSignatureType(r)
                st83 = 2
            case 2:   // element EE
                done83 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // Signature
                _signature = try decodeSignatureType(r)
                st83 = 2
            case 1:   // element EE
                done83 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(1) {
            case 0:   // element EE
                done83 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done83 = true
        }
    }
    return MessageHeaderType(sessionID: _sessionID, notification: _notification, signature: _signature)
}

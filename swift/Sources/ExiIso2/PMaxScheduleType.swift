// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import ExiRuntime

public final class PMaxScheduleType {
    public var pMaxScheduleEntry: [PMaxScheduleEntryType]

    public init(pMaxScheduleEntry: [PMaxScheduleEntryType]) {
        self.pMaxScheduleEntry = pMaxScheduleEntry
    }
}

internal func encodePMaxScheduleType(_ w: BitWriter, _ msg: PMaxScheduleType) {
    let list = msg.pMaxScheduleEntry
    precondition((1...1024).contains(list.count), "list size out of schema range")
    for (i, item) in list.enumerated() {
        w.writeBits(0, i == 0 ? 1 : 2)   // SE(item)
        encodePMaxScheduleEntryType(w, item)
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

internal func decodePMaxScheduleType(_ r: BitReader) throws -> PMaxScheduleType {
    var list = [PMaxScheduleEntryType]()
    _ = try r.readBits(1)   // SE(item) first
    list.append(try decodePMaxScheduleEntryType(r))
    while true {
        let ec = try r.readBits(2)
        if ec == 1 { break }   // element EE
        guard ec == 0, list.count < 1024 else {
            throw ExiError.invalidEventCode("repeating element")
        }
        list.append(try decodePMaxScheduleEntryType(r))
    }
    return PMaxScheduleType(pMaxScheduleEntry: list)
}

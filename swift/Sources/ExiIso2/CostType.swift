// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import ExiRuntime

public final class CostType {
    public var costKind: CostKind
    public var amount: UInt32
    public var amountMultiplier: Int8?

    public init(costKind: CostKind, amount: UInt32, amountMultiplier: Int8? = nil) {
        self.costKind = costKind
        self.amount = amount
        self.amountMultiplier = amountMultiplier
    }
}

internal func encodeCostType(_ w: BitWriter, _ msg: CostType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.costKind.rawValue), 2)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, UInt64(msg.amount))
    w.writeBits(0, 1)   // child EE
    var st62 = 0
    var done62 = false
    while !done62 {
        switch st62 {
        case 0:
            if let v = msg.amountMultiplier {
                w.writeBits(0, 2)   // AmountMultiplier
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(Int64(v) - -3), 3)
                w.writeBits(0, 1)   // child EE
                st62 = 1
            } else {
                w.writeBits(1, 2)   // element EE
                done62 = true
            }
        case 1:
            w.writeBits(0, 1)   // element EE
            done62 = true
        default:
            done62 = true
        }
    }
}

internal func decodeCostType(_ r: BitReader) throws -> CostType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _costKind = try exiEnum(CostKind.self, try r.readBits(2))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _amount = UInt32(try ExiPrimitives.readUnsignedInteger(r))
    _ = try r.readBits(1)   // child EE
    var _amountMultiplier: Int8? = nil
    var st63 = 0
    var done63 = false
    while !done63 {
        switch st63 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // AmountMultiplier
                _ = try r.readBits(1)   // value-start
                _amountMultiplier = Int8(Int64(try r.readBits(3)) + -3)
                _ = try r.readBits(1)   // child EE
                st63 = 1
            case 1:   // element EE
                done63 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // element EE
                done63 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done63 = true
        }
    }
    return CostType(costKind: _costKind, amount: _amount, amountMultiplier: _amountMultiplier)
}

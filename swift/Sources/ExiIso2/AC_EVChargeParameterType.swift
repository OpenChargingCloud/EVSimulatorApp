// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso2

import ExiRuntime

public final class AC_EVChargeParameterType: EVChargeParameterType {
    public var eAmount: PhysicalValueType
    public var eVMaxVoltage: PhysicalValueType
    public var eVMaxCurrent: PhysicalValueType
    public var eVMinCurrent: PhysicalValueType

    public init(departureTime: UInt32? = nil, eAmount: PhysicalValueType, eVMaxVoltage: PhysicalValueType, eVMaxCurrent: PhysicalValueType, eVMinCurrent: PhysicalValueType) {
        self.eAmount = eAmount
        self.eVMaxVoltage = eVMaxVoltage
        self.eVMaxCurrent = eVMaxCurrent
        self.eVMinCurrent = eVMinCurrent
        super.init(departureTime: departureTime)
    }
}

internal func encodeAC_EVChargeParameterType(_ w: BitWriter, _ msg: AC_EVChargeParameterType) {
    var st66 = 0
    var done66 = false
    while !done66 {
        switch st66 {
        case 0:
            if let v = msg.departureTime {
                w.writeBits(0, 2)   // DepartureTime
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st66 = 1
            } else {
                w.writeBits(1, 2)   // SE(EAmount)
                encodePhysicalValueType(w, msg.eAmount)
                done66 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(EAmount)
            encodePhysicalValueType(w, msg.eAmount)
            done66 = true
        default:
            done66 = true
        }
    }
    w.writeBits(0, 1)   // SE
    encodePhysicalValueType(w, msg.eVMaxVoltage)
    w.writeBits(0, 1)   // SE
    encodePhysicalValueType(w, msg.eVMaxCurrent)
    w.writeBits(0, 1)   // SE
    encodePhysicalValueType(w, msg.eVMinCurrent)
    w.writeBits(0, 1)   // element EE
}

internal func decodeAC_EVChargeParameterType(_ r: BitReader) throws -> AC_EVChargeParameterType {
    var _departureTime: UInt32? = nil
    var _eAmount: PhysicalValueType? = nil
    var st67 = 0
    var done67 = false
    while !done67 {
        switch st67 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // DepartureTime
                _ = try r.readBits(1)   // value-start
                _departureTime = UInt32(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st67 = 1
            case 1:   // SE(EAmount)
                _eAmount = try decodePhysicalValueType(r)
                done67 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(EAmount)
                _eAmount = try decodePhysicalValueType(r)
                done67 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done67 = true
        }
    }
    guard let _eAmount else {
        throw ExiError.invalidEventCode("EAmount is required but the element ended")
    }
    _ = try r.readBits(1)   // SE
    let _eVMaxVoltage = try decodePhysicalValueType(r)
    _ = try r.readBits(1)   // SE
    let _eVMaxCurrent = try decodePhysicalValueType(r)
    _ = try r.readBits(1)   // SE
    let _eVMinCurrent = try decodePhysicalValueType(r)
    _ = try r.readBits(1)   // element EE
    return AC_EVChargeParameterType(departureTime: _departureTime, eAmount: _eAmount, eVMaxVoltage: _eVMaxVoltage, eVMaxCurrent: _eVMaxCurrent, eVMinCurrent: _eVMinCurrent)
}

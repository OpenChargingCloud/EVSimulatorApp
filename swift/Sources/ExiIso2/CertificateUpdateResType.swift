// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import ExiRuntime

public final class CertificateUpdateResType: BodyBaseType {
    public var responseCode: ResponseCode
    public var sAProvisioningCertificateChain: CertificateChainType
    public var contractSignatureCertChain: CertificateChainType
    public var contractSignatureEncryptedPrivateKey: ContractSignatureEncryptedPrivateKeyType
    public var dHpublickey: DiffieHellmanPublickeyType
    public var eMAID: EMAIDType
    public var retryCounter: Int16?

    public init(responseCode: ResponseCode, sAProvisioningCertificateChain: CertificateChainType, contractSignatureCertChain: CertificateChainType, contractSignatureEncryptedPrivateKey: ContractSignatureEncryptedPrivateKeyType, dHpublickey: DiffieHellmanPublickeyType, eMAID: EMAIDType, retryCounter: Int16? = nil) {
        self.responseCode = responseCode
        self.sAProvisioningCertificateChain = sAProvisioningCertificateChain
        self.contractSignatureCertChain = contractSignatureCertChain
        self.contractSignatureEncryptedPrivateKey = contractSignatureEncryptedPrivateKey
        self.dHpublickey = dHpublickey
        self.eMAID = eMAID
        self.retryCounter = retryCounter
        super.init()
    }
}

internal func encodeCertificateUpdateResType(_ w: BitWriter, _ msg: CertificateUpdateResType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.responseCode.rawValue), 5)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    encodeCertificateChainType(w, msg.sAProvisioningCertificateChain)
    w.writeBits(0, 1)   // SE
    encodeCertificateChainType(w, msg.contractSignatureCertChain)
    w.writeBits(0, 1)   // SE
    encodeContractSignatureEncryptedPrivateKeyType(w, msg.contractSignatureEncryptedPrivateKey)
    w.writeBits(0, 1)   // SE
    encodeDiffieHellmanPublickeyType(w, msg.dHpublickey)
    w.writeBits(0, 1)   // SE
    encodeEMAIDType(w, msg.eMAID)
    var st22 = 0
    var done22 = false
    while !done22 {
        switch st22 {
        case 0:
            if let v = msg.retryCounter {
                w.writeBits(0, 2)   // RetryCounter
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st22 = 1
            } else {
                w.writeBits(1, 2)   // element EE
                done22 = true
            }
        case 1:
            w.writeBits(0, 1)   // element EE
            done22 = true
        default:
            done22 = true
        }
    }
}

internal func decodeCertificateUpdateResType(_ r: BitReader) throws -> CertificateUpdateResType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _responseCode = try exiEnum(ResponseCode.self, try r.readBits(5))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    let _sAProvisioningCertificateChain = try decodeCertificateChainType(r)
    _ = try r.readBits(1)   // SE
    let _contractSignatureCertChain = try decodeCertificateChainType(r)
    _ = try r.readBits(1)   // SE
    let _contractSignatureEncryptedPrivateKey = try decodeContractSignatureEncryptedPrivateKeyType(r)
    _ = try r.readBits(1)   // SE
    let _dHpublickey = try decodeDiffieHellmanPublickeyType(r)
    _ = try r.readBits(1)   // SE
    let _eMAID = try decodeEMAIDType(r)
    var _retryCounter: Int16? = nil
    var st23 = 0
    var done23 = false
    while !done23 {
        switch st23 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // RetryCounter
                _ = try r.readBits(1)   // value-start
                _retryCounter = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st23 = 1
            case 1:   // element EE
                done23 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // element EE
                done23 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done23 = true
        }
    }
    return CertificateUpdateResType(responseCode: _responseCode, sAProvisioningCertificateChain: _sAProvisioningCertificateChain, contractSignatureCertChain: _contractSignatureCertChain, contractSignatureEncryptedPrivateKey: _contractSignatureEncryptedPrivateKey, dHpublickey: _dHpublickey, eMAID: _eMAID, retryCounter: _retryCounter)
}

// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso2

import ExiRuntime

public final class SalesTariffType {
    public var id: String?
    public var salesTariffID: UInt8
    public var salesTariffDescription: String?
    public var numEPriceLevels: UInt8?
    public var salesTariffEntry: [SalesTariffEntryType]

    public init(id: String? = nil, salesTariffID: UInt8, salesTariffDescription: String? = nil, numEPriceLevels: UInt8? = nil, salesTariffEntry: [SalesTariffEntryType]) {
        self.id = id
        self.salesTariffID = salesTariffID
        self.salesTariffDescription = salesTariffDescription
        self.numEPriceLevels = numEPriceLevels
        self.salesTariffEntry = salesTariffEntry
    }
}

internal func encodeSalesTariffType(_ w: BitWriter, _ msg: SalesTariffType) {
    var st54 = 0
    var done54 = false
    while !done54 {
        switch st54 {
        case 0:
            if let v = msg.id {
                w.writeBits(0, 2)   // Id
                ExiPrimitives.writeStringValue(w, v)
                st54 = 1
            } else {
                w.writeBits(1, 2)   // SE(SalesTariffID)
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(Int64(msg.salesTariffID) - 1), 8)
                w.writeBits(0, 1)   // child EE
                done54 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(SalesTariffID)
            w.writeBits(0, 1)   // value-start
            w.writeBits(UInt32(Int64(msg.salesTariffID) - 1), 8)
            w.writeBits(0, 1)   // child EE
            done54 = true
        default:
            done54 = true
        }
    }
    var st55 = 0
    var done55 = false
    while !done55 {
        switch st55 {
        case 0:
            if let v = msg.salesTariffDescription {
                w.writeBits(0, 2)   // SalesTariffDescription
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeStringValue(w, v)
                w.writeBits(0, 1)   // child EE
                st55 = 1
            } else if let v = msg.numEPriceLevels {
                w.writeBits(1, 2)   // NumEPriceLevels
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 8)
                w.writeBits(0, 1)   // child EE
                st55 = 2
            } else {
                precondition((1...1024).contains(msg.salesTariffEntry.count), "list size out of schema range")
                w.writeBits(2, 2)   // SalesTariffEntry
                encodeSalesTariffEntryType(w, msg.salesTariffEntry[0])
                for ci in 1..<msg.salesTariffEntry.count {
                    w.writeBits(0, 2)   // SalesTariffEntry
                    encodeSalesTariffEntryType(w, msg.salesTariffEntry[ci])
                }
                w.writeBits(1, 2)   // element EE (list end)
                done55 = true
            }
        case 1:
            if let v = msg.numEPriceLevels {
                w.writeBits(0, 2)   // NumEPriceLevels
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 8)
                w.writeBits(0, 1)   // child EE
                st55 = 2
            } else {
                precondition((1...1024).contains(msg.salesTariffEntry.count), "list size out of schema range")
                w.writeBits(1, 2)   // SalesTariffEntry
                encodeSalesTariffEntryType(w, msg.salesTariffEntry[0])
                for ci in 1..<msg.salesTariffEntry.count {
                    w.writeBits(0, 2)   // SalesTariffEntry
                    encodeSalesTariffEntryType(w, msg.salesTariffEntry[ci])
                }
                w.writeBits(1, 2)   // element EE (list end)
                done55 = true
            }
        case 2:
            precondition((1...1024).contains(msg.salesTariffEntry.count), "list size out of schema range")
            w.writeBits(0, 1)   // SalesTariffEntry
            encodeSalesTariffEntryType(w, msg.salesTariffEntry[0])
            for ci in 1..<msg.salesTariffEntry.count {
                w.writeBits(0, 2)   // SalesTariffEntry
                encodeSalesTariffEntryType(w, msg.salesTariffEntry[ci])
            }
            w.writeBits(1, 2)   // element EE (list end)
            done55 = true
        default:
            done55 = true
        }
    }
}

internal func decodeSalesTariffType(_ r: BitReader) throws -> SalesTariffType {
    var _id: String? = nil
    var _salesTariffID: UInt8? = nil
    var st56 = 0
    var done56 = false
    while !done56 {
        switch st56 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // Id
                _id = try ExiPrimitives.readStringValue(r, slot: "Id")
                st56 = 1
            case 1:   // SE(SalesTariffID)
                _ = try r.readBits(1)   // value-start
                _salesTariffID = UInt8(Int64(try r.readBits(8)) + 1)
                _ = try r.readBits(1)   // child EE
                done56 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(SalesTariffID)
                _ = try r.readBits(1)   // value-start
                _salesTariffID = UInt8(Int64(try r.readBits(8)) + 1)
                _ = try r.readBits(1)   // child EE
                done56 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done56 = true
        }
    }
    guard let _salesTariffID else {
        throw ExiError.invalidEventCode("SalesTariffID is required but the element ended")
    }
    var _salesTariffDescription: String? = nil
    var _numEPriceLevels: UInt8? = nil
    var salesTariffEntryList = [SalesTariffEntryType]()
    var st57 = 0
    var done57 = false
    while !done57 {
        switch st57 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // SalesTariffDescription
                _ = try r.readBits(1)   // value-start
                _salesTariffDescription = try ExiPrimitives.readStringValue(r, slot: "SalesTariffDescription")
                _ = try r.readBits(1)   // child EE
                st57 = 1
            case 1:   // NumEPriceLevels
                _ = try r.readBits(1)   // value-start
                _numEPriceLevels = UInt8(try r.readBits(8))
                _ = try r.readBits(1)   // child EE
                st57 = 2
            case 2:   // SalesTariffEntry
                salesTariffEntryList.append(try decodeSalesTariffEntryType(r))
                while true {
                    let ec = try r.readBits(2)
                    if ec == 1 { break }   // element EE (list end)
                    guard ec == 0, salesTariffEntryList.count < 1024 else {
                        throw ExiError.invalidEventCode("repeating element")
                    }
                    salesTariffEntryList.append(try decodeSalesTariffEntryType(r))
                }
                done57 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // NumEPriceLevels
                _ = try r.readBits(1)   // value-start
                _numEPriceLevels = UInt8(try r.readBits(8))
                _ = try r.readBits(1)   // child EE
                st57 = 2
            case 1:   // SalesTariffEntry
                salesTariffEntryList.append(try decodeSalesTariffEntryType(r))
                while true {
                    let ec = try r.readBits(2)
                    if ec == 1 { break }   // element EE (list end)
                    guard ec == 0, salesTariffEntryList.count < 1024 else {
                        throw ExiError.invalidEventCode("repeating element")
                    }
                    salesTariffEntryList.append(try decodeSalesTariffEntryType(r))
                }
                done57 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(1) {
            case 0:   // SalesTariffEntry
                salesTariffEntryList.append(try decodeSalesTariffEntryType(r))
                while true {
                    let ec = try r.readBits(2)
                    if ec == 1 { break }   // element EE (list end)
                    guard ec == 0, salesTariffEntryList.count < 1024 else {
                        throw ExiError.invalidEventCode("repeating element")
                    }
                    salesTariffEntryList.append(try decodeSalesTariffEntryType(r))
                }
                done57 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done57 = true
        }
    }
    return SalesTariffType(id: _id, salesTariffID: _salesTariffID, salesTariffDescription: _salesTariffDescription, numEPriceLevels: _numEPriceLevels, salesTariffEntry: salesTariffEntryList)
}

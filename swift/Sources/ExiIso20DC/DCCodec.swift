// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso20.dc

import ExiRuntime

public enum DCCodec {

    public static let exiHeader: UInt8 = 0x80

    public static func encode(_ msg: DC_ChargeParameterDiscoveryReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(15, 6)   // document element selector
        encodeDC_ChargeParameterDiscoveryReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: DC_ChargeParameterDiscoveryRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(16, 6)   // document element selector
        encodeDC_ChargeParameterDiscoveryRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: DC_CableCheckReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(11, 6)   // document element selector
        encodeDC_CableCheckReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: DC_CableCheckRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(12, 6)   // document element selector
        encodeDC_CableCheckRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: DC_PreChargeReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(17, 6)   // document element selector
        encodeDC_PreChargeReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: DC_PreChargeRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(18, 6)   // document element selector
        encodeDC_PreChargeRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: DC_ChargeLoopReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(13, 6)   // document element selector
        encodeDC_ChargeLoopReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: DC_ChargeLoopRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(14, 6)   // document element selector
        encodeDC_ChargeLoopRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: DC_WeldingDetectionReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(19, 6)   // document element selector
        encodeDC_WeldingDetectionReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: DC_WeldingDetectionRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(20, 6)   // document element selector
        encodeDC_WeldingDetectionRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: DC_CPDReqEnergyTransferMode) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(9, 6)   // document element selector
        encodeDC_CPDReqEnergyTransferMode(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: DC_CPDResEnergyTransferMode) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(10, 6)   // document element selector
        encodeDC_CPDResEnergyTransferMode(w, msg)
        w.alignToByte()
        return w.bytes
    }

    /// Encodes any document element of this message set, dispatching on its runtime type.
    public static func encodeAny(_ msg: Any) throws -> [UInt8] {
        switch msg {
        case let m as DC_ChargeLoopReq: return encode(m)
        case let m as DC_ChargeLoopRes: return encode(m)
        case let m as DC_ChargeParameterDiscoveryReq: return encode(m)
        case let m as DC_ChargeParameterDiscoveryRes: return encode(m)
        case let m as DC_CableCheckReq: return encode(m)
        case let m as DC_CableCheckRes: return encode(m)
        case let m as DC_PreChargeReq: return encode(m)
        case let m as DC_PreChargeRes: return encode(m)
        case let m as DC_WeldingDetectionReq: return encode(m)
        case let m as DC_WeldingDetectionRes: return encode(m)
        case let m as DC_CPDReqEnergyTransferMode: return encode(m)
        case let m as DC_CPDResEnergyTransferMode: return encode(m)
        default: throw ExiError.invalidHeader
        }
    }

    public static func decodeAny(_ src: [UInt8]) throws -> Any {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        let sel = try r.readBits(6)
        switch sel {
        case 9: return try decodeDC_CPDReqEnergyTransferMode(r)
        case 10: return try decodeDC_CPDResEnergyTransferMode(r)
        case 11: return try decodeDC_CableCheckReq(r)
        case 12: return try decodeDC_CableCheckRes(r)
        case 13: return try decodeDC_ChargeLoopReq(r)
        case 14: return try decodeDC_ChargeLoopRes(r)
        case 15: return try decodeDC_ChargeParameterDiscoveryReq(r)
        case 16: return try decodeDC_ChargeParameterDiscoveryRes(r)
        case 17: return try decodeDC_PreChargeReq(r)
        case 18: return try decodeDC_PreChargeRes(r)
        case 19: return try decodeDC_WeldingDetectionReq(r)
        case 20: return try decodeDC_WeldingDetectionRes(r)
        default: throw ExiError.unknownDocumentIndex(sel)
        }
    }

    public static func encodeFragment_DC_ChargeParameterDiscoveryRes(_ content: DC_ChargeParameterDiscoveryResType) -> [UInt8] {
        let w = BitWriter(capacity: 512)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(26, 8)   // fragment SE(DC_ChargeParameterDiscoveryRes)
        encodeDC_ChargeParameterDiscoveryResType(w, content)
        w.writeBits(150, 8)   // End Fragment (ED)
        w.alignToByte()
        return w.bytes
    }

    public static func decodeFragment_DC_ChargeParameterDiscoveryRes(_ src: [UInt8]) throws -> DC_ChargeParameterDiscoveryResType {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        guard try r.readBits(8) == 26 else {
            throw ExiError.invalidEventCode("not a DC_ChargeParameterDiscoveryRes fragment")
        }
        let result = try decodeDC_ChargeParameterDiscoveryResType(r)
        guard try r.readBits(8) == 150 else {
            throw ExiError.invalidEventCode("missing End Fragment")
        }
        return result
    }

    public static func encodeFragment_SignedInfo(_ content: SignedInfoType) -> [UInt8] {
        let w = BitWriter(capacity: 512)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(129, 8)   // fragment SE(SignedInfo)
        encodeSignedInfoType(w, content)
        w.writeBits(150, 8)   // End Fragment (ED)
        w.alignToByte()
        return w.bytes
    }

    public static func decodeFragment_SignedInfo(_ src: [UInt8]) throws -> SignedInfoType {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        guard try r.readBits(8) == 129 else {
            throw ExiError.invalidEventCode("not a SignedInfo fragment")
        }
        let result = try decodeSignedInfoType(r)
        guard try r.readBits(8) == 150 else {
            throw ExiError.invalidEventCode("missing End Fragment")
        }
        return result
    }
}

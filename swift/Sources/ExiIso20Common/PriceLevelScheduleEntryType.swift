// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class PriceLevelScheduleEntryType {
    public var duration: UInt32
    public var priceLevel: UInt8

    public init(duration: UInt32, priceLevel: UInt8) {
        self.duration = duration
        self.priceLevel = priceLevel
    }
}

internal func encodePriceLevelScheduleEntryType(_ w: BitWriter, _ msg: PriceLevelScheduleEntryType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, UInt64(msg.duration))
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.priceLevel), 8)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

internal func decodePriceLevelScheduleEntryType(_ r: BitReader) throws -> PriceLevelScheduleEntryType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _duration = UInt32(try ExiPrimitives.readUnsignedInteger(r))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _priceLevel = UInt8(try r.readBits(8))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // element EE
    return PriceLevelScheduleEntryType(duration: _duration, priceLevel: _priceLevel)
}

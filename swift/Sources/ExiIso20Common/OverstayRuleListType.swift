// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class OverstayRuleListType {
    public var overstayTimeThreshold: UInt32?
    public var overstayPowerThreshold: RationalNumberType?
    public var overstayRule: [OverstayRuleType]

    public init(overstayTimeThreshold: UInt32? = nil, overstayPowerThreshold: RationalNumberType? = nil, overstayRule: [OverstayRuleType]) {
        self.overstayTimeThreshold = overstayTimeThreshold
        self.overstayPowerThreshold = overstayPowerThreshold
        self.overstayRule = overstayRule
    }
}

internal func encodeOverstayRuleListType(_ w: BitWriter, _ msg: OverstayRuleListType) {
    var st58 = 0
    var done58 = false
    while !done58 {
        switch st58 {
        case 0:
            if let v = msg.overstayTimeThreshold {
                w.writeBits(0, 2)   // OverstayTimeThreshold
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st58 = 1
            } else if let v = msg.overstayPowerThreshold {
                w.writeBits(1, 2)   // OverstayPowerThreshold
                encodeRationalNumberType(w, v)
                st58 = 2
            } else {
                precondition((1...5).contains(msg.overstayRule.count), "list size out of schema range")
                w.writeBits(2, 2)   // OverstayRule
                encodeOverstayRuleType(w, msg.overstayRule[0])
                for ci in 1..<msg.overstayRule.count {
                    w.writeBits(0, 2)   // OverstayRule
                    encodeOverstayRuleType(w, msg.overstayRule[ci])
                }
                w.writeBits(1, 2)   // element EE (list end)
                done58 = true
            }
        case 1:
            if let v = msg.overstayPowerThreshold {
                w.writeBits(0, 2)   // OverstayPowerThreshold
                encodeRationalNumberType(w, v)
                st58 = 2
            } else {
                precondition((1...5).contains(msg.overstayRule.count), "list size out of schema range")
                w.writeBits(1, 2)   // OverstayRule
                encodeOverstayRuleType(w, msg.overstayRule[0])
                for ci in 1..<msg.overstayRule.count {
                    w.writeBits(0, 2)   // OverstayRule
                    encodeOverstayRuleType(w, msg.overstayRule[ci])
                }
                w.writeBits(1, 2)   // element EE (list end)
                done58 = true
            }
        case 2:
            precondition((1...5).contains(msg.overstayRule.count), "list size out of schema range")
            w.writeBits(0, 1)   // OverstayRule
            encodeOverstayRuleType(w, msg.overstayRule[0])
            for ci in 1..<msg.overstayRule.count {
                w.writeBits(0, 2)   // OverstayRule
                encodeOverstayRuleType(w, msg.overstayRule[ci])
            }
            w.writeBits(1, 2)   // element EE (list end)
            done58 = true
        default:
            done58 = true
        }
    }
}

internal func decodeOverstayRuleListType(_ r: BitReader) throws -> OverstayRuleListType {
    var _overstayTimeThreshold: UInt32? = nil
    var _overstayPowerThreshold: RationalNumberType? = nil
    var overstayRuleList = [OverstayRuleType]()
    var st59 = 0
    var done59 = false
    while !done59 {
        switch st59 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // OverstayTimeThreshold
                _ = try r.readBits(1)   // value-start
                _overstayTimeThreshold = UInt32(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st59 = 1
            case 1:   // OverstayPowerThreshold
                _overstayPowerThreshold = try decodeRationalNumberType(r)
                st59 = 2
            case 2:   // OverstayRule
                overstayRuleList.append(try decodeOverstayRuleType(r))
                while true {
                    let ec = try r.readBits(2)
                    if ec == 1 { break }   // element EE (list end)
                    guard ec == 0, overstayRuleList.count < 5 else {
                        throw ExiError.invalidEventCode("repeating element")
                    }
                    overstayRuleList.append(try decodeOverstayRuleType(r))
                }
                done59 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // OverstayPowerThreshold
                _overstayPowerThreshold = try decodeRationalNumberType(r)
                st59 = 2
            case 1:   // OverstayRule
                overstayRuleList.append(try decodeOverstayRuleType(r))
                while true {
                    let ec = try r.readBits(2)
                    if ec == 1 { break }   // element EE (list end)
                    guard ec == 0, overstayRuleList.count < 5 else {
                        throw ExiError.invalidEventCode("repeating element")
                    }
                    overstayRuleList.append(try decodeOverstayRuleType(r))
                }
                done59 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(1) {
            case 0:   // OverstayRule
                overstayRuleList.append(try decodeOverstayRuleType(r))
                while true {
                    let ec = try r.readBits(2)
                    if ec == 1 { break }   // element EE (list end)
                    guard ec == 0, overstayRuleList.count < 5 else {
                        throw ExiError.invalidEventCode("repeating element")
                    }
                    overstayRuleList.append(try decodeOverstayRuleType(r))
                }
                done59 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done59 = true
        }
    }
    return OverstayRuleListType(overstayTimeThreshold: _overstayTimeThreshold, overstayPowerThreshold: _overstayPowerThreshold, overstayRule: overstayRuleList)
}

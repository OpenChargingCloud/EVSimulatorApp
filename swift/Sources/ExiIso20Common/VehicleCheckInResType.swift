// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class VehicleCheckInResType: V2GResponseType {
    public var parkingSpace: Int16?
    public var deviceLocation: Int16?
    public var targetDistance: Int16?

    public init(header: MessageHeaderType, responseCode: ResponseCode, parkingSpace: Int16? = nil, deviceLocation: Int16? = nil, targetDistance: Int16? = nil) {
        self.parkingSpace = parkingSpace
        self.deviceLocation = deviceLocation
        self.targetDistance = targetDistance
        super.init(header: header, responseCode: responseCode)
    }
}

internal func encodeVehicleCheckInResType(_ w: BitWriter, _ msg: VehicleCheckInResType) {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.responseCode.rawValue), 6)
    w.writeBits(0, 1)   // child EE
    var st18 = 0
    var done18 = false
    while !done18 {
        switch st18 {
        case 0:
            if let v = msg.parkingSpace {
                w.writeBits(0, 3)   // ParkingSpace
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st18 = 1
            } else if let v = msg.deviceLocation {
                w.writeBits(1, 3)   // DeviceLocation
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st18 = 2
            } else if let v = msg.targetDistance {
                w.writeBits(2, 3)   // TargetDistance
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st18 = 3
            } else {
                w.writeBits(3, 3)   // element EE
                done18 = true
            }
        case 1:
            if let v = msg.deviceLocation {
                w.writeBits(0, 2)   // DeviceLocation
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st18 = 2
            } else if let v = msg.targetDistance {
                w.writeBits(1, 2)   // TargetDistance
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st18 = 3
            } else {
                w.writeBits(2, 2)   // element EE
                done18 = true
            }
        case 2:
            if let v = msg.targetDistance {
                w.writeBits(0, 2)   // TargetDistance
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st18 = 3
            } else {
                w.writeBits(1, 2)   // element EE
                done18 = true
            }
        case 3:
            w.writeBits(0, 1)   // element EE
            done18 = true
        default:
            done18 = true
        }
    }
}

internal func decodeVehicleCheckInResType(_ r: BitReader) throws -> VehicleCheckInResType {
    _ = try r.readBits(1)   // SE
    let _header = try decodeMessageHeaderType(r)
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _responseCode = try exiEnum(ResponseCode.self, try r.readBits(6))
    _ = try r.readBits(1)   // child EE
    var _parkingSpace: Int16? = nil
    var _deviceLocation: Int16? = nil
    var _targetDistance: Int16? = nil
    var st19 = 0
    var done19 = false
    while !done19 {
        switch st19 {
        case 0:
            switch try r.readBits(3) {
            case 0:   // ParkingSpace
                _ = try r.readBits(1)   // value-start
                _parkingSpace = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st19 = 1
            case 1:   // DeviceLocation
                _ = try r.readBits(1)   // value-start
                _deviceLocation = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st19 = 2
            case 2:   // TargetDistance
                _ = try r.readBits(1)   // value-start
                _targetDistance = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st19 = 3
            case 3:   // element EE
                done19 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // DeviceLocation
                _ = try r.readBits(1)   // value-start
                _deviceLocation = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st19 = 2
            case 1:   // TargetDistance
                _ = try r.readBits(1)   // value-start
                _targetDistance = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st19 = 3
            case 2:   // element EE
                done19 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(2) {
            case 0:   // TargetDistance
                _ = try r.readBits(1)   // value-start
                _targetDistance = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st19 = 3
            case 1:   // element EE
                done19 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 3:
            switch try r.readBits(1) {
            case 0:   // element EE
                done19 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done19 = true
        }
    }
    return VehicleCheckInResType(header: _header, responseCode: _responseCode, parkingSpace: _parkingSpace, deviceLocation: _deviceLocation, targetDistance: _targetDistance)
}

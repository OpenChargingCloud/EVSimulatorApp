// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class Dynamic_SEResControlModeType {
    public var departureTime: UInt32?
    public var minimumSOC: Int8?
    public var targetSOC: Int8?
    public var absolutePriceSchedule: AbsolutePriceScheduleType?
    public var priceLevelSchedule: PriceLevelScheduleType?

    public init(departureTime: UInt32? = nil, minimumSOC: Int8? = nil, targetSOC: Int8? = nil, absolutePriceSchedule: AbsolutePriceScheduleType? = nil, priceLevelSchedule: PriceLevelScheduleType? = nil) {
        self.departureTime = departureTime
        self.minimumSOC = minimumSOC
        self.targetSOC = targetSOC
        self.absolutePriceSchedule = absolutePriceSchedule
        self.priceLevelSchedule = priceLevelSchedule
    }
}

internal func encodeDynamic_SEResControlModeType(_ w: BitWriter, _ msg: Dynamic_SEResControlModeType) {
    var st32 = 0
    var done32 = false
    while !done32 {
        switch st32 {
        case 0:
            if let v = msg.departureTime {
                w.writeBits(0, 3)   // DepartureTime
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st32 = 1
            } else if let v = msg.minimumSOC {
                w.writeBits(1, 3)   // MinimumSOC
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 7)
                w.writeBits(0, 1)   // child EE
                st32 = 2
            } else if let v = msg.targetSOC {
                w.writeBits(2, 3)   // TargetSOC
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 7)
                w.writeBits(0, 1)   // child EE
                st32 = 3
            } else if let v = msg.absolutePriceSchedule {
                w.writeBits(3, 3)   // AbsolutePriceSchedule
                encodeAbsolutePriceScheduleType(w, v)
                st32 = 4
            } else if let v = msg.priceLevelSchedule {
                w.writeBits(4, 3)   // PriceLevelSchedule
                encodePriceLevelScheduleType(w, v)
                st32 = 4
            } else {
                w.writeBits(5, 3)   // element EE
                done32 = true
            }
        case 1:
            if let v = msg.minimumSOC {
                w.writeBits(0, 3)   // MinimumSOC
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 7)
                w.writeBits(0, 1)   // child EE
                st32 = 2
            } else if let v = msg.targetSOC {
                w.writeBits(1, 3)   // TargetSOC
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 7)
                w.writeBits(0, 1)   // child EE
                st32 = 3
            } else if let v = msg.absolutePriceSchedule {
                w.writeBits(2, 3)   // AbsolutePriceSchedule
                encodeAbsolutePriceScheduleType(w, v)
                st32 = 4
            } else if let v = msg.priceLevelSchedule {
                w.writeBits(3, 3)   // PriceLevelSchedule
                encodePriceLevelScheduleType(w, v)
                st32 = 4
            } else {
                w.writeBits(4, 3)   // element EE
                done32 = true
            }
        case 2:
            if let v = msg.targetSOC {
                w.writeBits(0, 3)   // TargetSOC
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 7)
                w.writeBits(0, 1)   // child EE
                st32 = 3
            } else if let v = msg.absolutePriceSchedule {
                w.writeBits(1, 3)   // AbsolutePriceSchedule
                encodeAbsolutePriceScheduleType(w, v)
                st32 = 4
            } else if let v = msg.priceLevelSchedule {
                w.writeBits(2, 3)   // PriceLevelSchedule
                encodePriceLevelScheduleType(w, v)
                st32 = 4
            } else {
                w.writeBits(3, 3)   // element EE
                done32 = true
            }
        case 3:
            if let v = msg.absolutePriceSchedule {
                w.writeBits(0, 2)   // AbsolutePriceSchedule
                encodeAbsolutePriceScheduleType(w, v)
                st32 = 4
            } else if let v = msg.priceLevelSchedule {
                w.writeBits(1, 2)   // PriceLevelSchedule
                encodePriceLevelScheduleType(w, v)
                st32 = 4
            } else {
                w.writeBits(2, 2)   // element EE
                done32 = true
            }
        case 4:
            w.writeBits(0, 1)   // element EE
            done32 = true
        default:
            done32 = true
        }
    }
}

internal func decodeDynamic_SEResControlModeType(_ r: BitReader) throws -> Dynamic_SEResControlModeType {
    var _departureTime: UInt32? = nil
    var _minimumSOC: Int8? = nil
    var _targetSOC: Int8? = nil
    var _absolutePriceSchedule: AbsolutePriceScheduleType? = nil
    var _priceLevelSchedule: PriceLevelScheduleType? = nil
    var st33 = 0
    var done33 = false
    while !done33 {
        switch st33 {
        case 0:
            switch try r.readBits(3) {
            case 0:   // DepartureTime
                _ = try r.readBits(1)   // value-start
                _departureTime = UInt32(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st33 = 1
            case 1:   // MinimumSOC
                _ = try r.readBits(1)   // value-start
                _minimumSOC = Int8(try r.readBits(7))
                _ = try r.readBits(1)   // child EE
                st33 = 2
            case 2:   // TargetSOC
                _ = try r.readBits(1)   // value-start
                _targetSOC = Int8(try r.readBits(7))
                _ = try r.readBits(1)   // child EE
                st33 = 3
            case 3:   // AbsolutePriceSchedule
                _absolutePriceSchedule = try decodeAbsolutePriceScheduleType(r)
                st33 = 4
            case 4:   // PriceLevelSchedule
                _priceLevelSchedule = try decodePriceLevelScheduleType(r)
                st33 = 4
            case 5:   // element EE
                done33 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(3) {
            case 0:   // MinimumSOC
                _ = try r.readBits(1)   // value-start
                _minimumSOC = Int8(try r.readBits(7))
                _ = try r.readBits(1)   // child EE
                st33 = 2
            case 1:   // TargetSOC
                _ = try r.readBits(1)   // value-start
                _targetSOC = Int8(try r.readBits(7))
                _ = try r.readBits(1)   // child EE
                st33 = 3
            case 2:   // AbsolutePriceSchedule
                _absolutePriceSchedule = try decodeAbsolutePriceScheduleType(r)
                st33 = 4
            case 3:   // PriceLevelSchedule
                _priceLevelSchedule = try decodePriceLevelScheduleType(r)
                st33 = 4
            case 4:   // element EE
                done33 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(3) {
            case 0:   // TargetSOC
                _ = try r.readBits(1)   // value-start
                _targetSOC = Int8(try r.readBits(7))
                _ = try r.readBits(1)   // child EE
                st33 = 3
            case 1:   // AbsolutePriceSchedule
                _absolutePriceSchedule = try decodeAbsolutePriceScheduleType(r)
                st33 = 4
            case 2:   // PriceLevelSchedule
                _priceLevelSchedule = try decodePriceLevelScheduleType(r)
                st33 = 4
            case 3:   // element EE
                done33 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 3:
            switch try r.readBits(2) {
            case 0:   // AbsolutePriceSchedule
                _absolutePriceSchedule = try decodeAbsolutePriceScheduleType(r)
                st33 = 4
            case 1:   // PriceLevelSchedule
                _priceLevelSchedule = try decodePriceLevelScheduleType(r)
                st33 = 4
            case 2:   // element EE
                done33 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 4:
            switch try r.readBits(1) {
            case 0:   // element EE
                done33 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done33 = true
        }
    }
    return Dynamic_SEResControlModeType(departureTime: _departureTime, minimumSOC: _minimumSOC, targetSOC: _targetSOC, absolutePriceSchedule: _absolutePriceSchedule, priceLevelSchedule: _priceLevelSchedule)
}

// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class PriceLevelScheduleType: PriceScheduleType {
    public var id: String?
    public var numberOfPriceLevels: UInt8
    public var priceLevelScheduleEntries: PriceLevelScheduleEntryListType

    public init(id: String? = nil, timeAnchor: UInt64, priceScheduleID: UInt32, priceScheduleDescription: String? = nil, numberOfPriceLevels: UInt8, priceLevelScheduleEntries: PriceLevelScheduleEntryListType) {
        self.id = id
        self.numberOfPriceLevels = numberOfPriceLevels
        self.priceLevelScheduleEntries = priceLevelScheduleEntries
        super.init(timeAnchor: timeAnchor, priceScheduleID: priceScheduleID, priceScheduleDescription: priceScheduleDescription)
    }
}

internal func encodePriceLevelScheduleType(_ w: BitWriter, _ msg: PriceLevelScheduleType) {
    var st40 = 0
    var done40 = false
    while !done40 {
        switch st40 {
        case 0:
            if let v = msg.id {
                w.writeBits(0, 2)   // Id
                ExiPrimitives.writeStringValue(w, v)
                st40 = 1
            } else {
                w.writeBits(1, 2)   // SE(TimeAnchor)
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(msg.timeAnchor))
                w.writeBits(0, 1)   // child EE
                done40 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(TimeAnchor)
            w.writeBits(0, 1)   // value-start
            ExiPrimitives.writeUnsignedInteger(w, UInt64(msg.timeAnchor))
            w.writeBits(0, 1)   // child EE
            done40 = true
        default:
            done40 = true
        }
    }
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, UInt64(msg.priceScheduleID))
    w.writeBits(0, 1)   // child EE
    var st41 = 0
    var done41 = false
    while !done41 {
        switch st41 {
        case 0:
            if let v = msg.priceScheduleDescription {
                w.writeBits(0, 2)   // PriceScheduleDescription
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeStringValue(w, v)
                w.writeBits(0, 1)   // child EE
                st41 = 1
            } else {
                w.writeBits(1, 2)   // SE(NumberOfPriceLevels)
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(msg.numberOfPriceLevels), 8)
                w.writeBits(0, 1)   // child EE
                done41 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(NumberOfPriceLevels)
            w.writeBits(0, 1)   // value-start
            w.writeBits(UInt32(msg.numberOfPriceLevels), 8)
            w.writeBits(0, 1)   // child EE
            done41 = true
        default:
            done41 = true
        }
    }
    w.writeBits(0, 1)   // SE
    encodePriceLevelScheduleEntryListType(w, msg.priceLevelScheduleEntries)
    w.writeBits(0, 1)   // element EE
}

internal func decodePriceLevelScheduleType(_ r: BitReader) throws -> PriceLevelScheduleType {
    var _id: String? = nil
    var _timeAnchor: UInt64? = nil
    var st42 = 0
    var done42 = false
    while !done42 {
        switch st42 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // Id
                _id = try ExiPrimitives.readStringValue(r, slot: "Id")
                st42 = 1
            case 1:   // SE(TimeAnchor)
                _ = try r.readBits(1)   // value-start
                _timeAnchor = UInt64(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                done42 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(TimeAnchor)
                _ = try r.readBits(1)   // value-start
                _timeAnchor = UInt64(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                done42 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done42 = true
        }
    }
    guard let _timeAnchor else {
        throw ExiError.invalidEventCode("TimeAnchor is required but the element ended")
    }
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _priceScheduleID = UInt32(try ExiPrimitives.readUnsignedInteger(r))
    _ = try r.readBits(1)   // child EE
    var _priceScheduleDescription: String? = nil
    var _numberOfPriceLevels: UInt8? = nil
    var st43 = 0
    var done43 = false
    while !done43 {
        switch st43 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // PriceScheduleDescription
                _ = try r.readBits(1)   // value-start
                _priceScheduleDescription = try ExiPrimitives.readStringValue(r, slot: "PriceScheduleDescription")
                _ = try r.readBits(1)   // child EE
                st43 = 1
            case 1:   // SE(NumberOfPriceLevels)
                _ = try r.readBits(1)   // value-start
                _numberOfPriceLevels = UInt8(try r.readBits(8))
                _ = try r.readBits(1)   // child EE
                done43 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(NumberOfPriceLevels)
                _ = try r.readBits(1)   // value-start
                _numberOfPriceLevels = UInt8(try r.readBits(8))
                _ = try r.readBits(1)   // child EE
                done43 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done43 = true
        }
    }
    guard let _numberOfPriceLevels else {
        throw ExiError.invalidEventCode("NumberOfPriceLevels is required but the element ended")
    }
    _ = try r.readBits(1)   // SE
    let _priceLevelScheduleEntries = try decodePriceLevelScheduleEntryListType(r)
    _ = try r.readBits(1)   // element EE
    return PriceLevelScheduleType(id: _id, timeAnchor: _timeAnchor, priceScheduleID: _priceScheduleID, priceScheduleDescription: _priceScheduleDescription, numberOfPriceLevels: _numberOfPriceLevels, priceLevelScheduleEntries: _priceLevelScheduleEntries)
}

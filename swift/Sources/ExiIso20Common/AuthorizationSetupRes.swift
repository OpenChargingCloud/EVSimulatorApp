// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class AuthorizationSetupRes: V2GResponseType {
    public var authorizationServices: [Authorization]
    public var certificateInstallationService: Bool
    public var eIM_ASResAuthorizationMode: EIM_ASResAuthorizationModeType?
    public var pnC_ASResAuthorizationMode: PnC_ASResAuthorizationModeType?

    public init(header: MessageHeaderType, responseCode: ResponseCode, authorizationServices: [Authorization], certificateInstallationService: Bool, eIM_ASResAuthorizationMode: EIM_ASResAuthorizationModeType? = nil, pnC_ASResAuthorizationMode: PnC_ASResAuthorizationModeType? = nil) {
        self.authorizationServices = authorizationServices
        self.certificateInstallationService = certificateInstallationService
        self.eIM_ASResAuthorizationMode = eIM_ASResAuthorizationMode
        self.pnC_ASResAuthorizationMode = pnC_ASResAuthorizationMode
        super.init(header: header, responseCode: responseCode)
    }
}

internal func encodeAuthorizationSetupRes(_ w: BitWriter, _ msg: AuthorizationSetupRes) {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.responseCode.rawValue), 6)
    w.writeBits(0, 1)   // child EE
    precondition((1...2).contains(msg.authorizationServices.count), "list size out of schema range")
    w.writeBits(0, 1)   // SE(AuthorizationServices)
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.authorizationServices[0].rawValue), 1)
    w.writeBits(0, 1)   // child EE
    if msg.authorizationServices.count > 1 {
        w.writeBits(0, 2)   // AuthorizationServices (loop)
        w.writeBits(0, 1)   // value-start
        w.writeBits(UInt32(msg.authorizationServices[1].rawValue), 1)
        w.writeBits(0, 1)   // child EE
        w.writeBits(0, 1)   // CertificateInstallationService
        w.writeBits(0, 1)   // value-start
        w.writeBits(msg.certificateInstallationService ? 1 : 0, 1)
        w.writeBits(0, 1)   // child EE
    } else {
        w.writeBits(1, 2)   // CertificateInstallationService
        w.writeBits(0, 1)   // value-start
        w.writeBits(msg.certificateInstallationService ? 1 : 0, 1)
        w.writeBits(0, 1)   // child EE
    }
    if let v = msg.eIM_ASResAuthorizationMode {
        w.writeBits(0, 2)   // EIM_ASResAuthorizationMode
        encodeEIM_ASResAuthorizationModeType(w, v)
    } else if let v = msg.pnC_ASResAuthorizationMode {
        w.writeBits(1, 2)   // PnC_ASResAuthorizationMode
        encodePnC_ASResAuthorizationModeType(w, v)
    } else {
        preconditionFailure("no choice alternative set")
    }
    w.writeBits(0, 1)   // element EE
}

internal func decodeAuthorizationSetupRes(_ r: BitReader) throws -> AuthorizationSetupRes {
    _ = try r.readBits(1)   // SE
    let _header = try decodeMessageHeaderType(r)
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _responseCode = try exiEnum(ResponseCode.self, try r.readBits(6))
    _ = try r.readBits(1)   // child EE
    var authorizationServicesList = [Authorization]()
    _ = try r.readBits(1)   // SE(list)
    _ = try r.readBits(1)   // value-start
    let item = try exiEnum(Authorization.self, try r.readBits(1))
    _ = try r.readBits(1)   // child EE
    authorizationServicesList.append(item)
    var _certificateInstallationService: Bool? = nil
    switch try r.readBits(2) {
    case 0:   // another item
        _ = try r.readBits(1)   // value-start
        let item = try exiEnum(Authorization.self, try r.readBits(1))
        _ = try r.readBits(1)   // child EE
        authorizationServicesList.append(item)
        switch try r.readBits(1) {
        case 0:   // CertificateInstallationService
            _ = try r.readBits(1)   // value-start
            _certificateInstallationService = try r.readBits(1) != 0
            _ = try r.readBits(1)   // child EE
        default:
            throw ExiError.invalidEventCode("list tail")
        }
    case 1:   // CertificateInstallationService
        _ = try r.readBits(1)   // value-start
        _certificateInstallationService = try r.readBits(1) != 0
        _ = try r.readBits(1)   // child EE
    default:
        throw ExiError.invalidEventCode("repeating element with tail")
    }
    guard let _certificateInstallationService else {
        throw ExiError.invalidEventCode("CertificateInstallationService is required but the list ended without it")
    }
    var _eIM_ASResAuthorizationMode: EIM_ASResAuthorizationModeType? = nil
    var _pnC_ASResAuthorizationMode: PnC_ASResAuthorizationModeType? = nil
    switch try r.readBits(2) {
    case 0:   // EIM_ASResAuthorizationMode
        _eIM_ASResAuthorizationMode = try decodeEIM_ASResAuthorizationModeType(r)
    case 1:   // PnC_ASResAuthorizationMode
        _pnC_ASResAuthorizationMode = try decodePnC_ASResAuthorizationModeType(r)
    default:
        throw ExiError.invalidEventCode("inline choice")
    }
    _ = try r.readBits(1)   // element EE
    return AuthorizationSetupRes(header: _header, responseCode: _responseCode, authorizationServices: authorizationServicesList, certificateInstallationService: _certificateInstallationService, eIM_ASResAuthorizationMode: _eIM_ASResAuthorizationMode, pnC_ASResAuthorizationMode: _pnC_ASResAuthorizationMode)
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class PriceRuleStackListType {
    public var priceRuleStack: [PriceRuleStackType]

    public init(priceRuleStack: [PriceRuleStackType]) {
        self.priceRuleStack = priceRuleStack
    }
}

internal func encodePriceRuleStackListType(_ w: BitWriter, _ msg: PriceRuleStackListType) {
    let list = msg.priceRuleStack
    precondition((1...1024).contains(list.count), "list size out of schema range")
    for (i, item) in list.enumerated() {
        w.writeBits(0, i == 0 ? 1 : 2)   // SE(item)
        encodePriceRuleStackType(w, item)
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

internal func decodePriceRuleStackListType(_ r: BitReader) throws -> PriceRuleStackListType {
    var list = [PriceRuleStackType]()
    _ = try r.readBits(1)   // SE(item) first
    list.append(try decodePriceRuleStackType(r))
    while true {
        let ec = try r.readBits(2)
        if ec == 1 { break }   // element EE
        guard ec == 0, list.count < 1024 else {
            throw ExiError.invalidEventCode("repeating element")
        }
        list.append(try decodePriceRuleStackType(r))
    }
    return PriceRuleStackListType(priceRuleStack: list)
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class VehicleCheckInReq: V2GRequestType {
    public var eVCheckInStatus: EvCheckInStatus
    public var parkingMethod: ParkingMethod
    public var vehicleFrame: Int16?
    public var deviceOffset: Int16?
    public var vehicleTravel: Int16?

    public init(header: MessageHeaderType, eVCheckInStatus: EvCheckInStatus, parkingMethod: ParkingMethod, vehicleFrame: Int16? = nil, deviceOffset: Int16? = nil, vehicleTravel: Int16? = nil) {
        self.eVCheckInStatus = eVCheckInStatus
        self.parkingMethod = parkingMethod
        self.vehicleFrame = vehicleFrame
        self.deviceOffset = deviceOffset
        self.vehicleTravel = vehicleTravel
        super.init(header: header)
    }
}

internal func encodeVehicleCheckInReq(_ w: BitWriter, _ msg: VehicleCheckInReq) {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.eVCheckInStatus.rawValue), 2)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.parkingMethod.rawValue), 2)
    w.writeBits(0, 1)   // child EE
    var st110 = 0
    var done110 = false
    while !done110 {
        switch st110 {
        case 0:
            if let v = msg.vehicleFrame {
                w.writeBits(0, 3)   // VehicleFrame
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st110 = 1
            } else if let v = msg.deviceOffset {
                w.writeBits(1, 3)   // DeviceOffset
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st110 = 2
            } else if let v = msg.vehicleTravel {
                w.writeBits(2, 3)   // VehicleTravel
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st110 = 3
            } else {
                w.writeBits(3, 3)   // element EE
                done110 = true
            }
        case 1:
            if let v = msg.deviceOffset {
                w.writeBits(0, 2)   // DeviceOffset
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st110 = 2
            } else if let v = msg.vehicleTravel {
                w.writeBits(1, 2)   // VehicleTravel
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st110 = 3
            } else {
                w.writeBits(2, 2)   // element EE
                done110 = true
            }
        case 2:
            if let v = msg.vehicleTravel {
                w.writeBits(0, 2)   // VehicleTravel
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeSignedInteger(w, Int64(v))
                w.writeBits(0, 1)   // child EE
                st110 = 3
            } else {
                w.writeBits(1, 2)   // element EE
                done110 = true
            }
        case 3:
            w.writeBits(0, 1)   // element EE
            done110 = true
        default:
            done110 = true
        }
    }
}

internal func decodeVehicleCheckInReq(_ r: BitReader) throws -> VehicleCheckInReq {
    _ = try r.readBits(1)   // SE
    let _header = try decodeMessageHeaderType(r)
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _eVCheckInStatus = try exiEnum(EvCheckInStatus.self, try r.readBits(2))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _parkingMethod = try exiEnum(ParkingMethod.self, try r.readBits(2))
    _ = try r.readBits(1)   // child EE
    var _vehicleFrame: Int16? = nil
    var _deviceOffset: Int16? = nil
    var _vehicleTravel: Int16? = nil
    var st111 = 0
    var done111 = false
    while !done111 {
        switch st111 {
        case 0:
            switch try r.readBits(3) {
            case 0:   // VehicleFrame
                _ = try r.readBits(1)   // value-start
                _vehicleFrame = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st111 = 1
            case 1:   // DeviceOffset
                _ = try r.readBits(1)   // value-start
                _deviceOffset = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st111 = 2
            case 2:   // VehicleTravel
                _ = try r.readBits(1)   // value-start
                _vehicleTravel = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st111 = 3
            case 3:   // element EE
                done111 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // DeviceOffset
                _ = try r.readBits(1)   // value-start
                _deviceOffset = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st111 = 2
            case 1:   // VehicleTravel
                _ = try r.readBits(1)   // value-start
                _vehicleTravel = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st111 = 3
            case 2:   // element EE
                done111 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(2) {
            case 0:   // VehicleTravel
                _ = try r.readBits(1)   // value-start
                _vehicleTravel = Int16(try ExiPrimitives.readSignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st111 = 3
            case 1:   // element EE
                done111 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 3:
            switch try r.readBits(1) {
            case 0:   // element EE
                done111 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done111 = true
        }
    }
    return VehicleCheckInReq(header: _header, eVCheckInStatus: _eVCheckInStatus, parkingMethod: _parkingMethod, vehicleFrame: _vehicleFrame, deviceOffset: _deviceOffset, vehicleTravel: _vehicleTravel)
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class PriceRuleType {
    public var energyFee: RationalNumberType
    public var parkingFee: RationalNumberType?
    public var parkingFeePeriod: UInt32?
    public var carbonDioxideEmission: UInt16?
    public var renewableGenerationPercentage: UInt8?
    public var powerRangeStart: RationalNumberType

    public init(energyFee: RationalNumberType, parkingFee: RationalNumberType? = nil, parkingFeePeriod: UInt32? = nil, carbonDioxideEmission: UInt16? = nil, renewableGenerationPercentage: UInt8? = nil, powerRangeStart: RationalNumberType) {
        self.energyFee = energyFee
        self.parkingFee = parkingFee
        self.parkingFeePeriod = parkingFeePeriod
        self.carbonDioxideEmission = carbonDioxideEmission
        self.renewableGenerationPercentage = renewableGenerationPercentage
        self.powerRangeStart = powerRangeStart
    }
}

internal func encodePriceRuleType(_ w: BitWriter, _ msg: PriceRuleType) {
    w.writeBits(0, 1)   // SE
    encodeRationalNumberType(w, msg.energyFee)
    var st56 = 0
    var done56 = false
    while !done56 {
        switch st56 {
        case 0:
            if let v = msg.parkingFee {
                w.writeBits(0, 3)   // ParkingFee
                encodeRationalNumberType(w, v)
                st56 = 1
            } else if let v = msg.parkingFeePeriod {
                w.writeBits(1, 3)   // ParkingFeePeriod
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st56 = 2
            } else if let v = msg.carbonDioxideEmission {
                w.writeBits(2, 3)   // CarbonDioxideEmission
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st56 = 3
            } else if let v = msg.renewableGenerationPercentage {
                w.writeBits(3, 3)   // RenewableGenerationPercentage
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 8)
                w.writeBits(0, 1)   // child EE
                st56 = 4
            } else {
                w.writeBits(4, 3)   // SE(PowerRangeStart)
                encodeRationalNumberType(w, msg.powerRangeStart)
                done56 = true
            }
        case 1:
            if let v = msg.parkingFeePeriod {
                w.writeBits(0, 3)   // ParkingFeePeriod
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st56 = 2
            } else if let v = msg.carbonDioxideEmission {
                w.writeBits(1, 3)   // CarbonDioxideEmission
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st56 = 3
            } else if let v = msg.renewableGenerationPercentage {
                w.writeBits(2, 3)   // RenewableGenerationPercentage
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 8)
                w.writeBits(0, 1)   // child EE
                st56 = 4
            } else {
                w.writeBits(3, 3)   // SE(PowerRangeStart)
                encodeRationalNumberType(w, msg.powerRangeStart)
                done56 = true
            }
        case 2:
            if let v = msg.carbonDioxideEmission {
                w.writeBits(0, 2)   // CarbonDioxideEmission
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st56 = 3
            } else if let v = msg.renewableGenerationPercentage {
                w.writeBits(1, 2)   // RenewableGenerationPercentage
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 8)
                w.writeBits(0, 1)   // child EE
                st56 = 4
            } else {
                w.writeBits(2, 2)   // SE(PowerRangeStart)
                encodeRationalNumberType(w, msg.powerRangeStart)
                done56 = true
            }
        case 3:
            if let v = msg.renewableGenerationPercentage {
                w.writeBits(0, 2)   // RenewableGenerationPercentage
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 8)
                w.writeBits(0, 1)   // child EE
                st56 = 4
            } else {
                w.writeBits(1, 2)   // SE(PowerRangeStart)
                encodeRationalNumberType(w, msg.powerRangeStart)
                done56 = true
            }
        case 4:
            w.writeBits(0, 1)   // SE(PowerRangeStart)
            encodeRationalNumberType(w, msg.powerRangeStart)
            done56 = true
        default:
            done56 = true
        }
    }
    w.writeBits(0, 1)   // element EE
}

internal func decodePriceRuleType(_ r: BitReader) throws -> PriceRuleType {
    _ = try r.readBits(1)   // SE
    let _energyFee = try decodeRationalNumberType(r)
    var _parkingFee: RationalNumberType? = nil
    var _parkingFeePeriod: UInt32? = nil
    var _carbonDioxideEmission: UInt16? = nil
    var _renewableGenerationPercentage: UInt8? = nil
    var _powerRangeStart: RationalNumberType? = nil
    var st57 = 0
    var done57 = false
    while !done57 {
        switch st57 {
        case 0:
            switch try r.readBits(3) {
            case 0:   // ParkingFee
                _parkingFee = try decodeRationalNumberType(r)
                st57 = 1
            case 1:   // ParkingFeePeriod
                _ = try r.readBits(1)   // value-start
                _parkingFeePeriod = UInt32(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st57 = 2
            case 2:   // CarbonDioxideEmission
                _ = try r.readBits(1)   // value-start
                _carbonDioxideEmission = UInt16(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st57 = 3
            case 3:   // RenewableGenerationPercentage
                _ = try r.readBits(1)   // value-start
                _renewableGenerationPercentage = UInt8(try r.readBits(8))
                _ = try r.readBits(1)   // child EE
                st57 = 4
            case 4:   // SE(PowerRangeStart)
                _powerRangeStart = try decodeRationalNumberType(r)
                done57 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(3) {
            case 0:   // ParkingFeePeriod
                _ = try r.readBits(1)   // value-start
                _parkingFeePeriod = UInt32(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st57 = 2
            case 1:   // CarbonDioxideEmission
                _ = try r.readBits(1)   // value-start
                _carbonDioxideEmission = UInt16(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st57 = 3
            case 2:   // RenewableGenerationPercentage
                _ = try r.readBits(1)   // value-start
                _renewableGenerationPercentage = UInt8(try r.readBits(8))
                _ = try r.readBits(1)   // child EE
                st57 = 4
            case 3:   // SE(PowerRangeStart)
                _powerRangeStart = try decodeRationalNumberType(r)
                done57 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(2) {
            case 0:   // CarbonDioxideEmission
                _ = try r.readBits(1)   // value-start
                _carbonDioxideEmission = UInt16(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st57 = 3
            case 1:   // RenewableGenerationPercentage
                _ = try r.readBits(1)   // value-start
                _renewableGenerationPercentage = UInt8(try r.readBits(8))
                _ = try r.readBits(1)   // child EE
                st57 = 4
            case 2:   // SE(PowerRangeStart)
                _powerRangeStart = try decodeRationalNumberType(r)
                done57 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 3:
            switch try r.readBits(2) {
            case 0:   // RenewableGenerationPercentage
                _ = try r.readBits(1)   // value-start
                _renewableGenerationPercentage = UInt8(try r.readBits(8))
                _ = try r.readBits(1)   // child EE
                st57 = 4
            case 1:   // SE(PowerRangeStart)
                _powerRangeStart = try decodeRationalNumberType(r)
                done57 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 4:
            switch try r.readBits(1) {
            case 0:   // SE(PowerRangeStart)
                _powerRangeStart = try decodeRationalNumberType(r)
                done57 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done57 = true
        }
    }
    guard let _powerRangeStart else {
        throw ExiError.invalidEventCode("PowerRangeStart is required but the element ended")
    }
    _ = try r.readBits(1)   // element EE
    return PriceRuleType(energyFee: _energyFee, parkingFee: _parkingFee, parkingFeePeriod: _parkingFeePeriod, carbonDioxideEmission: _carbonDioxideEmission, renewableGenerationPercentage: _renewableGenerationPercentage, powerRangeStart: _powerRangeStart)
}

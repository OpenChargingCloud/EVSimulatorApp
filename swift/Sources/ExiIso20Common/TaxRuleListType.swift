// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class TaxRuleListType {
    public var taxRule: [TaxRuleType]

    public init(taxRule: [TaxRuleType]) {
        self.taxRule = taxRule
    }
}

internal func encodeTaxRuleListType(_ w: BitWriter, _ msg: TaxRuleListType) {
    let list = msg.taxRule
    precondition((1...10).contains(list.count), "list size out of schema range")
    for (i, item) in list.enumerated() {
        w.writeBits(0, i == 0 ? 1 : 2)   // SE(item)
        encodeTaxRuleType(w, item)
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

internal func decodeTaxRuleListType(_ r: BitReader) throws -> TaxRuleListType {
    var list = [TaxRuleType]()
    _ = try r.readBits(1)   // SE(item) first
    list.append(try decodeTaxRuleType(r))
    while true {
        let ec = try r.readBits(2)
        if ec == 1 { break }   // element EE
        guard ec == 0, list.count < 10 else {
            throw ExiError.invalidEventCode("repeating element")
        }
        list.append(try decodeTaxRuleType(r))
    }
    return TaxRuleListType(taxRule: list)
}

// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class CertificateInstallationResType: V2GResponseType {
    public var eVSEProcessing: Processing
    public var cPSCertificateChain: CertificateChainType
    public var signedInstallationData: SignedInstallationDataType
    public var remainingContractCertificateChains: UInt8

    public init(header: MessageHeaderType, responseCode: ResponseCode, eVSEProcessing: Processing, cPSCertificateChain: CertificateChainType, signedInstallationData: SignedInstallationDataType, remainingContractCertificateChains: UInt8) {
        self.eVSEProcessing = eVSEProcessing
        self.cPSCertificateChain = cPSCertificateChain
        self.signedInstallationData = signedInstallationData
        self.remainingContractCertificateChains = remainingContractCertificateChains
        super.init(header: header, responseCode: responseCode)
    }
}

internal func encodeCertificateInstallationResType(_ w: BitWriter, _ msg: CertificateInstallationResType) {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.responseCode.rawValue), 6)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.eVSEProcessing.rawValue), 2)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    encodeCertificateChainType(w, msg.cPSCertificateChain)
    w.writeBits(0, 1)   // SE
    encodeSignedInstallationDataType(w, msg.signedInstallationData)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.remainingContractCertificateChains), 8)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

internal func decodeCertificateInstallationResType(_ r: BitReader) throws -> CertificateInstallationResType {
    _ = try r.readBits(1)   // SE
    let _header = try decodeMessageHeaderType(r)
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _responseCode = try exiEnum(ResponseCode.self, try r.readBits(6))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _eVSEProcessing = try exiEnum(Processing.self, try r.readBits(2))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    let _cPSCertificateChain = try decodeCertificateChainType(r)
    _ = try r.readBits(1)   // SE
    let _signedInstallationData = try decodeSignedInstallationDataType(r)
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _remainingContractCertificateChains = UInt8(try r.readBits(8))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // element EE
    return CertificateInstallationResType(header: _header, responseCode: _responseCode, eVSEProcessing: _eVSEProcessing, cPSCertificateChain: _cPSCertificateChain, signedInstallationData: _signedInstallationData, remainingContractCertificateChains: _remainingContractCertificateChains)
}

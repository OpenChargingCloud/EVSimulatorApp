// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class SelectedServiceListType {
    public var selectedService: [SelectedServiceType]

    public init(selectedService: [SelectedServiceType]) {
        self.selectedService = selectedService
    }
}

internal func encodeSelectedServiceListType(_ w: BitWriter, _ msg: SelectedServiceListType) {
    let list = msg.selectedService
    precondition((1...16).contains(list.count), "list size out of schema range")
    for (i, item) in list.enumerated() {
        w.writeBits(0, i == 0 ? 1 : 2)   // SE(item)
        encodeSelectedServiceType(w, item)
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

internal func decodeSelectedServiceListType(_ r: BitReader) throws -> SelectedServiceListType {
    var list = [SelectedServiceType]()
    _ = try r.readBits(1)   // SE(item) first
    list.append(try decodeSelectedServiceType(r))
    while true {
        let ec = try r.readBits(2)
        if ec == 1 { break }   // element EE
        guard ec == 0, list.count < 16 else {
            throw ExiError.invalidEventCode("repeating element")
        }
        list.append(try decodeSelectedServiceType(r))
    }
    return SelectedServiceListType(selectedService: list)
}

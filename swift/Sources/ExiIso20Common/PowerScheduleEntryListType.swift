// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class PowerScheduleEntryListType {
    public var powerScheduleEntry: [PowerScheduleEntryType]

    public init(powerScheduleEntry: [PowerScheduleEntryType]) {
        self.powerScheduleEntry = powerScheduleEntry
    }
}

internal func encodePowerScheduleEntryListType(_ w: BitWriter, _ msg: PowerScheduleEntryListType) {
    let list = msg.powerScheduleEntry
    precondition((1...1024).contains(list.count), "list size out of schema range")
    for (i, item) in list.enumerated() {
        w.writeBits(0, i == 0 ? 1 : 2)   // SE(item)
        encodePowerScheduleEntryType(w, item)
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

internal func decodePowerScheduleEntryListType(_ r: BitReader) throws -> PowerScheduleEntryListType {
    var list = [PowerScheduleEntryType]()
    _ = try r.readBits(1)   // SE(item) first
    list.append(try decodePowerScheduleEntryType(r))
    while true {
        let ec = try r.readBits(2)
        if ec == 1 { break }   // element EE
        guard ec == 0, list.count < 1024 else {
            throw ExiError.invalidEventCode("repeating element")
        }
        list.append(try decodePowerScheduleEntryType(r))
    }
    return PowerScheduleEntryListType(powerScheduleEntry: list)
}

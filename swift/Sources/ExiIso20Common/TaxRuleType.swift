// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class TaxRuleType {
    public var taxRuleID: UInt32
    public var taxRuleName: String?
    public var taxRate: RationalNumberType
    public var taxIncludedInPrice: Bool?
    public var appliesToEnergyFee: Bool
    public var appliesToParkingFee: Bool
    public var appliesToOverstayFee: Bool
    public var appliesMinimumMaximumCost: Bool

    public init(taxRuleID: UInt32, taxRuleName: String? = nil, taxRate: RationalNumberType, taxIncludedInPrice: Bool? = nil, appliesToEnergyFee: Bool, appliesToParkingFee: Bool, appliesToOverstayFee: Bool, appliesMinimumMaximumCost: Bool) {
        self.taxRuleID = taxRuleID
        self.taxRuleName = taxRuleName
        self.taxRate = taxRate
        self.taxIncludedInPrice = taxIncludedInPrice
        self.appliesToEnergyFee = appliesToEnergyFee
        self.appliesToParkingFee = appliesToParkingFee
        self.appliesToOverstayFee = appliesToOverstayFee
        self.appliesMinimumMaximumCost = appliesMinimumMaximumCost
    }
}

internal func encodeTaxRuleType(_ w: BitWriter, _ msg: TaxRuleType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, UInt64(msg.taxRuleID))
    w.writeBits(0, 1)   // child EE
    var st52 = 0
    var done52 = false
    while !done52 {
        switch st52 {
        case 0:
            if let v = msg.taxRuleName {
                w.writeBits(0, 2)   // TaxRuleName
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeStringValue(w, v)
                w.writeBits(0, 1)   // child EE
                st52 = 1
            } else {
                w.writeBits(1, 2)   // SE(TaxRate)
                encodeRationalNumberType(w, msg.taxRate)
                done52 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(TaxRate)
            encodeRationalNumberType(w, msg.taxRate)
            done52 = true
        default:
            done52 = true
        }
    }
    var st53 = 0
    var done53 = false
    while !done53 {
        switch st53 {
        case 0:
            if let v = msg.taxIncludedInPrice {
                w.writeBits(0, 2)   // TaxIncludedInPrice
                w.writeBits(0, 1)   // value-start
                w.writeBits(v ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                st53 = 1
            } else {
                w.writeBits(1, 2)   // SE(AppliesToEnergyFee)
                w.writeBits(0, 1)   // value-start
                w.writeBits(msg.appliesToEnergyFee ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                done53 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(AppliesToEnergyFee)
            w.writeBits(0, 1)   // value-start
            w.writeBits(msg.appliesToEnergyFee ? 1 : 0, 1)
            w.writeBits(0, 1)   // child EE
            done53 = true
        default:
            done53 = true
        }
    }
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.appliesToParkingFee ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.appliesToOverstayFee ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.appliesMinimumMaximumCost ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

internal func decodeTaxRuleType(_ r: BitReader) throws -> TaxRuleType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _taxRuleID = UInt32(try ExiPrimitives.readUnsignedInteger(r))
    _ = try r.readBits(1)   // child EE
    var _taxRuleName: String? = nil
    var _taxRate: RationalNumberType? = nil
    var st54 = 0
    var done54 = false
    while !done54 {
        switch st54 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // TaxRuleName
                _ = try r.readBits(1)   // value-start
                _taxRuleName = try ExiPrimitives.readStringValue(r, slot: "TaxRuleName")
                _ = try r.readBits(1)   // child EE
                st54 = 1
            case 1:   // SE(TaxRate)
                _taxRate = try decodeRationalNumberType(r)
                done54 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(TaxRate)
                _taxRate = try decodeRationalNumberType(r)
                done54 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done54 = true
        }
    }
    guard let _taxRate else {
        throw ExiError.invalidEventCode("TaxRate is required but the element ended")
    }
    var _taxIncludedInPrice: Bool? = nil
    var _appliesToEnergyFee: Bool? = nil
    var st55 = 0
    var done55 = false
    while !done55 {
        switch st55 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // TaxIncludedInPrice
                _ = try r.readBits(1)   // value-start
                _taxIncludedInPrice = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                st55 = 1
            case 1:   // SE(AppliesToEnergyFee)
                _ = try r.readBits(1)   // value-start
                _appliesToEnergyFee = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                done55 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(AppliesToEnergyFee)
                _ = try r.readBits(1)   // value-start
                _appliesToEnergyFee = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                done55 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done55 = true
        }
    }
    guard let _appliesToEnergyFee else {
        throw ExiError.invalidEventCode("AppliesToEnergyFee is required but the element ended")
    }
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _appliesToParkingFee = try r.readBits(1) != 0
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _appliesToOverstayFee = try r.readBits(1) != 0
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _appliesMinimumMaximumCost = try r.readBits(1) != 0
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // element EE
    return TaxRuleType(taxRuleID: _taxRuleID, taxRuleName: _taxRuleName, taxRate: _taxRate, taxIncludedInPrice: _taxIncludedInPrice, appliesToEnergyFee: _appliesToEnergyFee, appliesToParkingFee: _appliesToParkingFee, appliesToOverstayFee: _appliesToOverstayFee, appliesMinimumMaximumCost: _appliesMinimumMaximumCost)
}

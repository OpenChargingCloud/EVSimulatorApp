// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class VehicleCheckOutResType: V2GResponseType {
    public var eVSECheckOutStatus: EvseCheckOutStatus

    public init(header: MessageHeaderType, responseCode: ResponseCode, eVSECheckOutStatus: EvseCheckOutStatus) {
        self.eVSECheckOutStatus = eVSECheckOutStatus
        super.init(header: header, responseCode: responseCode)
    }
}

internal func encodeVehicleCheckOutResType(_ w: BitWriter, _ msg: VehicleCheckOutResType) {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.responseCode.rawValue), 6)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.eVSECheckOutStatus.rawValue), 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

internal func decodeVehicleCheckOutResType(_ r: BitReader) throws -> VehicleCheckOutResType {
    _ = try r.readBits(1)   // SE
    let _header = try decodeMessageHeaderType(r)
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _responseCode = try exiEnum(ResponseCode.self, try r.readBits(6))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _eVSECheckOutStatus = try exiEnum(EvseCheckOutStatus.self, try r.readBits(1))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // element EE
    return VehicleCheckOutResType(header: _header, responseCode: _responseCode, eVSECheckOutStatus: _eVSECheckOutStatus)
}

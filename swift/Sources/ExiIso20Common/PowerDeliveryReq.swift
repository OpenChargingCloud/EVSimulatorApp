// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class PowerDeliveryReq: V2GRequestType {
    public var eVProcessing: Processing
    public var chargeProgress: ChargeProgress
    public var eVPowerProfile: EVPowerProfileType?
    public var bPT_ChannelSelection: ChannelSelection?

    public init(header: MessageHeaderType, eVProcessing: Processing, chargeProgress: ChargeProgress, eVPowerProfile: EVPowerProfileType? = nil, bPT_ChannelSelection: ChannelSelection? = nil) {
        self.eVProcessing = eVProcessing
        self.chargeProgress = chargeProgress
        self.eVPowerProfile = eVPowerProfile
        self.bPT_ChannelSelection = bPT_ChannelSelection
        super.init(header: header)
    }
}

internal func encodePowerDeliveryReq(_ w: BitWriter, _ msg: PowerDeliveryReq) {
    w.writeBits(0, 1)   // SE
    encodeMessageHeaderType(w, msg.header)
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.eVProcessing.rawValue), 2)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.chargeProgress.rawValue), 2)
    w.writeBits(0, 1)   // child EE
    var st102 = 0
    var done102 = false
    while !done102 {
        switch st102 {
        case 0:
            if let v = msg.eVPowerProfile {
                w.writeBits(0, 2)   // EVPowerProfile
                encodeEVPowerProfileType(w, v)
                st102 = 1
            } else if let v = msg.bPT_ChannelSelection {
                w.writeBits(1, 2)   // BPT_ChannelSelection
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v.rawValue), 1)
                w.writeBits(0, 1)   // child EE
                st102 = 2
            } else {
                w.writeBits(2, 2)   // element EE
                done102 = true
            }
        case 1:
            if let v = msg.bPT_ChannelSelection {
                w.writeBits(0, 2)   // BPT_ChannelSelection
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v.rawValue), 1)
                w.writeBits(0, 1)   // child EE
                st102 = 2
            } else {
                w.writeBits(1, 2)   // element EE
                done102 = true
            }
        case 2:
            w.writeBits(0, 1)   // element EE
            done102 = true
        default:
            done102 = true
        }
    }
}

internal func decodePowerDeliveryReq(_ r: BitReader) throws -> PowerDeliveryReq {
    _ = try r.readBits(1)   // SE
    let _header = try decodeMessageHeaderType(r)
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _eVProcessing = try exiEnum(Processing.self, try r.readBits(2))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _chargeProgress = try exiEnum(ChargeProgress.self, try r.readBits(2))
    _ = try r.readBits(1)   // child EE
    var _eVPowerProfile: EVPowerProfileType? = nil
    var _bPT_ChannelSelection: ChannelSelection? = nil
    var st103 = 0
    var done103 = false
    while !done103 {
        switch st103 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // EVPowerProfile
                _eVPowerProfile = try decodeEVPowerProfileType(r)
                st103 = 1
            case 1:   // BPT_ChannelSelection
                _ = try r.readBits(1)   // value-start
                _bPT_ChannelSelection = try exiEnum(ChannelSelection.self, try r.readBits(1))
                _ = try r.readBits(1)   // child EE
                st103 = 2
            case 2:   // element EE
                done103 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // BPT_ChannelSelection
                _ = try r.readBits(1)   // value-start
                _bPT_ChannelSelection = try exiEnum(ChannelSelection.self, try r.readBits(1))
                _ = try r.readBits(1)   // child EE
                st103 = 2
            case 1:   // element EE
                done103 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(1) {
            case 0:   // element EE
                done103 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done103 = true
        }
    }
    return PowerDeliveryReq(header: _header, eVProcessing: _eVProcessing, chargeProgress: _chargeProgress, eVPowerProfile: _eVPowerProfile, bPT_ChannelSelection: _bPT_ChannelSelection)
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public final class SubCertificatesType {
    public var certificate: [[UInt8]]

    public init(certificate: [[UInt8]]) {
        self.certificate = certificate
    }
}

internal func encodeSubCertificatesType(_ w: BitWriter, _ msg: SubCertificatesType) {
    let list = msg.certificate
    precondition((1...3).contains(list.count), "list size out of schema range")
    for (i, item) in list.enumerated() {
        w.writeBits(0, i == 0 ? 1 : 2)   // SE(item)
        w.writeBits(0, 1)   // value-start
        ExiPrimitives.writeBinary(w, item)
        w.writeBits(0, 1)   // child EE
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

internal func decodeSubCertificatesType(_ r: BitReader) throws -> SubCertificatesType {
    var list = [[UInt8]]()
    _ = try r.readBits(1)   // SE(item) first
    _ = try r.readBits(1)   // value-start
    let item = try ExiPrimitives.readBinary(r)
    _ = try r.readBits(1)   // child EE
    list.append(item)
    while true {
        let ec = try r.readBits(2)
        if ec == 1 { break }   // element EE
        guard ec == 0, list.count < 3 else {
            throw ExiError.invalidEventCode("repeating element")
        }
        _ = try r.readBits(1)   // value-start
        let item = try ExiPrimitives.readBinary(r)
        _ = try r.readBits(1)   // child EE
        list.append(item)
    }
    return SubCertificatesType(certificate: list)
}

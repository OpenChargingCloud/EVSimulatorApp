// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.common

import ExiRuntime

public enum CommonMessagesCodec {

    public static let exiHeader: UInt8 = 0x80

    public static func encode(_ msg: SessionSetupReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(35, 6)   // document element selector
        encodeSessionSetupReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: SessionSetupRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(36, 6)   // document element selector
        encodeSessionSetupRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: AuthorizationSetupReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(2, 6)   // document element selector
        encodeAuthorizationSetupReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: AuthorizationSetupRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(3, 6)   // document element selector
        encodeAuthorizationSetupRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: AuthorizationReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(0, 6)   // document element selector
        encodeAuthorizationReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: AuthorizationRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(1, 6)   // document element selector
        encodeAuthorizationRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: ServiceDiscoveryReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(31, 6)   // document element selector
        encodeServiceDiscoveryReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: ServiceDiscoveryRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(32, 6)   // document element selector
        encodeServiceDiscoveryRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: ServiceDetailReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(29, 6)   // document element selector
        encodeServiceDetailReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: ServiceDetailRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(30, 6)   // document element selector
        encodeServiceDetailRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: ServiceSelectionReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(33, 6)   // document element selector
        encodeServiceSelectionReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: ServiceSelectionRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(34, 6)   // document element selector
        encodeServiceSelectionRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: ScheduleExchangeReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(27, 6)   // document element selector
        encodeScheduleExchangeReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: ScheduleExchangeRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(28, 6)   // document element selector
        encodeScheduleExchangeRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: PowerDeliveryReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(21, 6)   // document element selector
        encodePowerDeliveryReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: PowerDeliveryRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(22, 6)   // document element selector
        encodePowerDeliveryRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: MeteringConfirmationReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(16, 6)   // document element selector
        encodeMeteringConfirmationReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: MeteringConfirmationRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(17, 6)   // document element selector
        encodeMeteringConfirmationRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: SessionStopReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(37, 6)   // document element selector
        encodeSessionStopReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: SessionStopRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(38, 6)   // document element selector
        encodeSessionStopRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: CertificateInstallationReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(7, 6)   // document element selector
        encodeCertificateInstallationReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: CertificateInstallationRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(8, 6)   // document element selector
        encodeCertificateInstallationRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: VehicleCheckInReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(49, 6)   // document element selector
        encodeVehicleCheckInReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: VehicleCheckInRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(50, 6)   // document element selector
        encodeVehicleCheckInRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: VehicleCheckOutReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(51, 6)   // document element selector
        encodeVehicleCheckOutReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: VehicleCheckOutRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(52, 6)   // document element selector
        encodeVehicleCheckOutRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: SignedInstallationData) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(45, 6)   // document element selector
        encodeSignedInstallationData(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: SignedMeteringData) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(46, 6)   // document element selector
        encodeSignedMeteringData(w, msg)
        w.alignToByte()
        return w.bytes
    }

    /// Encodes any document element of this message set, dispatching on its runtime type.
    public static func encodeAny(_ msg: Any) throws -> [UInt8] {
        switch msg {
        case let m as AuthorizationReq: return encode(m)
        case let m as AuthorizationRes: return encode(m)
        case let m as AuthorizationSetupReq: return encode(m)
        case let m as AuthorizationSetupRes: return encode(m)
        case let m as CertificateInstallationReq: return encode(m)
        case let m as CertificateInstallationRes: return encode(m)
        case let m as MeteringConfirmationReq: return encode(m)
        case let m as MeteringConfirmationRes: return encode(m)
        case let m as PowerDeliveryReq: return encode(m)
        case let m as PowerDeliveryRes: return encode(m)
        case let m as ScheduleExchangeReq: return encode(m)
        case let m as ScheduleExchangeRes: return encode(m)
        case let m as ServiceDetailReq: return encode(m)
        case let m as ServiceDetailRes: return encode(m)
        case let m as ServiceDiscoveryReq: return encode(m)
        case let m as ServiceDiscoveryRes: return encode(m)
        case let m as ServiceSelectionReq: return encode(m)
        case let m as ServiceSelectionRes: return encode(m)
        case let m as SessionSetupReq: return encode(m)
        case let m as SessionSetupRes: return encode(m)
        case let m as SessionStopReq: return encode(m)
        case let m as SessionStopRes: return encode(m)
        case let m as VehicleCheckInReq: return encode(m)
        case let m as VehicleCheckInRes: return encode(m)
        case let m as VehicleCheckOutReq: return encode(m)
        case let m as VehicleCheckOutRes: return encode(m)
        case let m as SignedInstallationData: return encode(m)
        case let m as SignedMeteringData: return encode(m)
        default: throw ExiError.invalidHeader
        }
    }

    public static func decodeAny(_ src: [UInt8]) throws -> Any {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        let sel = try r.readBits(6)
        switch sel {
        case 0: return try decodeAuthorizationReq(r)
        case 1: return try decodeAuthorizationRes(r)
        case 2: return try decodeAuthorizationSetupReq(r)
        case 3: return try decodeAuthorizationSetupRes(r)
        case 7: return try decodeCertificateInstallationReq(r)
        case 8: return try decodeCertificateInstallationRes(r)
        case 16: return try decodeMeteringConfirmationReq(r)
        case 17: return try decodeMeteringConfirmationRes(r)
        case 21: return try decodePowerDeliveryReq(r)
        case 22: return try decodePowerDeliveryRes(r)
        case 27: return try decodeScheduleExchangeReq(r)
        case 28: return try decodeScheduleExchangeRes(r)
        case 29: return try decodeServiceDetailReq(r)
        case 30: return try decodeServiceDetailRes(r)
        case 31: return try decodeServiceDiscoveryReq(r)
        case 32: return try decodeServiceDiscoveryRes(r)
        case 33: return try decodeServiceSelectionReq(r)
        case 34: return try decodeServiceSelectionRes(r)
        case 35: return try decodeSessionSetupReq(r)
        case 36: return try decodeSessionSetupRes(r)
        case 37: return try decodeSessionStopReq(r)
        case 38: return try decodeSessionStopRes(r)
        case 45: return try decodeSignedInstallationData(r)
        case 46: return try decodeSignedMeteringData(r)
        case 49: return try decodeVehicleCheckInReq(r)
        case 50: return try decodeVehicleCheckInRes(r)
        case 51: return try decodeVehicleCheckOutReq(r)
        case 52: return try decodeVehicleCheckOutRes(r)
        default: throw ExiError.unknownDocumentIndex(sel)
        }
    }

    public static func encodeFragment_AbsolutePriceSchedule(_ content: AbsolutePriceScheduleType) -> [UInt8] {
        let w = BitWriter(capacity: 512)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(0, 9)   // fragment SE(AbsolutePriceSchedule)
        encodeAbsolutePriceScheduleType(w, content)
        w.writeBits(282, 9)   // End Fragment (ED)
        w.alignToByte()
        return w.bytes
    }

    public static func decodeFragment_AbsolutePriceSchedule(_ src: [UInt8]) throws -> AbsolutePriceScheduleType {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        guard try r.readBits(9) == 0 else {
            throw ExiError.invalidEventCode("not a AbsolutePriceSchedule fragment")
        }
        let result = try decodeAbsolutePriceScheduleType(r)
        guard try r.readBits(9) == 282 else {
            throw ExiError.invalidEventCode("missing End Fragment")
        }
        return result
    }

    public static func encodeFragment_CertificateInstallationReq(_ content: CertificateInstallationReqType) -> [UInt8] {
        let w = BitWriter(capacity: 512)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(27, 9)   // fragment SE(CertificateInstallationReq)
        encodeCertificateInstallationReqType(w, content)
        w.writeBits(282, 9)   // End Fragment (ED)
        w.alignToByte()
        return w.bytes
    }

    public static func decodeFragment_CertificateInstallationReq(_ src: [UInt8]) throws -> CertificateInstallationReqType {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        guard try r.readBits(9) == 27 else {
            throw ExiError.invalidEventCode("not a CertificateInstallationReq fragment")
        }
        let result = try decodeCertificateInstallationReqType(r)
        guard try r.readBits(9) == 282 else {
            throw ExiError.invalidEventCode("missing End Fragment")
        }
        return result
    }

    public static func encodeFragment_MeteringConfirmationReq(_ content: MeteringConfirmationReqType) -> [UInt8] {
        let w = BitWriter(capacity: 512)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(119, 9)   // fragment SE(MeteringConfirmationReq)
        encodeMeteringConfirmationReqType(w, content)
        w.writeBits(282, 9)   // End Fragment (ED)
        w.alignToByte()
        return w.bytes
    }

    public static func decodeFragment_MeteringConfirmationReq(_ src: [UInt8]) throws -> MeteringConfirmationReqType {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        guard try r.readBits(9) == 119 else {
            throw ExiError.invalidEventCode("not a MeteringConfirmationReq fragment")
        }
        let result = try decodeMeteringConfirmationReqType(r)
        guard try r.readBits(9) == 282 else {
            throw ExiError.invalidEventCode("missing End Fragment")
        }
        return result
    }

    public static func encodeFragment_OEMProvisioningCertificateChain(_ content: SignedCertificateChainType) -> [UInt8] {
        let w = BitWriter(capacity: 512)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(128, 9)   // fragment SE(OEMProvisioningCertificateChain)
        encodeSignedCertificateChainType(w, content)
        w.writeBits(282, 9)   // End Fragment (ED)
        w.alignToByte()
        return w.bytes
    }

    public static func decodeFragment_OEMProvisioningCertificateChain(_ src: [UInt8]) throws -> SignedCertificateChainType {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        guard try r.readBits(9) == 128 else {
            throw ExiError.invalidEventCode("not a OEMProvisioningCertificateChain fragment")
        }
        let result = try decodeSignedCertificateChainType(r)
        guard try r.readBits(9) == 282 else {
            throw ExiError.invalidEventCode("missing End Fragment")
        }
        return result
    }

    public static func encodeFragment_PnC_AReqAuthorizationMode(_ content: PnC_AReqAuthorizationModeType) -> [UInt8] {
        let w = BitWriter(capacity: 512)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(151, 9)   // fragment SE(PnC_AReqAuthorizationMode)
        encodePnC_AReqAuthorizationModeType(w, content)
        w.writeBits(282, 9)   // End Fragment (ED)
        w.alignToByte()
        return w.bytes
    }

    public static func decodeFragment_PnC_AReqAuthorizationMode(_ src: [UInt8]) throws -> PnC_AReqAuthorizationModeType {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        guard try r.readBits(9) == 151 else {
            throw ExiError.invalidEventCode("not a PnC_AReqAuthorizationMode fragment")
        }
        let result = try decodePnC_AReqAuthorizationModeType(r)
        guard try r.readBits(9) == 282 else {
            throw ExiError.invalidEventCode("missing End Fragment")
        }
        return result
    }

    public static func encodeFragment_SignedInstallationData(_ content: SignedInstallationDataType) -> [UInt8] {
        let w = BitWriter(capacity: 512)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(231, 9)   // fragment SE(SignedInstallationData)
        encodeSignedInstallationDataType(w, content)
        w.writeBits(282, 9)   // End Fragment (ED)
        w.alignToByte()
        return w.bytes
    }

    public static func decodeFragment_SignedInstallationData(_ src: [UInt8]) throws -> SignedInstallationDataType {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        guard try r.readBits(9) == 231 else {
            throw ExiError.invalidEventCode("not a SignedInstallationData fragment")
        }
        let result = try decodeSignedInstallationDataType(r)
        guard try r.readBits(9) == 282 else {
            throw ExiError.invalidEventCode("missing End Fragment")
        }
        return result
    }

    public static func encodeFragment_SignedInfo(_ content: SignedInfoType) -> [UInt8] {
        let w = BitWriter(capacity: 512)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(230, 9)   // fragment SE(SignedInfo)
        encodeSignedInfoType(w, content)
        w.writeBits(282, 9)   // End Fragment (ED)
        w.alignToByte()
        return w.bytes
    }

    public static func decodeFragment_SignedInfo(_ src: [UInt8]) throws -> SignedInfoType {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        guard try r.readBits(9) == 230 else {
            throw ExiError.invalidEventCode("not a SignedInfo fragment")
        }
        let result = try decodeSignedInfoType(r)
        guard try r.readBits(9) == 282 else {
            throw ExiError.invalidEventCode("missing End Fragment")
        }
        return result
    }
}

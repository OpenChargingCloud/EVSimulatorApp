// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.acdp

import ExiRuntime

public final class EVTechnicalStatusType {
    public var eVReadyToCharge: Bool
    public var eVImmobilizationRequest: Bool
    public var eVImmobilized: Bool?
    public var eVWLANStrength: RationalNumberType?
    public var eVCPStatus: CpStatus?
    public var eVSOC: Int8?
    public var eVErrorCode: ErrorCode?
    public var eVTimeout: Bool?

    public init(eVReadyToCharge: Bool, eVImmobilizationRequest: Bool, eVImmobilized: Bool? = nil, eVWLANStrength: RationalNumberType? = nil, eVCPStatus: CpStatus? = nil, eVSOC: Int8? = nil, eVErrorCode: ErrorCode? = nil, eVTimeout: Bool? = nil) {
        self.eVReadyToCharge = eVReadyToCharge
        self.eVImmobilizationRequest = eVImmobilizationRequest
        self.eVImmobilized = eVImmobilized
        self.eVWLANStrength = eVWLANStrength
        self.eVCPStatus = eVCPStatus
        self.eVSOC = eVSOC
        self.eVErrorCode = eVErrorCode
        self.eVTimeout = eVTimeout
    }
}

internal func encodeEVTechnicalStatusType(_ w: BitWriter, _ msg: EVTechnicalStatusType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.eVReadyToCharge ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.eVImmobilizationRequest ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    var st0 = 0
    var done0 = false
    while !done0 {
        switch st0 {
        case 0:
            if let v = msg.eVImmobilized {
                w.writeBits(0, 3)   // EVImmobilized
                w.writeBits(0, 1)   // value-start
                w.writeBits(v ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                st0 = 1
            } else if let v = msg.eVWLANStrength {
                w.writeBits(1, 3)   // EVWLANStrength
                encodeRationalNumberType(w, v)
                st0 = 2
            } else if let v = msg.eVCPStatus {
                w.writeBits(2, 3)   // EVCPStatus
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v.rawValue), 3)
                w.writeBits(0, 1)   // child EE
                st0 = 3
            } else if let v = msg.eVSOC {
                w.writeBits(3, 3)   // EVSOC
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 7)
                w.writeBits(0, 1)   // child EE
                st0 = 4
            } else if let v = msg.eVErrorCode {
                w.writeBits(4, 3)   // EVErrorCode
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v.rawValue), 4)
                w.writeBits(0, 1)   // child EE
                st0 = 5
            } else if let v = msg.eVTimeout {
                w.writeBits(5, 3)   // EVTimeout
                w.writeBits(0, 1)   // value-start
                w.writeBits(v ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                st0 = 6
            } else {
                w.writeBits(6, 3)   // element EE
                done0 = true
            }
        case 1:
            if let v = msg.eVWLANStrength {
                w.writeBits(0, 3)   // EVWLANStrength
                encodeRationalNumberType(w, v)
                st0 = 2
            } else if let v = msg.eVCPStatus {
                w.writeBits(1, 3)   // EVCPStatus
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v.rawValue), 3)
                w.writeBits(0, 1)   // child EE
                st0 = 3
            } else if let v = msg.eVSOC {
                w.writeBits(2, 3)   // EVSOC
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 7)
                w.writeBits(0, 1)   // child EE
                st0 = 4
            } else if let v = msg.eVErrorCode {
                w.writeBits(3, 3)   // EVErrorCode
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v.rawValue), 4)
                w.writeBits(0, 1)   // child EE
                st0 = 5
            } else if let v = msg.eVTimeout {
                w.writeBits(4, 3)   // EVTimeout
                w.writeBits(0, 1)   // value-start
                w.writeBits(v ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                st0 = 6
            } else {
                w.writeBits(5, 3)   // element EE
                done0 = true
            }
        case 2:
            if let v = msg.eVCPStatus {
                w.writeBits(0, 3)   // EVCPStatus
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v.rawValue), 3)
                w.writeBits(0, 1)   // child EE
                st0 = 3
            } else if let v = msg.eVSOC {
                w.writeBits(1, 3)   // EVSOC
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 7)
                w.writeBits(0, 1)   // child EE
                st0 = 4
            } else if let v = msg.eVErrorCode {
                w.writeBits(2, 3)   // EVErrorCode
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v.rawValue), 4)
                w.writeBits(0, 1)   // child EE
                st0 = 5
            } else if let v = msg.eVTimeout {
                w.writeBits(3, 3)   // EVTimeout
                w.writeBits(0, 1)   // value-start
                w.writeBits(v ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                st0 = 6
            } else {
                w.writeBits(4, 3)   // element EE
                done0 = true
            }
        case 3:
            if let v = msg.eVSOC {
                w.writeBits(0, 3)   // EVSOC
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 7)
                w.writeBits(0, 1)   // child EE
                st0 = 4
            } else if let v = msg.eVErrorCode {
                w.writeBits(1, 3)   // EVErrorCode
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v.rawValue), 4)
                w.writeBits(0, 1)   // child EE
                st0 = 5
            } else if let v = msg.eVTimeout {
                w.writeBits(2, 3)   // EVTimeout
                w.writeBits(0, 1)   // value-start
                w.writeBits(v ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                st0 = 6
            } else {
                w.writeBits(3, 3)   // element EE
                done0 = true
            }
        case 4:
            if let v = msg.eVErrorCode {
                w.writeBits(0, 2)   // EVErrorCode
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v.rawValue), 4)
                w.writeBits(0, 1)   // child EE
                st0 = 5
            } else if let v = msg.eVTimeout {
                w.writeBits(1, 2)   // EVTimeout
                w.writeBits(0, 1)   // value-start
                w.writeBits(v ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                st0 = 6
            } else {
                w.writeBits(2, 2)   // element EE
                done0 = true
            }
        case 5:
            if let v = msg.eVTimeout {
                w.writeBits(0, 2)   // EVTimeout
                w.writeBits(0, 1)   // value-start
                w.writeBits(v ? 1 : 0, 1)
                w.writeBits(0, 1)   // child EE
                st0 = 6
            } else {
                w.writeBits(1, 2)   // element EE
                done0 = true
            }
        case 6:
            w.writeBits(0, 1)   // element EE
            done0 = true
        default:
            done0 = true
        }
    }
}

internal func decodeEVTechnicalStatusType(_ r: BitReader) throws -> EVTechnicalStatusType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _eVReadyToCharge = try r.readBits(1) != 0
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _eVImmobilizationRequest = try r.readBits(1) != 0
    _ = try r.readBits(1)   // child EE
    var _eVImmobilized: Bool? = nil
    var _eVWLANStrength: RationalNumberType? = nil
    var _eVCPStatus: CpStatus? = nil
    var _eVSOC: Int8? = nil
    var _eVErrorCode: ErrorCode? = nil
    var _eVTimeout: Bool? = nil
    var st1 = 0
    var done1 = false
    while !done1 {
        switch st1 {
        case 0:
            switch try r.readBits(3) {
            case 0:   // EVImmobilized
                _ = try r.readBits(1)   // value-start
                _eVImmobilized = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                st1 = 1
            case 1:   // EVWLANStrength
                _eVWLANStrength = try decodeRationalNumberType(r)
                st1 = 2
            case 2:   // EVCPStatus
                _ = try r.readBits(1)   // value-start
                _eVCPStatus = try exiEnum(CpStatus.self, try r.readBits(3))
                _ = try r.readBits(1)   // child EE
                st1 = 3
            case 3:   // EVSOC
                _ = try r.readBits(1)   // value-start
                _eVSOC = Int8(try r.readBits(7))
                _ = try r.readBits(1)   // child EE
                st1 = 4
            case 4:   // EVErrorCode
                _ = try r.readBits(1)   // value-start
                _eVErrorCode = try exiEnum(ErrorCode.self, try r.readBits(4))
                _ = try r.readBits(1)   // child EE
                st1 = 5
            case 5:   // EVTimeout
                _ = try r.readBits(1)   // value-start
                _eVTimeout = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                st1 = 6
            case 6:   // element EE
                done1 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(3) {
            case 0:   // EVWLANStrength
                _eVWLANStrength = try decodeRationalNumberType(r)
                st1 = 2
            case 1:   // EVCPStatus
                _ = try r.readBits(1)   // value-start
                _eVCPStatus = try exiEnum(CpStatus.self, try r.readBits(3))
                _ = try r.readBits(1)   // child EE
                st1 = 3
            case 2:   // EVSOC
                _ = try r.readBits(1)   // value-start
                _eVSOC = Int8(try r.readBits(7))
                _ = try r.readBits(1)   // child EE
                st1 = 4
            case 3:   // EVErrorCode
                _ = try r.readBits(1)   // value-start
                _eVErrorCode = try exiEnum(ErrorCode.self, try r.readBits(4))
                _ = try r.readBits(1)   // child EE
                st1 = 5
            case 4:   // EVTimeout
                _ = try r.readBits(1)   // value-start
                _eVTimeout = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                st1 = 6
            case 5:   // element EE
                done1 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(3) {
            case 0:   // EVCPStatus
                _ = try r.readBits(1)   // value-start
                _eVCPStatus = try exiEnum(CpStatus.self, try r.readBits(3))
                _ = try r.readBits(1)   // child EE
                st1 = 3
            case 1:   // EVSOC
                _ = try r.readBits(1)   // value-start
                _eVSOC = Int8(try r.readBits(7))
                _ = try r.readBits(1)   // child EE
                st1 = 4
            case 2:   // EVErrorCode
                _ = try r.readBits(1)   // value-start
                _eVErrorCode = try exiEnum(ErrorCode.self, try r.readBits(4))
                _ = try r.readBits(1)   // child EE
                st1 = 5
            case 3:   // EVTimeout
                _ = try r.readBits(1)   // value-start
                _eVTimeout = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                st1 = 6
            case 4:   // element EE
                done1 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 3:
            switch try r.readBits(3) {
            case 0:   // EVSOC
                _ = try r.readBits(1)   // value-start
                _eVSOC = Int8(try r.readBits(7))
                _ = try r.readBits(1)   // child EE
                st1 = 4
            case 1:   // EVErrorCode
                _ = try r.readBits(1)   // value-start
                _eVErrorCode = try exiEnum(ErrorCode.self, try r.readBits(4))
                _ = try r.readBits(1)   // child EE
                st1 = 5
            case 2:   // EVTimeout
                _ = try r.readBits(1)   // value-start
                _eVTimeout = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                st1 = 6
            case 3:   // element EE
                done1 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 4:
            switch try r.readBits(2) {
            case 0:   // EVErrorCode
                _ = try r.readBits(1)   // value-start
                _eVErrorCode = try exiEnum(ErrorCode.self, try r.readBits(4))
                _ = try r.readBits(1)   // child EE
                st1 = 5
            case 1:   // EVTimeout
                _ = try r.readBits(1)   // value-start
                _eVTimeout = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                st1 = 6
            case 2:   // element EE
                done1 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 5:
            switch try r.readBits(2) {
            case 0:   // EVTimeout
                _ = try r.readBits(1)   // value-start
                _eVTimeout = try r.readBits(1) != 0
                _ = try r.readBits(1)   // child EE
                st1 = 6
            case 1:   // element EE
                done1 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 6:
            switch try r.readBits(1) {
            case 0:   // element EE
                done1 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done1 = true
        }
    }
    return EVTechnicalStatusType(eVReadyToCharge: _eVReadyToCharge, eVImmobilizationRequest: _eVImmobilizationRequest, eVImmobilized: _eVImmobilized, eVWLANStrength: _eVWLANStrength, eVCPStatus: _eVCPStatus, eVSOC: _eVSOC, eVErrorCode: _eVErrorCode, eVTimeout: _eVTimeout)
}

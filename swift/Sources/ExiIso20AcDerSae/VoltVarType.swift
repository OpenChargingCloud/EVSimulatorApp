// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso20.acdersae

import ExiRuntime

public final class VoltVarType {
    public var enable: Bool
    public var priority: UInt16?
    public var xUnit: DerUnit
    public var yUnit: DerUnit
    public var curveDataPoints: CurveDataPointsListType
    public var curveDataPoints_L2: CurveDataPointsListType?
    public var curveDataPoints_L3: CurveDataPointsListType?
    public var openLoopResponseTime: RationalNumberType
    public var timeConstantPT1: UInt32?
    public var referenceVoltage: RationalNumberType
    public var autonomousReferenceVoltageAdjustmentEnable: Bool
    public var referenceVoltageAdjustmentTimeConstant: UInt32

    public init(enable: Bool, priority: UInt16? = nil, xUnit: DerUnit, yUnit: DerUnit, curveDataPoints: CurveDataPointsListType, curveDataPoints_L2: CurveDataPointsListType? = nil, curveDataPoints_L3: CurveDataPointsListType? = nil, openLoopResponseTime: RationalNumberType, timeConstantPT1: UInt32? = nil, referenceVoltage: RationalNumberType, autonomousReferenceVoltageAdjustmentEnable: Bool, referenceVoltageAdjustmentTimeConstant: UInt32) {
        self.enable = enable
        self.priority = priority
        self.xUnit = xUnit
        self.yUnit = yUnit
        self.curveDataPoints = curveDataPoints
        self.curveDataPoints_L2 = curveDataPoints_L2
        self.curveDataPoints_L3 = curveDataPoints_L3
        self.openLoopResponseTime = openLoopResponseTime
        self.timeConstantPT1 = timeConstantPT1
        self.referenceVoltage = referenceVoltage
        self.autonomousReferenceVoltageAdjustmentEnable = autonomousReferenceVoltageAdjustmentEnable
        self.referenceVoltageAdjustmentTimeConstant = referenceVoltageAdjustmentTimeConstant
    }
}

internal func encodeVoltVarType(_ w: BitWriter, _ msg: VoltVarType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.enable ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    var st88 = 0
    var done88 = false
    while !done88 {
        switch st88 {
        case 0:
            if let v = msg.priority {
                w.writeBits(0, 2)   // Priority
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st88 = 1
            } else {
                w.writeBits(1, 2)   // SE(XUnit)
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(msg.xUnit.rawValue), 4)
                w.writeBits(0, 1)   // child EE
                done88 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(XUnit)
            w.writeBits(0, 1)   // value-start
            w.writeBits(UInt32(msg.xUnit.rawValue), 4)
            w.writeBits(0, 1)   // child EE
            done88 = true
        default:
            done88 = true
        }
    }
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.yUnit.rawValue), 4)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    encodeCurveDataPointsListType(w, msg.curveDataPoints)
    var st89 = 0
    var done89 = false
    while !done89 {
        switch st89 {
        case 0:
            if let v = msg.curveDataPoints_L2 {
                w.writeBits(0, 2)   // CurveDataPoints_L2
                encodeCurveDataPointsListType(w, v)
                st89 = 1
            } else if let v = msg.curveDataPoints_L3 {
                w.writeBits(1, 2)   // CurveDataPoints_L3
                encodeCurveDataPointsListType(w, v)
                st89 = 2
            } else {
                w.writeBits(2, 2)   // SE(OpenLoopResponseTime)
                encodeRationalNumberType(w, msg.openLoopResponseTime)
                done89 = true
            }
        case 1:
            if let v = msg.curveDataPoints_L3 {
                w.writeBits(0, 2)   // CurveDataPoints_L3
                encodeCurveDataPointsListType(w, v)
                st89 = 2
            } else {
                w.writeBits(1, 2)   // SE(OpenLoopResponseTime)
                encodeRationalNumberType(w, msg.openLoopResponseTime)
                done89 = true
            }
        case 2:
            w.writeBits(0, 1)   // SE(OpenLoopResponseTime)
            encodeRationalNumberType(w, msg.openLoopResponseTime)
            done89 = true
        default:
            done89 = true
        }
    }
    var st90 = 0
    var done90 = false
    while !done90 {
        switch st90 {
        case 0:
            if let v = msg.timeConstantPT1 {
                w.writeBits(0, 2)   // TimeConstantPT1
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st90 = 1
            } else {
                w.writeBits(1, 2)   // SE(ReferenceVoltage)
                encodeRationalNumberType(w, msg.referenceVoltage)
                done90 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(ReferenceVoltage)
            encodeRationalNumberType(w, msg.referenceVoltage)
            done90 = true
        default:
            done90 = true
        }
    }
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.autonomousReferenceVoltageAdjustmentEnable ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, UInt64(msg.referenceVoltageAdjustmentTimeConstant))
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // element EE
}

internal func decodeVoltVarType(_ r: BitReader) throws -> VoltVarType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _enable = try r.readBits(1) != 0
    _ = try r.readBits(1)   // child EE
    var _priority: UInt16? = nil
    var _xUnit: DerUnit? = nil
    var st91 = 0
    var done91 = false
    while !done91 {
        switch st91 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // Priority
                _ = try r.readBits(1)   // value-start
                _priority = UInt16(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st91 = 1
            case 1:   // SE(XUnit)
                _ = try r.readBits(1)   // value-start
                _xUnit = try exiEnum(DerUnit.self, try r.readBits(4))
                _ = try r.readBits(1)   // child EE
                done91 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(XUnit)
                _ = try r.readBits(1)   // value-start
                _xUnit = try exiEnum(DerUnit.self, try r.readBits(4))
                _ = try r.readBits(1)   // child EE
                done91 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done91 = true
        }
    }
    guard let _xUnit else {
        throw ExiError.invalidEventCode("XUnit is required but the element ended")
    }
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _yUnit = try exiEnum(DerUnit.self, try r.readBits(4))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    let _curveDataPoints = try decodeCurveDataPointsListType(r)
    var _curveDataPoints_L2: CurveDataPointsListType? = nil
    var _curveDataPoints_L3: CurveDataPointsListType? = nil
    var _openLoopResponseTime: RationalNumberType? = nil
    var st92 = 0
    var done92 = false
    while !done92 {
        switch st92 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // CurveDataPoints_L2
                _curveDataPoints_L2 = try decodeCurveDataPointsListType(r)
                st92 = 1
            case 1:   // CurveDataPoints_L3
                _curveDataPoints_L3 = try decodeCurveDataPointsListType(r)
                st92 = 2
            case 2:   // SE(OpenLoopResponseTime)
                _openLoopResponseTime = try decodeRationalNumberType(r)
                done92 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // CurveDataPoints_L3
                _curveDataPoints_L3 = try decodeCurveDataPointsListType(r)
                st92 = 2
            case 1:   // SE(OpenLoopResponseTime)
                _openLoopResponseTime = try decodeRationalNumberType(r)
                done92 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(1) {
            case 0:   // SE(OpenLoopResponseTime)
                _openLoopResponseTime = try decodeRationalNumberType(r)
                done92 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done92 = true
        }
    }
    guard let _openLoopResponseTime else {
        throw ExiError.invalidEventCode("OpenLoopResponseTime is required but the element ended")
    }
    var _timeConstantPT1: UInt32? = nil
    var _referenceVoltage: RationalNumberType? = nil
    var st93 = 0
    var done93 = false
    while !done93 {
        switch st93 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // TimeConstantPT1
                _ = try r.readBits(1)   // value-start
                _timeConstantPT1 = UInt32(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st93 = 1
            case 1:   // SE(ReferenceVoltage)
                _referenceVoltage = try decodeRationalNumberType(r)
                done93 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(ReferenceVoltage)
                _referenceVoltage = try decodeRationalNumberType(r)
                done93 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done93 = true
        }
    }
    guard let _referenceVoltage else {
        throw ExiError.invalidEventCode("ReferenceVoltage is required but the element ended")
    }
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _autonomousReferenceVoltageAdjustmentEnable = try r.readBits(1) != 0
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _referenceVoltageAdjustmentTimeConstant = UInt32(try ExiPrimitives.readUnsignedInteger(r))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // element EE
    return VoltVarType(enable: _enable, priority: _priority, xUnit: _xUnit, yUnit: _yUnit, curveDataPoints: _curveDataPoints, curveDataPoints_L2: _curveDataPoints_L2, curveDataPoints_L3: _curveDataPoints_L3, openLoopResponseTime: _openLoopResponseTime, timeConstantPT1: _timeConstantPT1, referenceVoltage: _referenceVoltage, autonomousReferenceVoltageAdjustmentEnable: _autonomousReferenceVoltageAdjustmentEnable, referenceVoltageAdjustmentTimeConstant: _referenceVoltageAdjustmentTimeConstant)
}

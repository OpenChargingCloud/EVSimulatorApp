// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.acdersae

import ExiRuntime

public final class ConstantVarType {
    public var enable: Bool
    public var priority: UInt16?
    public var varSetpoint: RationalNumberType
    public var varSetpoint_L2: RationalNumberType?
    public var varSetpoint_L3: RationalNumberType?
    public var unit: DerUnit

    public init(enable: Bool, priority: UInt16? = nil, varSetpoint: RationalNumberType, varSetpoint_L2: RationalNumberType? = nil, varSetpoint_L3: RationalNumberType? = nil, unit: DerUnit) {
        self.enable = enable
        self.priority = priority
        self.varSetpoint = varSetpoint
        self.varSetpoint_L2 = varSetpoint_L2
        self.varSetpoint_L3 = varSetpoint_L3
        self.unit = unit
    }
}

internal func encodeConstantVarType(_ w: BitWriter, _ msg: ConstantVarType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.enable ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    var st76 = 0
    var done76 = false
    while !done76 {
        switch st76 {
        case 0:
            if let v = msg.priority {
                w.writeBits(0, 2)   // Priority
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st76 = 1
            } else {
                w.writeBits(1, 2)   // SE(VarSetpoint)
                encodeRationalNumberType(w, msg.varSetpoint)
                done76 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(VarSetpoint)
            encodeRationalNumberType(w, msg.varSetpoint)
            done76 = true
        default:
            done76 = true
        }
    }
    var st77 = 0
    var done77 = false
    while !done77 {
        switch st77 {
        case 0:
            if let v = msg.varSetpoint_L2 {
                w.writeBits(0, 2)   // VarSetpoint_L2
                encodeRationalNumberType(w, v)
                st77 = 1
            } else if let v = msg.varSetpoint_L3 {
                w.writeBits(1, 2)   // VarSetpoint_L3
                encodeRationalNumberType(w, v)
                st77 = 2
            } else {
                w.writeBits(2, 2)   // SE(Unit)
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(msg.unit.rawValue), 4)
                w.writeBits(0, 1)   // child EE
                done77 = true
            }
        case 1:
            if let v = msg.varSetpoint_L3 {
                w.writeBits(0, 2)   // VarSetpoint_L3
                encodeRationalNumberType(w, v)
                st77 = 2
            } else {
                w.writeBits(1, 2)   // SE(Unit)
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(msg.unit.rawValue), 4)
                w.writeBits(0, 1)   // child EE
                done77 = true
            }
        case 2:
            w.writeBits(0, 1)   // SE(Unit)
            w.writeBits(0, 1)   // value-start
            w.writeBits(UInt32(msg.unit.rawValue), 4)
            w.writeBits(0, 1)   // child EE
            done77 = true
        default:
            done77 = true
        }
    }
    w.writeBits(0, 1)   // element EE
}

internal func decodeConstantVarType(_ r: BitReader) throws -> ConstantVarType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _enable = try r.readBits(1) != 0
    _ = try r.readBits(1)   // child EE
    var _priority: UInt16? = nil
    var _varSetpoint: RationalNumberType? = nil
    var st78 = 0
    var done78 = false
    while !done78 {
        switch st78 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // Priority
                _ = try r.readBits(1)   // value-start
                _priority = UInt16(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st78 = 1
            case 1:   // SE(VarSetpoint)
                _varSetpoint = try decodeRationalNumberType(r)
                done78 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(VarSetpoint)
                _varSetpoint = try decodeRationalNumberType(r)
                done78 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done78 = true
        }
    }
    guard let _varSetpoint else {
        throw ExiError.invalidEventCode("VarSetpoint is required but the element ended")
    }
    var _varSetpoint_L2: RationalNumberType? = nil
    var _varSetpoint_L3: RationalNumberType? = nil
    var _unit: DerUnit? = nil
    var st79 = 0
    var done79 = false
    while !done79 {
        switch st79 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // VarSetpoint_L2
                _varSetpoint_L2 = try decodeRationalNumberType(r)
                st79 = 1
            case 1:   // VarSetpoint_L3
                _varSetpoint_L3 = try decodeRationalNumberType(r)
                st79 = 2
            case 2:   // SE(Unit)
                _ = try r.readBits(1)   // value-start
                _unit = try exiEnum(DerUnit.self, try r.readBits(4))
                _ = try r.readBits(1)   // child EE
                done79 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // VarSetpoint_L3
                _varSetpoint_L3 = try decodeRationalNumberType(r)
                st79 = 2
            case 1:   // SE(Unit)
                _ = try r.readBits(1)   // value-start
                _unit = try exiEnum(DerUnit.self, try r.readBits(4))
                _ = try r.readBits(1)   // child EE
                done79 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(1) {
            case 0:   // SE(Unit)
                _ = try r.readBits(1)   // value-start
                _unit = try exiEnum(DerUnit.self, try r.readBits(4))
                _ = try r.readBits(1)   // child EE
                done79 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done79 = true
        }
    }
    guard let _unit else {
        throw ExiError.invalidEventCode("Unit is required but the element ended")
    }
    _ = try r.readBits(1)   // element EE
    return ConstantVarType(enable: _enable, priority: _priority, varSetpoint: _varSetpoint, varSetpoint_L2: _varSetpoint_L2, varSetpoint_L3: _varSetpoint_L3, unit: _unit)
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.acdersae

import ExiRuntime

public final class ActivePowerSupportCPDResType {
    public var frequencyDroop: FrequencyDroopType
    public var voltWatt: VoltWattType
    public var constantWatt: ConstantWattType
    public var limitMaxDischargePower: LimitMaxDischargePowerType

    public init(frequencyDroop: FrequencyDroopType, voltWatt: VoltWattType, constantWatt: ConstantWattType, limitMaxDischargePower: LimitMaxDischargePowerType) {
        self.frequencyDroop = frequencyDroop
        self.voltWatt = voltWatt
        self.constantWatt = constantWatt
        self.limitMaxDischargePower = limitMaxDischargePower
    }
}

internal func encodeActivePowerSupportCPDResType(_ w: BitWriter, _ msg: ActivePowerSupportCPDResType) {
    w.writeBits(0, 1)   // SE
    encodeFrequencyDroopType(w, msg.frequencyDroop)
    w.writeBits(0, 1)   // SE
    encodeVoltWattType(w, msg.voltWatt)
    w.writeBits(0, 1)   // SE
    encodeConstantWattType(w, msg.constantWatt)
    w.writeBits(0, 1)   // SE
    encodeLimitMaxDischargePowerType(w, msg.limitMaxDischargePower)
    w.writeBits(0, 1)   // element EE
}

internal func decodeActivePowerSupportCPDResType(_ r: BitReader) throws -> ActivePowerSupportCPDResType {
    _ = try r.readBits(1)   // SE
    let _frequencyDroop = try decodeFrequencyDroopType(r)
    _ = try r.readBits(1)   // SE
    let _voltWatt = try decodeVoltWattType(r)
    _ = try r.readBits(1)   // SE
    let _constantWatt = try decodeConstantWattType(r)
    _ = try r.readBits(1)   // SE
    let _limitMaxDischargePower = try decodeLimitMaxDischargePowerType(r)
    _ = try r.readBits(1)   // element EE
    return ActivePowerSupportCPDResType(frequencyDroop: _frequencyDroop, voltWatt: _voltWatt, constantWatt: _constantWatt, limitMaxDischargePower: _limitMaxDischargePower)
}

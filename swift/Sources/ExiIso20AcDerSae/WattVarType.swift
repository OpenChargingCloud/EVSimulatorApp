// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.acdersae

import ExiRuntime

public final class WattVarType {
    public var enable: Bool
    public var priority: UInt16?
    public var xUnit: DerUnit
    public var yUnit: DerUnit
    public var curveDataPoints: CurveDataPointsListType
    public var curveDataPoints_L2: CurveDataPointsListType?
    public var curveDataPoints_L3: CurveDataPointsListType?
    public var openLoopResponseTime: RationalNumberType?
    public var timeConstantPT1: UInt32?

    public init(enable: Bool, priority: UInt16? = nil, xUnit: DerUnit, yUnit: DerUnit, curveDataPoints: CurveDataPointsListType, curveDataPoints_L2: CurveDataPointsListType? = nil, curveDataPoints_L3: CurveDataPointsListType? = nil, openLoopResponseTime: RationalNumberType? = nil, timeConstantPT1: UInt32? = nil) {
        self.enable = enable
        self.priority = priority
        self.xUnit = xUnit
        self.yUnit = yUnit
        self.curveDataPoints = curveDataPoints
        self.curveDataPoints_L2 = curveDataPoints_L2
        self.curveDataPoints_L3 = curveDataPoints_L3
        self.openLoopResponseTime = openLoopResponseTime
        self.timeConstantPT1 = timeConstantPT1
    }
}

internal func encodeWattVarType(_ w: BitWriter, _ msg: WattVarType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.enable ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    var st94 = 0
    var done94 = false
    while !done94 {
        switch st94 {
        case 0:
            if let v = msg.priority {
                w.writeBits(0, 2)   // Priority
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st94 = 1
            } else {
                w.writeBits(1, 2)   // SE(XUnit)
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(msg.xUnit.rawValue), 4)
                w.writeBits(0, 1)   // child EE
                done94 = true
            }
        case 1:
            w.writeBits(0, 1)   // SE(XUnit)
            w.writeBits(0, 1)   // value-start
            w.writeBits(UInt32(msg.xUnit.rawValue), 4)
            w.writeBits(0, 1)   // child EE
            done94 = true
        default:
            done94 = true
        }
    }
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.yUnit.rawValue), 4)
    w.writeBits(0, 1)   // child EE
    w.writeBits(0, 1)   // SE
    encodeCurveDataPointsListType(w, msg.curveDataPoints)
    var st95 = 0
    var done95 = false
    while !done95 {
        switch st95 {
        case 0:
            if let v = msg.curveDataPoints_L2 {
                w.writeBits(0, 3)   // CurveDataPoints_L2
                encodeCurveDataPointsListType(w, v)
                st95 = 1
            } else if let v = msg.curveDataPoints_L3 {
                w.writeBits(1, 3)   // CurveDataPoints_L3
                encodeCurveDataPointsListType(w, v)
                st95 = 2
            } else if let v = msg.openLoopResponseTime {
                w.writeBits(2, 3)   // OpenLoopResponseTime
                encodeRationalNumberType(w, v)
                st95 = 3
            } else if let v = msg.timeConstantPT1 {
                w.writeBits(3, 3)   // TimeConstantPT1
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st95 = 4
            } else {
                w.writeBits(4, 3)   // element EE
                done95 = true
            }
        case 1:
            if let v = msg.curveDataPoints_L3 {
                w.writeBits(0, 3)   // CurveDataPoints_L3
                encodeCurveDataPointsListType(w, v)
                st95 = 2
            } else if let v = msg.openLoopResponseTime {
                w.writeBits(1, 3)   // OpenLoopResponseTime
                encodeRationalNumberType(w, v)
                st95 = 3
            } else if let v = msg.timeConstantPT1 {
                w.writeBits(2, 3)   // TimeConstantPT1
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st95 = 4
            } else {
                w.writeBits(3, 3)   // element EE
                done95 = true
            }
        case 2:
            if let v = msg.openLoopResponseTime {
                w.writeBits(0, 2)   // OpenLoopResponseTime
                encodeRationalNumberType(w, v)
                st95 = 3
            } else if let v = msg.timeConstantPT1 {
                w.writeBits(1, 2)   // TimeConstantPT1
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st95 = 4
            } else {
                w.writeBits(2, 2)   // element EE
                done95 = true
            }
        case 3:
            if let v = msg.timeConstantPT1 {
                w.writeBits(0, 2)   // TimeConstantPT1
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st95 = 4
            } else {
                w.writeBits(1, 2)   // element EE
                done95 = true
            }
        case 4:
            w.writeBits(0, 1)   // element EE
            done95 = true
        default:
            done95 = true
        }
    }
}

internal func decodeWattVarType(_ r: BitReader) throws -> WattVarType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _enable = try r.readBits(1) != 0
    _ = try r.readBits(1)   // child EE
    var _priority: UInt16? = nil
    var _xUnit: DerUnit? = nil
    var st96 = 0
    var done96 = false
    while !done96 {
        switch st96 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // Priority
                _ = try r.readBits(1)   // value-start
                _priority = UInt16(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st96 = 1
            case 1:   // SE(XUnit)
                _ = try r.readBits(1)   // value-start
                _xUnit = try exiEnum(DerUnit.self, try r.readBits(4))
                _ = try r.readBits(1)   // child EE
                done96 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // SE(XUnit)
                _ = try r.readBits(1)   // value-start
                _xUnit = try exiEnum(DerUnit.self, try r.readBits(4))
                _ = try r.readBits(1)   // child EE
                done96 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done96 = true
        }
    }
    guard let _xUnit else {
        throw ExiError.invalidEventCode("XUnit is required but the element ended")
    }
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _yUnit = try exiEnum(DerUnit.self, try r.readBits(4))
    _ = try r.readBits(1)   // child EE
    _ = try r.readBits(1)   // SE
    let _curveDataPoints = try decodeCurveDataPointsListType(r)
    var _curveDataPoints_L2: CurveDataPointsListType? = nil
    var _curveDataPoints_L3: CurveDataPointsListType? = nil
    var _openLoopResponseTime: RationalNumberType? = nil
    var _timeConstantPT1: UInt32? = nil
    var st97 = 0
    var done97 = false
    while !done97 {
        switch st97 {
        case 0:
            switch try r.readBits(3) {
            case 0:   // CurveDataPoints_L2
                _curveDataPoints_L2 = try decodeCurveDataPointsListType(r)
                st97 = 1
            case 1:   // CurveDataPoints_L3
                _curveDataPoints_L3 = try decodeCurveDataPointsListType(r)
                st97 = 2
            case 2:   // OpenLoopResponseTime
                _openLoopResponseTime = try decodeRationalNumberType(r)
                st97 = 3
            case 3:   // TimeConstantPT1
                _ = try r.readBits(1)   // value-start
                _timeConstantPT1 = UInt32(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st97 = 4
            case 4:   // element EE
                done97 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(3) {
            case 0:   // CurveDataPoints_L3
                _curveDataPoints_L3 = try decodeCurveDataPointsListType(r)
                st97 = 2
            case 1:   // OpenLoopResponseTime
                _openLoopResponseTime = try decodeRationalNumberType(r)
                st97 = 3
            case 2:   // TimeConstantPT1
                _ = try r.readBits(1)   // value-start
                _timeConstantPT1 = UInt32(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st97 = 4
            case 3:   // element EE
                done97 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(2) {
            case 0:   // OpenLoopResponseTime
                _openLoopResponseTime = try decodeRationalNumberType(r)
                st97 = 3
            case 1:   // TimeConstantPT1
                _ = try r.readBits(1)   // value-start
                _timeConstantPT1 = UInt32(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st97 = 4
            case 2:   // element EE
                done97 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 3:
            switch try r.readBits(2) {
            case 0:   // TimeConstantPT1
                _ = try r.readBits(1)   // value-start
                _timeConstantPT1 = UInt32(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st97 = 4
            case 1:   // element EE
                done97 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 4:
            switch try r.readBits(1) {
            case 0:   // element EE
                done97 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done97 = true
        }
    }
    return WattVarType(enable: _enable, priority: _priority, xUnit: _xUnit, yUnit: _yUnit, curveDataPoints: _curveDataPoints, curveDataPoints_L2: _curveDataPoints_L2, curveDataPoints_L3: _curveDataPoints_L3, openLoopResponseTime: _openLoopResponseTime, timeConstantPT1: _timeConstantPT1)
}

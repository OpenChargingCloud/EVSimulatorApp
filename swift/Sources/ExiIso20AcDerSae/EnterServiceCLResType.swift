// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.acdersae

import ExiRuntime

public final class EnterServiceCLResType {
    public var permitService: Bool
    public var enterServiceVoltageHigh: RationalNumberType?
    public var enterServiceVoltageLow: RationalNumberType?
    public var enterServiceFrequencyHigh: RationalNumberType?
    public var enterServiceFrequencyLow: RationalNumberType?
    public var enterServiceDelay: RationalNumberType?
    public var enterServiceRandomizedDelay: RationalNumberType?
    public var enterServiceRampTime: RationalNumberType?

    public init(permitService: Bool, enterServiceVoltageHigh: RationalNumberType? = nil, enterServiceVoltageLow: RationalNumberType? = nil, enterServiceFrequencyHigh: RationalNumberType? = nil, enterServiceFrequencyLow: RationalNumberType? = nil, enterServiceDelay: RationalNumberType? = nil, enterServiceRandomizedDelay: RationalNumberType? = nil, enterServiceRampTime: RationalNumberType? = nil) {
        self.permitService = permitService
        self.enterServiceVoltageHigh = enterServiceVoltageHigh
        self.enterServiceVoltageLow = enterServiceVoltageLow
        self.enterServiceFrequencyHigh = enterServiceFrequencyHigh
        self.enterServiceFrequencyLow = enterServiceFrequencyLow
        self.enterServiceDelay = enterServiceDelay
        self.enterServiceRandomizedDelay = enterServiceRandomizedDelay
        self.enterServiceRampTime = enterServiceRampTime
    }
}

internal func encodeEnterServiceCLResType(_ w: BitWriter, _ msg: EnterServiceCLResType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.permitService ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    var st64 = 0
    var done64 = false
    while !done64 {
        switch st64 {
        case 0:
            if let v = msg.enterServiceVoltageHigh {
                w.writeBits(0, 4)   // EnterServiceVoltageHigh
                encodeRationalNumberType(w, v)
                st64 = 1
            } else if let v = msg.enterServiceVoltageLow {
                w.writeBits(1, 4)   // EnterServiceVoltageLow
                encodeRationalNumberType(w, v)
                st64 = 2
            } else if let v = msg.enterServiceFrequencyHigh {
                w.writeBits(2, 4)   // EnterServiceFrequencyHigh
                encodeRationalNumberType(w, v)
                st64 = 3
            } else if let v = msg.enterServiceFrequencyLow {
                w.writeBits(3, 4)   // EnterServiceFrequencyLow
                encodeRationalNumberType(w, v)
                st64 = 4
            } else if let v = msg.enterServiceDelay {
                w.writeBits(4, 4)   // EnterServiceDelay
                encodeRationalNumberType(w, v)
                st64 = 5
            } else if let v = msg.enterServiceRandomizedDelay {
                w.writeBits(5, 4)   // EnterServiceRandomizedDelay
                encodeRationalNumberType(w, v)
                st64 = 6
            } else if let v = msg.enterServiceRampTime {
                w.writeBits(6, 4)   // EnterServiceRampTime
                encodeRationalNumberType(w, v)
                st64 = 7
            } else {
                w.writeBits(7, 4)   // element EE
                done64 = true
            }
        case 1:
            if let v = msg.enterServiceVoltageLow {
                w.writeBits(0, 3)   // EnterServiceVoltageLow
                encodeRationalNumberType(w, v)
                st64 = 2
            } else if let v = msg.enterServiceFrequencyHigh {
                w.writeBits(1, 3)   // EnterServiceFrequencyHigh
                encodeRationalNumberType(w, v)
                st64 = 3
            } else if let v = msg.enterServiceFrequencyLow {
                w.writeBits(2, 3)   // EnterServiceFrequencyLow
                encodeRationalNumberType(w, v)
                st64 = 4
            } else if let v = msg.enterServiceDelay {
                w.writeBits(3, 3)   // EnterServiceDelay
                encodeRationalNumberType(w, v)
                st64 = 5
            } else if let v = msg.enterServiceRandomizedDelay {
                w.writeBits(4, 3)   // EnterServiceRandomizedDelay
                encodeRationalNumberType(w, v)
                st64 = 6
            } else if let v = msg.enterServiceRampTime {
                w.writeBits(5, 3)   // EnterServiceRampTime
                encodeRationalNumberType(w, v)
                st64 = 7
            } else {
                w.writeBits(6, 3)   // element EE
                done64 = true
            }
        case 2:
            if let v = msg.enterServiceFrequencyHigh {
                w.writeBits(0, 3)   // EnterServiceFrequencyHigh
                encodeRationalNumberType(w, v)
                st64 = 3
            } else if let v = msg.enterServiceFrequencyLow {
                w.writeBits(1, 3)   // EnterServiceFrequencyLow
                encodeRationalNumberType(w, v)
                st64 = 4
            } else if let v = msg.enterServiceDelay {
                w.writeBits(2, 3)   // EnterServiceDelay
                encodeRationalNumberType(w, v)
                st64 = 5
            } else if let v = msg.enterServiceRandomizedDelay {
                w.writeBits(3, 3)   // EnterServiceRandomizedDelay
                encodeRationalNumberType(w, v)
                st64 = 6
            } else if let v = msg.enterServiceRampTime {
                w.writeBits(4, 3)   // EnterServiceRampTime
                encodeRationalNumberType(w, v)
                st64 = 7
            } else {
                w.writeBits(5, 3)   // element EE
                done64 = true
            }
        case 3:
            if let v = msg.enterServiceFrequencyLow {
                w.writeBits(0, 3)   // EnterServiceFrequencyLow
                encodeRationalNumberType(w, v)
                st64 = 4
            } else if let v = msg.enterServiceDelay {
                w.writeBits(1, 3)   // EnterServiceDelay
                encodeRationalNumberType(w, v)
                st64 = 5
            } else if let v = msg.enterServiceRandomizedDelay {
                w.writeBits(2, 3)   // EnterServiceRandomizedDelay
                encodeRationalNumberType(w, v)
                st64 = 6
            } else if let v = msg.enterServiceRampTime {
                w.writeBits(3, 3)   // EnterServiceRampTime
                encodeRationalNumberType(w, v)
                st64 = 7
            } else {
                w.writeBits(4, 3)   // element EE
                done64 = true
            }
        case 4:
            if let v = msg.enterServiceDelay {
                w.writeBits(0, 3)   // EnterServiceDelay
                encodeRationalNumberType(w, v)
                st64 = 5
            } else if let v = msg.enterServiceRandomizedDelay {
                w.writeBits(1, 3)   // EnterServiceRandomizedDelay
                encodeRationalNumberType(w, v)
                st64 = 6
            } else if let v = msg.enterServiceRampTime {
                w.writeBits(2, 3)   // EnterServiceRampTime
                encodeRationalNumberType(w, v)
                st64 = 7
            } else {
                w.writeBits(3, 3)   // element EE
                done64 = true
            }
        case 5:
            if let v = msg.enterServiceRandomizedDelay {
                w.writeBits(0, 2)   // EnterServiceRandomizedDelay
                encodeRationalNumberType(w, v)
                st64 = 6
            } else if let v = msg.enterServiceRampTime {
                w.writeBits(1, 2)   // EnterServiceRampTime
                encodeRationalNumberType(w, v)
                st64 = 7
            } else {
                w.writeBits(2, 2)   // element EE
                done64 = true
            }
        case 6:
            if let v = msg.enterServiceRampTime {
                w.writeBits(0, 2)   // EnterServiceRampTime
                encodeRationalNumberType(w, v)
                st64 = 7
            } else {
                w.writeBits(1, 2)   // element EE
                done64 = true
            }
        case 7:
            w.writeBits(0, 1)   // element EE
            done64 = true
        default:
            done64 = true
        }
    }
}

internal func decodeEnterServiceCLResType(_ r: BitReader) throws -> EnterServiceCLResType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _permitService = try r.readBits(1) != 0
    _ = try r.readBits(1)   // child EE
    var _enterServiceVoltageHigh: RationalNumberType? = nil
    var _enterServiceVoltageLow: RationalNumberType? = nil
    var _enterServiceFrequencyHigh: RationalNumberType? = nil
    var _enterServiceFrequencyLow: RationalNumberType? = nil
    var _enterServiceDelay: RationalNumberType? = nil
    var _enterServiceRandomizedDelay: RationalNumberType? = nil
    var _enterServiceRampTime: RationalNumberType? = nil
    var st65 = 0
    var done65 = false
    while !done65 {
        switch st65 {
        case 0:
            switch try r.readBits(4) {
            case 0:   // EnterServiceVoltageHigh
                _enterServiceVoltageHigh = try decodeRationalNumberType(r)
                st65 = 1
            case 1:   // EnterServiceVoltageLow
                _enterServiceVoltageLow = try decodeRationalNumberType(r)
                st65 = 2
            case 2:   // EnterServiceFrequencyHigh
                _enterServiceFrequencyHigh = try decodeRationalNumberType(r)
                st65 = 3
            case 3:   // EnterServiceFrequencyLow
                _enterServiceFrequencyLow = try decodeRationalNumberType(r)
                st65 = 4
            case 4:   // EnterServiceDelay
                _enterServiceDelay = try decodeRationalNumberType(r)
                st65 = 5
            case 5:   // EnterServiceRandomizedDelay
                _enterServiceRandomizedDelay = try decodeRationalNumberType(r)
                st65 = 6
            case 6:   // EnterServiceRampTime
                _enterServiceRampTime = try decodeRationalNumberType(r)
                st65 = 7
            case 7:   // element EE
                done65 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(3) {
            case 0:   // EnterServiceVoltageLow
                _enterServiceVoltageLow = try decodeRationalNumberType(r)
                st65 = 2
            case 1:   // EnterServiceFrequencyHigh
                _enterServiceFrequencyHigh = try decodeRationalNumberType(r)
                st65 = 3
            case 2:   // EnterServiceFrequencyLow
                _enterServiceFrequencyLow = try decodeRationalNumberType(r)
                st65 = 4
            case 3:   // EnterServiceDelay
                _enterServiceDelay = try decodeRationalNumberType(r)
                st65 = 5
            case 4:   // EnterServiceRandomizedDelay
                _enterServiceRandomizedDelay = try decodeRationalNumberType(r)
                st65 = 6
            case 5:   // EnterServiceRampTime
                _enterServiceRampTime = try decodeRationalNumberType(r)
                st65 = 7
            case 6:   // element EE
                done65 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(3) {
            case 0:   // EnterServiceFrequencyHigh
                _enterServiceFrequencyHigh = try decodeRationalNumberType(r)
                st65 = 3
            case 1:   // EnterServiceFrequencyLow
                _enterServiceFrequencyLow = try decodeRationalNumberType(r)
                st65 = 4
            case 2:   // EnterServiceDelay
                _enterServiceDelay = try decodeRationalNumberType(r)
                st65 = 5
            case 3:   // EnterServiceRandomizedDelay
                _enterServiceRandomizedDelay = try decodeRationalNumberType(r)
                st65 = 6
            case 4:   // EnterServiceRampTime
                _enterServiceRampTime = try decodeRationalNumberType(r)
                st65 = 7
            case 5:   // element EE
                done65 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 3:
            switch try r.readBits(3) {
            case 0:   // EnterServiceFrequencyLow
                _enterServiceFrequencyLow = try decodeRationalNumberType(r)
                st65 = 4
            case 1:   // EnterServiceDelay
                _enterServiceDelay = try decodeRationalNumberType(r)
                st65 = 5
            case 2:   // EnterServiceRandomizedDelay
                _enterServiceRandomizedDelay = try decodeRationalNumberType(r)
                st65 = 6
            case 3:   // EnterServiceRampTime
                _enterServiceRampTime = try decodeRationalNumberType(r)
                st65 = 7
            case 4:   // element EE
                done65 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 4:
            switch try r.readBits(3) {
            case 0:   // EnterServiceDelay
                _enterServiceDelay = try decodeRationalNumberType(r)
                st65 = 5
            case 1:   // EnterServiceRandomizedDelay
                _enterServiceRandomizedDelay = try decodeRationalNumberType(r)
                st65 = 6
            case 2:   // EnterServiceRampTime
                _enterServiceRampTime = try decodeRationalNumberType(r)
                st65 = 7
            case 3:   // element EE
                done65 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 5:
            switch try r.readBits(2) {
            case 0:   // EnterServiceRandomizedDelay
                _enterServiceRandomizedDelay = try decodeRationalNumberType(r)
                st65 = 6
            case 1:   // EnterServiceRampTime
                _enterServiceRampTime = try decodeRationalNumberType(r)
                st65 = 7
            case 2:   // element EE
                done65 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 6:
            switch try r.readBits(2) {
            case 0:   // EnterServiceRampTime
                _enterServiceRampTime = try decodeRationalNumberType(r)
                st65 = 7
            case 1:   // element EE
                done65 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 7:
            switch try r.readBits(1) {
            case 0:   // element EE
                done65 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done65 = true
        }
    }
    return EnterServiceCLResType(permitService: _permitService, enterServiceVoltageHigh: _enterServiceVoltageHigh, enterServiceVoltageLow: _enterServiceVoltageLow, enterServiceFrequencyHigh: _enterServiceFrequencyHigh, enterServiceFrequencyLow: _enterServiceFrequencyLow, enterServiceDelay: _enterServiceDelay, enterServiceRandomizedDelay: _enterServiceRandomizedDelay, enterServiceRampTime: _enterServiceRampTime)
}

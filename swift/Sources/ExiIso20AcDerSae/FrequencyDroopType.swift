// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso20.acdersae

import ExiRuntime

public final class FrequencyDroopType {
    public var enable: Bool
    public var priority: UInt16?
    public var overFrequencyDroop: FrequencyDroopSettingsType?
    public var underFrequencyDroop: FrequencyDroopSettingsType?

    public init(enable: Bool, priority: UInt16? = nil, overFrequencyDroop: FrequencyDroopSettingsType? = nil, underFrequencyDroop: FrequencyDroopSettingsType? = nil) {
        self.enable = enable
        self.priority = priority
        self.overFrequencyDroop = overFrequencyDroop
        self.underFrequencyDroop = underFrequencyDroop
    }
}

internal func encodeFrequencyDroopType(_ w: BitWriter, _ msg: FrequencyDroopType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(msg.enable ? 1 : 0, 1)
    w.writeBits(0, 1)   // child EE
    var st104 = 0
    var done104 = false
    while !done104 {
        switch st104 {
        case 0:
            if let v = msg.priority {
                w.writeBits(0, 3)   // Priority
                w.writeBits(0, 1)   // value-start
                ExiPrimitives.writeUnsignedInteger(w, UInt64(v))
                w.writeBits(0, 1)   // child EE
                st104 = 1
            } else if let v = msg.overFrequencyDroop {
                w.writeBits(1, 3)   // OverFrequencyDroop
                encodeFrequencyDroopSettingsType(w, v)
                st104 = 2
            } else if let v = msg.underFrequencyDroop {
                w.writeBits(2, 3)   // UnderFrequencyDroop
                encodeFrequencyDroopSettingsType(w, v)
                st104 = 3
            } else {
                w.writeBits(3, 3)   // element EE
                done104 = true
            }
        case 1:
            if let v = msg.overFrequencyDroop {
                w.writeBits(0, 2)   // OverFrequencyDroop
                encodeFrequencyDroopSettingsType(w, v)
                st104 = 2
            } else if let v = msg.underFrequencyDroop {
                w.writeBits(1, 2)   // UnderFrequencyDroop
                encodeFrequencyDroopSettingsType(w, v)
                st104 = 3
            } else {
                w.writeBits(2, 2)   // element EE
                done104 = true
            }
        case 2:
            if let v = msg.underFrequencyDroop {
                w.writeBits(0, 2)   // UnderFrequencyDroop
                encodeFrequencyDroopSettingsType(w, v)
                st104 = 3
            } else {
                w.writeBits(1, 2)   // element EE
                done104 = true
            }
        case 3:
            w.writeBits(0, 1)   // element EE
            done104 = true
        default:
            done104 = true
        }
    }
}

internal func decodeFrequencyDroopType(_ r: BitReader) throws -> FrequencyDroopType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _enable = try r.readBits(1) != 0
    _ = try r.readBits(1)   // child EE
    var _priority: UInt16? = nil
    var _overFrequencyDroop: FrequencyDroopSettingsType? = nil
    var _underFrequencyDroop: FrequencyDroopSettingsType? = nil
    var st105 = 0
    var done105 = false
    while !done105 {
        switch st105 {
        case 0:
            switch try r.readBits(3) {
            case 0:   // Priority
                _ = try r.readBits(1)   // value-start
                _priority = UInt16(try ExiPrimitives.readUnsignedInteger(r))
                _ = try r.readBits(1)   // child EE
                st105 = 1
            case 1:   // OverFrequencyDroop
                _overFrequencyDroop = try decodeFrequencyDroopSettingsType(r)
                st105 = 2
            case 2:   // UnderFrequencyDroop
                _underFrequencyDroop = try decodeFrequencyDroopSettingsType(r)
                st105 = 3
            case 3:   // element EE
                done105 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // OverFrequencyDroop
                _overFrequencyDroop = try decodeFrequencyDroopSettingsType(r)
                st105 = 2
            case 1:   // UnderFrequencyDroop
                _underFrequencyDroop = try decodeFrequencyDroopSettingsType(r)
                st105 = 3
            case 2:   // element EE
                done105 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(2) {
            case 0:   // UnderFrequencyDroop
                _underFrequencyDroop = try decodeFrequencyDroopSettingsType(r)
                st105 = 3
            case 1:   // element EE
                done105 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 3:
            switch try r.readBits(1) {
            case 0:   // element EE
                done105 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done105 = true
        }
    }
    return FrequencyDroopType(enable: _enable, priority: _priority, overFrequencyDroop: _overFrequencyDroop, underFrequencyDroop: _underFrequencyDroop)
}

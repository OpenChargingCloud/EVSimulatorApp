// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
//
// Copyright (c) 2021-2026 GraphDefined GmbH <achim.friedland@graphdefined.com>
// This file is part of WWCP ISO/IEC 15118 <https://github.com/OpenChargingCloud/WWCP_ISO15118>
//
// Licensed under the Affero GPL license, Version 3.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.gnu.org/licenses/agpl.html
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// Schema target namespace: cloud.charging.v2g.iso20.acdersae

import ExiRuntime

public final class ReceiptType {
    public var timeAnchor: UInt64
    public var energyCosts: DetailedCostType?
    public var occupancyCosts: DetailedCostType?
    public var additionalServicesCosts: DetailedCostType?
    public var overstayCosts: DetailedCostType?
    public var taxCosts: [DetailedTaxType]

    public init(timeAnchor: UInt64, energyCosts: DetailedCostType? = nil, occupancyCosts: DetailedCostType? = nil, additionalServicesCosts: DetailedCostType? = nil, overstayCosts: DetailedCostType? = nil, taxCosts: [DetailedTaxType]) {
        self.timeAnchor = timeAnchor
        self.energyCosts = energyCosts
        self.occupancyCosts = occupancyCosts
        self.additionalServicesCosts = additionalServicesCosts
        self.overstayCosts = overstayCosts
        self.taxCosts = taxCosts
    }
}

internal func encodeReceiptType(_ w: BitWriter, _ msg: ReceiptType) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    ExiPrimitives.writeUnsignedInteger(w, UInt64(msg.timeAnchor))
    w.writeBits(0, 1)   // child EE
    var st256 = 0
    var done256 = false
    while !done256 {
        switch st256 {
        case 0:
            if let v = msg.energyCosts {
                w.writeBits(0, 3)   // EnergyCosts
                encodeDetailedCostType(w, v)
                st256 = 1
            } else if let v = msg.occupancyCosts {
                w.writeBits(1, 3)   // OccupancyCosts
                encodeDetailedCostType(w, v)
                st256 = 2
            } else if let v = msg.additionalServicesCosts {
                w.writeBits(2, 3)   // AdditionalServicesCosts
                encodeDetailedCostType(w, v)
                st256 = 3
            } else if let v = msg.overstayCosts {
                w.writeBits(3, 3)   // OverstayCosts
                encodeDetailedCostType(w, v)
                st256 = 4
            } else if !msg.taxCosts.isEmpty {
                precondition(msg.taxCosts.count <= 10, "list size out of schema range")
                w.writeBits(4, 3)   // TaxCosts
                encodeDetailedTaxType(w, msg.taxCosts[0])
                for ci in 1..<msg.taxCosts.count {
                    w.writeBits(0, 2)   // TaxCosts
                    encodeDetailedTaxType(w, msg.taxCosts[ci])
                }
                w.writeBits(1, 2)   // element EE (list end)
                done256 = true
            } else {
                w.writeBits(5, 3)   // element EE
                done256 = true
            }
        case 1:
            if let v = msg.occupancyCosts {
                w.writeBits(0, 3)   // OccupancyCosts
                encodeDetailedCostType(w, v)
                st256 = 2
            } else if let v = msg.additionalServicesCosts {
                w.writeBits(1, 3)   // AdditionalServicesCosts
                encodeDetailedCostType(w, v)
                st256 = 3
            } else if let v = msg.overstayCosts {
                w.writeBits(2, 3)   // OverstayCosts
                encodeDetailedCostType(w, v)
                st256 = 4
            } else if !msg.taxCosts.isEmpty {
                precondition(msg.taxCosts.count <= 10, "list size out of schema range")
                w.writeBits(3, 3)   // TaxCosts
                encodeDetailedTaxType(w, msg.taxCosts[0])
                for ci in 1..<msg.taxCosts.count {
                    w.writeBits(0, 2)   // TaxCosts
                    encodeDetailedTaxType(w, msg.taxCosts[ci])
                }
                w.writeBits(1, 2)   // element EE (list end)
                done256 = true
            } else {
                w.writeBits(4, 3)   // element EE
                done256 = true
            }
        case 2:
            if let v = msg.additionalServicesCosts {
                w.writeBits(0, 3)   // AdditionalServicesCosts
                encodeDetailedCostType(w, v)
                st256 = 3
            } else if let v = msg.overstayCosts {
                w.writeBits(1, 3)   // OverstayCosts
                encodeDetailedCostType(w, v)
                st256 = 4
            } else if !msg.taxCosts.isEmpty {
                precondition(msg.taxCosts.count <= 10, "list size out of schema range")
                w.writeBits(2, 3)   // TaxCosts
                encodeDetailedTaxType(w, msg.taxCosts[0])
                for ci in 1..<msg.taxCosts.count {
                    w.writeBits(0, 2)   // TaxCosts
                    encodeDetailedTaxType(w, msg.taxCosts[ci])
                }
                w.writeBits(1, 2)   // element EE (list end)
                done256 = true
            } else {
                w.writeBits(3, 3)   // element EE
                done256 = true
            }
        case 3:
            if let v = msg.overstayCosts {
                w.writeBits(0, 2)   // OverstayCosts
                encodeDetailedCostType(w, v)
                st256 = 4
            } else if !msg.taxCosts.isEmpty {
                precondition(msg.taxCosts.count <= 10, "list size out of schema range")
                w.writeBits(1, 2)   // TaxCosts
                encodeDetailedTaxType(w, msg.taxCosts[0])
                for ci in 1..<msg.taxCosts.count {
                    w.writeBits(0, 2)   // TaxCosts
                    encodeDetailedTaxType(w, msg.taxCosts[ci])
                }
                w.writeBits(1, 2)   // element EE (list end)
                done256 = true
            } else {
                w.writeBits(2, 2)   // element EE
                done256 = true
            }
        case 4:
            if !msg.taxCosts.isEmpty {
                precondition(msg.taxCosts.count <= 10, "list size out of schema range")
                w.writeBits(0, 2)   // TaxCosts
                encodeDetailedTaxType(w, msg.taxCosts[0])
                for ci in 1..<msg.taxCosts.count {
                    w.writeBits(0, 2)   // TaxCosts
                    encodeDetailedTaxType(w, msg.taxCosts[ci])
                }
                w.writeBits(1, 2)   // element EE (list end)
                done256 = true
            } else {
                w.writeBits(1, 2)   // element EE
                done256 = true
            }
        case 5:
            w.writeBits(0, 1)   // element EE
            done256 = true
        default:
            done256 = true
        }
    }
}

internal func decodeReceiptType(_ r: BitReader) throws -> ReceiptType {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _timeAnchor = UInt64(try ExiPrimitives.readUnsignedInteger(r))
    _ = try r.readBits(1)   // child EE
    var _energyCosts: DetailedCostType? = nil
    var _occupancyCosts: DetailedCostType? = nil
    var _additionalServicesCosts: DetailedCostType? = nil
    var _overstayCosts: DetailedCostType? = nil
    var taxCostsList = [DetailedTaxType]()
    var st257 = 0
    var done257 = false
    while !done257 {
        switch st257 {
        case 0:
            switch try r.readBits(3) {
            case 0:   // EnergyCosts
                _energyCosts = try decodeDetailedCostType(r)
                st257 = 1
            case 1:   // OccupancyCosts
                _occupancyCosts = try decodeDetailedCostType(r)
                st257 = 2
            case 2:   // AdditionalServicesCosts
                _additionalServicesCosts = try decodeDetailedCostType(r)
                st257 = 3
            case 3:   // OverstayCosts
                _overstayCosts = try decodeDetailedCostType(r)
                st257 = 4
            case 4:   // TaxCosts
                taxCostsList.append(try decodeDetailedTaxType(r))
                while true {
                    let ec = try r.readBits(2)
                    if ec == 1 { break }   // element EE (list end)
                    guard ec == 0, taxCostsList.count < 10 else {
                        throw ExiError.invalidEventCode("repeating element")
                    }
                    taxCostsList.append(try decodeDetailedTaxType(r))
                }
                done257 = true
            case 5:   // element EE
                done257 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(3) {
            case 0:   // OccupancyCosts
                _occupancyCosts = try decodeDetailedCostType(r)
                st257 = 2
            case 1:   // AdditionalServicesCosts
                _additionalServicesCosts = try decodeDetailedCostType(r)
                st257 = 3
            case 2:   // OverstayCosts
                _overstayCosts = try decodeDetailedCostType(r)
                st257 = 4
            case 3:   // TaxCosts
                taxCostsList.append(try decodeDetailedTaxType(r))
                while true {
                    let ec = try r.readBits(2)
                    if ec == 1 { break }   // element EE (list end)
                    guard ec == 0, taxCostsList.count < 10 else {
                        throw ExiError.invalidEventCode("repeating element")
                    }
                    taxCostsList.append(try decodeDetailedTaxType(r))
                }
                done257 = true
            case 4:   // element EE
                done257 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(3) {
            case 0:   // AdditionalServicesCosts
                _additionalServicesCosts = try decodeDetailedCostType(r)
                st257 = 3
            case 1:   // OverstayCosts
                _overstayCosts = try decodeDetailedCostType(r)
                st257 = 4
            case 2:   // TaxCosts
                taxCostsList.append(try decodeDetailedTaxType(r))
                while true {
                    let ec = try r.readBits(2)
                    if ec == 1 { break }   // element EE (list end)
                    guard ec == 0, taxCostsList.count < 10 else {
                        throw ExiError.invalidEventCode("repeating element")
                    }
                    taxCostsList.append(try decodeDetailedTaxType(r))
                }
                done257 = true
            case 3:   // element EE
                done257 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 3:
            switch try r.readBits(2) {
            case 0:   // OverstayCosts
                _overstayCosts = try decodeDetailedCostType(r)
                st257 = 4
            case 1:   // TaxCosts
                taxCostsList.append(try decodeDetailedTaxType(r))
                while true {
                    let ec = try r.readBits(2)
                    if ec == 1 { break }   // element EE (list end)
                    guard ec == 0, taxCostsList.count < 10 else {
                        throw ExiError.invalidEventCode("repeating element")
                    }
                    taxCostsList.append(try decodeDetailedTaxType(r))
                }
                done257 = true
            case 2:   // element EE
                done257 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 4:
            switch try r.readBits(2) {
            case 0:   // TaxCosts
                taxCostsList.append(try decodeDetailedTaxType(r))
                while true {
                    let ec = try r.readBits(2)
                    if ec == 1 { break }   // element EE (list end)
                    guard ec == 0, taxCostsList.count < 10 else {
                        throw ExiError.invalidEventCode("repeating element")
                    }
                    taxCostsList.append(try decodeDetailedTaxType(r))
                }
                done257 = true
            case 1:   // element EE
                done257 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 5:
            switch try r.readBits(1) {
            case 0:   // element EE
                done257 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done257 = true
        }
    }
    return ReceiptType(timeAnchor: _timeAnchor, energyCosts: _energyCosts, occupancyCosts: _occupancyCosts, additionalServicesCosts: _additionalServicesCosts, overstayCosts: _overstayCosts, taxCosts: taxCostsList)
}

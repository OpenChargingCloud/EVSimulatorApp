// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.acdersae

import ExiRuntime

public final class TransformsType {
    public var transform: [TransformType]

    public init(transform: [TransformType]) {
        self.transform = transform
    }
}

internal func encodeTransformsType(_ w: BitWriter, _ msg: TransformsType) {
    let list = msg.transform
    precondition((1...2147483647).contains(list.count), "list size out of schema range")
    for (i, item) in list.enumerated() {
        w.writeBits(0, i == 0 ? 1 : 2)   // SE(item)
        encodeTransformType(w, item)
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

internal func decodeTransformsType(_ r: BitReader) throws -> TransformsType {
    var list = [TransformType]()
    _ = try r.readBits(1)   // SE(item) first
    list.append(try decodeTransformType(r))
    while true {
        let ec = try r.readBits(2)
        if ec == 1 { break }   // element EE
        guard ec == 0, list.count < 2147483647 else {
            throw ExiError.invalidEventCode("repeating element")
        }
        list.append(try decodeTransformType(r))
    }
    return TransformsType(transform: list)
}

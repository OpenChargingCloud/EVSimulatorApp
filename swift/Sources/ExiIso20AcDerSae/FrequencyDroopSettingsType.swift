// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.acdersae

import ExiRuntime

public final class FrequencyDroopSettingsType {
    public var db: RationalNumberType
    public var droopFactor: RationalNumberType
    public var droopFactor_L2: RationalNumberType?
    public var droopFactor_L3: RationalNumberType?
    public var powerReference: PowerReference
    public var powerReference_L2: PowerReference?
    public var powerReference_L3: PowerReference?
    public var openLoopResponseTime: RationalNumberType

    public init(db: RationalNumberType, droopFactor: RationalNumberType, droopFactor_L2: RationalNumberType? = nil, droopFactor_L3: RationalNumberType? = nil, powerReference: PowerReference, powerReference_L2: PowerReference? = nil, powerReference_L3: PowerReference? = nil, openLoopResponseTime: RationalNumberType) {
        self.db = db
        self.droopFactor = droopFactor
        self.droopFactor_L2 = droopFactor_L2
        self.droopFactor_L3 = droopFactor_L3
        self.powerReference = powerReference
        self.powerReference_L2 = powerReference_L2
        self.powerReference_L3 = powerReference_L3
        self.openLoopResponseTime = openLoopResponseTime
    }
}

internal func encodeFrequencyDroopSettingsType(_ w: BitWriter, _ msg: FrequencyDroopSettingsType) {
    w.writeBits(0, 1)   // SE
    encodeRationalNumberType(w, msg.db)
    w.writeBits(0, 1)   // SE
    encodeRationalNumberType(w, msg.droopFactor)
    var st106 = 0
    var done106 = false
    while !done106 {
        switch st106 {
        case 0:
            if let v = msg.droopFactor_L2 {
                w.writeBits(0, 2)   // DroopFactor_L2
                encodeRationalNumberType(w, v)
                st106 = 1
            } else if let v = msg.droopFactor_L3 {
                w.writeBits(1, 2)   // DroopFactor_L3
                encodeRationalNumberType(w, v)
                st106 = 2
            } else {
                w.writeBits(2, 2)   // SE(PowerReference)
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(msg.powerReference.rawValue), 1)
                w.writeBits(0, 1)   // child EE
                done106 = true
            }
        case 1:
            if let v = msg.droopFactor_L3 {
                w.writeBits(0, 2)   // DroopFactor_L3
                encodeRationalNumberType(w, v)
                st106 = 2
            } else {
                w.writeBits(1, 2)   // SE(PowerReference)
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(msg.powerReference.rawValue), 1)
                w.writeBits(0, 1)   // child EE
                done106 = true
            }
        case 2:
            w.writeBits(0, 1)   // SE(PowerReference)
            w.writeBits(0, 1)   // value-start
            w.writeBits(UInt32(msg.powerReference.rawValue), 1)
            w.writeBits(0, 1)   // child EE
            done106 = true
        default:
            done106 = true
        }
    }
    var st107 = 0
    var done107 = false
    while !done107 {
        switch st107 {
        case 0:
            if let v = msg.powerReference_L2 {
                w.writeBits(0, 2)   // PowerReference_L2
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v.rawValue), 1)
                w.writeBits(0, 1)   // child EE
                st107 = 1
            } else if let v = msg.powerReference_L3 {
                w.writeBits(1, 2)   // PowerReference_L3
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v.rawValue), 1)
                w.writeBits(0, 1)   // child EE
                st107 = 2
            } else {
                w.writeBits(2, 2)   // SE(OpenLoopResponseTime)
                encodeRationalNumberType(w, msg.openLoopResponseTime)
                done107 = true
            }
        case 1:
            if let v = msg.powerReference_L3 {
                w.writeBits(0, 2)   // PowerReference_L3
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v.rawValue), 1)
                w.writeBits(0, 1)   // child EE
                st107 = 2
            } else {
                w.writeBits(1, 2)   // SE(OpenLoopResponseTime)
                encodeRationalNumberType(w, msg.openLoopResponseTime)
                done107 = true
            }
        case 2:
            w.writeBits(0, 1)   // SE(OpenLoopResponseTime)
            encodeRationalNumberType(w, msg.openLoopResponseTime)
            done107 = true
        default:
            done107 = true
        }
    }
    w.writeBits(0, 1)   // element EE
}

internal func decodeFrequencyDroopSettingsType(_ r: BitReader) throws -> FrequencyDroopSettingsType {
    _ = try r.readBits(1)   // SE
    let _db = try decodeRationalNumberType(r)
    _ = try r.readBits(1)   // SE
    let _droopFactor = try decodeRationalNumberType(r)
    var _droopFactor_L2: RationalNumberType? = nil
    var _droopFactor_L3: RationalNumberType? = nil
    var _powerReference: PowerReference? = nil
    var st108 = 0
    var done108 = false
    while !done108 {
        switch st108 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // DroopFactor_L2
                _droopFactor_L2 = try decodeRationalNumberType(r)
                st108 = 1
            case 1:   // DroopFactor_L3
                _droopFactor_L3 = try decodeRationalNumberType(r)
                st108 = 2
            case 2:   // SE(PowerReference)
                _ = try r.readBits(1)   // value-start
                _powerReference = try exiEnum(PowerReference.self, try r.readBits(1))
                _ = try r.readBits(1)   // child EE
                done108 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // DroopFactor_L3
                _droopFactor_L3 = try decodeRationalNumberType(r)
                st108 = 2
            case 1:   // SE(PowerReference)
                _ = try r.readBits(1)   // value-start
                _powerReference = try exiEnum(PowerReference.self, try r.readBits(1))
                _ = try r.readBits(1)   // child EE
                done108 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(1) {
            case 0:   // SE(PowerReference)
                _ = try r.readBits(1)   // value-start
                _powerReference = try exiEnum(PowerReference.self, try r.readBits(1))
                _ = try r.readBits(1)   // child EE
                done108 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done108 = true
        }
    }
    guard let _powerReference else {
        throw ExiError.invalidEventCode("PowerReference is required but the element ended")
    }
    var _powerReference_L2: PowerReference? = nil
    var _powerReference_L3: PowerReference? = nil
    var _openLoopResponseTime: RationalNumberType? = nil
    var st109 = 0
    var done109 = false
    while !done109 {
        switch st109 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // PowerReference_L2
                _ = try r.readBits(1)   // value-start
                _powerReference_L2 = try exiEnum(PowerReference.self, try r.readBits(1))
                _ = try r.readBits(1)   // child EE
                st109 = 1
            case 1:   // PowerReference_L3
                _ = try r.readBits(1)   // value-start
                _powerReference_L3 = try exiEnum(PowerReference.self, try r.readBits(1))
                _ = try r.readBits(1)   // child EE
                st109 = 2
            case 2:   // SE(OpenLoopResponseTime)
                _openLoopResponseTime = try decodeRationalNumberType(r)
                done109 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(2) {
            case 0:   // PowerReference_L3
                _ = try r.readBits(1)   // value-start
                _powerReference_L3 = try exiEnum(PowerReference.self, try r.readBits(1))
                _ = try r.readBits(1)   // child EE
                st109 = 2
            case 1:   // SE(OpenLoopResponseTime)
                _openLoopResponseTime = try decodeRationalNumberType(r)
                done109 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(1) {
            case 0:   // SE(OpenLoopResponseTime)
                _openLoopResponseTime = try decodeRationalNumberType(r)
                done109 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done109 = true
        }
    }
    guard let _openLoopResponseTime else {
        throw ExiError.invalidEventCode("OpenLoopResponseTime is required but the element ended")
    }
    _ = try r.readBits(1)   // element EE
    return FrequencyDroopSettingsType(db: _db, droopFactor: _droopFactor, droopFactor_L2: _droopFactor_L2, droopFactor_L3: _droopFactor_L3, powerReference: _powerReference, powerReference_L2: _powerReference_L2, powerReference_L3: _powerReference_L3, openLoopResponseTime: _openLoopResponseTime)
}

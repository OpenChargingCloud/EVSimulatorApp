// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.iso20.acdersae

import ExiRuntime

public final class ActivePowerSupportCLResType {
    public var frequencyDroop: FrequencyDroopType?
    public var voltWatt: VoltWattType?
    public var constantWatt: ConstantWattType?
    public var limitMaxDischargePower: LimitMaxDischargePowerType?

    public init(frequencyDroop: FrequencyDroopType? = nil, voltWatt: VoltWattType? = nil, constantWatt: ConstantWattType? = nil, limitMaxDischargePower: LimitMaxDischargePowerType? = nil) {
        self.frequencyDroop = frequencyDroop
        self.voltWatt = voltWatt
        self.constantWatt = constantWatt
        self.limitMaxDischargePower = limitMaxDischargePower
    }
}

internal func encodeActivePowerSupportCLResType(_ w: BitWriter, _ msg: ActivePowerSupportCLResType) {
    var st68 = 0
    var done68 = false
    while !done68 {
        switch st68 {
        case 0:
            if let v = msg.frequencyDroop {
                w.writeBits(0, 3)   // FrequencyDroop
                encodeFrequencyDroopType(w, v)
                st68 = 1
            } else if let v = msg.voltWatt {
                w.writeBits(1, 3)   // VoltWatt
                encodeVoltWattType(w, v)
                st68 = 2
            } else if let v = msg.constantWatt {
                w.writeBits(2, 3)   // ConstantWatt
                encodeConstantWattType(w, v)
                st68 = 3
            } else if let v = msg.limitMaxDischargePower {
                w.writeBits(3, 3)   // LimitMaxDischargePower
                encodeLimitMaxDischargePowerType(w, v)
                st68 = 4
            } else {
                w.writeBits(4, 3)   // element EE
                done68 = true
            }
        case 1:
            if let v = msg.voltWatt {
                w.writeBits(0, 3)   // VoltWatt
                encodeVoltWattType(w, v)
                st68 = 2
            } else if let v = msg.constantWatt {
                w.writeBits(1, 3)   // ConstantWatt
                encodeConstantWattType(w, v)
                st68 = 3
            } else if let v = msg.limitMaxDischargePower {
                w.writeBits(2, 3)   // LimitMaxDischargePower
                encodeLimitMaxDischargePowerType(w, v)
                st68 = 4
            } else {
                w.writeBits(3, 3)   // element EE
                done68 = true
            }
        case 2:
            if let v = msg.constantWatt {
                w.writeBits(0, 2)   // ConstantWatt
                encodeConstantWattType(w, v)
                st68 = 3
            } else if let v = msg.limitMaxDischargePower {
                w.writeBits(1, 2)   // LimitMaxDischargePower
                encodeLimitMaxDischargePowerType(w, v)
                st68 = 4
            } else {
                w.writeBits(2, 2)   // element EE
                done68 = true
            }
        case 3:
            if let v = msg.limitMaxDischargePower {
                w.writeBits(0, 2)   // LimitMaxDischargePower
                encodeLimitMaxDischargePowerType(w, v)
                st68 = 4
            } else {
                w.writeBits(1, 2)   // element EE
                done68 = true
            }
        case 4:
            w.writeBits(0, 1)   // element EE
            done68 = true
        default:
            done68 = true
        }
    }
}

internal func decodeActivePowerSupportCLResType(_ r: BitReader) throws -> ActivePowerSupportCLResType {
    var _frequencyDroop: FrequencyDroopType? = nil
    var _voltWatt: VoltWattType? = nil
    var _constantWatt: ConstantWattType? = nil
    var _limitMaxDischargePower: LimitMaxDischargePowerType? = nil
    var st69 = 0
    var done69 = false
    while !done69 {
        switch st69 {
        case 0:
            switch try r.readBits(3) {
            case 0:   // FrequencyDroop
                _frequencyDroop = try decodeFrequencyDroopType(r)
                st69 = 1
            case 1:   // VoltWatt
                _voltWatt = try decodeVoltWattType(r)
                st69 = 2
            case 2:   // ConstantWatt
                _constantWatt = try decodeConstantWattType(r)
                st69 = 3
            case 3:   // LimitMaxDischargePower
                _limitMaxDischargePower = try decodeLimitMaxDischargePowerType(r)
                st69 = 4
            case 4:   // element EE
                done69 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(3) {
            case 0:   // VoltWatt
                _voltWatt = try decodeVoltWattType(r)
                st69 = 2
            case 1:   // ConstantWatt
                _constantWatt = try decodeConstantWattType(r)
                st69 = 3
            case 2:   // LimitMaxDischargePower
                _limitMaxDischargePower = try decodeLimitMaxDischargePowerType(r)
                st69 = 4
            case 3:   // element EE
                done69 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 2:
            switch try r.readBits(2) {
            case 0:   // ConstantWatt
                _constantWatt = try decodeConstantWattType(r)
                st69 = 3
            case 1:   // LimitMaxDischargePower
                _limitMaxDischargePower = try decodeLimitMaxDischargePowerType(r)
                st69 = 4
            case 2:   // element EE
                done69 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 3:
            switch try r.readBits(2) {
            case 0:   // LimitMaxDischargePower
                _limitMaxDischargePower = try decodeLimitMaxDischargePowerType(r)
                st69 = 4
            case 1:   // element EE
                done69 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 4:
            switch try r.readBits(1) {
            case 0:   // element EE
                done69 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done69 = true
        }
    }
    return ActivePowerSupportCLResType(frequencyDroop: _frequencyDroop, voltWatt: _voltWatt, constantWatt: _constantWatt, limitMaxDischargePower: _limitMaxDischargePower)
}

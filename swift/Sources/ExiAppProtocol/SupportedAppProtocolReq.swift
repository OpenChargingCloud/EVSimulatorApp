// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.appprotocol

import ExiRuntime

public final class SupportedAppProtocolReq {
    public var appProtocol: [AppProtocolType]

    public init(appProtocol: [AppProtocolType]) {
        self.appProtocol = appProtocol
    }
}

internal func encodeSupportedAppProtocolReq(_ w: BitWriter, _ msg: SupportedAppProtocolReq) {
    let list = msg.appProtocol
    precondition((1...20).contains(list.count), "list size out of schema range")
    for (i, item) in list.enumerated() {
        w.writeBits(0, i == 0 ? 1 : 2)   // SE(item)
        encodeAppProtocolType(w, item)
    }
    w.writeBits(1, 2)   // list terminator / element EE
}

internal func decodeSupportedAppProtocolReq(_ r: BitReader) throws -> SupportedAppProtocolReq {
    var list = [AppProtocolType]()
    _ = try r.readBits(1)   // SE(item) first
    list.append(try decodeAppProtocolType(r))
    while true {
        let ec = try r.readBits(2)
        if ec == 1 { break }   // element EE
        guard ec == 0, list.count < 20 else {
            throw ExiError.invalidEventCode("repeating element")
        }
        list.append(try decodeAppProtocolType(r))
    }
    return SupportedAppProtocolReq(appProtocol: list)
}

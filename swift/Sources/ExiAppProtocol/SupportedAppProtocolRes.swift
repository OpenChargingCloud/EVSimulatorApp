// <auto-generated/>
// Generated by Vanaheimr.V2G.Exi.SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.appprotocol

import ExiRuntime

public struct SupportedAppProtocolRes: Equatable, Sendable {
    public var responseCode: ResponseCode
    public var schemaID: UInt8?

    public init(responseCode: ResponseCode, schemaID: UInt8? = nil) {
        self.responseCode = responseCode
        self.schemaID = schemaID
    }
}

internal func encodeSupportedAppProtocolRes(_ w: BitWriter, _ msg: SupportedAppProtocolRes) {
    w.writeBits(0, 1)   // SE
    w.writeBits(0, 1)   // value-start
    w.writeBits(UInt32(msg.responseCode.rawValue), 2)
    w.writeBits(0, 1)   // child EE
    var st0 = 0
    var done0 = false
    while !done0 {
        switch st0 {
        case 0:
            if let v = msg.schemaID {
                w.writeBits(0, 2)   // SchemaID
                w.writeBits(0, 1)   // value-start
                w.writeBits(UInt32(v), 8)
                w.writeBits(0, 1)   // child EE
                st0 = 1
            } else {
                w.writeBits(1, 2)   // element EE
                done0 = true
            }
        case 1:
            w.writeBits(0, 1)   // element EE
            done0 = true
        default:
            done0 = true
        }
    }
}

internal func decodeSupportedAppProtocolRes(_ r: BitReader) throws -> SupportedAppProtocolRes {
    _ = try r.readBits(1)   // SE
    _ = try r.readBits(1)   // value-start
    let _responseCode = try decodeResponseCode(r, 2)
    _ = try r.readBits(1)   // child EE
    var _schemaID: UInt8? = nil
    var st1 = 0
    var done1 = false
    while !done1 {
        switch st1 {
        case 0:
            switch try r.readBits(2) {
            case 0:   // SchemaID
                _ = try r.readBits(1)   // value-start
                _schemaID = UInt8(try r.readBits(8))
                _ = try r.readBits(1)   // child EE
                st1 = 1
            case 1:   // element EE
                done1 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        case 1:
            switch try r.readBits(1) {
            case 0:   // element EE
                done1 = true
            default:
                throw ExiError.invalidEventCode("optional run")
            }
        default:
            done1 = true
        }
    }
    return SupportedAppProtocolRes(responseCode: _responseCode, schemaID: _schemaID)
}

// <auto-generated/>
// Generated by WWCP_ISO15118_EXI_SourceGenerator (Swift back end). Do not edit by hand.
// Schema target namespace: cloud.charging.v2g.appprotocol

import ExiRuntime

public enum SupportedAppProtocolCodec {

    public static let exiHeader: UInt8 = 0x80

    public static func encode(_ msg: SupportedAppProtocolReq) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(0, 2)   // document element selector
        encodeSupportedAppProtocolReq(w, msg)
        w.alignToByte()
        return w.bytes
    }

    public static func encode(_ msg: SupportedAppProtocolRes) -> [UInt8] {
        let w = BitWriter(capacity: 256)
        w.writeBits(UInt32(exiHeader), 8)
        w.writeBits(1, 2)   // document element selector
        encodeSupportedAppProtocolRes(w, msg)
        w.alignToByte()
        return w.bytes
    }

    /// Encodes any document element of this message set, dispatching on its runtime type.
    public static func encodeAny(_ msg: Any) throws -> [UInt8] {
        switch msg {
        case let m as SupportedAppProtocolReq: return encode(m)
        case let m as SupportedAppProtocolRes: return encode(m)
        default: throw ExiError.invalidHeader
        }
    }

    public static func decodeAny(_ src: [UInt8]) throws -> Any {
        guard src.first == exiHeader else { throw ExiError.invalidHeader }
        let r = BitReader(src, offset: 1)
        let sel = try r.readBits(2)
        switch sel {
        case 0: return try decodeSupportedAppProtocolReq(r)
        case 1: return try decodeSupportedAppProtocolRes(r)
        default: throw ExiError.unknownDocumentIndex(sel)
        }
    }
}
